# 고객지원 시스템 구축 기획서 v2.4

**부제:** 계획 · 실행 · 확인 — 업무가 늘어지지 않고 빠지지 않게

| 항목 | 내용 |
|------|------|
| **문서 목적** | 고객지원 시스템의 기준정보, 업무흐름, 데이터 구조, 운영 원칙, **관리 지표의 수집 책임**을 정의 |
| **적용 범위** | 계획(일일 업무 등록·정기점검 일정) · 실행(AS 접수·조치·행정지원) · 확인(현황·통계·보고서) · 기준정보(고객·공간·담당자·자산·사업) · 관리(코드·권한·데이터) |
| **핵심 방향** | 계약 중심이 아닌 **기관/설치자산/지원이력 중심** 관리. **계획 없는 업무는 만들지 않는다.** |
| **활용 목표** | AS 운영 효율화 + 관리 지표의 실측 + 장비 교체주기 파악 + 교체/업그레이드 영업 연계 |
| **작성일** | 2026-03-12 (v1.0) |
| **개정일** | 2026-08-14 (v2.0) · (v2.1 §13 다자 협업) · (v2.2 — v1.0 §10.3.2 GTD 병합) · (v2.3 §12.9 AS 접수 사진) · **(v2.4 — §16.1 주간업무보고서 전면 개정)** |
| **개정 근거** | 베타 운영 데이터 실측 검토 (AS 1,174건 · 자산 549건 · 정기점검 76건) + 구동·E2E 검증 + 현업 사례(IRM 평가지표 작성) |
| **문서 지위** | 본 문서가 기획·구현·DB·테스트의 **유일 기준**이다. v1.0은 폐기하고 이력 참조용으로만 보관한다. |

---

## 0. 개정 요지 (v1.0 → v2.0)

### 0.1 왜 고쳤는가

v1.0은 기능 정의가 충실했고, 구현도 그 정의를 잘 따랐다. 그런데 베타 데이터를 열어 보니 **관리 지표 3종이 모두 계산되지 않는 상태**였다.

| 지표 | v1.0 정의 | 베타 실측 | 원인 |
|------|-----------|-----------|------|
| 계획대비 실행율 90% | 계획대로 완료 ÷ 예정 | 6~7월 **0%** | 방문예정일 입력 **2.6%** — 분모가 없음 |
| 접수→방문 3일 | AVG(방문일 − 접수일) | 표본 주당 6~17건 (접수 40~80건 대비) | `start_datetime` 기록 **3.2%** |
| 접수→완료 7일 | AVG(완료일 − 접수일) | 1.3일 ↔ 9.4일 요동 | 데이터의 **95.7%가 과거 엑셀 이관분**, 그중 69%가 접수일=완료일 |

원인은 하나로 모인다. **v1.0은 지표의 "정의"는 완벽하게 썼지만, 그 값을 "누가 어느 화면에서 언제 입력하는가"를 쓰지 않았다.** 그래서 입력이 선택 사항으로 구현됐고, 선택 사항은 채워지지 않았다.

부수적으로 드러난 것:

- **일정확정 1,141건 중 예정일이 있는 건은 31건.** v1.0 §8이 "확정하려면 예정업무일 필수"라고 명시했으나 제약이 없어 유령 상태가 대량 생성됐다.
- **조치가 1회차에서 끝남 (1,172건 중 1,149건).** 하부업무 `[확인]`/`[재방문]`은 10건. 재방문·추가조치를 누적하는 v1.0 §8의 핵심 설계가 실제로는 거의 발동하지 않았다.
- **일일 업무 등록 사용률 10% 미만** (`work_tasks` 118건 vs AS 1,174건). 기능은 정상(118건 중 116건이 시간 배치 완료)이나 습관이 정착하지 않았다.
- **정기점검 자동 수립 실사용 0건** (`auto_generated=0`). 대신 수동 캘린더 배정이 잘 돌아간다.
- **소요시간·내근외근·원인분류 수집률 0~2.8%.** 최장 소요 통계와 영업 활용 분석의 근거가 없다.

### 0.2 무엇이 바뀌었는가

| 구분 | v1.0 | v2.0 |
|------|------|------|
| **문서 구조** | 기능 나열 (§1~§18) | **원칙 → 지표 → 메뉴 → 계획 → 실행 → 확인 → 기준정보 → 정기점검 → 비기능** 9부 |
| **지표 위치** | §13 통계 안의 한 표 | **제2부로 승격.** 기능보다 먼저 정의 |
| **지표 수집** | 정의만 있음 | **지표마다 「입력 지점 · 시점 · 책임자 · 필수 여부」 명시** (§4.2) |
| **데이터 공백** | 규정 없음 | **분모 0 / 표본 부족 / 이관 데이터 혼입 표시 규칙 신설** (§4.3) |
| **필수 입력** | 개별 항목에 산재 | **§5로 일원화 + DB 제약 일람 (부록 B)** |
| **메뉴 구조** | 대메뉴 11개 (기능별) | **대메뉴 6개 (계획→실행→확인 업무 흐름별)** |
| **미계획 업무** | 개념 없음 | **「미계획 업무함」 신설** — 빠짐 방지의 실질적 안전망 (§8) |
| **정기점검 자동 수립** | 이동시간·TSP·17시 제약까지 요구 | **주기 도래 사이트를 평일에 나열하는 초안까지로 축소.** 배정은 수동 캘린더 확정 (§23.5) |
| **이관 데이터** | 구분 없음 | **`data_origin` 도입.** 지표에서 기본 제외 (§4.3) |
| **주간업무보고서** [v2.4] | 시트 4개 · 담당자 드롭다운으로 한 명씩 | **시트 2개(통계·전체 리스트).** 담당자 전원을 한 표에. **일일 평균·일일 최대·이월 건수** 신설. 접수는 **배정 기준** (§16.1) |
| **AS 접수 사진** [v2.3] | 조치 화면에서만 첨부 가능 | **접수 화면에서 증상 사진 등록.** 썸네일 미리보기 · 사진별 메모 · 자동 축소 · 자산 사진 승격 (§12.9) |
| **행정·지원업무** [v2.2] | 업무 1건 = 완료 체크 | **GTD 기반 — 수집함 · 다음 행동 · 조치 이력 · 상태 9종.** 여기에 **접수일/완료일 컬럼**과 **리드타임(실작업/외부대기) 분리 지표**를 얹음 (§13) |

### 0.3 v2.0에서 유지한 것

v1.0의 다음 판단은 옳았고 그대로 간다. 재검토 대상이 아니다.

- 계약 여부와 무관한 **기관 중심** 자산 관리 (§2.2)
- **접수 1건 = 1 Case**, 같은 번호 재오픈 금지, 완료 후 동일 증상은 **재접수** (§12.2 · §12.7)
- 이력을 덮어쓰지 않고 **추가**하는 원칙 — `as_processes` Timeline (§12.6)
- 접수 상세와 조치 화면의 **분리**
- 현재 정보와 이력 정보의 **분리** (담당자 이력·변경 이력)
- 모든 엔터티를 **고유 ID**로 연결, 코드 체계 표준화
- **엔터티 신설 최소화** — `RECEIPT`/`ACTION_HISTORY` 같은 중복 테이블을 만들지 않는다
- 정기점검 **방문 1건 = 날짜 × 기관 × 점검대상** 단위, 엑셀 월별 캘린더 출력

---

# 제1부 — 원칙

## 1. 추진 배경 및 목표

### 1.1 추진 배경

본 시스템은 고객기관의 기본 정보와 담당자, 설치자산, 수행관계, 지원이력을 표준화하여 AS 접수·처리·현황 관리를 체계화하기 위한 고객지원 시스템이다.

- 고객기관의 장비·시스템은 다수의 제조사, 협력사, 과거 설치업체가 혼재되어 있어 현장 정보가 분산된다.
- 담당자 교체가 잦아 현재 담당자만 관리하면 업무 이력과 대응 맥락이 단절된다.
- AS 접수 시 설치 위치, 제품 정보, 설치환경, 요청 주체를 즉시 확인할 수 있는 기준정보가 필요하다.
- 타사 설치 장비라도 교체주기, 노후도, 장애 빈도를 파악하면 향후 교체 영업과 제안 활동으로 연결 가능하다.
- **[v2 추가]** 업무가 접수 시점에만 기록되고 진행 과정이 남지 않으면, 늦어지는 건과 빠지는 건을 사전에 발견할 수 없다.

### 1.2 시스템 목표

목표는 **담당자 관점**과 **관리자 관점**으로 나뉜다. 두 관점은 같은 데이터를 쓰지만 요구하는 것이 다르다.

| 관점 | 목표 | 성공 판단 기준 |
|------|------|----------------|
| **담당자** | 맡은 업무가 **빠짐없이** 진행된다 | 예정일 없는 미완료 건 = **0건**. 예정일 경과 후 처리 이력 없는 건 = **0건** |
| **담당자** | 업무가 **늘어지지 않는다** | 접수→방문 3일 이내, 접수→완료 7일 이내 |
| **관리자** | 팀의 계획과 실행을 **측정**한다 | 계획대비 실행율 90% 이상. **지표 신뢰도 🟢(§4.4) 유지** |
| **관리자** | 주간/월간 보고를 **자동으로** 만든다 | 주간업무보고서 xlsx를 수기 가공 없이 배포 |
| **공통** | 기관·공간·담당자·설치자산 정보를 일관된 구조로 축적한다 | 자산 연결률 80% 이상, 담당자 이력 단절 없음 |
| **공통** | 노후장비·장애다발장비·지원빈도 높은 기관을 식별해 영업에 활용한다 | 교체대상 후보 목록이 실데이터로 산출됨 |

**v2.0의 강조점:** 위 표의 「성공 판단 기준」은 모두 **숫자로 측정 가능**해야 한다. 측정할 수 없는 목표는 목표가 아니라 희망이다. 이것이 v2.0에서 제2부(지표)를 기능보다 앞에 둔 이유다.

---

## 2. 시스템 성격과 운영 원칙

### 2.1 시스템 정의

본 시스템은 일반적인 계약관리형 CRM이 아니라, **기관 중심의 운영형 고객지원 시스템**이다. 핵심 관리 축은 기관 기준정보, 공간정보, 담당자 및 이력, 설치자산, 수행관계, AS 운영정보, **그리고 계획-실행 기록**이다.

### 2.2 설계 원칙

1. 계약 여부와 무관하게 고객기관 내 운영대상 자산을 관리한다.
2. 모든 주요 엔터티는 이름이 아닌 **고유 ID**로 연결한다.
3. **현재 정보와 이력 정보를 분리**하여 변경 추적이 가능하도록 한다.
4. 설치 위치는 자유입력을 최소화하고 **건물-층-실** 구조로 표준화한다.
5. AS 정보는 설치자산과 연결하여 동일 장비의 반복 장애와 교체주기를 추적한다.
6. 비밀번호 등 민감정보는 평문 저장을 지양하고 암호화 파일 또는 별도 금고 체계와 연결한다.
7. **엔터티를 함부로 신설하지 않는다.** 새 개념은 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 검토한다.

### 2.3 운영 3원칙 [v2 신설]

v2.0의 핵심이다. 이 세 문장이 지켜지지 않으면 아래의 모든 기능 정의는 무의미하다.

> **제1원칙 — 계획 없는 업무는 만들지 않는다.**
> 모든 단위업무는 **예정일**을 가진다. 예정일을 정할 수 없으면 「미정」과 **사유**를 남긴다. 예정일도 사유도 없는 업무는 시스템이 생성을 거부한다.

> **제2원칙 — 실행은 흔적을 남긴다.**
> 조치를 저장하면 **착수 시각 · 소요 시간 · 근무 구분 · 원인 분류**가 함께 남는다. 이 값들은 담당자가 "나중에 채우는" 것이 아니라 조치 저장의 **구성 요소**다.

> **제3원칙 — 측정할 수 없으면 표시하지 않는다.**
> 분모가 없거나 표본이 부족하면 숫자를 만들어 내지 않는다. `0%`가 아니라 `—`로 표시하고, 왜 측정할 수 없는지 알려 준다.

**제1원칙의 근거:** 베타에서 예정일 입력률이 2.6%였다. 예정일이 없으면 ① 실행율의 분모가 없고 ② 지연 판정이 불가능하며 ③ 일일 업무 등록 화면에 올라오지 않아 ④ 담당자가 잊어도 아무도 모른다. 빠짐 방지의 출발점은 예정일이다.

**제2원칙의 근거:** `start_datetime` 3.2%, `time_spent` 0%, `work_place` 0%, `cause_type` 2.8%. 조치 화면에서 이 값들이 **선택**이었기 때문이다. 담당자에게 추가 입력을 요구하는 것이 아니라, **서버가 자동으로 채울 수 있는 것은 자동으로 채우고**(착수 시각), 사람만 아는 것만 필수로 받는다(소요시간·근무구분·원인).

**제3원칙의 근거:** 실행률 분모 0일 때 화면에 `0%`가 떴다. 관리자는 이를 "실행을 안 했다"로 읽는다. 실제로는 "계획을 안 세웠다"이고, 필요한 조치가 정반대다.

---

## 3. 범위 정의

| 구분 | 내용 |
|------|------|
| **포함** | 고객마스터, 공간정보, 담당자/이력, 설치자산, SW상세, 수행관계, 사업(프로젝트), AS 접수·조치·현황, **행정·지원 업무(수집함·다음 행동·조치 이력, §13)**, **계획(일일 업무 등록·미계획 업무함)**, 정기점검 일정·엑셀 캘린더, 통계·보고서, 영업활용 분석, 코드·권한·데이터 관리 |
| **후순위** | 직접 계약관리, 재무/청구, 외부 고객포털, 모바일 앱, 외부 캘린더 연동(구글·애플) |
| **[v2] 범위 축소** | 정기점검 **자동 일정 수립의 이동시간 최적화·17시 제약 역산·지역 클러스터링**은 범위에서 제외한다 (§23.5) |

---

# 제2부 — 관리 지표 [v2 신설·최상위]

> 이 부는 v2.0에서 새로 만들었고, 기능 정의보다 **앞에** 둔다.
> 지표를 먼저 정하고, 그 지표가 채워지도록 화면을 설계한다. 순서를 뒤집으면 베타에서 겪은 일이 반복된다.

## 4. 지표 정의와 수집 책임

### 4.1 관리 지표 3종 (사업팀 목표)

| 지표 | 목표 | 산식 | 대상 |
|------|------|------|------|
| **계획대비 실행율** | **90% 이상** | 계획대로 완료 ÷ 예정 × 100 | AS + 정기점검 + 행정/지원 |
| **접수→방문(조치시작)** | **평균 3일 이내** | AVG(방문일 − 접수일) | AS |
| **접수→조치완료** | **평균 7일 이내** | AVG(완료일 − 접수일) | AS |

**공통 규칙**

- 관리 단위 = **기간**(일/주/월/사용자 지정) × **담당**(팀전체/담당자별) × **사업**(선택 시). 업무별(제품) 필터 병행.
- 대시보드 상단 KPI와 `/review/stats`는 **동일 산식**을 쓴다. 두 화면의 숫자가 다르면 버그다.
- 목표 대비 **충족/미충족**과 **이전 기간 대비 증감**을 함께 표시한다.
- **실행율은 100%를 넘지 않는다.** 계획대로 완료 건수가 예정 건수를 넘으면 예정 건수로 자른다.
- 실행율의 「계획대로」 = **예정일 당일에 완료**. 일정이 바뀐 건(계획수정)은 분자에서 제외한다.
  예: 예정 4건 중 2건이 다른 날 완료 → **50%**

### 4.2 지표별 입력 지점 [v2 신설 — 본 개정의 핵심]

**모든 지표는 자기를 채우는 화면·필드·시점·책임자를 가진다.** 이 표에 없는 값은 지표로 쓰지 않는다.

| 지표/기초값 | DB 필드 | 입력 화면 | 입력 시점 | 책임 | 필수 |
|---|---|---|---|---|---|
| 실행율 **분모** (AS) | `as_receipts.visit_scheduled_date` | AS 접수 (`/do/as/new`) 또는 일일 업무 등록 | **접수 등록 시** | 접수담당 | **필수** (또는 「미정+사유」) |
| 실행율 **분모** (정기점검) | `maintenance_visits.visit_date` | 정기점검 일정 → 방문 추가 | 방문 생성 시 | 관리자·기술담당 | **필수** |
| 실행율 **분모** (행정/지원) | `work_tasks.work_date` (없으면 `due_date`) | 일일 업무 등록 | 업무 생성 시 | 등록자 | **필수** |
| 실행율 **분자** | `as_receipts.complete_datetime` = `visit_scheduled_date` | AS 조치 (`/do/as/{id}/action`) | 결과 저장 시 | **서버 자동** | 자동 |
| **접수→방문** | `as_receipts.start_datetime` | — | **첫 조치 등록 시 `as_processes.process_datetime`을 자동 기록** (비어 있을 때만) | **서버 자동** | 자동 |
| **접수→완료** | `as_receipts.complete_datetime` | AS 조치 | 결과코드 저장 시 | **서버 자동** | 자동 |
| **최장 소요** | `as_processes.time_spent` (분) | AS 조치 → 소요시간 | 조치 저장 시 | 조치자 | **필수** (기본 30분 제시) |
| **내근/외근 비율** | `as_receipts.work_place` | AS 조치 → 근무구분 | 조치 저장 시 | 조치자 | **필수** (`office`/`field`) |
| **원인 분석** | `as_receipts.cause_type` | AS 조치 → 원인분류 | 조치 저장 시 | 조치자 | **필수** |
| **처리 유형** | `as_receipts.process_type` | AS 조치 → 처리유형 | 조치 저장 시 | 조치자 | **필수** |
| **장애빈도**(영업) | `as_receipts.asset_id` | AS 접수 → 설치자산 콤보 | 접수 등록 시 | 접수담당 | **필수** (또는 「미연결+사유」) |
| **노후도**(영업) | `assets.install_date` | 설치자산 등록 | 자산 등록 시 | 관리자·접수담당 | **필수** |
| **교체주기**(영업) | `assets.retire_date` | 설치자산 → 철수 처리 | 철수 시 | 관리자 | 철수 시 필수 |
| **행정 완료일** [v2.1] | `work_tasks.complete_date` | 행정·지원업무 상세 | 완료 처리 시 | **서버 자동** | 자동 |
| **행정 착수** [v2.1] | 첫 `work_activities` 일시 | 진행 이력 추가 | 첫 이력 등록 시 | **서버 자동** | 자동 |
| **실작업 시간** [v2.1] | `work_activities.spent_minutes` | 진행 이력 → 소요시간 | 이력 저장 시 | 작성자 | **필수** |
| **외부 대기** [v2.1] | `work_actions` 요청→회신 구간 | 하위 요청 상태 변경 | 회신 등록 시 | **서버 자동** | 자동 |
| **계획 수립률** | `work_tasks` 생성 건수 ÷ 접수 건수 | (파생) | — | — | — |
| **방문 차수** | `as_processes` 행 수 | (파생) | — | — | — |

**설계 지침**

- **서버가 알 수 있는 값은 사람에게 묻지 않는다.** 착수 시각·완료 시각은 조치 저장 순간에 서버가 채운다. 담당자가 별도로 입력하는 화면을 두지 않는다.
- **사람만 아는 값은 저장 버튼에 묶는다.** 소요시간·근무구분·원인분류는 조치 저장의 필수 항목이다. "나중에 채우기"를 허용하지 않는다. 대신 **기본값을 제시**해 클릭 수를 줄인다(소요 30분, 근무구분은 직전 값 기억).
- **필수를 늘릴 때는 반드시 회피 경로를 준다.** 예정일 「미정+사유」, 자산 「미연결+사유」. 회피 경로 없는 필수는 사용자가 아무 값이나 넣게 만들고, 그러면 지표가 더 나빠진다.

### 4.3 데이터 공백 표시 규칙 [v2 신설]

v1.0은 "데이터가 있을 때 어떻게 집계할지"만 정의했다. v2.0은 **없을 때 무엇을 보여줄지**를 정의한다.

| 상황 | 판정 | 화면 표시 | 금지 |
|------|------|-----------|------|
| **분모 0** (예정 건 없음) | 측정 불가 | **`—`** + 툴팁 「해당 기간 예정 업무 없음」 | `0%` 표시 **금지** |
| **표본 < 5건** | 참고값 | 값 + 회색 배지 **`표본 n건`**. 추세선·전기 대비 증감에서 **제외** | 목표 충족/미충족 판정 금지 |
| **표본 5~19건** | 참고값 | 값 + **`표본 n건`** 배지 | — |
| **표본 ≥ 20건** | 신뢰값 | 값 + 목표 대비 색상(충족 녹색 / 미충족 적색) | — |
| **이관 데이터 포함** | 오염 | 지표에서 **기본 제외**. 상단 토글 「과거 이관 데이터 포함」으로만 합산 | 기본 집계에 포함 금지 |
| **필수값 누락 건 존재** | 경고 | 통계 화면 상단 배너 「입력 누락 n건 — 보기」 → 해당 목록으로 이동 | 조용히 제외 금지 |

**「이관 데이터」 정의** — `as_receipts.data_origin = 'import'`. 과거 엑셀에서 적재된 건으로, 접수일·완료일이 같은 날로 기록되어 소요일 지표를 왜곡한다. 베타 기준 1,124건(95.7%)이 여기 해당한다.

> **[v2 신설 필드]** `as_receipts.data_origin` — `app`(앱 입력) / `import`(엑셀 이관) / `migrated`(구시스템 이관).
> 기존 `import_key IS NOT NULL` 건을 `import`로 일괄 설정한다. 신규 접수는 `app`이 기본값이다.

### 4.4 지표 신뢰도 등급 [v2 신설]

대시보드·통계 화면의 각 KPI 카드에 등급을 함께 표시한다. **숫자만 보고 판단하지 않게 하는 장치**다.

| 등급 | 조건 | 표시 | 의미 |
|------|------|------|------|
| 🟢 **신뢰** | 표본 ≥ 20건 · 이관 데이터 제외 · 분모 존재 · 필수값 누락 0건 | 값 + 목표 대비 색상 | 관리 판단에 사용 가능 |
| 🟡 **참고** | 표본 5~19건 · 또는 필수값 누락 존재 | 값 + 회색 배지 | 추세 참고만. 목표 판정 안 함 |
| ⚪ **측정불가** | 표본 < 5건 · 또는 분모 0 | `—` + 사유 | 판단 금지. **원인 해소가 먼저** |

**운영 규칙:** 주간 회의에서 ⚪ 등급 지표가 있으면 그 지표의 숫자를 논하지 않고, **왜 측정할 수 없는지**를 먼저 다룬다.

---

## 5. 필수 입력 규칙과 데이터 정합성 [v2 신설]

### 5.1 필수 입력 원칙

- 필수 입력은 **저장 시점에 서버에서 검증**한다. 화면 검증만으로는 API 직접 호출·이관 스크립트를 막지 못한다.
- 검증 가능한 규칙은 **DB 제약(CHECK/UNIQUE/NOT NULL)으로도 이중 방어**한다. 애플리케이션 버그가 데이터를 오염시키지 못하게 한다.
- 필수 항목에는 **기본값 또는 회피 경로**를 함께 제공한다.

전체 목록은 **부록 A**, DDL은 **부록 B** 참조.

### 5.2 핵심 제약 5가지

| # | 규칙 | 근거 | 위반 시 |
|---|------|------|---------|
| 1 | **`schedule_confirmed=1`이면 `visit_scheduled_date`가 반드시 있다** | 베타에서 1,141건이 예정일 없이 확정 상태 | 저장 거부 (400) + DB CHECK |
| 2 | **조치 저장 시 `start_datetime`이 비어 있으면 서버가 채운다** | 베타 기록률 3.2% | 서버 자동 (사용자 개입 없음) |
| 3 | **`as_processes.time_spent`는 0보다 크다** | 베타 기록률 0% | 저장 거부 + DB CHECK |
| 4 | **결과코드 `partial`·`revisit_needed`는 방문예정일이 반드시 있다** | v1.0 §8 규정, 미구현 | 저장 거부 |
| 5 | **정기점검 방문은 `날짜 + 기관 + 점검대상`이 유일하다** | v1.0 §17.3.1 규정, 제약 없음 | UNIQUE 인덱스 |

### 5.3 기존 데이터 보정 계획

v2.0 적용 시 **1회 실행**하는 보정 작업이다. 실행 전 백업 필수.

| # | 대상 | 조치 | 예상 건수 |
|---|------|------|-----------|
| B-1 | 예정일 없이 `schedule_confirmed=1` | `schedule_confirmed = 0`으로 변경 | **1,124건** (실측) |
| B-2 | `start_datetime`이 비고 `as_processes`가 있는 건 | 최초 `process_datetime`으로 백필 | **1,128건** (실측) |
| B-3 | `import_key IS NOT NULL` | `data_origin = 'import'` 설정 | **1,124건** |
| B-4 | 나머지 전건 | `data_origin = 'app'` 설정 | **50건** (실측) |
| B-5 | `time_spent` NULL/0 | 이관 건은 NULL 유지(지표 제외), 앱 입력 건은 관리자 확인 후 입력 | 1,172건 |
| B-6 | `maintenance_visits` | `project_id` 컬럼 추가 (§23.3) | 스키마 변경 |

> **주의:** B-1은 「확정」 표시를 1,124건에서 걷어낸다. 담당자에게 사전 공지하고, 보정 직후 **미계획 업무함**(§8)에서 실제 예정일을 채우는 작업을 병행한다. 보정만 하고 방치하면 화면이 비어 보인다.

---

# 제3부 — 메뉴 구조

## 6. 메뉴 구조 [v2 재편]

### 6.1 재편 사유

v1.0의 대메뉴 11개는 **기능별**로 나뉘어 있었다(업무 관리 / 사업관리 / 행정관련업무 / 기준정보 / 자산관리 / AS관리 / 통계 / 분석·영업 / 정기점검 관리 / 관리 / 홈). 이 구조에서는 "계획을 세운다"는 행위가 **업무 관리 > 일일 업무 등록** 안에 숨어 있어, 하루를 시작할 때 어디로 가야 하는지가 드러나지 않는다. 베타에서 계획 화면 사용률이 10% 미만이었던 데에는 메뉴 구조의 책임도 있다.

v2.0은 **업무 흐름 순서**로 재편한다.

### 6.2 대메뉴 6개

```
홈          대시보드 — 역할별 요약 · KPI

계획        오늘·이번 주에 무엇을 할지 정한다
 ├ 일일 업무 등록      일·주·월 시간표에 배치
 ├ 미계획 업무함  ★신설  예정일 없는 업무를 여기서 처리
 ├ 정기점검 일정       연간 계획 · 캘린더/칸반/목록
 └ 일일 업무 회의      전일 실적 · 오늘 예정

실행        정한 것을 한다
 ├ 오늘 내 업무        통합 업무 리스트 · 칸반
 ├ AS 접수·조치        접수 등록 · 조치 · Timeline
 └ 행정·지원업무       등록 · 처리 · 현황

확인        얼마나 됐는지 본다
 ├ 업무처리현황        접수 / 예정업무 / 조치 시간표
 ├ 통계                KPI · 업무분석 · 그래프
 ├ 보고서              주간업무보고서 · 상세통계 엑셀
 └ 교체대상현황        노후·장애다발·타사설치 (영업)

기준정보    업무의 재료
 ├ 고객현황 · 공간관리 · 담당자관리 · 담당자이력
 ├ 설치자산 · SW상세 · 수행관계
 ├ 사업(프로젝트)관리
 └ 점검 사이트 설정

관리        코드관리 · 사용자/권한 · 데이터 관리
```

### 6.3 URL 정책 [중요]

**기존 URL은 그대로 유지한다.** 메뉴의 라벨과 그룹만 재편한다.

베타가 이미 돌고 있고 테스트 60여 개가 URL에 결합되어 있다. URL을 함께 바꾸면 재편의 효용보다 회귀 위험이 크다. 신설 화면만 새 URL을 받는다.

| 화면 | URL | 상태 |
|------|-----|------|
| 대시보드 | `/` | 유지 |
| 일일 업무 등록 | `/workboard/register` | 유지 |
| **미계획 업무함** | `/plan/unplanned` | **신설** |
| 정기점검 일정 | `/maintenance/{계획}` | 유지 |
| 일일 업무 회의 | `/meeting` | 유지 (**메뉴 노출로 전환**) |
| 오늘 내 업무 | `/work` | 유지 |
| 일일 업무 현황(칸반) | `/workboard/kanban`, `/workboard/tasks` | 유지 |
| AS 접수·목록 | `/as`, `/as/{id}` | 유지 |
| AS 조치 | `/as/{id}/action` | 유지 |
| AS 칸반 | `/as/kanban` | 유지 |
| 행정·지원업무 | `/admin-work`, `/admin-work/stats` | 유지 |
| 업무처리현황 | `/work-status` | 유지 |
| 통계 | `/stats`, `/stats/detail` | 유지 |
| **보고서** | `/stats/reports` | **신설** (기존 `weekly-report.xlsx`를 여기로 모음) |
| 교체대상현황 | `/analysis/replacement` | 유지 |
| 기준정보 각 화면 | `/customers`, `/spaces`, `/contacts`, … | 유지 |
| 사업(프로젝트) | `/projects` | 유지 |
| 점검 사이트 설정 | `/maintenance/sites` | 유지 |
| 관리 | `/codes`, `/users`, `/admin/data` | 유지 |

### 6.4 v1.0 → v2.0 메뉴 대응

| v1.0 대메뉴 | v2.0 위치 |
|---|---|
| 홈 | **홈** |
| 업무 관리 > 일일 업무 등록 | **계획** > 일일 업무 등록 |
| 업무 관리 > 일일 업무 현황(칸반·목록) | **실행** > 오늘 내 업무 |
| 업무 관리 > 업무처리현황 | **확인** > 업무처리현황 |
| (메뉴 비노출) 일일 업무 회의 | **계획** > 일일 업무 회의 — **노출로 전환** |
| 사업(프로젝트)관리 | **기준정보** > 사업(프로젝트)관리 |
| 행정관련업무 (등록/처리·현황) | **실행** > 행정·지원업무 |
| 기준정보 (고객·공간·담당자·이력) | **기준정보** |
| 자산관리 (설치자산·SW상세·수행관계) | **기준정보** — 통합 |
| AS관리 (접수·처리·현황·칸반) | **실행** > AS 접수·조치 |
| 통계 | **확인** > 통계 + 보고서 |
| 분석/영업 > 교체대상현황 | **확인** > 교체대상현황 |
| 정기점검 관리 > 정기점검 일정 | **계획** > 정기점검 일정 |
| 정기점검 관리 > 점검 사이트 설정 | **기준정보** > 점검 사이트 설정 |
| 관리 (코드·권한·데이터) | **관리** |
| — | **계획** > 미계획 업무함 ★신설 |

**설계 의도 세 가지**

1. **정기점검 「일정」은 계획으로, 「사이트 설정」은 기준정보로 갈라 놓는다.** 전자는 매달 하는 일이고 후자는 어쩌다 하는 일인데, v1.0에서는 같은 대메뉴에 있었다.
2. **자산관리를 기준정보에 흡수한다.** 설치자산·SW상세·수행관계는 모두 "업무의 재료"이지 그 자체가 업무가 아니다.
3. **일일 업무 회의를 메뉴에 올린다.** v1.0은 URL로만 접근 가능했다(§10.5 "메뉴 비노출"). 계획-실행 사이클의 시작점인데 숨어 있을 이유가 없다.

### 6.5 역할별 메뉴 노출

| 대메뉴 | 관리자 | 접수담당 | 기술담당 | 영업담당 | 열람 |
|--------|:---:|:---:|:---:|:---:|:---:|
| 홈 | ● | ● | ● | ◐ | ◐ |
| 계획 | ● | ● | ● (본인) | ○ | ○ |
| 실행 | ● | ● (접수·배정) | ● | ○ | ○ |
| 확인 | ● | ● | ● (본인) | ◐ (교체대상만) | ◐ |
| 기준정보 | ● (쓰기) | ◐ (읽기) | ◐ (읽기) | ◐ (읽기) | ◐ |
| 관리 | ● | ○ | ○ | ○ | ○ |

● 전체 · ◐ 제한 · ○ 비노출

영업담당·열람사용자는 v1.0과 동일하게 **일시 중단**(로그인 후 「준비 중」) 상태를 유지한다. 위 표는 재개 시 적용할 기준이다.

---

# 제4부 — 계획

> **목적:** 하루/한 주에 무엇을 할지 정하는 곳. 여기서 정해진 것이 실행율의 **분모**가 된다.

## 7. 일일 업무 등록 (시간표 배치)

메뉴 **계획 > 일일 업무 등록** · `/workboard/register?view=day|week|month&date=YYYY-MM-DD&assignee=`

해야 할 업무를 오른쪽 카드 목록에서 골라 왼쪽 시간표에 끌어다 놓아 일정을 잡는다. 단위업무(AS·정기점검·행정·지원)를 일·주·월 시간표에 올리며, 등록 시 **업무 고유번호**(`work_tasks.task_id`)가 부여된다.

### 7.1 기간 단위 3종

| 뷰 | 세로축 | 가로축(열) |
|----|--------|-----------|
| **일일 업무 등록** | 07:00~20:00 · 15분 | 해당 날짜 1개 열 |
| **주간 업무 등록** | 07:00~20:00 · 15분 | 월~일 **7개 열** |
| **월간 업무 등록** | — | **일~토 캘린더 그리드**(날짜 칸에 업무 목록·미배정·더보기) |

**겹침 규칙:** 같은 담당자는 같은 날 시간이 겹치지 않는다(일·주 동일). 자동 배정·수동 배치 모두 담당자별 빈 슬롯을 쓰며, 겹치면 거부한다. 다른 담당자끼리만 가로로 나란히 표시한다.

### 7.2 「+ 일일 업무 등록」 모달

1단계에서 종류를 고른다.

| 종류 | 입력 | 결과 |
|------|------|------|
| **행정 / 지원업무** | 거래처·업무명·설명·**예정일**·소요시간 등 직접 입력 | 행정·지원 업무 생성 |
| **AS 일일업무** | 미배치 AS 접수번호 검색·선택 · **예정일** | AS 업무 생성 후 AS 업무 화면으로 이동 |
| **정기점검 일일업무** | 미배치 정기점검번호 검색·선택 · **예정일** | 점검 업무 생성 후 정기점검 업무 화면으로 이동 |

**[v2 개정] 예정일은 모든 종류에서 필수다.** v1.0은 AS·정기점검에서 예정일을 「선택」으로 두었다. 예정일을 정할 수 없으면 **「미정」 체크 + 사유**를 입력하며, 이 업무는 **미계획 업무함**(§8)으로 들어간다.

### 7.3 오른쪽 카드 3열

아직 시간표에 올리지 않은 업무만 표시한다.

| 열 | 출처 | 카드 표기 |
|----|------|-----------|
| **AS** | 미완료 AS 접수(아직 업무 미생성) | 업무명 `[AS]고객명_접수번호` · 접수번호(텍스트) · 증상 · 예정일 |
| **정기점검** | 정기점검 방문 계획(오늘 −7일 ~ +45일, 미생성) | 업무명 `[점검]고객_점검번호(방문일·제품)` · 점검번호 · 설명 `[제품]고객 정기점검` · 방문예정일. **제품이 다르면 같은 날이어도 별도 카드** |
| **행정관련업무** | 시간 미지정 행정·지원·하위업무 · (구현 후) 당일 다음 행동 | 업무명 · 설명 · 마감일 · 소요분. **수집함(`inbox`)은 분류 전까지 올리지 않거나 「수집함」으로 구분**(§13.11) |

### 7.4 AS·정기점검 → 업무 등록 규칙

| 항목 | AS | 정기점검 |
|------|-----|----------|
| 업무 구분 | `AS` | `정기점검` |
| 업무명 | `[AS]고객명_접수번호` | `[점검]고객명_점검번호` |
| 설명 | 접수 내용(증상) | `[점검제품]고객명 정기점검` |
| 예정일 | 방문예정일 (**필수**) | 방문예정일 (**필수**) |
| 업무 화면 | 예정일·접수내용·**AS접수이동** | 예정일·점검내용·**정기점검등록이동** |
| 원본 번호 | 접수번호·점검번호는 **텍스트 표시만**(카드에서 링크하지 않음) | 동일 |
| 고유번호 | `work_tasks.task_id` 부여 | 동일 |

### 7.5 배치 규칙

- 카드를 **드래그해서 칸에 놓거나**, 카드를 클릭해 선택한 뒤 칸을 클릭해도 된다. **대기열(AS·정기점검)** 을 놓으면 일일 업무 등록 모달이 열리며 접수번호·예정일(드롭 날짜)·업무 배정일·시작·종료(기본 소요 **30분**)가 채워진다(수정 가능). **이미 배치된 카드·행정/지원 대기**는 칸에 바로 재배치된다.
- **소요 예상 시간·조치 시간**은 **분 단위**로 관리한다. 시간표 **칸 표시**만 15분 단위다. 종료는 시작+소요분(기본 30분)이며 **20:00을 넘지 않는다**.
- **이미 등록된 업무** 중 **배정일(`work_date`)은 있고 시간만 비어 있는** 건은 해당 날짜 **07:00부터** 자동 배치될 수 있다. `×`로 배정일을 비운 건·아직 올리지 않은 건은 대기열에 남고 자동으로 다시 올라가지 않는다.
- AS·정기점검 모달 등록 시 **업무(`work_tasks`)로 복사**되고 원본과 연결(`source_type`/`source_id`)된다. **같은 원본은 한 번만** 업무로 만든다.
- **정기점검 일정 ↔ 일일 업무 동기화:** 점검 일정 화면에서 방문일·담당자·점검대상·**사업명**을 바꾸면 연결된 일일업무의 예정일·배정일·담당자·업무명·사업명도 같은 값으로 맞춘다. **방문 예정일을 바꾸면 그 날짜에만 1건으로 표시**하고, 미완료 일일업무도 그 날로 옮긴다. 일일 업무 등록에서 카드를 다른 날로 옮기면 점검 방문일도 따라간다. **완료된 방문의 실적일(`work_date`)은 바꾸지 않는다.** 일일 업무 등록·업무처리현황 진입 시 어긋난 건을 보정한다.
- **미생성 방문:** 아직 일일업무가 없는 **미완료** 정기점검 방문은 화면 진입 시 업무를 만들고 해당 방문일에 올린다(같은 원본 1건). `×`로 내린 뒤에는 행을 유지한 채 배정일만 비우므로 다시 자동 생성·배치하지 않는다.
- 배치된 카드의 `×`를 누르면 배정일·시간이 비워지고 대기열로 돌아간다. **AS·점검도 업무 행은 유지**한다(원본 연결 재사용). 행정/지원은 예정일까지 비운다.
- **버튼 클릭:** 배치된 카드의 **조치** → AS는 조치 화면(`/as/{id}/action`), 정기점검·행정은 업무 화면. **수정** → 일일 업무 수정(`/workboard/tasks/{id}`). 조치 화면의 **접수 보기**는 iframe 팝업. 미배치 카드의 **등록** → 등록 모달.
- **정기점검 카드 색**(팔레트·시간표 공통): 초록=앤로보틱스, 파랑=K-LAS(KLAS), 보라=세종 K-LAS.
- 월간 뷰에서 놓으면 그 **주에서 이번 달에 속하는 첫 날**로 배치된다.
- 상단에서 **기간 이동**(이전/다음/오늘/날짜 선택).
- **담당자 필터:** 제목 옆 드롭다운. 기본값 **팀전체**. 담당자를 고르면 시간표·팔레트를 해당 담당자 건만 표시. 일·주·월·날짜 이동 시에도 선택 유지(`?assignee=`).

**권한:** 관리자·접수담당·기술담당 (등록·조회)

---

## 8. 미계획 업무함 [v2 신설]

메뉴 **계획 > 미계획 업무함** · `/plan/unplanned`

> **이 화면이 v2.0에서 신설된 이유:** 베타에서 예정일 없는 업무가 1,100건 넘게 쌓였는데, 그것들이 **어디에도 보이지 않았다.** 담당자는 잊었고 관리자는 몰랐다. 「빠짐 방지」는 결국 "예정일 없는 업무를 눈에 띄게 만드는 것"이다.

### 8.1 화면 구성

**미계획**이란 아래 중 하나에 해당하는 미완료 업무다.

| 유형 | 판정 조건 | 뱃지 |
|------|-----------|------|
| **예정일 없음** | 미완료 + 예정일(`visit_scheduled_date` / `visit_date` / `work_date`) 비어 있음 | 🔴 `예정없음` |
| **예정일 경과** | 예정일 < 오늘 + **처리 이력 없음** | 🔴 `D+n` |
| **다음 일정 미정** | 예정일 < 오늘 + **처리 이력 있음** + 다음 일정 없음 | 🟡 `방문함` |
| **담당자 미배정** | 담당자 없음 (전체 대상) | 🟠 `미배정` |
| **미정 사유 만료** | 「미정+사유」로 등록된 지 **14일 경과** | 🟡 `재검토` |

### 8.2 동작

- 목록에서 **날짜를 직접 입력해 즉시 배정**할 수 있다(일일 업무 등록으로 넘어가지 않아도 된다).
- **일괄 배정:** 여러 건을 체크해 같은 날짜·담당자로 한 번에 배정한다.
- **미정 처리:** 예정일을 정할 수 없으면 「미정」 + 사유(부품 대기 / 고객 일정 미확정 / 외부 업체 회신 대기 / 기타+직접입력)를 남긴다. 14일 후 자동으로 `재검토` 뱃지가 붙어 다시 올라온다.
- 각 행에서 **원본 화면으로 이동**(AS 조치 / 정기점검 방문 / 업무 상세).
- 기본 필터는 **본인 배정**(기술담당) / **전체**(관리자·접수담당). 상단 토글로 전환.

### 8.3 대시보드 연동

홈 대시보드 KPI 카드에 **「미계획 n건」** 을 추가하고, 클릭 시 이 화면으로 이동한다. **이 숫자가 0에 수렴하는 것이 §2.3 제1원칙의 운영 목표**다.

### 8.4 계획 수립률 [v2 신설 지표]

```
계획 수립률 = (예정일이 있는 업무 건수) ÷ (전체 미완료 업무 건수) × 100
```

- 주간업무보고서와 통계 화면에 **보조 지표**로 표시한다.
- 목표: **95% 이상** (미정 사유 등록분은 분자에 포함)
- 이 지표가 낮으면 실행율 지표는 ⚪ 측정불가로 떨어진다. **선행 지표**로 관리한다.

---

## 9. 정기점검 일정 (계획 관점)

메뉴 **계획 > 정기점검 일정** · `/maintenance/{계획}?view=calendar|kanban|list`

정기점검의 **기준 설정**(사이트·주기·점검대상)은 기준정보에 두고(§23.2), **월별 일정 수립·수정·완료**는 계획에 둔다. 상세는 **§23**을 참조한다. 여기서는 계획 관점의 요구만 적는다.

- 메뉴 진입 시 **현재(또는 최신) 연도 계획 화면**으로 바로 이동한다. 제목 옆 **연도 선택 콤보**로 연도 전환·(관리자) 연도 추가.
- 화면 **상단**에 **「일정 배정안됨」** 을 상시 노출한다. 보고 있는 달의 점검주기에 해당하는데 **아직 방문일이 없는** 사이트×점검대상(KLAS·RFID 각각 1건)을 나열하고, 날짜를 넣어 바로 배정한다.
- **[v2] 「일정 배정안됨」 건수를 대시보드 KPI와 미계획 업무함에 합산한다.** 정기점검도 「계획되지 않은 업무」다.
- 달 선택은 1~12월 탭. 달을 안 고르면 올해 계획이면 이번 달, 지난·다음 해 계획이면 방문이 있는 첫 달을 연다.
- **방문 추가**와 **지난 방문 일괄 완료**는 화면 위 **버튼**으로 두고 팝업에서 입력한다.
- 완료·삭제 후에는 **보던 보기와 달로 되돌아온다.**

---

## 10. 일일 업무 회의

메뉴 **계획 > 일일 업무 회의** · `/meeting?date=YYYY-MM-DD`

> **[v2 개정]** v1.0은 「메뉴 비노출, URL로만 접근」이었다. 계획-실행 사이클의 시작점이므로 **메뉴에 노출**한다.

아침 회의용으로 **전일 실적 목록**과 **오늘(기준일) 예정 목록**을 한 화면에서 본다. 새 테이블 없이 기존 `/work`·통계 집계 로직을 재사용한다.

| 항목 | 내용 |
|------|------|
| 권한 | 관리자·접수·기술담당 (조회). 담당자 필터는 `/work`와 동일(기술담당 기본=본인, `mine=0`이면 팀전체) |
| 기준일 | 기본=오늘. `date`로 이동. 왼쪽=전일(`date-1`), 오른쪽=기준일 |
| 상단 요약 | 통계 일별 열(전일·오늘) 재사용 — 실행률·예정/처리 건수. `/stats?view=day`와 동일 식 |
| 전일 실적 | AS: 통계 완료 계열(`completed`·`closed`·`partial_complete`) + `date(COALESCE(complete_datetime, updated_at))=전일`. 정기점검: `completed=1` + `COALESCE(completed_date, visit_date)=전일`. 행정/지원: `work_tasks` `status=complete` + **`date(complete_date)=전일`** ([v2.1] 종전 `updated_at` 대체). 하부업무 완료·`work_other` 완료 포함 |
| 오늘 예정 | AS: 미완료 + `schedule_confirmed=1` + `visit_scheduled_date=기준일`. 정기점검: `visit_date=기준일` AND `completed=0`. 행정/지원: `work_type IN (admin,support)` + `COALESCE(work_date, due_date)=기준일` + 미완료. 하부업무(`[확인]`/`[재방문]`) 일정 확정 건·`work_other` 미완료 포함 |
| 화면 | 2열 목록(유형 뱃지·기관·담당자·상태·상세 링크) |
| **[v2] 3열 추가** | **미계획 n건** 요약 — 미계획 업무함으로 링크. 회의에서 "빠진 것 없나"를 확인하는 자리 |
| 범위 밖 | 회의 의사록·출석·안건 저장 |

---

# 제5부 — 실행

> **목적:** 계획한 것을 수행하고 흔적을 남기는 곳. 여기서 남는 흔적이 실행율의 **분자**와 소요일 지표가 된다.

## 11. 오늘 내 업무 (통합 업무 리스트)

메뉴 **실행 > 오늘 내 업무** · `/work?bucket=…` · 칸반 `/workboard/kanban` · 목록 `/workboard/tasks`

행마다 접두어 `[AS]` · `[정기점검]` · `[확인]` · `[일반업무]` 를 표시한다. **관리자·접수담당** 화면에서는 각 행 앞에 **담당자명**을 표기한다. 확인·재방문 하부업무·정기점검·기타업무를 AS와 함께 한 목록에서 본다.

**기준일:** 칸반·목록은 **오늘**만 표시한다. 배정일(`work_date`, 없으면 예정일 `due_date`) = 오늘.

**칸반:** 상단에 전체·**할 일**·진행중·검토·완료·긴급 건수 요약. 아래는 **할 일/진행중/검토/완료 4열**. **검토 열 = 보류·이관·검토중·회신 대기**(AS 원본 상태가 보류·이관이면 검토 열). 행정·지원의 **수집함(`inbox`)은 칸반에 올리지 않는다**(§13.5). 카드에는 업무 구분·우선순위 뱃지, 업무명, 사업명, 진행률 막대, 담당자, **D-day**를 표시한다.

**목록:** #, 구분, 업무명, 사업명, 상태, 우선순위, 담당자, 진행률, 마감일. 오늘 건만.

등록·수정은 **관리자·접수담당·기술담당**만 가능하고, 그 외 역할은 조회만 한다.

### 11.1 예정일 경과 판정

일정을 조정해 예정일보다 늦게(또는 이르게) 다녀오는 일이 잦다. 그래서 **예정일 당일 이후에 처리 이력이 있으면 그 예정일은 이미 소화된 것**으로 보고 지연·경과로 세지 않는다.

- 예정일이 지났고 **처리 이력 없음** → `예정일 경과`(빨강, `D+n`)
- 예정일이 지났고 **처리 이력 있음** → `다음 일정 미정`(노랑, `방문함`). 다음 방문일을 잡거나 완료 처리하면 사라진다.

### 11.2 홈 대시보드 KPI 카드

홈은 **로그인한 사용자 역할**에 따라 업무 요약을 보여 준다. 기술담당은 **본인 배정**, 관리자·접수담당은 **전체**(단, 담당자 미배정은 항상 전체). 카드·숫자를 누르면 해당 목록으로 이동한다.

**상단(표시 전용):** **주간 통계** KPI 3종(실행률 90% · 방문 3일 · 완료 7일). 팀전체·금주 기준. `/stats?view=week`와 동일 식. **[v2] 각 KPI에 신뢰도 등급(§4.4)을 함께 표시한다.** 필터·편집 없음.

**KPI 카드 (한 줄):**

| 카드 | 내용 |
|------|------|
| 전체 업무 | 진행중·미완료 업무 건수 |
| 오늘 예정 업무 | 오늘 확정 예정일 업무. **접두어별 건수**(AS·확인·정기점검·일반) |
| 지연 업무 | 확정 예정일이 지났는데 미완료 **· 아직 다녀오지 않은 건만** |
| 오늘 완료 | 오늘 완료 건. **접두어별 건수** |
| 방문 미확정 | 담당자 배정됐으나 **일정 미확정**, 또는 **다녀왔지만 다음 일정을 안 잡은 건** |
| 담당자 미배정 | 담당자 없는 건(**전체 대상**) |
| **미계획** ★신설 | **예정일 없는 미완료 건 + 정기점검 배정안됨.** `/plan/unplanned` 이동 |

---

## 12. AS 접수

메뉴 **실행 > AS 접수·조치** · `/as`, `/as/{id}`

### 12.1 단계별 처리

| 단계 | 핵심 처리 | 주요 입력/출력 |
|------|-----------|----------------|
| 1. 접수 | 기관·설치자산 식별, 증상 등록 | 접수번호, 고객, 설치ID, 요청내용, 긴급도, **예정일** |
| 2. 분류 | 장애유형, 제품구분, 요청주체, 담당 배정 | 담당자, 처리우선순위 |
| 3. 처리 | 원인분석, 현장/원격조치, 부품교체, 협력사 연계 | 처리내역, 원인, 사용부품, **소요시간·근무구분** |
| 4. 완료 | 조치결과 확인 및 완료처리 | 완료일시, 결과코드, 고객확인 |
| 5. 후속 | 재발방지, 교체검토, 영업전환 | 후속조치, 교체대상 여부, 영업메모 |

### 12.2 접수 Case 원칙 [v1.0 유지]

접수 1건은 **하나의 업무 Case**다. 한 번의 조치로 끝나지 않아도 **새 접수로 나누지 않는다.** 재방문·추가조치·대기(보류)·이관은 같은 접수번호 아래 **조치 이력을 누적**한다.

```text
접수 → 1차 조치 → (부품 부족·이관·대기) → 2차 조치 → 완료
         └ 전부 동일 접수번호 · Timeline으로 조회
```

**엔티티는 신설하지 않는다.**

| 개념 | 테이블 | 역할 |
|------|--------|------|
| 접수 Case · 현재 상태 | `as_receipts` | 접수번호, 증상, **현재** 상태·담당자·결과코드 |
| 조치 이력 | `as_processes` | 1:N. Timeline. **각 회차의** 조치내용·결과·다음일정·이관/대기 사유 |
| 다음 일정 | `as_work_items` | `[확인]` · `[재방문]` (`{접수번호}-Wnn`) |
| 관리자 수정 이력 | `data_change_logs` | 변경 전·후 JSON. 완료 건 잠금 해제는 `as_edit_unlocks` |

- **화면 분리를 유지한다.** 접수 상세(`/as/{id}`)에는 접수 내용만 둔다. Timeline·조치 추가는 **조치 화면**(`/as/{id}/action`)에 둔다.
- **이력은 덮어쓰지 않고 추가한다.** 일반 사용자는 과거 접수·조치 내용을 수정·삭제하지 못하고 **새 조치만** 등록한다. 접수의 `result_code`·상태는 **마지막 조치 결과로 갱신**하되, 이전 회차는 `as_processes` 각 행에 남는다.
- **완료 건을 같은 접수번호로 다시 여는 재오픈은 하지 않는다.** 오기재는 관리자 잠금 해제 후 수정(이력 필수). 완료 후 같은 증상이 다시 나오면 **재접수**(새 번호, 원 건 연결).
- **삭제는 일반 사용자에게 제공하지 않는다.** 오접수는 **접수취소**. 테스트·중복 삭제는 **관리자만**.

### 12.3 AS 항목 정의

| 항목 | 설명 | 비고 |
|------|------|------|
| AS번호(접수번호) | 접수 건 고유 ID | `R{YYMM}-{NNN}` (예: `R2607-001`) |
| 처리번호 | 처리 이력 고유 ID | `{접수번호}-P{NN}` |
| 접수일시 | 접수 시간 | 필수. 기본 현재시각, 화면에서 수정 가능 |
| 접수자 | 접수한 담당자 | 로그인 사용자 이름 기본, 수정 가능 |
| 고객ID / 설치ID | 기관 및 자산 식별 | **[v2] 자산 연결 필수** 또는 「미연결+사유」. 자산 선택 시 건물/층/실 표시 |
| 접수채널 | 전화, 이메일, 방문, 협력사요청, 원콜, 제조사요청 | 코드화 |
| 요청자 | 고객 담당자 또는 협력사 담당자 | 기관 담당자 콤보 + 직접입력 |
| 증상내용 | 장애 또는 요청 상세 | 자유기술 |
| 긴급도 / 중요도 | 우선순위 설정 | 상/중/하 |
| 요청주체유형 / 요청주체명 | 업무 흐름 판단 | 고객직접, 제조사, 협력사, 원청, 내부, 원콜. 드롭다운+직접입력 |
| 배정담당자 | 당사 처리 담당자 | 드롭다운(사용자)+직접입력. 조치 화면에서는 **처리담당자**로 표시 |
| 상태 | 접수, 담당자 배정, 진행중, 보류, 이관, 부분완료, 접수취소, 완료, 종료 | §12.4 |
| **예정업무일** | 현장·업무 예정 일자 | **[v2] 필수** (또는 「미정+사유」). 날짜만(YYYY-MM-DD). 수정 권한: 관리자·배정담당자 |
| **일정 확정** | 예정 일정 확정 여부 | **확정 시 상태=진행중.** **확정하려면 예정업무일 필수 — DB CHECK로 강제** |
| 처리시작일시 | 착수 시각 | **[v2] 서버 자동** — 첫 조치 등록 시 `process_datetime`으로 채움 |
| 완료일시 | 완료 시각 | 서버 자동 (결과코드 저장 시) |
| **처리유형** | 원격지원, 현장방문, 부품교체, 설정변경, 문의응대 | **[v2] 조치 저장 시 필수** |
| **근무구분** | **내근 / 외근** | **[v2] 조치 저장 시 필수.** `office`/`field` (`as_receipts.work_place`) |
| **원인분류** | HW고장, SW오류, 네트워크, 사용자문의, 환경문제 | **[v2] 조치 저장 시 필수** |
| **소요시간** | 조치 소요 분 | **[v2] 필수** (`as_processes.time_spent`, 기본 30분 제시) |
| 조치내용 | 실제 수행 작업 | |
| 사용부품 / 교체품 | 소모품 및 교체 장비 이력 | |
| 재발여부 / 재접수여부 | 품질관리 지표 | 재접수=`is_reopen`. 같은 번호 재오픈 없음 |
| 처리결과코드 | `done` · `partial` · `transfer` · `revisit_needed` | §12.5 |
| 타사이관 세부 | 처리 확인 후 완료 / 우리 팀 추가 작업 | `completed` / `waiting` |
| 확인예정일자 | 이관(결과 대기) 시 회신 확인일 | **필수**. 하부업무 `[확인]` 등록 |
| 확인대상자 / 연락처 | 회신·확인 연락 대상 | 조치 결과 대기 시 |
| 방문예정일자 | 재방문필요 시 방문 예정일 | **필수**. 하부업무 `[재방문]` 등록 |
| 재방문 사유 / 준비사항 | 다음 방문 참고 | 선택 |
| 하부업무번호 | 접수 하위 확인·재방문 일정 | `{접수번호}-W{NN}` |
| 보류 후속 / 사유 | 조치 / 이관 / 접수취소 + 대기 사유 | 보류(화면 **대기**) 시 |
| 접수취소일 | 접수취소 일자 | 기본=버튼 누른 날, 수정 가능 |
| 고객확인자 / 확인일시 | 완료 확인 | 기관 담당자 콤보+직접입력 |
| 후속조치 | 정기점검, 교체제안, 교육안내 등 | |
| **접수 사진** | 증상 사진·자료 | **[v2.3 신설]** 최대 10장. 썸네일 미리보기·사진별 메모·자산 사진 승격. §12.9 |
| **데이터 출처** | `app` / `import` / `migrated` | **[v2 신설]** `data_origin`. 지표 집계 시 `import` 기본 제외 |

### 12.4 상태 전이

| 상태 | 코드 | 전이 방식 |
|------|------|-----------|
| 접수 | `received` | **자동** — 접수일자만 있음 |
| 담당자 배정 | `assigned` | **자동** — 배정담당자 있음 |
| 진행중 | `in_progress` | **자동** — 일정 확정 체크 (예정업무일 필수) |
| 보류(화면 「대기」) | `hold` | 수동 |
| 이관 | `transfer` | 수동 |
| 부분완료 | `partial_complete` | 조치 결과에 따라 |
| 접수취소 | `cancelled` | 수동 |
| 완료 | `completed` | 조치 결과 |
| 종료 | `closed` | 수동 |

**부분완료** = 통계는 완료, 운영(목록·일일업무)은 진행중. 운영 의미는 「재조치예정」이다. 화면 뱃지는 **「부분완료」** 를 쓰고, 맥락(재방문 / 확인 대기)은 하부업무로 구분한다. **부모 접수는 통계 완료**에 포함하고, 그때 생성된 **하위업무는 통계 접수·미완료(진행중)** 로 별도 집계한다. 재조치예정 상태를 새로 만들지 않는다.

### 12.5 조치 결과 → 상태·일정

조치 등록 화면의 결과는 아래 다섯 가지로 고른다.

| 화면 문구 | 저장 결과코드 | 접수 상태 | 다음에 생기는 것 |
|-----------|---------------|-----------|------------------|
| 완료 | `done` | 완료 | — |
| 추가조치 필요 | `partial` | 부분완료 | **방문예정일 필수** · `[재방문]` |
| 재방문 필요 | `revisit_needed` | 부분완료 | **방문예정일 필수** · `[재방문]` · 재방문 사유·준비사항(선택) |
| 이관 · 처리 확인 후 완료 | `transfer` + `completed` | 완료 | — |
| 이관 · 우리 팀 추가 작업 | `transfer` + `waiting` | 부분완료 | **확인예정일·대상자·연락처 필수** · `[확인]` · 이후 필요 시 `[재방문]` |
| 대기 | `hold` | 보류 | 대기 사유·후속(조치/이관/접수취소) 필수. 확인 예정일 |

코드명 `TRANSFER_CONFIRM_ONLY` / `TRANSFER_RETURN_REQUIRED` 를 새로 만들지 않고 `transfer_detail` = `completed` / `waiting` 을 쓴다. 임시조치·제조사에스컬레이션은 사용하지 않는다.

**[v2 강제] 결과 저장 시 아래를 서버에서 검증한다.**

1. `partial`·`revisit_needed` → **방문예정일 없으면 저장 거부**
2. `transfer` + `waiting` → **확인예정일·대상자·연락처 없으면 저장 거부**
3. 모든 결과 → **소요시간·근무구분·원인분류·처리유형 없으면 저장 거부**
4. `start_datetime`이 비어 있으면 이 조치의 `process_datetime`으로 **자동 기록**

### 12.6 목록·조치 화면 규칙

- **AS 목록:** 접수번호 행 다음에 하부업무를 **들여쓰기**로 표시(`[확인]`/`[재방문]` · 번호 · 예정일 · 확정여부). 클릭 시 부모 접수 상세로 이동.
- **완료·종료 읽기 전용:** `completed`·`closed` 건은 기본 **조회만** 가능하다. **같은 번호로 재오픈하지 않는다.**
- **관리자 수정 잠금 해제:** 관리자만 「수정」 버튼이 보인다. **로그인 비밀번호와 다른** 수정 전용 비밀번호(`app_settings.as_completed_edit_password_hash`, 기본 `as-edit`, `/users`에서 변경)를 입력하면 해당 건에 대해 **30분** 잠금 해제(`as_edit_unlocks`). 시도는 `as_edit_unlock_log`에 남긴다. 수정 시 `data_change_logs`에 대상·전·후·수정자·일시·사유를 남긴다.
- **처리 이력(Timeline):** 조치 화면에 **시간순 Timeline**으로 표시한다. 각 `as_processes` 행에 조치일시·조치자·내용뿐 아니라 **조치결과·소요시간·근무구분·다음 작업 예정일·대기/이관 사유**를 저장한다. **장비 AS 내역**은 접수 상세에서 팝업 조회(현재 건 제외).
- **접수 상세(`/as/{id}`):** **접수 내용만** 표시. 조치·처리이력·**조치 첨부**·하부업무·워크플로 콤보는 두지 않는다. **[v2.3] 접수 사진(§12.9)은 접수 내용의 일부이므로 접수 상세에 둔다.** **일일 업무 등록** 버튼으로 일일 업무를 만들고, 이미 있으면 **일일 업무**로 이동한다.
- **조치 첨부:** 조치완료보고서·관련 파일. `data/uploads/as/{접수ID}/`에 저장하고 DB(`attachments`, `ref_type='as'`)에는 원본 파일명과 저장 경로만 둔다. 화면에는 **파일명만** 표시(미리보기 없음). **[v2.3] 접수 사진(`ref_type='as_receipt'`)과 구분한다(§12.9.1). 조치 화면 상단에 접수 사진을 읽기 전용 썸네일로 보인다(§12.9.7).**
- **보류(화면 「대기」):** 작업을 포기한 것이 아니라 조건을 기다리는 상태다. 코드·URL은 `hold` 유지(`/as?status=hold`). 콤보로 **조치 / 이관 / 접수취소**를 고른 뒤 대기 사유를 입력한다. **보류 삭제**는 접수를 지우지 않고 **접수 상태**로 되돌린다.
- **이관:** 목록 `/as?status=transfer`(회신 대기), `/as?status=transfer_overdue`(회신확인일 경과). **담당자 미완료(`open`)** 에 이관·보류를 포함한다.
- **접수취소:** `/as?status=cancelled`. 취소일 기본=당일(수정 가능).
- **미완료 업무:** `received`·`assigned`·`in_progress`·`partial_complete`·`hold`·`transfer`.

### 12.7 동일 증상 재접수

완료 뒤 같은 증상이 다시 나오면 끝난 건을 되살리지 않고 **새 접수**를 만들어 둘을 잇는다. 완료 이력이 그대로 남아야 재발 횟수를 셀 수 있다.

| 항목 | 규칙 |
|------|------|
| 대상 | **완료·종료** 건만. 진행중·부분완료·보류·이관은 같은 접수의 조치로 이어간다 |
| 결과 | **새 접수번호**를 받는다. 원 건의 상태·완료일시는 그대로 |
| 승계 | 기관·자산·요청자·증상·긴급도·담당자를 가져오고 화면에서 수정 가능 |
| 입력 | **재접수 사유 필수** + **[v2] 예정일 필수** |
| 표시 | 재접수 건 상단에 **원 완료 건(1차)** 카드, 재접수 사유. 원 건 상세에는 **재접수 목록**. 목록에 **재접수** 뱃지 |
| 이력 | 원 건에 「동일 증상 재접수 — {새 접수번호} (사유: …)」 처리 이력이 남는다 |
| 권한 | 접수 권한(관리자·접수담당) |

### 12.8 AS 칸반

`/as/kanban` — 상태별(접수/배정/진행중/보류/완료) 카드 보드. 목록 화면과 상단 버튼으로 전환.

### 12.9 접수 사진 (증상 사진) [v2.3 신설]

**접수 화면에서 사진을 함께 등록한다.** 전화·이메일 접수 시 고객이 보내 온 증상 사진, 접수 담당자가 현장에서 받은 사진을 접수 시점에 붙인다.

> **왜 접수 시점인가.** 지금은 사진을 조치 화면에서만 올릴 수 있어, 기술담당이 **현장에 가 본 뒤에야** 증상을 시각적으로 확인한다. 접수 시점에 사진이 있으면 배정 전에 증상을 파악해 **원격 처리 가능 여부·필요 부품·소요 시간**을 미리 판단할 수 있다. 접수→방문 3일, 접수→완료 7일 지표(§4.1)에 직접 작용한다.

#### 12.9.1 조치 첨부와의 구분

**두 첨부는 성격이 다르므로 저장과 화면을 분리한다.**

| | 접수 사진 | 조치 첨부 |
|---|---|---|
| 무엇 | **증상**을 보여 주는 사진·자료 | 조치완료보고서·작업 결과물 |
| 언제 | 접수 시점 (조치 전) | 조치 시점 (조치 후) |
| `ref_type` | **`as_receipt`** | `as` (기존 유지) |
| 저장 경로 | `data/uploads/as/{접수ID}/receipt/` | `data/uploads/as/{접수ID}/` |
| 표시 | **썸네일 미리보기** | **파일명만** (기존 규칙 유지) |
| 화면 | 접수 등록·수정·상세 | 조치 화면 |
| 등록 권한 | 관리자·접수담당·배정담당자 | 관리자·기술담당 |

한 통에 섞으면 목록에서 증상 사진과 결과 보고서가 뒤엉켜, 나중에 「접수 당시 상태」를 찾기 어렵다. `ref_type`을 나누면 §12.6의 **「접수 상세에는 접수 내용만」** 원칙도 유지된다 — 사진은 조치 이력이 아니라 **접수 내용의 일부**다.

#### 12.9.2 등록 규칙

| 항목 | 규칙 |
|------|------|
| 화면 | AS 접수 등록(`/as/new`) · 수정(`/as/{id}/edit`) · 상세(`/as/{id}`) |
| 개수 | **최대 10장.** 증상은 각도·부위별로 여러 장이 필요하다(설치자산 3장 슬롯 방식과 다르게 목록형) |
| 형식 | 이미지(jpg · png · webp · heic) + 일반 파일(pdf 등). **이미지만 썸네일**, 나머지는 파일명 표시 |
| 용량 | 원본 1장당 **20MB** 이하. 업로드 후 자동 축소(§12.9.4) |
| 필수 여부 | **선택.** 사진 없이도 접수할 수 있다 |
| 잠금 | 접수 상태가 **완료·종료**면 추가·삭제 불가(§12.6 읽기 전용 규칙 적용). 관리자 잠금 해제 시에만 가능 |
| 이력 | 등록·삭제를 `data_change_logs`에 남긴다(§25.2) |
| 백업 | `data/uploads` 하위이므로 일일 백업에 자동 포함(§25.1) |

#### 12.9.3 표시 — 썸네일 미리보기

**조치 첨부의 「파일명만·미리보기 없음」 규칙은 접수 사진에 적용하지 않는다.** 사진을 눈으로 확인하는 것이 이 기능의 목적이기 때문이다.

```
접수 상세 · 증상 사진 (3)                    [+ 사진 추가]
┌────────┐ ┌────────┐ ┌────────┐
│ [썸네일] │ │ [썸네일] │ │ [썸네일] │
│ 오류 화면│ │ 배선 상태│ │ 라벨    │   ← 사진별 메모
└────────┘ └────────┘ └────────┘
  클릭 → 원본 확대(라이트박스, 좌우 이동)

📎 접수서_스캔.pdf                          ← 이미지 아닌 파일은 파일명만
```

- 썸네일 클릭 시 **원본을 확대**한다(라이트박스). 여러 장이면 좌우로 넘긴다.
- AS **목록**(`/as`)에서는 사진 유무만 **📷 n** 뱃지로 표시한다. 목록에 썸네일을 깔지 않는다.

#### 12.9.4 자동 용량 축소

휴대폰 사진은 장당 5~10MB다. 그대로 쌓이면 디스크와 백업 시간에 부담이 된다.

- 업로드 시 서버에서 **긴 변 1600px** 로 축소해 저장한다. 원본은 보관하지 않는다.
- 1600px면 증상 확인·라벨 판독에 충분하다. 정밀 판독이 필요한 건은 파일(pdf 등)로 따로 올린다.
- **썸네일(긴 변 320px)** 을 별도 생성해 목록 로딩을 가볍게 한다.
- EXIF 회전 정보를 반영해 저장한다(세로로 찍은 사진이 눕지 않도록).
- 이미지가 아닌 파일은 축소하지 않는다.

#### 12.9.5 사진별 메모

각 사진에 짧은 설명을 단다. 예: `오류 화면`, `배선 상태`, `모델 라벨`.

- 저장은 기존 `attachments.keywords` 컬럼을 재사용한다. **새 컬럼을 만들지 않는다.**
- 설치자산 사진의 키워드(§21.1)와 같은 역할이며, 검색·AS 매뉴얼 연동에 함께 쓴다.
- 선택 입력이다.

#### 12.9.6 자산 사진으로 승격

접수 사진 중 **설치 상태를 잘 보여 주는 것**을 해당 설치자산의 사진 슬롯으로 복사한다.

| 항목 | 규칙 |
|------|------|
| 조건 | 접수에 **설치자산이 연결**되어 있을 것(`asset_id`) |
| 동작 | 사진 옆 **「자산 사진으로」** 버튼 → 자산 사진 슬롯(최대 3장, §21.1)에 **복사** |
| 슬롯 | 빈 슬롯에 넣는다. 3장이 다 차 있으면 **교체할 슬롯을 고르게** 한다 |
| 원본 | 접수 사진은 **그대로 남는다**(이동이 아니라 복사) |
| 메모 | 사진별 메모를 자산 사진 키워드로 함께 옮긴다 |
| 권한 | 기준정보 쓰기 권한(관리자·접수담당) |
| 이력 | `data_change_logs`에 남긴다 |

> **이 기능을 넣는 이유.** 설치자산 549건 중 사진이 붙은 것은 일부이고, 현장 사진은 대부분 AS 과정에서 찍힌다. 승격 버튼 하나로 **AS 운영이 기준정보를 채우는 구조**가 된다. §17 교체대상현황과 §9 영업 활용의 기반 데이터가 자연히 쌓인다.

#### 12.9.7 조치 화면에서의 읽기 전용 표시

조치 화면(`/as/{id}/action`) 상단에 **접수 사진을 썸네일로 보여 준다.**

- **읽기 전용**이다. 추가·삭제·메모 수정은 접수 화면에서만 한다.
- 기술담당이 조치 중 증상 사진을 다시 보려고 접수 상세로 오갈 필요를 없앤다.
- 조치 첨부 영역과 **시각적으로 구분**한다(제목: 「접수 시 증상 사진」 / 「조치 첨부」).
- 사진이 없으면 이 영역을 표시하지 않는다.

---

## 13. 행정·지원업무 (GTD 기반) [v2.2 병합·확정]

메뉴 **실행 > 행정·지원업무** · `/admin-work`, `/admin-work/stats`

> **v2.2 병합 안내.** 이 절은 두 갈래를 합친 것이다.
> ① 2026-08-14 v1.0 §10.3.2에 추가된 **GTD 기반 설계**(수집함·다음 행동·조치 이력·상태 9종·구현 단계) — **뼈대로 채택**
> ② v2.1 §13의 **지표 층**(접수일/완료일 컬럼·리드타임 분리·소요시간 필수) — **위에 얹음**
> 두 갈래가 충돌하는 곳은 ①을 따랐고, ①에 없던 것만 ②에서 가져왔다. 차이는 §13.14에 정리한다.

---

### 13.1 왜 이 설계가 필요한가 — 실제 사례

> **충남교육청통합도서관 — 행안부 IRM(정보자원관리) 사이트 평가지표 세부 레벨 작성**
>
> 접수 후 **홈페이지 · 자료관리 · 전자도서관** 세 부문으로 나눠 각각 다른 회사·담당자에게 자료를 요청했다. 부문마다 1~2차례 회신·수정 요청을 주고받았고, 취합 후 사내에서 재검토·수정한 뒤 고객에게 전달하고 완료 확인을 받아 종료했다.
>
> **실제:** 2일 소요 · 외부 3개사 · 왕복 5회
> **업무일지 기록:** **1건 · 1일 · 완료**

**무엇이 왜곡되는가**

| 항목 | 실제 | 기록 | 결과 |
|------|------|------|------|
| 리드타임 | 2일 | 1일 | 접수→완료 7일 지표가 **과소 계상** |
| 계획 대비 | 예정 D → 완료 D+1 | 당일 완료 | 계획수정 0건 · **실행률 과대평가** |
| 투입 공수 | 외부 3사 왕복 5회 + 내부 취합·검토 | 없음 | **최장 소요·업무 부하 분석 불가** |
| 지연 책임 | 업체 회신 대기 구간 존재 | 없음 | **우리가 늦은 건지 상대가 늦은 건지 구분 불가** |
| 진행 가시성 | 어느 부문이 아직 회신 안 왔는지 | 없음 | **빠짐 방지 불가 — 담당자 머릿속에만 있음** |

마지막 두 줄이 핵심이다. 관리 목적이 「업무가 늘어지지 않게」인데 **늘어진 원인이 우리인지 외부인지 구분되지 않으면 개선할 대상을 찾을 수 없다.** 그리고 「빠짐없이」가 목적인데, 세 부문 중 하나가 회신을 안 보내도 시스템은 모른다.

---

### 13.2 적용 범위와 원칙

GTD의 **수집 → 명확화 → 실행 → 검토** 를 **행정업무·지원업무에만** 적용한다.
**AS·정기점검은 §12·§23을 따르며 이 절의 상태·엔티티를 쓰지 않는다.**

| 항목 | 규칙 |
|------|------|
| 대상 | `work_type` = `admin`(행정) · `support`(지원) **만** |
| 제외 | AS 접수·조치, 정기점검 방문. **시간표·통계 산식은 그대로 유지** |
| 사업명 | 기존 `work_projects`(계약·사업 마스터). GTD의 「프로젝트」와 **이름을 섞지 않는다** |
| 복합업무 | 완료까지 행동이 2개 이상인 **행정/지원 업무 1건**. 새 `projects` 테이블을 만들지 않는다 |
| 업무번호 | 기존 `work_tasks.task_id` **유지**. `WK-YYYYMM-NNNN` 으로 재채번하지 않는다 |

> **`tasks` / `projects` / `inbox_items` 테이블을 새로 만들어 기존 업무를 대체하지 않는다.**
> v2.0 §2.2-7(엔터티 신설 최소화)의 적용이다.

---

### 13.3 엔티티

| 개념 | 저장 | 역할 |
|------|------|------|
| **수집함** | `work_tasks.status='inbox'` | 제목만으로 빠른 등록. 아직 분류 전 |
| **업무 Case** | `work_tasks` | 결과·현재 상태·담당·기한. **행정 1건 = 1 Case** |
| **다음 행동** | `work_actions` (업무 1:N) | 지금 할 최소 실행. 상태: 할 일 / 진행중 / 대기 / 완료 / 취소 |
| **조치 이력** | `work_activities` (업무 1:N) | 누가 언제 무엇을 했는지. **덮어쓰지 않고 추가.** AS `as_processes`와 같은 역할 |
| **회신 대기** | 다음 행동 상태 `waiting` + 대기 항목 필드 | 대상·요청내용·회신예정일·**다음 확인일** 필수 |
| **일자 하위업무** | `work_tasks.parent_task_id` | 시간표에 올리는 **그날 칸**. 다음 행동과 별개 |
| 반복 템플릿 | `work_recurrence_rules` | **2단계.** 1단계 범위 밖 |

---

### 13.4 업무 항목

| 항목 | 내용 | 필수 |
|------|------|:---:|
| 업무 구분 | 행정업무 / 지원업무 / AS / 정기점검 | ● |
| **거래처** | 기관 콤보 + 직접입력. 주간업무보고서·통계의 사이트명으로 쓰며 **업무명과 섞지 않는다** | ● |
| 업무명 | 접수 내용 | ● |
| 설명 | 요청 상세 | ○ |
| 사업명 | `work_projects` 콤보. **행정업무는 선택**(해당하는 건만), 지원업무는 **필수** | 지원 ● |
| **접수일** | `receipt_date` — 업무가 들어온 날 **[v2.2 신설]** | ● (기본 오늘) |
| **예정일** | `work_date` (없으면 `due_date`) | ◐ (미정+사유) |
| **완료일** | `complete_date` **[v2.2 신설]** | 완료 시 ● |
| 소요 예상 시간 | 시작·종료 (**분 단위** 관리, 표시만 15분) | ● |
| 상태 | §13.5 (9종) | ● |
| 우선순위 · 담당자 · 태그 · 진행률 | | 담당자 ● |
| **완료 메모** | `complete_note` — 무엇을 어떻게 마무리했는지 | 완료 시 ● |
| 보류 사유 · 재검토일 | `hold_reason` · `review_date` | 보류 시 ● |
| 취소 사유 | `cancel_reason` | 취소 시 ● |

> **[v2.2] `receipt_date` · `complete_date` 신설이 중요한 이유.**
> 현재 `work_tasks`에는 완료일 컬럼이 **없어서** 통계가 `updated_at`을 완료일로 쓴다(§10 전일 실적 참조). 완료 후 담당자나 진행률을 한 번만 고쳐도 **완료일이 그날로 밀린다.** §13.1 사례에서 "2일 걸린 일이 1일로 남는" 문제는 이 컬럼 없이는 구조적으로 해결되지 않는다.

---

### 13.5 상태 (업무 전체)

저장 코드는 아래를 쓰고, 화면 문구는 왼쪽 열을 쓴다.

| 화면 문구 | 저장 코드 | 의미 | 칸반 열 |
|-----------|-----------|------|---------|
| **수집함** | `inbox` | 미분류. 제목만 등록된 상태 | (칸반 제외) |
| **할 일** | `waiting` | 시작 전·실행 예정 | 할 일 |
| 진행중 | `in_progress` | 담당자가 수행 중 | 진행중 |
| **회신 대기** | `waiting_for` | **타인·외부의 회신·승인**을 기다림 | 검토 |
| 보류 | `hold` | 내부 사정으로 중단. **사유·재검토일 필수** | 검토 |
| 이관 | `transfer` | 타 부서·타사로 넘김 | 검토 |
| 검토중 | `review` | 결과물 확인·피드백 | 검토 |
| 완료 | `complete` | 필수 다음 행동·미확인 대기가 없을 때 | 완료 |
| 취소 | `cancelled` | **삭제하지 않고 보존.** 사유 필수 | (목록만) |

**「할 일」과 「회신 대기」를 같은 말로 쓰지 않는다.**

- 「할 일」은 **기존 코드 `waiting`을 그대로 쓴다.** 화면 문구만 「대기」→「할 일」로 바꾼다. 코드를 바꾸면 기존 데이터·테스트가 전부 흔들리므로 **하위호환을 택한다.**
- 「회신 대기」는 신규 코드 `waiting_for` 다.
- 현황 카드의 「대기」는 단계적으로 「할 일」로 바꾸고, 회신 기다림은 「회신 대기」로 둔다.

> **둘을 구분하는 이유.** 둘 다 멈춰 있지만 관리 행동이 다르다. 회신 대기는 **상대를 재촉해야 하는 일**이고(다음 확인일 필수), 할 일은 **우리가 착수하면 되는 일**이다. 합치면 재촉해야 할 건이 대기 더미에 묻힌다.

**전환 시 필수값**

| 전환 | 필수 |
|------|------|
| → 회신 대기(`waiting_for`) | 대기 대상(내부 담당·부서·외부 업체·고객), 요청 내용, **회신 예정일 또는 다음 확인일** |
| → 보류(`hold`) | 사유, **재검토일** |
| → 취소(`cancelled`) | 사유 |
| → 완료(`complete`) | 완료 메모. **§13.9 완료 검증 통과** |

필수 다음 행동이 모두 완료되면 상태를 **진행중**으로 자동 되돌린다.

---

### 13.6 다음 행동 (`work_actions`)

**업무는 결과를 관리하고, 다음 행동은 실행을 관리한다.**

| 항목 | 설명 | 예시 |
|------|------|------|
| 제목 | 지금 할 최소 실행 | `홈페이지 부문 지표 작성 요청` |
| 상태 | 할 일 / 진행중 / **대기(`waiting`)** / 완료 / 취소 | |
| 필수 여부 | 이 행동 없이 업무를 끝낼 수 있는가 | 필수 |
| 예정일 · 기한 | | |
| 담당자 | 우리 쪽 담당 | |
| **대기 대상 구분** | 내부 담당 / 부서 / 외부 업체 / 고객 | 외부 업체 |
| **대기 대상** | 회사명 + 담당자명 | `OO소프트 김OO` |
| **요청 내용** | 무엇을 달라고 했는지 | `IRM 지표 3-1~3-4 세부 레벨 기술` |
| **회신 예정일** | 언제까지 받기로 했는지 | `2026-08-14` |
| **다음 확인일** | 회신이 없을 때 다시 연락할 날 | `2026-08-15` |

**규칙**

- 상태가 **대기(`waiting`)** 이면 대기 대상·요청 내용·회신 예정일 또는 다음 확인일이 **필수**다.
- **며칠에 걸친 일도 새 업무로 나누지 않는다.** 같은 행정 건에 다음 행동·조치 이력을 누적한다.
- **다음 행동이 없는 진행 중 업무**는 주간 검토에서 「다음 행동 미지정」으로 경고한다.
- 회신 예정일이 지나고 회신이 없으면 **미계획 업무함(§8)** 과 대시보드 「지연」에 잡힌다.
- 상위 업무 진행률 = `완료 다음 행동 수 ÷ 필수 다음 행동 수` (자동 계산, 수동 입력도 허용).

---

### 13.7 조치 이력 (`work_activities`)

AS의 `as_processes` Timeline과 같은 역할이다.

| 항목 | 설명 |
|------|------|
| 이력 유형 | **작성 · 수정 · 검토 요청 · 회신 · 발송 · 독촉 · 완료** 외 |
| 내용 | 무엇을 했는지 / 주고받았는지 |
| 작성자 | 기록한 사람 |
| **소요 시간(분)** | `spent_minutes` — 이 행위에 실제로 쓴 시간 **[v2.2 필수화]** |
| 일시 | 자동 |
| 연결 | 특정 다음 행동에 속하면 `action_id` |
| 첨부 | 받은 자료·보낸 자료 |

**규칙**

- **일반 사용자는 과거 이력을 수정·삭제하지 못하고 새 이력만 추가한다.** 관리자 수정은 `data_change_logs`에 남는다(§25.2).
- **소요 시간은 우리가 실제로 일한 시간만 적는다.** 상대 회신을 기다린 시간은 여기 넣지 않는다(§13.10에서 따로 계산).
- 업무를 완료 처리하면 완료 메모가 **완료 이력**으로 자동 기록된다.

**§13.1 사례를 이 구조로 옮기면**

```
[업무] 충남교육청통합도서관 — 행안부 IRM 평가지표 세부 레벨 작성
       접수 08-13 · 예정 08-13 · 완료 08-14 · 리드타임 2일

├ [다음 행동] 홈페이지 부문 요청 → OO소프트 김OO    회신예정 08-13
│   08-13 요청(15분) → 08-13 회신 → 08-14 수정요청(10분) → 08-14 재회신  ✔
├ [다음 행동] 자료관리 부문 요청 → △△시스템 이OO   회신예정 08-13
│   08-13 요청(10분) → 08-14 회신  ✔
├ [다음 행동] 전자도서관 부문 요청 → □□정보 박OO   회신예정 08-13
│   08-13 요청(10분) → 08-13 회신 → 08-13 수정요청(10분) → 08-14 재회신  ✔
├ 08-14 내부검토·취합·수정 (90분)
├ 08-14 고객 전달 (20분)
└ 08-14 고객 완료 확인 → 종료

실작업 155분 · 외부대기 약 1일 · 왕복 5회 · 외부 3사
```

---

### 13.8 다음 행동 vs 일자 하위업무 vs 시간표

셋을 섞으면 설계가 무너진다. **일자 하위업무(`parent_task_id`)는 시간표 배치용이며, GTD의 다음 행동과 섞지 않는다. 기존 하위업무를 다음 행동으로 자동 변환하지 않는다.**

| | 일자 하위업무 (`parent_task_id`) | 다음 행동 (`work_actions`) |
|---|---|---|
| 무엇 | **우리가** 여러 날에 나눠 할 일 | 업무를 끝내기 위한 **실행 단위** |
| 시간표 | **올린다** (그날 칸) | 예정일이 그날인 것만 올린다 |
| 완료 | 각자 완료 | 완료 / 대기 회신 확인 |
| 예시 | 3일간 매일 2시간 문서 작성 | 3개사에 부문별 자료 요청 |

- **오늘 시간표에 올리는 것**은 그날 실행할 **다음 행동**(예정일=그날) 또는 **일자 하위업무**다.
- **부모 업무 전체를 매일 복제하지 않는다.**
- 상태 `waiting`(대기 회신)인 다음 행동은 시간표에 올리지 않는다. 우리가 하는 일이 아니라 기다리는 일이다. 대신 **다음 확인일**이 오면 그날 할 일로 표시한다.

---

### 13.9 완료 검증

- **미완료 필수 다음 행동** 또는 **미확인 회신 대기**가 있으면 일반 사용자는 완료할 수 없다.
- **관리자는 사유를 남기고 강제 완료**할 수 있다. 사유는 조치 이력과 `data_change_logs`에 남는다.
- 완료 시 **최종 조치 내용(완료 메모) 필수**.
- 완료 처리 시 **`complete_date`를 서버가 기록**한다(§13.4).

> 이것이 「빠짐 방지」의 실질이다. §13.1 사례에서 세 부문 중 하나가 회신하지 않았다면 업무가 완료되지 않는다.

---

### 13.10 지표 반영 [v2.2 — 이 절이 v1.0 §10.3.2에 없던 층]

| 지표 | 행정·지원업무 산식 |
|------|-------------------|
| 접수 | **`receipt_date`** 기준 |
| 예정 | `work_date` (없으면 `due_date`) |
| 방문(착수) | **첫 조치 이력의 일시** — AS의 `start_datetime`에 대응 |
| 처리(완료) | **`complete_date`** — 종전 `updated_at` 대체 |
| **총 리드타임** | `complete_date − receipt_date` |
| **실작업 시간** | `work_activities.spent_minutes` 합 |
| **외부 대기 시간** | 다음 행동의 `요청 이력 → 회신 이력` 구간 합. **구간이 겹치면 한 번만** 센다 |
| 실행률 | 예정일 당일 완료 여부 — AS·정기점검과 동일 산식(§15.3) |

**[v2.2 신설 지표 — 대기 분리]**

```
총 리드타임 = 실작업 시간 + 외부 대기 시간 + 내부 유휴
```

통계 화면 「업무 분석」(§15.5)에 **평균 리드타임 · 그중 외부 대기 비중**을 표시한다.

> **이 지표가 필요한 이유.** 접수→완료 7일을 넘겼을 때, 우리가 늦은 것인지 업체 회신이 늦은 것인지가 나뉘어야 조치가 달라진다. 전자는 인력·일정 문제이고 후자는 **회신 예정일 관리·업체 커뮤니케이션** 문제다. §13.1 사례의 2일 중 상당 부분이 외부 대기였다면 그건 우리 팀의 지연이 아니다.
>
> 구간 중복 제거가 중요하다. 3개사에 동시에 요청한 하루를 3일로 세면 안 된다.

**주간업무보고서 반영(§16.1)** — 통계 시트에 **평균 리드타임**과 **외부 대기 비중**을 행으로 추가한다. 「회신 대기」 건은 **미계획건 시트**에 대기 대상·회신 예정일과 함께 싣는다.

---

### 13.11 화면

| 화면 | URL | 내용 |
|------|-----|------|
| 수집함·분류·목록 | `/admin-work` | 수집함(제목만 빠른 등록) · 분류 · 목록. 상태·담당자·사업·거래처 필터. 행에 **회신 대기 n건** 뱃지 |
| 현황 | `/admin-work/stats` | **수집함 · 할 일 · 진행중 · 회신 대기 · 보류 · 이관 · 완료 · 오늘** 건수 카드 + 목록. 회신 대기 카드는 **기한 경과 건수** 별도 표기 |
| 업무 상세 | `/workboard/tasks/{id}` | **다음 행동 · 조치 Timeline** |
| 칸반·목록 | `/workboard/kanban` · `/workboard/tasks` | §11 |

**업무 상세 레이아웃**

```
┌ 업무 헤더 ──────────────────────────────────────┐
│ 거래처 · 업무명 · 사업명 · 담당자                 │
│ 접수 08-13 │ 예정 08-13 │ 완료 08-14 │ 리드타임 2일│
│ 상태 [회신 대기 ▾]   진행률 ██████░░ 3/3 완료     │
└────────────────────────────────────────────────┘
┌ 다음 행동 ─────────────────── [+ 행동 추가] ──┐
│ ✔ 홈페이지 부문 요청   OO소프트 김OO  회신 08-13 │
│ ✔ 자료관리 부문 요청   △△시스템 이OO 회신 08-13 │
│ ⏳ 전자도서관 부문 요청 □□정보 박OO   회신 08-13🔴│
│    └ 다음 확인일 08-15                          │
└────────────────────────────────────────────────┘
┌ 조치 이력 ─────────────────── [+ 이력 추가] ──┐
│ 08-14 14:20 완료      고객 완료 확인 회신 받음   │
│ 08-14 13:40 발송      최종본 메일 발송    20분   │
│ 08-14 11:00 작성      3사 자료 취합·수정  90분   │
│ 08-14 09:30 회신      □□정보 수정본 수신        │
│ ...                                             │
└────────────────────────────────────────────────┘
```

- **완료 버튼**은 §13.9 검증을 통과해야 활성화된다.
- 「회신 예정일 경과」 행동은 🔴 로 표시하고 **미계획 업무함(§8)** 과 대시보드 「지연」에 함께 잡힌다.
- 수집함(`inbox`) 건은 분류 전까지 **일일 업무 등록 팔레트에 올리지 않거나 「수집함」으로 구분**해 표시한다(§7.3).

---

### 13.12 구현 범위

**1단계 (이 절을 구현할 때)**

수집함 · 상태 세분화(`inbox`·`waiting_for`·`cancelled`) · 다음 행동 · 조치 이력 · 회신 대기·보류 필수값 · 완료 검증 · 오늘 할 일·기한 초과·수집함 현황 · **기존 `task_id` 보존 이관**
**[v2.2 추가]** `receipt_date`·`complete_date` 컬럼 · 통계 완료 기준 교체 · 소요 시간 필수화 · 리드타임/외부대기 지표

**2단계**

반복업무 템플릿(`work_recurrence_rules`) · 문서 버전 · 알림 · 주간 검토 전용 화면 · 댓글·참조자

**하지 않음**

- 전자결재·메일 서버 구축
- 행정 상태를 AS에 적용
- 사업 마스터(`work_projects`)를 GTD 프로젝트로 변경
- 업무번호 재채번
- `tasks`/`projects`/`inbox_items` 테이블로 기존 업무 대체

---

### 13.13 구현 상태 [2026-08-14 확인]

이 기능은 **착수되어 있으나 미완성이며, 현재 소스가 빌드되지 않는다.**

| 구성 요소 | 상태 |
|-----------|------|
| `work_actions` 테이블 DDL (`db.go`) | ✅ 작성됨 — 대기 대상·요청내용·회신예정일·다음확인일 포함 |
| `work_activities` 테이블 DDL | ✅ 작성됨 — 유형·내용·작성자·`spent_minutes` 포함 |
| `work_tasks` 확장 컬럼 DDL | ✅ `hold_reason`·`review_date`·`cancel_reason`·`wait_party_kind`·`wait_party`·`wait_request`·`reply_due_date`·`next_check_date`·`complete_note` |
| **`work_tasks.receipt_date` · `complete_date`** | ❌ **없음** — §13.4·§13.10의 핵심 누락 |
| `model.WorkAction` / `WorkActivity` 타입 | ❌ 미정의 |
| `WBTaskInbox` / `WBTaskWaitingFor` 상수 | ❌ 미정의 (`model/workboard.go:184,186`이 참조만 함) |
| `IsAdminGTDTask` / `AdminGTDErr` / `applyAdminGTDForm` | ❌ 미정의 (`admin_work.go`가 호출) |
| `WBRepo.CreateAction` / `CreateActivity` | ❌ 미정의 (`admin_work.go:145,155`가 호출) |
| `WorkboardHandler.CreateAction` / `UpdateAction` / `CreateActivity` | ❌ 미정의 (`main.go:268~270`이 라우트 등록) |
| 화면 (다음 행동·조치 이력 UI) | ❌ 없음 |

> **`go build ./...` 이 실패합니다.** 라우트·호출부는 있는데 타입·리포지토리·핸들러가 없습니다. 배포는커녕 컴파일이 안 되는 상태이므로 **다음 배포 전에 반드시 완성하거나 되돌려야 합니다.**

**완성 순서**

1. `model`에 `WorkAction`·`WorkActivity` 타입, `WBTaskInbox`·`WBTaskWaitingFor`·`WBTaskCancelled` 상수, `IsAdminGTDTask`·`AdminGTDErr` 정의
2. `work_tasks.receipt_date`·`complete_date` 컬럼 추가 (§13.4)
3. `WBRepo`에 `CreateAction`·`UpdateAction`·`ListActions`·`CreateActivity`·`ListActivities` 구현
4. `WorkboardHandler` 대응 핸들러 + `task_show.html`에 다음 행동·조치 이력 UI
5. 완료 검증(§13.9) + 상태 전환 필수값(§13.5) + 자동 복귀
6. 지표 산식 반영(§13.10) — `updated_at` → `complete_date` 교체

---

### 13.14 v1.0 §10.3.2 대비 변경·추가 [v2.2]

v1.0에 추가하신 GTD 설계를 **뼈대로 채택**했다. 아래만 다르거나 새로 얹었다.

| 구분 | 내용 | 이유 |
|------|------|------|
| **추가** | `receipt_date` · `complete_date` 컬럼 (§13.4) | v1.0 §10.3.2에 완료일 개념이 없다. 현재 통계는 `updated_at`을 완료일로 써서 **완료 후 아무 필드나 고치면 소요일이 밀린다.** §13.1 사례의 핵심 문제 |
| **추가** | 지표 반영 절 (§13.10) | 접수/예정/착수/처리 기준이 정의돼 있지 않았다. 지표가 정의 없이 굴러가면 화면마다 숫자가 달라진다 |
| **추가** | 리드타임 = 실작업 + 외부대기 분리 | 「7일 초과」가 우리 탓인지 업체 탓인지 구분해야 조치가 달라진다 |
| **추가** | `spent_minutes` **필수화** | v1.0은 필드만 정의. AS의 `time_spent`가 필드는 있는데 입력란이 없어 1,172건 전부 0인 전례가 있다 |
| **추가** | 구현 상태 표 (§13.13) | 현재 빌드가 깨져 있다는 사실이 문서에 없었다 |
| **추가** | IRM 사례 (§13.1) | 설계 근거를 문서에 고정 |
| **유지** | 상태 9종·「할 일」=`waiting` 하위호환 | v1.0 판단이 옳다. 코드를 바꾸면 기존 데이터·테스트가 흔들린다 |
| **유지** | 다음 행동 ↔ 일자 하위업무 분리 | v1.0·v2.1 양쪽 결론이 같다 |
| **정정** | v2.1의 「하위 요청」 → **「다음 행동」** | v2.1은 외부 요청 전용으로 좁게 봤다. v1.0의 「다음 행동」이 더 일반적이고 옳다. 외부 요청은 그중 상태가 `waiting`인 경우다 |
| **정정** | v2.1의 상태 6종 → **9종** | v2.1에 `inbox`·`review`·`cancelled`가 빠져 있었다 |
| **정정** | 행정업무 사업명 「선택 없이」 → **「선택」** | v1.0 개정본을 따른다 |

---

# 제6부 — 확인

> **목적:** 계획과 실행의 차이를 본다. 여기서 나오는 숫자는 제2부의 지표 정의와 **완전히 동일한 산식**을 쓴다.

## 14. 업무처리현황

메뉴 **확인 > 업무처리현황** · `/work-status?view=day|week|month&date=&assignee=&project=&kind=action|receipt|planned`

**일일 업무 등록(§7)과 동일한 화면 구성.** 사업 필터 옆에 **접수 / 예정업무 / 조치** 전환.

| 항목 | 내용 |
|------|------|
| 보기 | **일일·주간·월간** 시간표(07:00~20:00 · 15분). 기본=일일 |
| 필터 | **팀전체/담당자**, **사업(프로젝트)**, **접수\|예정업무\|조치**(`kind`) |
| 조치(기본) | 기간 안 완료 실적. AS=`completed`·`closed`·`partial_complete`, 정기점검=`completed=1`, 행정/지원=`status=complete` |
| 예정업무 | 기간 안 **아직 완료되지 않은** 업무. 일일업무로 올린 건 + 아직 안 올린 AS 방문예정·정기점검 방문. 완료·종료 제외 |
| 접수 | 기간 안 **신규 접수** AS(`date(receipt_datetime)`). 접수 시각을 시간표에 표시(15분 반올림, 업무시간 밖은 09:00) |
| 카드 | **조치·수정·배치 없음** — 상태 뱃지(접수/예정·진행중/완료·부분완료·방문 완료). 담당자 색은 배정자 순번 고정 팔레트 |
| 권한 | 관리자·접수·기술담당 (조회) |

---

## 15. 통계

메뉴 **확인 > 통계** · `/stats` (KPI·업무분석·그래프·기간 요약) · `/stats/detail` (상세·엑셀)

모든 통계는 **관리 단위**로 집계한다. 관리 단위 = **기간**(일별/주별/월별/사용자 지정) × **담당**(팀전체/담당자별) × **사업**(선택 시). 업무별(제품) 필터는 병행.

### 15.1 KPI 영역

- **[v2]** 각 KPI 카드에 **신뢰도 등급(§4.4)** 과 **표본 건수**를 함께 표시한다.
- **[v2]** 상단에 **「과거 이관 데이터 포함」 토글**(기본 꺼짐). 켜면 `data_origin='import'` 건이 합산되고, 카드에 「이관 데이터 포함」 경고 배지가 붙는다.
- **[v2]** 필수값 누락 건이 있으면 상단에 **「입력 누락 n건 — 보기」** 배너를 띄우고, 클릭 시 해당 목록으로 이동한다.
- **[v2]** **계획 수립률**(§8.4)을 KPI 카드에 추가한다. 실행율의 **선행 지표**이므로 실행율 카드 바로 왼쪽에 둔다.

### 15.2 기간 요약 표

| 구분 | 내용 |
|------|------|
| 표시 형태 | **숫자(KPI 카드)** · **업무 분석** · **막대 그래프**(일자별 접수·조치중·완료) · **선 그래프**(실행률 추이) · **전·현·다음** 표 |
| 요약 표 | **예정업무 / 접수 / 처리** · **계획수정** · **계획대비 실행률(%)** 을 AS·정기점검·행정/지원으로 표시 |
| 열별 표시 | **이미 시작한 기간**(시작일 ≤ 오늘): 예정·접수·처리·계획수정·실행률. **아직 시작 전**: **예정업무 수만**. 날짜를 과거로 옮기면 그 기준의 「오늘/금주/당월」도 실적 표시 |
| 일일 | 열 = **전일 / 오늘 / 익일** · 차트 = 최근 14일 |
| 주간 | 열 = **전주 / 금주 / 차주** (월~일) · 차트 = 전전주·전주·이번주 |
| 월간 | 열 = **전월 / 당월 / 익월** · 차트 = 전전월·전월·이번달 |
| 기간 지정 | 시작일~종료일(포함). 차트는 구간 길이에 따라 일/주/월 버킷. 동일 길이 이전/다음 구간 비교 |

### 15.3 집계 기준

| 구분 | 기준 |
|------|------|
| 예정 | AS=방문예정일 · 정기점검=방문일(예약) · 행정/지원=배정일(없으면 예정일) |
| 접수 | AS=접수일시 · 정기점검=그 기간에 방문 예약된 건(`visit_date`) · 행정/지원=예정과 동일 |
| 방문(조치시작) | AS=`start_datetime`(없으면 방문예정일) · 정기점검=방문일 |
| 처리 | AS=완료·종료(완료일시) · 정기점검=조치 완료(`completed=1`, 완료일) · 행정/지원=완료 상태 |
| 계획수정 | 예정일과 완료일(또는 배정일)이 다른 건 |
| 실행률 | **계획대로 완료 ÷ 예정 × 100.** 일정 변경된 건은 분자에서 제외. **100% 초과 없음.** 지난 일은 `daily_meeting_stats`에 저장 |

### 15.4 정기점검 집계

| 항목 | 규칙 |
|------|------|
| 단위 | **방문 건수.** 한 사이트에 KLAS와 RFID자동화가 모두 있으면 **2건** |
| 월 목표 | 점검 사이트 설정의 **점검주기**로 그 달에 도래하는 사이트 × 점검대상(KLAS 1 + RFID 1). 둘 다 미지정이면 1건. **별도 집계 테이블 없이** `maintenance_site_config`·`maintenance_visits`로 산출 |
| 누적·남은 | **누적** = 그 달 1일 ~ 해당 열 종료일까지 조치 완료 건. **남은** = 월 목표 − 누적(0 미만이면 0). 주가 월을 걸치면 **기간 시작일(월요일)이 속한 달**을 월 목표로 한다 |
| 예정 표시 | 일·주·월 모두 예정 칸에 **그 기간 방문 예약 건수**. **주간(및 일간)** 예정 칸에는 이어서 **`(누적/남은)`** 을 붙인다. 예: 월 목표 75, 1주 10곳 완료 → `10 (10/65)`, 2주 11곳 완료 → `11 (21/54)` |

### 15.5 업무 분석

**AS + 정기점검 + 행정/지원 합산.** 선택 기간의 **접수 · 방문(조치시작) · 완료 · 업무 이관(이월)** 을 유형별로도 표시한다. 이관 = 상태 「이관」이 아니라 **이전 기간에서 미완료로 넘어온 수(Carry-in)** 와 **기간 종료 시 다음으로 넘긴 수(Carry-out)**.

| 항목 | 내용 |
|------|------|
| 일자 목록 | 관리 단위 기간에 걸친 건의 **접수일 / 방문일 / 조치완료일** |
| 최장 소요 | 기간 내 소요시간(`as_processes.time_spent` 합, 없으면 조치시작→완료). 표시 건수: 기간 접수 10건 미만=1건, 10건 이상=2건, 20건 이상=3건. **시간 기준**: 소요 1시간 초과 건 목록 |
| 방문 차수 | `as_processes` 건수(없으면 조치시작 있으면 1차). 정기점검은 1차. **1차 이상**인 건을 차수 많은 순 |
| 긴급·중요 상 | **긴급도=상 그리고 중요도=상** 인 AS 건 |
| **[v2] 내근/외근 비율** | `work_place` 기준. 기간별 외근 비율 추이 |
| **[v2] 원인 분류 분포** | `cause_type` 기준. 상위 원인 5종 + 전기 대비 증감 |

### 15.6 상세 통계 (`/stats/detail`)

| 항목 | 내용 |
|------|------|
| 종류 | 업무진행 / 완료 / 접수 / 지연 |
| 범위 | 팀전체 · 담당자별 |
| 기간 | 일 · 주 · 월 · 분기 · 기간지정 |
| 집계 기준일 | 접수업무·지연·업무진행 → **접수일시**. 완료업무 → **완료일시**. 지연 = 미완료이면서 `접수일+3일` 초과 |
| 열 | 제품분류, 제품모델명, 업무형태, 접수형태, 거래처명, 수행담당자, 접수내용, 처리내용, 처리상태 |
| 엑셀 | 선택한 통계종류·기간·범위 기준 |

---

## 16. 보고서 [v2 분리]

메뉴 **확인 > 보고서** · `/stats/reports`

v1.0은 주간업무보고서를 통계 화면 안에 두었다. v2.0은 **정기 산출물**을 한곳에 모아 보고 주기별로 접근하게 한다.

### 16.1 주간업무보고서 (`/stats/weekly-report.xlsx`) [v2.4 전면 개정]

**시트는 2개다.** 통계와 전체 리스트.

> **v2.4 개정 사유.** 종전 §16.1은 시트 4개(통계·업무리스트·진행중건·미계획건)에, 통계 시트는 **담당자 드롭다운으로 한 사람씩** 보는 구조였다. 관리자가 팀 전체를 한눈에 비교할 수 없고, **일일 처리량·이월**처럼 실제로 묻는 숫자가 빠져 있었다. 주간 보고의 목적은 「이번 주에 무슨 일이 있었고, 누가 얼마나 했고, 무엇이 밀렸는가」를 한 장에 담는 것이다.

**파일명:** `YYYYMMDD_주간업무보고.xlsx` (기준 주의 금요일 또는 다운로드일)
**기간:** 월요일 ~ 일요일. 기본은 이번 주, 화면에서 주차 선택 가능
**대상:** AS + 정기점검 + 행정/지원 **합산**
**이관 데이터:** `data_origin='import'` 건은 **제외**(§4.3). 표 상단에 명시한다

---

#### 시트 1 — 통계

**행 구성:** 첫 행에 **사업팀 전체**, 그 아래 **담당자별**로 한 사람당 한 행.

| 열 | 정의 |
|----|------|
| 구분 | `사업팀 전체` / 담당자명 |
| **계획대비 실행률** | 계획대로 완료 ÷ 예정 × 100 (§4.1과 동일 산식). 분모 0이면 `—` (§4.3) |
| **접수 건수** | **본인에게 배정된 업무 수.** 접수자가 아니라 **배정담당자** 기준 |
| **완료 건수** | 그 주에 완료 처리된 건수 |
| **일일 평균** | 완료 건수 ÷ **워킹데이 수** (소수 1자리) |
| **일일 최대** | 하루에 가장 많이 완료한 건수 + **그 날짜** |
| **이월 건수** | **주말 시점에 완료되지 않아 다음 주로 넘어가는 미완료 건수** |
| 진행중 | 미완료 건 중 진행 상태인 건수 |
| 미계획 | 예정일이 없는 미완료 건 + 정기점검 배정안됨 (§8) |

```
[통계]   2026-08-10 ~ 2026-08-16 (워킹데이 5일) · 과거 이관 데이터 제외

구분          실행률   접수   완료   일평균   일최대      이월   진행중  미계획
사업팀 전체     78%     23     18     3.6    7 (08-12)    5      9      3
──────────────────────────────────────────────────────────────────────
최혜경          82%      9      8     1.6    3 (08-12)    1      3      1
양기헌          75%      8      6     1.2    3 (08-11)    2      4      0
최혜영          70%      6      4     0.8    2 (08-12)    2      2      2
```

**워킹데이 정의** — 기간 내 **주말·공휴일을 뺀 날 수**다. 공휴일은 `holidays` 테이블(§23.6)을 쓴다. 미등록 연도면 주말만 빼고, 시트에 「공휴일 미등록」 주석을 남긴다.

**이월 건수 정의** — 기간 종료일(일요일) 24시 기준으로 **완료되지 않은 건**이다. 이전 주에서 넘어온 건도 아직 미완료면 포함한다. 「지금 누가 얼마나 밀려 있는가」를 보는 숫자다.

**함께 넣는 행**

- **접수→방문 평균 일수**(목표 3일) · **접수→완료 평균 일수**(목표 7일) — 각 담당자별
- **계획 수립률**(§8.4)
- 각 지표 옆에 **신뢰도 등급**(🟢/🟡/⚪)과 표본 건수 (§4.4)
- **정기점검 월 진행** — 월목표 · 접수(예약) · 방문(완료) · 누적 · 남은

**표시 규칙**

- 담당자 행은 **완료 건수 내림차순**. 미배정 건은 맨 아래 `(미배정)` 행으로 모은다.
- 목표 미달은 **빨강**, 충족은 **초록**. 측정 불가는 회색 `—`.
- 담당자 드롭다운은 **없앤다.** 전원이 한 표에 보여야 비교가 된다.

---

#### 시트 2 — 전체 리스트

**그 주에 무슨 일이든 일어난 건을 모두 담는다.** 접수·조치·완료·방문 중 **하나라도 그 기간에 발생**했으면 싣는다.

| 열 | 내용 |
|----|------|
| **구분** | `접수` / `조치` / `완료` / `점검` / `행정` |
| 업무번호 | `R2608-051` · `MV-0142` · `WT-137` |
| **발생일** | 그 구분에 해당하는 날짜 |
| 유형 | AS / 정기점검 / 행정 / 지원 |
| 거래처 | 기관명 |
| 제품·모델 | 제품분류·모델명 (AS·점검) |
| 담당자 | 배정담당자 |
| 내용 | 접수내용 또는 조치내용 |
| 결과·상태 | 처리결과 / 현재 상태 |
| 소요(분) | `time_spent` (AS) · `spent_minutes` (행정) |

```
[전체 리스트]
구분  업무번호     발생일   유형   거래처        담당자  내용              상태
접수  R2608-051   08-11   AS     충남교육청     최혜경  대출반납기 오류    진행중
조치  R2608-051   08-12   AS     충남교육청     최혜경  부품 교체          진행중
완료  R2608-045   08-12   AS     세종도서관     양기헌  원격 재기동        완료
점검  MV-0142     08-11   점검   ○○도서관      양기헌  KLAS 정기점검      완료
행정  WT-137      08-12   행정   충남교육청     최혜경  IRM 지표 작성      완료
```

- **한 건이 접수·조치·완료를 모두 거쳤으면 각각 한 행**으로 나온다. 그 주에 실제로 일어난 일을 세기 위해서다.
- **엑셀 필터**를 헤더에 건다. 구분·유형·거래처·담당자·상태로 걸러 볼 수 있어야 한다.
- 발생일 오름차순 정렬. 같은 날은 업무번호 순.
- 지난주 접수 건을 이번 주에 처리했으면 **`조치`·`완료` 행만** 나온다(접수 행은 지난주 보고서에 있다).

---

#### 구현 메모

- 시트 1의 담당자 목록은 **그 주에 접수·완료·이월 중 하나라도 있는 사람**만 넣는다. 활동이 없는 사람은 행을 만들지 않는다.
- 실행률·소요일 산식은 **`model/stats.go`의 기존 함수를 재사용**한다. 보고서용으로 따로 짜지 않는다(§27).
- 「일일 최대」는 동점이면 **가장 이른 날짜**를 쓴다.
- 시트 1의 팀 전체 행은 담당자 행의 단순 합이 **아니다.** 실행률·평균은 팀 전체 기준으로 다시 계산한다.

### 16.2 월간 보고 [v2 신설]

**§16.1과 동일한 2시트 구조**를 월 단위로 만든다. 파일명 `YYYYMM_월간업무보고.xlsx`.

- 워킹데이는 그 달의 평일−공휴일로 센다.
- 이월 건수는 **월말 시점 미완료**다.
- 정기점검 월 진행 표는 **월별 목표 대비 달성**으로 바꾼다.
- 시트 1에 **전월 대비 증감**을 열로 덧붙인다.

### 16.3 정기점검 월별 캘린더 엑셀

§23.7 참조. 메뉴상 **계획 > 정기점검 일정** 화면에서 내보내되, 산출물 목록으로 **확인 > 보고서**에도 링크를 둔다.

---

## 17. 교체대상현황 (영업 활용)

메뉴 **확인 > 교체대상현황** · `/analysis/replacement`

노후장비·장애다발·타사설치 전체 목록(페이지당 20건, 이전/다음).

| 영업활용 항목 | 활용 방식 | 필요 데이터 | 베타 충족도 |
|---------------|-----------|-------------|-------------|
| 설치연수 | 설치일 기준 노후도 산정, 교체 예상시기 도출 | `assets.install_date` | 60.5% |
| 장애빈도 | 반복 장애 장비를 교체 제안 대상으로 분류 | `as_receipts.asset_id` | **43.1%** |
| 부품단종/OS종료 | 지원 불가 또는 위험 자산 식별 | `asset_sw_details` | **0%** |
| 타사 설치 장비 | 현재 운영지원 경험 기반 당사 전환 제안 | `assets.management_type='third_party'` | 60.3% |
| 기관별 지원 밀도 | 관계가 깊은 기관을 업셀링 후보로 선정 | `as_receipts.customer_id` | 100% |
| 협력사/제조사 관계 | 원청/협력사 네트워크로 신규 영업 접점 확보 | `performance_relations` | **0%** |

> **[v2 판단]** 이 화면은 **§4.2의 입력 지점이 채워진 뒤에 열어야 한다.** 자산 연결률 43%·S/N 0%·철수일 0% 상태에서 「교체대상」 목록을 내면 틀린 목록을 신뢰하게 된다. 단계별 구축안(§30)에서 **4단계**로 미룬다.

---

# 제7부 — 기준정보

> **목적:** 업무의 재료. 여기가 부실하면 AS 화면을 아무리 잘 만들어도 소용이 없다.
> v1.0 §18의 판단은 v2.0에서도 유효하다 — **"AS 화면보다 먼저, 기준정보의 완성도에 달려 있다."**

## 18. 고객 마스터

| 구분 | 항목 | 설명 | 필수 |
|------|------|------|------|
| 기본 | 고객ID | 내부 고유 식별자 | Y |
| 기본 | 기관명 | 실무상 사용하는 명칭 | Y |
| 기본 | 공식명칭 | 계약서·공문 기준 명칭 | Y |
| 연락 | 기관 이메일 / 대표전화 / 홈페이지 | 대표전화는 고객번호 지역코드 산출에 사용 | 대표전화 Y |
| 법인 | 사업자번호 / 대표자 | 중복 기관 판별 기준 | N |
| 분류 | 업종 | 도서관, 학교, 공공기관 등 | Y |
| 계층 | 상위기관여부 / 상위기관ID | 본청-산하기관 구조 관리 | N |
| 주소 | **우편번호 / 시도 / 군구 / 동읍면 / 상세주소** | 구조화 입력. 시도는 콤보. 엑셀 지역은 시도 우선 | N |
| 관리 | 사용여부 / 비고 | 통합, 폐쇄, 휴면 등 | Y |
| 관리 | **확인 필요 표식 / 사유** | 사람이 손으로 확인·보정해야 하는 기관 표시 | N |

**확인 필요 표식** — 외부 데이터를 넣다가 **기존 고객과 매칭되지 않아 자동 생성된 기관**은 `확인 필요` 표식과 사유가 붙는다.

- 고객현황 상단에 **「확인 필요 N건 보기」** 필터, 목록 기관명 옆에 **확인 필요** 뱃지(사유는 툴팁)
- 고객 상세 상단에 사유 배너 + **정보 수정 / 확인 완료** 버튼
- **확인 완료**를 누르면 표식과 사유가 함께 지워진다. 반대로 상세에서 임의의 기관을 **확인 필요로 표시**할 수도 있다.
- 고객 정보를 수정해도 표식은 유지된다(명시적 해제 필요). 표식 자동 부여는 **최초 1회만**.
- 권한: 기준정보 쓰기(관리자·접수담당)

**화면(`/customers`):** 기관 기본정보, 상위기관, 주소, 연락처. **상위기관 카테고리 콤보**, **업종 콤보·컬럼 정렬**, **엑셀 다운로드**(지역·사이트명 콤보). 지역은 **시도** 우선, 없으면 주소에서 추출. 상세는 기본정보 + **탭**(설치자산 / 담당자 / AS 이력).

---

## 19. 공간정보 (건물 · 층 · 실)

| 테이블 | 주요 항목 | 설계 포인트 |
|--------|-----------|-------------|
| 고객건물 | 건물ID, 고객ID, 건물명, 건물구분, 주소, 사용여부 | 기관 내 복수 건물 관리 |
| 고객층 | 층ID, 건물ID, 층명, 정렬순서 | B1, 1F, 2F 등 표준화 |
| 고객실 | 실ID, 층ID, 실명, 실번호, 용도 | 전산실, 자료실, 서버실 등 표준화 |
| 상세위치 | 설치자산에 연결 | 복도 좌측, 안내데스크 옆 등 자유 서술 |

**공간관리 화면(`/spaces`):** 한 화면에서 **기관 콤보+기관추가**, **건물 콤보+건물추가/수정/삭제**, **층 콤보+층추가/수정/삭제**를 각각 한 줄로 배치한다. 선택한 층의 호실은 **시트**(00호·01호…)로 표시하고 **호추가/수정/삭제**로 일괄 관리한다.

---

## 20. 담당자 정보 및 이력

### 20.1 담당자 정보

| 항목 | 설명 | 비고 |
|------|------|------|
| 담당자ID / 고객ID | 고유 식별자 · 연결 기관 | 기관별 복수 담당자 허용 |
| 이름 | 담당자 실명 | 동명이인 구분 필요 |
| 소속 | **소속기관 / 통합사업자 / 협력업체 / 기타** | 콤보 |
| 담당업무 | 전산, 도서관시스템, 네트워크 등 | 코드화 |
| 직함 / 직급 | 과장·사서 등 / **사서직 / 전산직 / 기타 / 직접입력** | 직급은 콤보 |
| 전화번호 / 핸드폰 / 이메일 | 연락처 | 전화·핸드폰 둘 다 권장 |
| 부임일 / 퇴임일 | 재직 기간 | 이력 연결 핵심 |
| 재직상태 | 재직 / 전보 / 퇴직 | 코드값 |
| 담당구분 | **주담당 / 부담당 / 일반 담당자** | 콤보 |

### 20.2 담당자 이력 관리

- 현재 담당자 정보는 최신값만 유지하고, **담당자가 바뀐 경우**(전보·퇴직·업무조정) 과거 구간은 별도 이력 행으로 보존한다.
- 단순 연락처 수정 등 **정보 수정 로그는 이력 목록에 올리지 않는다.**
- **특정 AS 건이 발생한 시점의 실제 담당자를 재현할 수 있어야 한다.**

**담당자 이력 화면(`/contacts/history`):** 기관별 **담당자 변경 이력**을 조회한다. 컬럼은 **이름 · 소속 · 부임일 · 퇴임일 · 담당구분 · 변경사유 · 전화번호 · 핸드폰 · 이메일**. 아직 담당자가 바뀌지 않았다면 **현행 담당자 정보만** 표시한다. 기관·담당자 콤보로 필터.

이력 항목: 담당자이력ID · 담당자ID/고객ID · 부임일/퇴임일 · 소속 · 담당구분 · 연락처/핸드폰/이메일 · **변경사유(전보 / 퇴직 / 업무조정)** · 등록일/등록자

---

## 21. 설치자산

### 21.1 설치 기본정보

| 구분 | 항목 | 설명 | v2 필수 |
|------|------|------|:---:|
| 식별 | 설치ID | `A{모델코드}{구분}-{NNN}` (키오스크=1, SW=2, 키오스크외HW=3) | 자동 |
| 기관 | 고객ID | 소속 기관 | Y |
| 제품 | 제품명 / **제품분류** / 제품구분 / 모델명 | 제품분류: 홈페이지, 전자도서관, 모바일, RFID자동화, 자료관리, 기타. 미선택 시 **기타**(타사 장비 오분류 방지) | 제품명·분류 Y |
| 제조 | 제조사명 / **S/N** | 식별과 보증 조회 기반 | **[v2] S/N 필수** (없으면 「없음」 명시) |
| 일정 | **설치일** / 철수일 | 사용수명 및 교체주기 분석 | **[v2] 설치일 필수** (불명 시 「추정」 체크+연도) |
| 설치주체 | 설치주체 / 원설치사명 | 자사, 타사, 제조사, 협력사, 미상 | Y |
| 운영 | 운영상태 / **관리유형** / 현재관리대상여부 | 관리유형 `타사장비` = 당사가 유지보수하지 않음. 설치주체와는 별개 | Y |
| 유지보수계약 | 계약구분 / 점검주기 / 계약기간 / 청구처 / 청구주기 | 유상·무상·CALL·미계약 등. 관리유형과 별개 — 계약·과금 성격 | 계약구분 Y |
| 관계 | 요청주체유형 / 요청주체명 | 고객직접, 제조사, 협력사, 원청 | N |
| 담당 | 고객담당자ID / 당사담당자ID | 현행 담당 연결 | N |
| 위치 | 건물ID / 층ID / 실ID / **설치위치** / 상세위치 | 자산 폼의 **공간정보**는 읽기전용(없으면 `-`). **공간관리 추가/연동**으로만 변경. **설치위치**는 공간정보(건물 층 호)를 반영해 입력 | 설치위치 Y |
| 기타 | 비고 / **설치사진** | 최대 3장. `data/uploads/assets/{자산번호}/`, 파일명 `{자산번호}_img_{1\|2\|3}.{확장자}`. 사진별 **키워드** — 검색·AS 매뉴얼 연동용 | N |

> **[v2] S/N·설치일을 필수로 올린 근거:** 베타에서 각각 0%·60.5%였고, 그 결과 §17 교체대상현황이 산출 불가 상태다. 다만 **회피 경로**(「S/N 없음」 체크, 설치일 「추정」 + 연도만)를 반드시 함께 제공한다.

### 21.2 유지보수 계약 항목

현업 유지보수현황(유/무상·점검주기·청구처)을 반영한다. **관리유형**(직접유지보수·장애대응·타사장비 등)과 혼동하지 않는다.

| 항목 | 예시 |
|------|------|
| 계약구분 | 유상, 무상, CALL, 미계약 |
| 점검주기 | 월, 분기, 반기, 홀수격월, 짝수격월, 직접입력 |
| 계약시작일 / 계약종료일 | 2026-01-01 ~ 2026-12-31 |
| 청구처 | 나이콤, 원청사명 |
| 청구주기 | 월, 분기, 반기, 홀수격월, 짝수격월, 직접입력 |

### 21.3 타사 장비

기관 현장에는 당사 계약 밖 타사 장비가 함께 있다. **기록은 하되 장비 자체를 당사가 관리하지 않는다.**

- **관리유형 `타사장비`:** 타사 장비 구분값. 현재관리대상여부는 **아니오**.
- **사업(프로젝트):** 타사 장비는 사업에 **넣지 않는 것이 기본**. 자산 등록 시 사업을 자동으로 넣지 않으며, 사용자가 선택한 경우만 연결한다.
- **연관 선택:** 당사 관리 제품과 연동되어 현장 파악이 필요할 때만 사업을 선택할 수 있다. 예: KLAS는 타사 게이트·무인대출기와 연동되어 운영되는 경우가 많다. 이 경우에도 유지보수 대상은 KLAS다.
- **설치주체와 구분:** 설치주체 `타사`는 누가 납품·설치했는지만 나타낸다. 타사 납품이어도 당사가 유지보수하면 `직접유지보수`를 쓴다.

### 21.4 SW 설치 상세정보

| 항목 | 예시 |
|------|------|
| 설치ID | `AEZ-200E23-001` |
| 소프트웨어명 / 버전 | 통합도서관리 5.2 |
| 설치형태 | PC설치형, 서버형, 웹형 |
| 설치 HW 정보 | PC, 전용장비, 서버 |
| OS / OS버전 | Windows 11, Ubuntu 22.04 |
| DBMS / DB버전 | MS SQL 2019 |
| 접속방식 / 접속주소 | RDP, 웹 URL |
| 설치경로 / 백업경로 | `D:\App`, NAS 경로 |
| 환경설정파일위치 | `config.ini` 위치 |
| **[v2] 지원종료일(EOL)** | OS·DBMS 지원 종료 예정일. 교체대상 산출에 사용 |

### 21.5 접속정보 관리 원칙

- ID/PASSWORD는 시스템 DB에 **평문 저장하지 않는다.**
- 접속정보는 암호화 파일, 금고 시스템 또는 접근권한 제한 저장소에 보관한다.
- 시스템에는 **접속정보 파일경로, 저장방식, 최종확인일, 관리주체만** 저장한다.

### 21.6 수행관계 및 요청 주체

| 항목 | 설명 |
|------|------|
| 수행관계ID | 고유 식별자 |
| 고객ID / 설치ID | 기관 단위 또는 설치건 단위 연결 |
| 관계구분 | 제조사 요청 수행, 협력사 요청 수행, 직접 유지보수, 장애대응 협조 등 |
| 상대회사구분 / 상대회사명 | 제조사, 협력사, 원청사, 고객기관 |
| 담당자명 / 연락처 / 이메일 | 실무 연락창구 |
| 시작일 / 종료일 / 현재유효여부 / 비고 | 현행 관계 관리 |

---

## 22. 사업 · 코드 · 고유번호

### 22.1 사업(프로젝트) 관리

메뉴 **기준정보 > 사업(프로젝트)관리** · `/projects`

사업(계약) 마스터를 전용 메뉴에서 CRUD한다. 일일업무의 지원업무·통계 사업별 필터와 **동일 `work_projects`** 를 쓴다. 일일 업무 현황 모달의 프로젝트 등록은 **빠른 등록용**으로 병행 유지한다.

| 항목 | 내용 |
|------|------|
| 기본 | 사업명(필수), 짧은이름, **사업연도**, 발주처(고객 콤보+직접입력), 고객(권장·콤보), 계약·청구방식(코드 콤보+직접입력), 계약기간, 유상/무상, 색상, 상태(진행/완료/보관), 비고, 고객 담당자 |
| **범위 규칙** | 사업 1건에 규칙 N건. **상위기관** × **제품키**(klas / sejong_klas / anrobotics) × **업무유형**(as / maintenance / admin). AS는 원본에 `project_id`를 쓰지 않고 **규칙 매칭**으로 귀속 조회. **정기점검은 방문에 사업명(`project_id`)을 직접 둘 수 있다**(없으면 규칙 매칭) |
| 화면 | 목록(검색: 사업명·발주처·고객 / 연도·상태 필터)·등록·상세·수정. 상세에서 **보관/재개**, 연결 지원업무 건수·최근 목록·일일 업무 현황 링크 |
| 삭제 | **관리자만.** 연결 일일업무(`work_tasks`)가 있으면 거부 |
| 권한 | 조회·등록·수정·보관/재개: 관리자·접수담당·기술담당. 삭제: 관리자 |
| 콤보 연동 | `status=active` 사업만 일일업무 사업명 콤보에 표시 |

**예시 사업(시드):** 충남교육청 SW 유지관리(KLAS) · 세종 ICT(세종 KLAS) · 앤로보틱스 RFID · 제천 홈페이지 · 세종 무상(KLAS+RFID) · 차년도 충남(행정/지원 중심)

### 22.2 코드 체계

| 코드그룹 | 예시 값 |
|----------|---------|
| 업종 | 도서관, 학교, 공공기관, 민간 |
| 담당업무 | 전산, 네트워크, 도서관리시스템, 행정시스템 |
| 제품구분 | SW, HW, 서버, 네트워크장비, 주변장비, **키오스크** |
| 제품분류 | 홈페이지, 전자도서관, 모바일, RFID자동화, 자료관리, 기타 |
| 설치주체 | 자사, 타사, 제조사, 협력사, 미상 |
| 관리유형 | 직접유지보수, 장애대응, 정기점검, 요청시지원, 참고관리, **타사장비** |
| 유지보수계약구분 | 유상, 무상, CALL, 미계약 |
| 유지보수점검주기 / 청구주기 | 월, 분기, 반기, 홀수격월, 짝수격월, 직접입력 |
| 담당자직급 | 사서직, 전산직, 기타, 직접입력 |
| 요청주체유형 | 고객직접, 제조사, 협력사, 원청, 내부, **원콜** |
| AS상태 | 접수, 담당자 배정, 진행중, 보류(화면 대기), 이관, 부분완료, 접수취소, 완료, 종료 |
| 처리결과 | 완료, 부분완료(화면 추가조치 필요), 타사이관, 재방문필요. 이관 세부: 처리 확인 후 완료 / 우리 팀 추가 작업 |
| 원인분류 | HW고장, SW오류, 네트워크, 환경문제, 사용자오류 |
| 처리유형 | 원격지원, 방문, 교체, 설정변경, 문의응대 |
| 운영상태 | 운영중, 점검중, 장애, 철수, 폐기 |
| **[v2] 근무구분** | 내근(`office`), 외근(`field`) |
| **[v2] 예정일 미정 사유** | 부품 대기, 고객 일정 미확정, 외부 업체 회신 대기, 기타(직접입력) |
| **[v2] 자산 미연결 사유** | 자산 미등록, 기관 공통 문의, 타사 장비, 기타(직접입력) |
| **[v2] 데이터 출처** | `app`, `import`, `migrated` |
| **[v2.2] 행정·지원 상태** | 수집함(`inbox`) · 할 일(`waiting`) · 진행중 · 회신 대기(`waiting_for`) · 보류 · 이관 · 검토중(`review`) · 완료 · 취소(`cancelled`) |
| **[v2.2] 다음 행동 상태** | 할 일 · 진행중 · 대기(`waiting`) · 완료 · 취소 |
| **[v2.2] 대기 대상 구분** | 내부 담당 · 부서 · 외부 업체 · 고객 |
| **[v2.2] 조치 이력 유형** | 작성 · 수정 · 검토 요청 · 회신 · 발송 · 독촉 · 완료 · 기타 |

### 22.3 업무 고유번호 원칙

| 대상 | 포맷 | 비고 |
|------|------|------|
| 고객번호 | `C{지역코드}-{YY}-{NNN}` | 대표전화에서 지역번호 추출(없으면 `000`). 예: `C02-26-001`, `C041-26-012` |
| 설치자산 | `A{모델코드}{구분}-{NNN}` | 구분: 키오스크=1, SW=2, 키오스크외HW=3. 예: `AEZ-200E23-001` |
| 접수번호 | `R{YYMM}-{NNN}` | 예: `R2607-001` |
| 처리번호 | `{접수번호}-P{NN}` | 예: `R2607-001-P01` |
| 하부업무 | `{접수번호}-W{NN}` | 예: `R2607-001-W01` |

**모델코드 규칙(타사 장비 포함):** 자산번호는 URL 경로·이미지 파일 경로에 그대로 쓰이므로 **ASCII 영숫자와 하이픈만** 사용한다. 모델명 → 제품명 → 제조사 순으로 ASCII 문자를 찾아 대문자로 정규화하고(한글 등 비ASCII는 구분자 처리), 셋 다 ASCII가 없으면 `ETC`를 쓴다. 예: 제품명 `자료검색대`·제조사 `채움씨앤아이`만 있으면 `AETC3-001`.

일련은 키별 증가(고객·자산·접수는 3자리, 처리·하부업무는 2자리). 기존 데이터도 동일 규칙으로 이관한다.

---

# 제8부 — 정기점검

## 23. 정기점검 일정 관리

### 23.1 목적 및 성격

매월 반복되는 **정기 점검** 일정을 수립하고, 담당자가 검토·수정·승인한 뒤 캘린더·엑셀로 공유한다. 기존 **고객기관(사이트)·공간·설치자산** 정보와 연계하여 사이트별 점검 대상 영역(KLAS / RFID 자동화 장비)을 반영한다.

| 점검 영역 | 표준 소요시간 |
|-----------|---------------|
| **KLAS** | 사이트당 **20분** (오버라이드 가능) |
| **RFID 자동화 장비** | 장비당 **15분** (사이트별 오버라이드 가능) |

### 23.2 점검 사이트 설정 (기준정보)

메뉴 **기준정보 > 점검 사이트 설정** · `/maintenance/sites`

- 사이트(고객기관)마다 **KLAS만**, **RFID만**, **둘 다** 대상인 경우가 있다. 사이트별로 대상 영역을 지정한다.
- 사이트별 **점검주기**를 등록한다: **월, 홀수격월, 짝수격월, 분기, 반기, 년1회**.
  - 홀수격월=1·3·5·7·9·11월, 짝수격월=2·4·6·8·10·12월, 분기=1·4·7·10월, 반기=1·7월, 년1회=1월, 월=매월.
- **이번 달 방문 목표:** 주기가 그 달에 도래하는 사이트마다 점검대상 수만큼 센다. KLAS만=1, RFID만=1, **둘 다=2**. 사이트 수보다 방문 건수가 클 수 있다.
- 화면 상단에 **이번 달 주기 해당 사이트 수**와 **방문 건수(KLAS·RFID 합)** 를 주기별로 표시한다.
- 목록은 **엑셀 다운로드**로 추출한다(기관명·표시명·지역·점검주기·KLAS/RFID·유형·고정규칙).
- **자산 기준 일괄 추가:** 설치자산의 **유지보수 계약구분이 유상**이고 **점검주기가 정기 주기**인 기관을 점검 사이트로 한 번에 등록한다.
  - 표시명 = 기관명, 지역 = 기관 주소의 시도 축약(충청남도 → 충남), 유형 = 일반.
  - **RFID** = 제품분류 `rfid` 자산 보유 시, **KLAS** = 제품명·모델명에 `KLAS` 포함 자산 보유 시.
  - 한 기관에 점검주기가 여러 개면 **간격이 가장 짧은 주기**를 적용한다(월 + 분기 → 월).
  - 점검주기가 **수시·Call** 이거나 계약이 유상이 아닌 기관은 제외한다.
  - **이미 등록된 사이트는 덮어쓰지 않는다.** 반복 실행해도 신규 기관만 추가된다.

### 23.3 방문 1건의 구성과 완료 처리

방문 1건은 **날짜 · 사이트 · 점검 대상 · 담당자 · 사업명 · 완료 여부**로 이뤄진다.

- **점검 대상**(KLAS·앤로보틱스 등)이 다르면 **같은 날 같은 기관이라도 별도 방문**이다. 담당자가 다르고 소요시간도 따로 잡히기 때문이다. **중복 방지 기준 = `날짜 + 기관 + 점검 대상`** (**[v2] UNIQUE 인덱스로 강제**).
- **같은 달 · 같은 기관 · 같은 점검 대상은 화면·집계 모두 1건만** 둔다. 방문 예정일을 바꾸면 **바꾼 날짜 칸에만** 나타난다.
- **사업명:** 방문 추가·수정·조치 화면에서 사업(프로젝트)을 고를 수 있다. **`maintenance_visits.project_id`에 저장**하고 연결된 일일업무에도 맞춘다.
  > **[v2 결함 수정]** 현재 `maintenance_visits`에 `project_id` 컬럼이 **없다.** 반면 통계 코드(`stats_dashboard.go`)는 이 컬럼을 참조한다. 사업 필터를 걸면 정기점검 집계에서 `no such column` 오류가 난다. **컬럼 추가가 선행되어야 한다.**
- **방문 완료**는 방문마다 켜고 끈다. 완료에는 **실제 방문일**이 함께 남고, 비워 두면 예정일을 그대로 쓴다.
- **지난 방문 일괄 완료:** 기준일을 넣으면 그날까지의 예정 방문을 한 번에 완료로 바꾼다. 이미 완료된 건은 건드리지 않는다.
- 완료한 방문은 **일일 업무 등록의 정기점검 카드에서 빠지고**, 대시보드의 오늘·지연 목록에도 잡히지 않는다. 대신 **완료일 기준 완료 목록**에 나타난다.

### 23.4 계획 화면의 보기 방식

한 해 계획(`/maintenance/{계획}`)은 **캘린더 · 칸반 · 목록** 셋 중 하나로 본다(`?view=`). 어느 보기든 **사이트명과 점검 대상을 함께** 적는다.

| 보기 | 내용 |
|------|------|
| **캘린더**(기본) | 해당 달의 월력. 칸마다 `사이트 · 점검 대상`과 담당자, 완료 표시. **점검 대상 색:** 초록=앤로보틱스, 파랑=K-LAS(KLAS), 보라=세종 K-LAS. 칸에서 바로 완료 처리 |
| **칸반** | **예정일 경과 / 오늘 / 예정 / 방문 완료** 4열. 카드에 사이트·점검 대상·날짜·담당자와 완료·삭제 |
| **목록** | 날짜·사이트·점검 대상·담당자·상태 표. **전체(연간)** 를 볼 수 있는 유일한 보기 |

**방문 수정·삭제** — 일정이 잡힌 뒤에도 **날짜 · 사이트 · 점검 대상 · 담당자 · 사업명 · 표시 유형 · 비고 · 완료 여부(실제 방문일)** 를 고칠 수 있다.

| 권한 | 가능 작업 |
|------|-----------|
| **관리자** | 전체 일정 읽기·방문 추가·수정·완료·삭제, 지난 방문 일괄 완료. 연도 계획 생성·삭제, 자동 초안, 승인/승인 취소 |
| **기술담당** | **기본은 본인 배정 일정만** 표시. 방문 추가·수정·완료·삭제, 지난 방문 일괄 완료(본인 범위). **전체 일정 보기** 버튼으로 타 담당자 일정 조회(조회 중에도 수정·삭제 가능) |

### 23.5 자동 초안 생성 [v2 대폭 축소]

> **축소 결정의 근거.** v1.0 §17.7은 지역 클러스터링 · 이동시간 누적 · 09:30 출발 · 17:00 도착 역산 · 순서 최적화를 요구했다. 실제 구현은 **지역명 가나다순 정렬 + 하루 4건 하드코딩**에 그쳤고, **실사용 건수는 0건**(`auto_generated=0` / 76건 전부 수동)이었다. 반면 §23.4의 「일정 배정안됨 목록 → 캘린더 수동 배정」은 잘 돌아간다.
>
> 요구 수준을 낮춰 **실제로 쓰이는 수준까지만** 정의하고, 배정의 최종 판단은 사람에게 남긴다.

**v2.0에서 자동 초안이 하는 일 (전부):**

1. 해당 월에 **점검주기가 도래하는 사이트**를 추린다. (← v1.0 구현에서 누락됐던 부분. 통계의 월 목표 산식과 **동일 기준**을 쓴다)
2. 사이트마다 **점검 대상별로 방문을 나눈다.** KLAS만=1건, RFID만=1건, 둘 다=2건. (`product_type`을 반드시 채운다)
3. **고정 규칙 사이트**를 먼저 해당 슬롯에 배치한다. (예: 국립세종도서관 — 매월 마지막 주 월요일)
4. 나머지를 **지역별로 묶어**, **25일 이전 평일**에 순서대로 나열한다. 하루 배치 건수 상한은 **설정값**(기본 4건)으로 둔다.
5. **토·일·공휴일은 제외**한다.
6. 25일 이전에 다 넣지 못하면 **남은 건을 「일정 배정안됨」 목록으로 보낸다.** 26일 이후로 억지로 밀어 넣지 않는다.

**v2.0에서 자동 초안이 하지 않는 일 (범위 제외):**

| 제외 항목 | 대체 수단 |
|-----------|-----------|
| 이동시간 API 연동 (네이버 지도 등) | — |
| 지역 간 표준 이동시간 테이블 | — |
| 소요시간 누적 기반 일자 분할 | 하루 상한 건수(설정값) |
| 09:30 출발 · 17:00 마지막 도착 역산 | 담당자 판단 |
| 방문 순서 최적화(TSP 근사) | 캘린더에서 수동 순서 조정 |

> **소요시간 기준(KLAS 20분·RFID 15분/대)은 폐기하지 않는다.** 자동 배치에는 쓰지 않지만, **계획 화면에 하루 예상 소요 합계를 표시**해 담당자가 무리한 날을 눈으로 확인하게 한다. (예: `8/12 — 5건 · 예상 3시간 20분`)

### 23.6 근무일 제약

| 제약 | 내용 |
|------|------|
| 비근무일 | **토·일 및 공휴일**에는 일정에 넣지 않는다 |
| 공휴일 데이터 | **[v2 개정]** 연도별 하드코딩을 유지하되, **미등록 연도 진입 시 화면에 경고를 띄운다.** 「{연도} 공휴일이 등록되지 않았습니다 — 주말만 제외됩니다」 |
| **[v2 신설] 관리자 휴무일 등록** | `/admin/holidays`에서 연도별 공휴일·회사 휴무일을 직접 등록·수정한다. 하드코딩 값은 **초기값**으로만 쓰고, 등록값이 있으면 그것을 우선한다 |
| 월간 목표일 | 고정일·지정일 사이트를 제외하고, 월간 정기 점검 전체가 **매월 25일 이전에 마무리**되는 것을 목표로 한다 |

> **[v2] 공휴일 하드코딩의 위험:** 현재 `kr_holidays.go`는 2025~2027만 담고 있다. 2028년 진입 시 `IsKRHoliday()`가 **false를 반환**해 공휴일에 일정이 배치된다. 대체공휴일 지정 변경도 반영되지 않는다. 관리자 등록 화면은 선택이 아니라 필수다.

### 23.7 엑셀 월별 캘린더 (유지보수계획표)

현업에서 쓰는 **유지보수계획 엑셀**(월별 달력)과 동등한 정보 구조·가독성을 갖춘 파일을 생성한다.

| 항목 | 요구 |
|------|------|
| 시트 | **12개**: `1월` … `12월`(해당 연도 기준) |
| 파일명 | `YYYYMMDD유지보수계획.xlsx`. 다운로드 시점 또는 계획 기준일이 식별되도록 |
| 인코딩·글꼴 | 한글 사이트명이 깨지지 않아야 하며 Windows Excel 기본 환경(**맑은 고딕**)에서 정상 표시 |
| 열 머리글 | **일요일 ~ 토요일** (왼쪽부터 일요일 시작) |
| 월 표제 | 시트 상단에 **연·월**을 크게 표시 (예: `2026년 3월`). 강조색 사용 가능 |
| 날짜 칸 | 상단 왼쪽에 **일자(숫자)**. 그 아래에 그날 방문 예정 **사이트 표시명**을 **줄바꿈**으로 나열. 복수 사이트면 모두 표기. **점검 대상이 있으면 `사이트 · 점검 대상`** 으로 적어 같은 기관 두 번 방문을 구분 |
| 월 밖 칸 | 비우거나 배경만 구분해 달력 형태 유지 |
| 비근무일 | **토·일·공휴일**에는 일정을 넣지 않는다. **회색 배경** 등으로 휴무 표시 가능 |
| 글자색 | 점검 대상 기준: **앤로보틱스=초록 · K-LAS=파랑 · 세종 K-LAS=보라**. 점검 대상이 비면 예전 엑셀 유형(일반=녹색·고정=적색·사무소=청색)을 보조로 |
| 데이터 일치 | 화면의 월간 정기점검 일정과 **동일한 배치 결과**를 반영. 화면 수정 후 다시 받으면 **수정 반영본** |
| 비기능 | 셀 **자동 줄바꿈**, 행 높이·열 너비 조정으로 다줄 사이트명이 잘리지 않게. 인쇄용 **가로 방향(A4/A3)** 프리셋(선택) |

### 23.8 월간 일정 엑셀 적재

월별 점검 일정 엑셀(`기관명·구분·제품분류·원담당자·방문예정일`)을 읽어 해당 연도 계획에 방문을 넣는다. 기준일까지는 완료로 표시한다.

- 기관명은 **고객 기관명·공식명칭·점검사이트 표시명**과 대조한다. 공백·괄호와 **눈에 안 보이는 문자**를 지우고, 한글은 **완성형(NFC)으로 맞춰** 비교한다(조합형으로 저장된 이름이 섞여 있다).
- **방문예정일이 없는 행**과 **기관을 못 찾은 행**은 건너뛰고 목록으로 보고한다.
- 이미 있는 `날짜 + 기관 + 점검 대상`은 건너뛴다. 같은 파일을 다시 넣어도 중복되지 않는다.
- **[v2]** 적재된 방문은 `data_origin = 'import'`로 표시한다.

### 23.9 승인과 캘린더 연동

- 자동 생성 직후 상태는 **초안**이다. 담당자는 날짜·순서·담당자·현장 시간을 수정할 수 있다.
- **승인** 절차(승인자·승인 시각)를 두어, 승인된 버전만 외부 연동·대외 공유의 기준이 되도록 한다.
- **엑셀 월별 캘린더**는 초안·승인본 모두 보낼 수 있으나 대외 공유 시 승인본을 권장한다.
- **외부 캘린더 연동(구글·애플)은 v2.0에서 후순위로 유지한다.** 승인 기능만 먼저 두고, 연동은 5단계(§30)에서 검토한다.

---

# 제9부 — 비기능 · 운영

## 24. 권한 및 사용자 역할

| 역할 | 주요 권한 | 현재 상태 |
|------|-----------|-----------|
| 관리자 | 전 영역 조회·입력·수정·삭제, 코드/사용자 관리, 통계, 데이터 관리 | **활성** |
| 접수담당 | AS 접수 등록, 목록·현황 조회, 고객·자산 조회, 계획 수립 | **활성** (기준정보 쓰기·AS 완료 입력 불가) |
| 기술담당 | AS 접수·조치·현황 전체, 고객·자산 조회, 본인 계획 | **활성** (기준정보 쓰기·코드/사용자 관리 불가) |
| 영업담당 | 기관 현황·교체분석·영업메모 | **일시 중단** — 로그인 후 「준비 중」 |
| 열람사용자 | 조회 전용 | **일시 중단** — 로그인 후 「준비 중」 |

역할별 메뉴 노출은 **§6.5** 참조.

**[v2] 권한과 필수 입력의 관계** — 필수 입력 규칙(§5)은 **역할과 무관하게** 적용한다. 관리자라고 해서 예정일 없이 확정하거나 소요시간 없이 조치를 저장할 수 없다. 예외는 **관리자 잠금 해제**(§12.6)를 통한 사후 수정뿐이며, 이때도 `data_change_logs`에 사유가 남는다.

---

## 25. 데이터 백업 · 변경 이력

### 25.1 백업

- 매일 **01:00**(Asia/Seoul)에 `data/` 복사본을 `data/backups/자동백업_YYYY-MM-DD/`에 만든다.
- SQLite(`app.db`)는 운영 중에도 **일관된 스냅샷**으로 저장하고, 첨부(`uploads`)도 함께 복사한다. `backups` 폴더 자신은 복사하지 않는다.
- 서버가 01:00 이후 기동되면 당일 백업이 없을 때 한 번 보정 실행한다.
- **관리 > 데이터 관리**에서 관리자가 즉시 저장할 수 있다(폴더명 `수동저장_YYYY-MM-DD_HH시MM분SS초`).
- 모든 백업은 **목록**으로 관리하고 **정기 백업 / 사용자 백업**을 구분한다.
- 일 단위 스냅샷은 **최근 14일**만 유지한다. `관리로그_*` 폴더는 14일 정리 대상이 아니다.

> **[v2 결함]** 베타에서 백업 **폴더는 생성되는데 `data_backups` 테이블은 0건**이다. 백업 목록 화면이 비어 보인다. 폴더 생성 시 테이블에도 행을 남기고, 기동 시 폴더를 스캔해 누락분을 보정한다.

### 25.2 변경 이력

- 업무 데이터의 등록·수정·삭제는 `data_change_logs`에 남긴다. **언제, 어떤 데이터, 누가, 어떻게**(등록/수정/삭제)와 **변경 전·후 JSON**을 기록한다.
- 수정·삭제는 이력에서 **롤백**할 수 있다.
- `data_change_logs`는 **3개월**마다 `data/backups/관리로그_시작일-종료일/`로 파일 백업한 뒤 테이블을 비운다. 기존 백업 폴더의 DB를 열어 **건수·최근 이력 분석**할 수 있다.

> **[v2 결함]** 베타에서 `data_change_logs`가 **1건**이다. 롤백 기능의 전제가 성립하지 않는다. **어떤 조작이 이력 대상인지**를 코드가 아니라 기획에서 먼저 정해야 한다.

**[v2] 이력 기록 대상 (명시)**

| 대상 | 등록 | 수정 | 삭제 |
|------|:---:|:---:|:---:|
| 고객·담당자·설치자산·공간 | ● | ● | ● |
| AS 접수 (완료 건 잠금 해제 후 수정) | ○ | ● | ● |
| AS 조치(`as_processes`) | ○ | ● | ● |
| 정기점검 방문 | ○ | ● | ● |
| 사업(프로젝트) | ● | ● | ● |
| 코드·사용자·설정 | ● | ● | ● |
| 일일업무(`work_tasks`) 시간 배치 | ○ | ○ | ○ |

● 기록 · ○ 미기록. **일상적인 배치·조회는 기록하지 않는다.** 이력이 노이즈로 가득 차면 롤백에 쓸 수 없다.

---

## 26. 배포 · 운영 환경

| 항목 | 현행 | v2.0 요구 |
|------|------|-----------|
| 언어·프레임워크 | Go (서버 렌더링 · `html/template`) | 유지 |
| DB | SQLite (`data/app.db`, WAL) | 유지. PostgreSQL 전환 경로 문서·compose에 준비 |
| 컨테이너 | Docker / docker-compose | 유지 |
| 타임존 | `Asia/Seoul` | 유지 |
| **빌드 재현성** | `COPY go.mod ./` + 빌드 중 `go mod tidy`, `go.sum` 미복사 | **[v2] `COPY go.mod go.sum ./` → `go mod download`. `go mod tidy` 제거.** 빌드마다 의존성이 달라지면 안 된다 |
| **운영 볼륨** | `./web:/app/web` 마운트 (개발용) | **[v2] 운영 compose에서 제거.** 이미지 내 템플릿 사용 |
| **JWT 시크릿** | `.env.example` 기본값 `change-this-secret-in-production` | **[v2] 기동 시 기본값이면 경고 로그 + 관리자 화면 배너.** 운영 환경에서는 기동 거부 검토 |
| **TLS** | 8080 직접 노출 | 사내망 전용이면 수용. 외부 접근 시 **리버스 프록시 필수** |
| 정적 파일 | `web/static` | 유지 |

**PostgreSQL 전환 판단 기준 [v2 신설]** — 아래 중 하나라도 해당하면 전환을 검토한다.

- 동시 사용자 **15명 초과**
- `app.db` **200MB 초과**
- 백업 스냅샷 소요 **30초 초과**
- 쓰기 잠금(`database is locked`) 오류가 주 1회 이상 발생

현재(2.4MB · 10명 · 1,174건)는 SQLite로 충분하다. **선제 전환하지 않는다.**

---

## 27. 개발 시 고려사항

- 검색은 **기관명, 공식명칭, 담당자명, 제품명, S/N, AS번호** 기준으로 빠르게 제공한다.
- 접수 화면에서 고객/설치자산/담당자를 즉시 조회할 수 있는 **통합 검색 UX**가 중요하다.
- **AS 접수 폼:** 기관을 선택하면 해당 기관의 설치자산 목록을 **콤보박스**로 불러온다. `GET /api/assets/:customer_id`. **[v2] 자산 미선택 시 「미연결 사유」 필수.**
- **AS 접수 · 요청자:** 기관 선택 시 담당자 목록을 콤보로 제시하고 직접 입력도 가능하다. `GET /api/contacts/:customer_id`.
- **AS 처리 이력 보존:** `as_processes`에 회차별 조치결과·이관 세부·다음 예정일·대기/재방문 사유·**소요시간·근무구분**을 둔다. 접수의 현재 결과코드만 덮어쓰지 않는다. `RECEIPT`/`ACTION_HISTORY` 테이블을 새로 만들지 않는다.
- 설치자산 상세에서 최근 AS 이력, 요청 주체, 수행관계가 한눈에 보여야 한다.
- 첨부파일은 설치사진, 점검표, 장애사진, 계정문서, **조치완료보고서** 등을 지원한다. AS 조치 파일은 `data/uploads/as/{접수ID}/`에 두고 DB에는 경로·파일명만 저장하며 화면에는 파일명만 보여 준다.
- **설치자산 사진:** 자산당 최대 3장. `data/uploads/assets/{자산번호}/`에 `{자산번호}_img_{N}` 형식. 각 사진에 **키워드**를 둔다.
- **[v2.3] AS 접수 사진:** `data/uploads/as/{접수ID}/receipt/`. 업로드 시 **긴 변 1600px 축소 + 320px 썸네일 생성 + EXIF 회전 반영**이 필요하므로, Go 이미지 처리 라이브러리 선정 시 **HEIC 입력 지원 여부**를 함께 검증한다(휴대폰 사진이 HEIC로 들어온다). 사진별 메모는 기존 `attachments.keywords`를 재사용하고 새 컬럼을 만들지 않는다.
- 개인정보 및 접속정보는 **접근권한, 로그기록, 암호화 저장** 정책을 적용한다.
- 엑셀 월별 캘린더(§23.7)는 **병합 셀·스타일·다중 시트**가 필요하므로 xlsx 생성 라이브러리 선정 시 **스타일 API 지원 여부**를 검증한다.
- **[v2] 필수 입력 검증은 핸들러가 아니라 서비스 계층에 둔다.** 화면·API·이관 스크립트가 같은 검증을 거치게 해야 우회 경로가 생기지 않는다.
- **[v2] 지표 산식은 한 곳에만 둔다.** 대시보드·통계·주간보고서가 각자 SQL을 쓰면 반드시 어긋난다. `model/stats.go`의 산식 함수를 세 화면이 공유하고, **세 화면의 값이 일치하는지 확인하는 테스트**를 둔다.

---

## 28. 논리 데이터 모델 (요약)

| 테이블 | 주요 역할 | 핵심 연결 |
|--------|-----------|-----------|
| `customers` | 기관 기준정보 | 상위기관, 건물, 담당자, 설치자산 |
| `customer_buildings` / `_floors` / `_rooms` | 공간 정보 | 고객 → 건물 → 층 → 실 |
| `contacts` / `contact_history` | 현재 담당자 / 변경 이력 | 고객 → 담당자 → 이력 |
| `assets` | 운영자산 핵심 정보 | 고객, 위치, 담당자, 수행관계, AS |
| `asset_sw_details` | SW 환경 상세 | `assets`와 1:0..1 또는 1:N |
| `access_info_references` | 접속정보 위치 관리 | `assets`와 연결 |
| `performance_relations` | 제조사/협력사/원청 관계 | 고객·자산과 연결 |
| `as_receipts` | 접수 및 **현재** 상태 | 고객, 자산, 담당자. **1건 = 1 Case** |
| `as_processes` | 조치 이력(Timeline) | `as_receipts`와 1:N |
| `as_work_items` | 확인·재방문 다음 일정 | `as_receipts`와 1:N |
| `work_tasks` | 일일 업무(계획·실행 단위) · **행정 1건 = 1 Case** | 원본(`source_type`/`source_id`)과 연결 |
| `work_actions` | **행정·지원 다음 행동**(실행 단위) | `work_tasks`와 1:N (§13.6) |
| `work_activities` | **행정·지원 조치 이력**(Timeline) | `work_tasks`와 1:N (§13.7) |
| `work_projects` / `project_scope_rules` | 사업 마스터 / 범위 규칙 | 상위기관 × 제품 × 업무유형 |
| `maintenance_plans` / `_visits` / `_site_config` | 정기점검 계획·방문·사이트 설정 | 고객, 담당자, **사업** |
| `daily_meeting_stats` | 일별 실행률 스냅샷 | — |
| `attachments` | 사진/문서 연결 | 고객, 자산, AS 범용 연결. `ref_type`: `asset` · `as`(조치 첨부) · **`as_receipt`(접수 사진, v2.3)** |
| `codes` | 드롭다운/표준값 | 모든 입력의 표준화 |
| `data_change_logs` / `data_log_archives` | 변경 이력 / 아카이브 | 롤백 근거 |
| `data_backups` | 백업 목록 | 정기/사용자 구분 |
| `users` | 사용자·역할 | 권한 |
| `id_sequences` | 고유번호 채번 | 키별 증가 |

**[v2 스키마 변경]**

| # | 변경 | 대상 |
|---|------|------|
| S-1 | `as_receipts.data_origin` **추가** (`app`/`import`/`migrated`, 기본 `app`) | 지표 오염 방지 |
| S-2 | `as_receipts.schedule_no_date_reason` **추가** (예정일 미정 사유) | §2.3 제1원칙 |
| S-3 | `as_receipts.asset_unlinked_reason` **추가** (자산 미연결 사유) | §4.2 |
| S-4 | `maintenance_visits.project_id` **추가** | §23.3 결함 수정 |
| S-5 | `maintenance_visits` **UNIQUE(`plan_id`,`visit_date`,`customer_id`,`product_type`)** | §23.3 중복 방지 |
| S-6 | `as_receipts` **CHECK** (`schedule_confirmed=0 OR visit_scheduled_date` 존재) | §5.2-1 |
| S-7 | `as_processes.time_spent` **NOT NULL CHECK(> 0)** | §5.2-3 |
| S-8 | `as_receipts.work_place` **CHECK IN ('office','field')** | §4.2 |
| S-9 | `holidays` 테이블 **신설** (연도·일자·명칭·구분) | §23.6 |
| S-10 | `maintenance_visits.data_origin` **추가** | §23.8 |
| **S-11** | `work_tasks.receipt_date` · **`complete_date`** **추가** | **§13.4 · §13.10 — 리드타임 산출** |
| **S-12** | `work_actions` 테이블 **완성** (DDL 있음, 코드 없음) | §13.3 다자 요청 |
| **S-13** | `work_activities` 테이블 **완성** (DDL 있음, 코드 없음) | §13.4 왕복 이력 |
| **S-14** | `work_tasks.status` 에 `inbox` · `waiting_for` · `cancelled` 추가 | §13.5 상태 9종 |

DDL은 **부록 B** 참조.

---

## 29. 핵심 업무 규칙

1. 기관명과 공식명칭은 분리 관리한다.
2. 사업자번호 또는 내부 중복 검증 규칙으로 동일 기관 중복 등록을 방지한다.
3. 담당자는 삭제하지 않고 재직상태와 이력으로 관리한다.
4. 설치자산의 위치는 건물-층-실 기준으로 선택하고, **설치위치**로 동일 층·실의 동일모델을 구분한다. AS 접수 시 자산 선택 콤보에 설치위치가 함께 표시된다.
5. 설치주체, 관리유형, 요청주체는 반드시 코드화한다.
6. AS 접수는 **가급적** 설치자산과 연결한다. **[v2] 연결하지 않으려면 사유를 남긴다.**
7. AS 접수 1건은 하나의 Case다. 진행 중 재방문·추가조치·대기·이관은 같은 접수에 조치 이력을 쌓는다. **완료 후 동일 증상은 재접수**(새 번호). **같은 번호 재오픈은 하지 않는다.**
8. 처리 완료 시 후속조치와 교체검토 여부를 함께 기록한다.
9. 노후도와 장애빈도 기준으로 교체영업 후보를 산출한다. **[v2] 단, §4.2의 기초값이 채워진 뒤에 연다.**
10. 정기 점검 일정은 자동 초안 이후에도 담당자 수정이 가능하며, 승인 전에는 외부 캘린더에 반영하지 않는다.
11. **[v2] 모든 단위업무는 예정일을 가진다.** 정할 수 없으면 「미정 + 사유」를 남긴다.
12. **[v2] 조치를 저장하면 소요시간·근무구분·원인분류·처리유형이 함께 남는다.**
13. **[v2] 측정할 수 없는 지표는 숫자를 만들지 않고 `—`로 표시한다.**
14. **[v2.2] 행정·지원 1건은 하나의 Case다.** 다음 행동·조치 이력을 누적하고, **며칠에 걸친 일도 새 업무로 나누지 않는다.** 사업명(`work_projects`)과 GTD 복합업무를 섞지 않는다. **「할 일(`waiting`)」과 「회신 대기(`waiting_for`)」를 구분한다.** 필수 다음 행동·미확인 대기가 있으면 일반 사용자는 완료할 수 없다.
15. **[v2.2] 행정·지원 GTD는 `work_tasks`를 유지하고 `work_actions`·`work_activities`만 추가한다.** `tasks`/`projects`/`inbox_items` 로 대체하지 않고 기존 `task_id`를 보존한다.
16. **[v2.3] AS 접수 사진과 조치 첨부는 섞지 않는다.** 접수 사진은 증상(조치 전), 조치 첨부는 결과(조치 후)다. `ref_type`으로 나누고 화면도 분리한다. 접수에 자산이 연결돼 있으면 접수 사진을 **자산 사진으로 승격**할 수 있다.

---

## 30. 단계별 구축안 [v2 개정]

v1.0의 5단계는 "기능을 무엇부터 만들까"의 순서였다. 베타를 거친 지금은 **"이미 만든 것을 어떻게 쓰이게 할까"** 가 먼저다.

| 단계 | 범위 | 완료 판정 |
|------|------|-----------|
| **0단계 (신규·최우선)**<br>**지표 복구** | ① 예정일-확정 정합성 제약 + 기존 1,124건 보정<br>② `start_datetime` 자동 기록 + 1,128건 백필<br>③ `time_spent`·`work_place`·`cause_type`·`process_type` 필수화<br>④ 분모 0 → `—` 표시, 신뢰도 등급 표시<br>⑤ `data_origin` 도입 · 이관 데이터 KPI 제외 | 주간보고서 KPI 3종이 모두 🟡 이상 |
| **1단계**<br>**계획 습관 정착** | ⑥ 미계획 업무함 신설<br>⑦ 대시보드 「미계획」 카드<br>⑧ 접수 시 예정일 필수(+미정 사유)<br>⑨ 계획 수립률 지표<br>⑩ 메뉴 재편(계획→실행→확인) | 계획 수립률 **95% 이상** 4주 연속 |
| **2단계**<br>**정기점검 정합** | ⑪ `maintenance_visits.project_id` 추가<br>⑫ 자동 초안에 점검주기·점검대상 반영(§23.5)<br>⑬ 방문 UNIQUE 제약<br>⑭ 관리자 휴무일 등록 화면 | 사업 필터 정상 · 초안과 월 목표 건수 일치 |
| **2.5단계**<br>**행정·지원 GTD** [v2.2]<br>(1~5단계와 별도 진행) | ⑭-1 `model`·`repo`·`handler` 미정의 심볼 완성 → **빌드 복구**<br>⑭-2 `receipt_date`·`complete_date` 추가, 통계 완료 기준 교체<br>⑭-3 수집함 · 상태 세분화(`inbox`·`waiting_for`·`cancelled`)<br>⑭-4 다음 행동 · 조치 이력 화면<br>⑭-5 회신 대기·보류 필수값 + 완료 검증<br>⑭-6 리드타임 · 외부 대기 지표<br>**2단계:** 반복업무 템플릿 · 문서 버전 · 알림 · 주간 검토 화면 · 댓글·참조자 | `go build` 통과 · 사례(IRM) 업무를 §13.7 구조대로 기록 가능 · 기존 `task_id` 보존 |
| **3단계**<br>**운영 신뢰성** | ⑮ 변경 이력 기록 대상 정비(§25.2)<br>⑯ 백업 목록 테이블 반영<br>⑰ Dockerfile 재현성 · 운영 compose 정리<br>⑱ JWT 시크릿 검증 | 롤백 시연 성공 · 백업 목록 화면 정상 |
| **4단계**<br>**영업 활용** | ⑲ 자산 S/N·설치일·철수일 수집<br>⑳ 자산 연결률 80% 달성<br>㉑ 교체대상현황 오픈 | 자산 연결률 80% · 교체 후보 목록 검증 |
| **5단계**<br>**확장** | 외부 캘린더 연동(구글·애플) · PostgreSQL 전환(§26 기준 도달 시) · 모바일 · 고객포털 | — |

> **0단계를 건너뛰지 않는다.** 1단계 이후의 모든 기능은 지표로 효과를 확인해야 하는데, 0단계 없이는 그 지표 자체를 믿을 수 없다.

---

## 31. 결론

고객지원 시스템의 성공 여부는 AS 화면보다 먼저 **기준정보의 완성도**에 달려 있다. 이 판단은 v1.0에서나 v2.0에서나 같다.

v2.0이 추가한 것은 그다음 문장이다. **기준정보가 갖춰져도, 계획과 실행의 흔적이 남지 않으면 관리는 불가능하다.**

베타는 이것을 정확히 보여 줬다. 화면은 다 있었고 산식도 맞았지만, 예정일이 2.6%·착수 시각이 3.2%·소요시간이 0%였다. 엔진은 완성됐는데 연료가 없었다.

그래서 v2.0은 순서를 바꿨다. **지표를 먼저 정의하고, 그 지표를 채우는 입력 지점을 명시하고, 그다음에 화면을 배치한다.** 메뉴를 계획→실행→확인으로 재편한 것도, 미계획 업무함을 신설한 것도, 정기점검 자동 수립의 범위를 줄인 것도 모두 같은 원칙에서 나왔다 — **측정되지 않는 기능은 만들지 않는다.**

축적된 데이터는 단순 운영 기록에 머물지 않고 타사 납품 자산의 교체시기 파악과 교체영업 제안으로 이어져야 한다. 다만 그 활용은 **기초값이 채워진 뒤**의 일이다(§30 4단계). 순서를 지키는 것이 v2.0의 요지다.

---

# 부록

## 부록 A. 필수 입력 규칙 일람

기호: **●** 필수 · **◐** 필수이나 회피 경로 있음 · **○** 선택 · **A** 서버 자동

### A.1 AS 접수 (`/as/new`)

| 항목 | v1.0 | v2.0 | 회피 경로 | 검증 위치 |
|------|:---:|:---:|---|---|
| 접수일시 | ● | ● | — | 서버 |
| 고객(기관) | ● | ● | — | 서버 |
| **설치자산** | ○ | **◐** | 「미연결」 + 사유 코드 | 서버 |
| 접수채널 | ● | ● | — | 서버 |
| 요청자 | ● | ● | 직접입력 허용 | 화면 |
| 증상내용 | ● | ● | — | 서버 |
| 긴급도 / 중요도 | ● | ● | 기본 「중」 | 화면 |
| 배정담당자 | ○ | ○ | — | — |
| **예정업무일** | ○ | **◐** | 「미정」 + 사유 코드 | **서버 + 트리거** |
| 일정 확정 | ○ | ○ | **예정업무일 없으면 체크 불가** | **서버 + 트리거** |
| **접수 사진** [v2.3] | — | ○ | 최대 10장 · 자동 축소 | 서버 |
| 데이터 출처 | — | **A** | — | 서버(기본 `app`) |

### A.2 AS 조치 (`/as/{id}/action`)

| 항목 | v1.0 | v2.0 | 기본값 | 검증 위치 |
|------|:---:|:---:|---|---|
| 조치일시 | ● | ● | 현재시각 | 서버 |
| 조치내용 | ● | ● | — | 서버 |
| **소요시간(분)** | ○ | **●** | **30분** | **서버 + 트리거** |
| **근무구분(내근/외근)** | ○ | **●** | 직전 값 기억 | **서버 + 트리거** |
| **원인분류** | ○ | **●** | — | 서버 |
| **처리유형** | ○ | **●** | — | 서버 |
| 결과코드 | ● | ● | — | 서버 |
| 방문예정일 (`partial`·`revisit_needed`) | ● | ● | — | 서버 |
| 확인예정일·대상자·연락처 (`transfer`+`waiting`) | ● | ● | — | 서버 |
| 대기 사유·후속 (`hold`) | ● | ● | — | 서버 |
| **처리시작일시** | ○ | **A** | 첫 조치의 `process_datetime` | 서버 자동 |
| 완료일시 | A | **A** | 저장 시각 | 서버 자동 |

### A.3 일일 업무 등록 (`/workboard/register`)

| 항목 | v1.0 | v2.0 | 회피 경로 |
|------|:---:|:---:|---|
| 업무 구분 | ● | ● | — |
| 업무명 | ● | ● | AS·정기점검은 자동 생성 |
| **예정일** | ○ | **◐** | 「미정」 + 사유 |
| 소요 예상 시간 | ○ | ● | 기본 30분 |
| 사업명 (지원업무) | ● | ● | — |
| 거래처 (행정업무) | ● | ● | 직접입력 허용 |
| 담당자 | ○ | ● | — |
| **접수일** [v2.1] | — | ● | 기본 오늘 |
| **완료일** [v2.1] | — | ● (완료 시) | 서버 자동 |
| **완료 메모** [v2.1] | — | ● (완료 시) | — |
| **하위 요청 회신** [v2.1] | — | ● (필수 요청 전건) | 관리자 강제 완료 + 사유 |

### A.4 설치자산 (`/assets/new`)

| 항목 | v1.0 | v2.0 | 회피 경로 |
|------|:---:|:---:|---|
| 고객·제품명·제품분류 | ● | ● | — |
| **S/N** | ○ | **◐** | 「S/N 없음」 체크 |
| **설치일** | ○ | **◐** | 「추정」 체크 + 연도만 입력 |
| 설치주체 | ● | ● | 「미상」 허용 |
| 관리유형 | ● | ● | — |
| 유지보수 계약구분 | ○ | ● | 「미계약」 허용 |
| 설치위치 | ● | ● | — |
| 철수일 | ○ | ● (철수 시) | — |

### A.5 정기점검 방문

| 항목 | v1.0 | v2.0 |
|------|:---:|:---:|
| 방문일 · 사이트 · 점검대상 | ● | ● |
| 담당자 | ○ | ● |
| 사업명 | ○ | ○ (`project_id`) |
| 실제 방문일 (완료 시) | ○ | ○ (없으면 예정일 사용) |

---

## 부록 B. DB 정합성 제약 (DDL)

> **SQLite 주의:** `ALTER TABLE ... ADD CONSTRAINT CHECK`을 지원하지 않는다. 테이블 재생성(12단계 절차) 대신 **트리거로 동등한 제약**을 구현한다. PostgreSQL 전환 시에는 트리거를 `CHECK` 제약으로 바꾼다.
>
> **아래 DDL은 운영 DB 사본(`app.db`, 2026-08-14)에 실제 실행해 문법·동작을 검증했다.**

### B.1 컬럼 추가 (S-1 ~ S-4, S-10)

```sql
ALTER TABLE as_receipts        ADD COLUMN data_origin TEXT NOT NULL DEFAULT 'app';
ALTER TABLE as_receipts        ADD COLUMN schedule_no_date_reason TEXT;
ALTER TABLE as_receipts        ADD COLUMN asset_unlinked_reason   TEXT;
ALTER TABLE maintenance_visits ADD COLUMN project_id  TEXT;
ALTER TABLE maintenance_visits ADD COLUMN data_origin TEXT NOT NULL DEFAULT 'app';
```

> **`maintenance_visits.project_id`는 결함 수정이다.** 현재 `stats_dashboard.go`가 이 컬럼을 참조하는데 스키마에 없어, 사업 필터를 걸면 `no such column: v.project_id` 오류가 난다.

### B.2 휴무일 테이블 신설 (S-9)

```sql
CREATE TABLE IF NOT EXISTS holidays (
  holiday_date TEXT PRIMARY KEY,          -- YYYY-MM-DD
  holiday_year INTEGER NOT NULL,
  name         TEXT NOT NULL,
  kind         TEXT NOT NULL DEFAULT 'public',  -- public | company | substitute
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_holidays_year ON holidays(holiday_year);
```

기존 `kr_holidays.go` 하드코딩 값은 **초기 시드**로만 넣고, 이후에는 이 테이블을 우선한다. 해당 연도 행이 하나도 없으면 화면에 경고를 띄운다(§23.6).

### B.3 정기점검 방문 중복 방지 (S-5)

```sql
CREATE UNIQUE INDEX IF NOT EXISTS ux_mnt_visit_unique
  ON maintenance_visits(plan_id, visit_date, customer_id, COALESCE(product_type,''));
```

> **검증 결과:** 현재 데이터에 중복 조합 **0건** — 인덱스를 즉시 적용할 수 있다.

### B.4 트리거 제약 (S-6 ~ S-8)

```sql
-- S-6: 일정확정에는 예정업무일이 반드시 있어야 한다
DROP TRIGGER IF EXISTS trg_as_sched_confirm_ins;
CREATE TRIGGER trg_as_sched_confirm_ins BEFORE INSERT ON as_receipts
FOR EACH ROW WHEN NEW.schedule_confirmed = 1
                 AND TRIM(COALESCE(NEW.visit_scheduled_date,'')) = ''
BEGIN SELECT RAISE(ABORT, '일정확정에는 예정업무일이 필요합니다'); END;

DROP TRIGGER IF EXISTS trg_as_sched_confirm_upd;
CREATE TRIGGER trg_as_sched_confirm_upd BEFORE UPDATE ON as_receipts
FOR EACH ROW WHEN NEW.schedule_confirmed = 1
                 AND TRIM(COALESCE(NEW.visit_scheduled_date,'')) = ''
BEGIN SELECT RAISE(ABORT, '일정확정에는 예정업무일이 필요합니다'); END;

-- S-7: 조치 소요시간은 0보다 커야 한다
DROP TRIGGER IF EXISTS trg_proc_time_spent_ins;
CREATE TRIGGER trg_proc_time_spent_ins BEFORE INSERT ON as_processes
FOR EACH ROW WHEN COALESCE(NEW.time_spent, 0) <= 0
BEGIN SELECT RAISE(ABORT, '소요시간(분)은 0보다 커야 합니다'); END;

-- S-8: 근무구분은 office 또는 field
DROP TRIGGER IF EXISTS trg_as_work_place_upd;
CREATE TRIGGER trg_as_work_place_upd BEFORE UPDATE ON as_receipts
FOR EACH ROW WHEN TRIM(COALESCE(NEW.work_place,'')) <> ''
                 AND NEW.work_place NOT IN ('office','field')
BEGIN SELECT RAISE(ABORT, '근무구분은 office 또는 field 여야 합니다'); END;
```

**검증 결과 (사본 실행)**

| 시나리오 | 결과 |
|----------|------|
| 예정일 없이 `schedule_confirmed=1` UPDATE | ✅ 차단 — 「일정확정에는 예정업무일이 필요합니다」 |
| `work_place='home'` UPDATE | ✅ 차단 — 「근무구분은 office 또는 field 여야 합니다」 |
| `time_spent=0` INSERT | ✅ 차단 — 「소요시간(분)은 0보다 커야 합니다」 |
| 예정일과 확정을 **함께** UPDATE | ✅ 통과 |
| `work_place='field'` UPDATE | ✅ 통과 |

> **트리거는 최후 방어선이다.** 사용자에게는 화면·서버에서 친절한 메시지를 먼저 보여 주고, 트리거는 API 직접 호출·이관 스크립트가 규칙을 우회하지 못하게 막는 용도로 둔다.

### B.5 기존 데이터 보정 (§5.3)

> **반드시 백업 후 실행.** 아래 건수는 운영 DB 사본에 실제 실행해 확인한 값이다.

```sql
-- B-1: 예정일 없이 확정된 건의 확정 해제  → 실행 결과 1,124건
UPDATE as_receipts SET schedule_confirmed = 0
 WHERE schedule_confirmed = 1
   AND TRIM(COALESCE(visit_scheduled_date,'')) = '';

-- B-2: start_datetime 백필 (첫 조치 시각)  → 실행 결과 1,128건
UPDATE as_receipts SET start_datetime = (
        SELECT MIN(p.process_datetime) FROM as_processes p WHERE p.as_id = as_receipts.as_id)
 WHERE TRIM(COALESCE(start_datetime,'')) = ''
   AND EXISTS (SELECT 1 FROM as_processes p WHERE p.as_id = as_receipts.as_id);

-- B-3: 이관 데이터 표시  → 실행 결과 1,124건
UPDATE as_receipts SET data_origin = 'import'
 WHERE TRIM(COALESCE(import_key,'')) <> '';
-- 나머지 50건은 기본값 'app' 유지
```

**보정 순서:** B-3 → B-2 → B-1 → 트리거 생성 → UNIQUE 인덱스.
(트리거를 먼저 만들면 B-1 UPDATE가 자기 트리거에 걸리지 않지만, 순서를 고정해 두면 재현이 쉽다.)

### B.6 보정 효과 검증 (실측)

B-2 백필 전후의 **접수→방문 KPI 표본 수** 변화다. 지표가 실제로 살아나는지 확인한 것이다.

| 주 | 보정 전 | 보정 후(전체) | 보정 후(앱 입력만) |
|---|---|---|---|
| 2026-06-22 | — | 48건, 6.7일 | — |
| 2026-06-29 | — | 47건, 2.4일 | — |
| 2026-07-06 | — | 83건, 2.4일 | — |
| 2026-07-13 | — | 44건, 5.7일 | — |
| 2026-07-20 | 6건, 23.7일 | **69건, 9.0일** | 6건, 23.7일 |
| 2026-07-27 | 9건, 9.6일 | **53건, 4.0일** | 9건, 9.6일 |
| 2026-08-03 | 17건, 7.4일 | 34건, 4.0일 | 18건, 7.5일 |
| 2026-08-10 | 10건, 9.0일 | 11건, 8.2일 | 11건, 8.2일 |

**읽는 법**

- 백필만으로 표본이 **6건 → 69건**으로 늘어 지표가 ⚪ 측정불가에서 🟢 신뢰로 올라간다.
- 다만 「보정 후(전체)」는 **이관 데이터가 섞인 값**이다. 6~7월이 2.4~6.7일로 나오는 것은 실제 성과가 아니라 과거 데이터의 성질이다.
- 「앱 입력만」 열이 **v2.0이 기본으로 보여 줄 값**이다. 지금은 표본이 6~18건이라 🟡 참고 등급이며, 앱 사용이 늘면서 자연히 🟢로 올라간다.
- **8~9일이 나오는 것이 현재의 진짜 수준이다.** 목표 3일과 5일 이상 차이 난다. 이 격차를 아는 것이 개선의 출발점이다.

---

## 부록 C. 지표 검증 쿼리

운영 중 정기적으로(주 1회 권장) 실행해 데이터 품질을 확인한다. **관리 > 데이터 관리** 화면에 「정합성 점검」 버튼으로 넣는 것을 권장한다.

| # | 목적 | 쿼리 | 기대값 | 베타 실측 |
|---|------|------|--------|-----------|
| V-1 | 예정일 없이 확정된 건 | `SELECT COUNT(*) FROM as_receipts WHERE schedule_confirmed=1 AND TRIM(COALESCE(visit_scheduled_date,''))=''` | **0** | 보정 후 **0** |
| V-2 | 미계획 AS (예정일·사유 모두 없음) | `SELECT COUNT(*) FROM as_receipts WHERE status NOT IN ('completed','closed','partial_complete','cancelled') AND TRIM(COALESCE(visit_scheduled_date,''))='' AND TRIM(COALESCE(schedule_no_date_reason,''))=''` | **0** | **3** |
| V-3 | 조치는 있는데 착수시각 없음 | `SELECT COUNT(*) FROM as_receipts r WHERE TRIM(COALESCE(r.start_datetime,''))='' AND EXISTS(SELECT 1 FROM as_processes p WHERE p.as_id=r.as_id)` | **0** | 보정 후 **0** |
| V-4 | 소요시간 미입력 조치 | `SELECT COUNT(*) FROM as_processes WHERE COALESCE(time_spent,0)<=0` | 신규분 **0** | **1,172** (전건 — 기존분) |
| V-5 | 근무구분 미입력 완료건 | `SELECT COUNT(*) FROM as_receipts WHERE status IN ('completed','closed','partial_complete') AND TRIM(COALESCE(work_place,''))=''` | 신규분 **0** | **1,165** |
| V-6 | 원인분류 미입력 완료건 | `SELECT COUNT(*) FROM as_receipts WHERE status IN ('completed','closed','partial_complete') AND TRIM(COALESCE(cause_type,''))=''` | 신규분 **0** | **1,135** |
| V-7 | 자산 미연결·사유도 없음 | `SELECT COUNT(*) FROM as_receipts WHERE TRIM(COALESCE(asset_id,''))='' AND TRIM(COALESCE(asset_unlinked_reason,''))=''` | 신규분 **0** | **668** |
| V-8 | 데이터 출처 분포 | `SELECT data_origin, COUNT(*) FROM as_receipts GROUP BY 1` | `app` 비중 증가 | `app` 50 / `import` 1,124 |
| V-9 | 정기점검 방문 중복 | `SELECT COUNT(*) FROM (SELECT plan_id,visit_date,customer_id,COALESCE(product_type,'') FROM maintenance_visits GROUP BY 1,2,3,4 HAVING COUNT(*)>1)` | **0** | **0** |
| V-10 | 사업 연결된 정기점검 방문 | `SELECT COUNT(*) FROM maintenance_visits WHERE TRIM(COALESCE(project_id,''))<>''` | 증가 추세 | **0** (컬럼 신설 직후) |

**계획 수립률 (§8.4)**

```sql
SELECT
  SUM(CASE WHEN TRIM(COALESCE(visit_scheduled_date,'')) <> ''
            OR TRIM(COALESCE(schedule_no_date_reason,'')) <> ''
           THEN 1 ELSE 0 END) * 100.0 / COUNT(*) AS 계획수립률
FROM as_receipts
WHERE status NOT IN ('completed','closed','partial_complete','cancelled');
```

베타 실측(보정 후): 미완료 8건 중 5건 → **62%**. 목표 95%.

> **V-4~V-7의 기존분은 소급 입력하지 않는다.** 이관 데이터(`data_origin='import'`)는 지표에서 제외되므로 채울 필요가 없다. **신규 건이 0을 유지하는지**만 본다. 검증 화면에서는 `data_origin='app'` 조건을 붙인 값을 기본으로 보여 준다.

---

## 부록 D. v1.0 → v2.0 변경 대조표

| v1.0 | v2.0 | 변경 내용 |
|------|------|-----------|
| §1 추진 배경 및 목표 | **§1** | 목표를 담당자/관리자 관점으로 분리, **성공 판단 기준** 추가 |
| §2 시스템 성격과 기본 원칙 | **§2** | **§2.3 운영 3원칙 신설** |
| §3 범위 정의 | **§3** | 정기점검 자동 수립 일부를 **범위에서 제외** |
| §4 정보 구조 | (§28에 흡수) | 논리 데이터 모델로 통합 |
| — | **§4 지표 정의와 수집 책임** | **신설.** 입력 지점·표시 규칙·신뢰도 등급 |
| — | **§5 필수 입력과 정합성** | **신설.** 제약 5종·보정 계획 |
| §13 화면(메뉴) 구성안 | **§6 메뉴 구조** | **계획→실행→확인 재편.** URL은 유지 |
| §10.4 일일 업무 등록 | **§7** | 예정일 **필수화** |
| — | **§8 미계획 업무함** | **신설.** 계획 수립률 지표 포함 |
| §17 정기점검(계획 부분) | **§9** | 「일정 배정안됨」을 대시보드·미계획 업무함에 합산 |
| §10.5 일일 업무 회의 | **§10** | **메뉴 노출로 전환**, 미계획 요약 열 추가 |
| §10.1 역할별 대시보드 / §10.3 워크보드 | **§11** | **「미계획」 KPI 카드 추가**, KPI에 신뢰도 등급 |
| §8 AS 접수·처리·현황 | **§12** | 예정일·소요시간·근무구분·원인분류 **필수화**, `start_datetime` **자동 기록**, `data_origin` 추가 |
| §10.3 업무(행정/지원)<br>**§10.3.2 (v1.0 추가분)** | **§13** | **[v2.2] 병합·확정** — v1.0 §10.3.2의 GTD 설계(수집함·다음 행동·조치 이력·상태 9종·구현 단계)를 **뼈대로 채택**하고, 여기에 **접수일/완료일 컬럼·지표 반영·리드타임 분리·소요시간 필수화**를 얹음. 차이는 §13.14 |
| §10.6 업무처리현황 | **§14** | 유지 |
| §13 통계 | **§15** | 신뢰도 등급·이관 데이터 토글·누락 배너·계획 수립률·내근외근/원인 분석 추가 |
| §13 주간업무보고서 | **§16 보고서** | **별도 화면으로 분리.** 「미계획건」 시트·월간 보고 추가 |
| §9 영업 활용 | **§17 교체대상현황** | **4단계로 연기.** 필요 데이터와 충족도 명시 |
| §5 기준정보 | **§18~§20** | 유지 |
| §6 설치자산 | **§21** | **S/N·설치일 필수화**(회피 경로 포함), EOL 항목 추가 |
| §7 수행관계 | **§21.6** | 유지 |
| §10.3.1 사업(프로젝트) | **§22.1** | 기준정보로 이동 |
| §11 코드관리 | **§22.2** | 근무구분·미정 사유·미연결 사유·데이터 출처 코드 추가 |
| §10.2 업무 고유번호 | **§22.3** | 하부업무 번호 명시 |
| §17 정기 점검 | **§23** | **§17.7 자동 수립 대폭 축소**, `project_id` 결함 수정, UNIQUE 제약, 휴무일 등록 화면 |
| §10 권한 | **§24** | 필수 입력은 역할 무관 적용 명시 |
| §15 백업·변경이력 | **§25** | **이력 기록 대상 표 신설**, 백업 목록 결함 지적 |
| — | **§26 배포·운영 환경** | **신설.** Dockerfile 재현성·JWT·PostgreSQL 전환 기준 |
| §15 개발 시 고려사항 | **§27** | 검증 계층 위치·지표 산식 단일화 추가 |
| §12 논리 데이터 모델 | **§28** | 실제 테이블명 기준으로 갱신, **스키마 변경 10종** |
| §14 핵심 업무 규칙 | **§29** | 3개 규칙 추가(11~13) |
| §16 단계별 구축안 | **§30** | **0단계(지표 복구) 신설**, 완료 판정 기준 추가 |
| §18 결론 | **§31** | 개정 취지 반영 |
| — | **부록 A~D** | **신설.** 필수 입력·DDL·검증 쿼리·변경 대조 |

### D.1 삭제된 요구사항

| v1.0 위치 | 내용 | 삭제 사유 |
|---|---|---|
| §17.4 | 네이버 지도 등 경로 API 연동 | 실사용 0건. 대체 수단(수동 테이블)도 미구현 |
| §17.4 / §17.10 | 지역 간 표준 이동시간 테이블·캐시 | 위와 동일 |
| §17.5 | 09:30 출발 · 17:00 마지막 도착 역산 | 자동 배치에서 제외. 하루 예상 소요 **표시**로 대체 |
| §17.7-2 | 지역 클러스터링 | 지역별 묶음 나열까지만 유지 |
| §17.7-4 | 방문 순서 최적화(TSP 근사) | 캘린더 수동 조정으로 대체 |
| §8 | 처리결과 「임시조치」·「제조사에스컬레이션」 | v1.0에서 이미 미사용 명시 — 코드 목록에서도 제거 |

### D.2 우선 착수 5개 (§30 0단계 = 검토 의견서 P0)

| # | 작업 | 근거 조항 | 효과 |
|---|------|-----------|------|
| 1 | 예정일-확정 정합성 제약 + 1,124건 보정 | §5.2-1 · 부록 B.4 · B.5 | 실행율 **분모 복구** |
| 2 | `start_datetime` 자동 기록 + 1,128건 백필 | §4.2 · §12.5-4 · 부록 B.5 | 방문 KPI 표본 **6건 → 69건** |
| 3 | `time_spent`·`work_place`·`cause_type`·`process_type` 필수화 | §5.2-3 · 부록 A.2 | 최장 소요·원인 분석 **작동** |
| 4 | 분모 0 → `—` 표시 + 신뢰도 등급 | §4.3 · §4.4 | **오독 차단** |
| 5 | `data_origin` 도입 · 이관 데이터 KPI 제외 | §4.3 · 부록 B.1 | 7일 지표 **오염 제거** |

---

*문서 끝. 본 기획서 v2.0이 기획·구현·DB·테스트의 유일 기준이다.*
