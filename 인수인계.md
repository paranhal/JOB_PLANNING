# 인수인계 — 고객지원시스템 (JOB_PLANNING)

**작성일:** 2026-09-10  
**목적:** 이 저장소(소스·문서·배포 이미지)를 **다른 시스템으로 통째로 복사**한 뒤, 그쪽에서 독자적으로 수정·운영하기 위한 안내입니다.  
**상태:** 베타 운영 중. 기획서 **v2.34** 기준.

---

## 1. 이 프로그램이 무엇인가

도서관·학교 등 고객 기관의 **AS · 정기점검 · 일일 업무 · 행정지원 · 영업**을 한 화면에서 다루는 **웹 업무일지**입니다.

- 브라우저로 접속하는 **Go 서버** 하나입니다. (`server/`)
- 화면은 서버가 HTML을 그립니다. (별도 React/SPA 없음)
- 데이터는 **SQLite** 파일 하나입니다. (`server/data/app.db` 또는 운영 볼륨 `deploy/data/app.db`)
- 운영은 **Docker 이미지** (`deploy/server-app.tar`)를 서버에 올려 실행합니다. 호스트 포트 **8888** → 컨테이너 8080.

**지금 실제로 쓰는 코드는 `server/` 입니다.** 루트의 Python 패키지 `as_support/` 는 초기 데스크톱 실험본이며, 운영 대상이 아닙니다.

---

## 2. 복사할 때

### 2.1 권장

프로젝트 폴더 `JOB_PLANNING` 전체를 복사합니다. (Git이 있으면 `.git` 포함해도 됩니다.)

받는 쪽에서 **바로 실행·수정**하려면 아래가 있으면 됩니다.

| 넣을 것 | 이유 |
|---------|------|
| `server/` | 애플리케이션 전부 |
| `기획_업무일지_프로그램_요구사항_v2.md` | **유일한 요구사항 기준** |
| `화면설계서_요약.md`, `검수_배포_탭.md`, `AGENTS.md` | 화면·검수·작업 규칙 |
| `deploy/` | 배포 스크립트·이미지. `server-app.tar`는 2026-09-09 빌드본 |
| `docs/` | 연동 API, 데이터 동기화 등 참고 |

### 2.2 빼거나 따로 다루기

| 항목 | 이유 |
|------|------|
| `server/.env` | JWT·API 키. **평문을 메일에 넣지 말 것.** 별도로 전달하거나 받는 쪽에서 `.env.example`을 복사해 새로 만듦 |
| `server/data/app.db` 및 `*.db-wal` | **운영 데이터.** 운영 DB를 넘길지, 빈 DB로 시작할지 먼저 정함. 넘기면 개인정보·업무 이력이 함께 갑니다 |
| `deploy/data/` | 위와 같음. **운영 서버에 이미 DB가 있으면 앱 이미지만 올리고 data는 덮어쓰지 말 것** |
| `.venv/`, `server/node_modules/` | 재설치하면 됨 |
| `비젼아이티 웹서버 접속 정보.md` | **계정·비밀번호가 들어 있을 수 있음.** 복사본에서 빼거나, 권한을 제한하고 별도 채널로만 전달 |
| Cursor 로컬 캐시 (`.cursor/`, agent-transcripts) | 개발 PC 전용. 필수는 아님 |

운영 서버 SSH/SFTP, JWT 시크릿, 연동 API 키, 공휴일 API 키는 **이 문서에 적지 않습니다.** 담당자가 따로 넘기세요.

### 2.3 받는 쪽 최소 환경

- **Go 1.22** (모듈 `go 1.22.0`. 예: 1.22.12)
- Git (선택)
- 로컬 실행만: Go. Docker는 없어도 `go run` 가능
- 운영 배포: Docker Engine + Compose, 리눅스 amd64

Windows에서 리눅스 바이너리를 만들 때는 `CGO_ENABLED=0 GOOS=linux GOARCH=amd64` 로 빌드합니다. Docker가 없으면 `server/scripts/pack_server_app_tar.py`로 `deploy/server-app.tar`를 패킹한 전례가 있습니다. (사전에 `TEMP/jp-image-root/app/server` 리눅스 실행 파일과 `TEMP/jp-image-assets` 가 필요합니다.)

---

## 3. 문서를 이렇게 읽으세요

**유일 기준은 v2 기획서입니다.** 구현·DB·테스트·패치는 이 문서를 따릅니다.

| 문서 | 역할 | 비고 |
|------|------|------|
| **`기획_업무일지_프로그램_요구사항_v2.md`** | 기획·요구사항 **유일 기준** (현재 v2.34) | 이걸로 작업 |
| `화면설계서_요약.md` | 화면·메뉴. v2 §6 | 현행 |
| `검수_배포_탭.md` | 검수·배포 절차 | 현행. 일부 「배포 전 결함」은 이후 코드에서 이미 고친 것이 있음. 코드가 우선 |
| `AGENTS.md` | Cursor로 고칠 때 역할 나누기 | 현행 |
| `deploy/서버배포매뉴얼.md` | 우분투에 tar 올리는 방법 | 현행 |
| `docs/영업관리_연동_API.md` | 외부 시스템이 고객·담당자를 조회하는 API | 현행 |
| `인수인계.md` | 본 문서 | |

**작업 기준으로 쓰지 말 것**

| 문서 | 이유 |
|------|------|
| `기획_업무일지_프로그램_요구사항.md` 및 `docs/archive/` 의 v1 | 폐기. §번호가 다름. 구 §17.7 정기점검 자동 수립 등은 범위가 축소됨 |
| `README_업무일지.md`, `다음에_할_일.md`, `구현_탭_파일구조_설계.md` | 초기의 Python/JSON 설계. **현재 스택과 다름** |
| `server/README.md` 의 「구현 현황」표·CDN 설명 | 오래됨. Tailwind/Alpine/htmx는 **`server/web/static/vendor/` 고정 파일** (오프라인) |

새 기능을 넣을 때는 기획서 **§4.2 지표 입력 지점**에 먼저 쓰고, 그다음 코드를 고칩니다.

---

## 4. 디렉터리

```
JOB_PLANNING/
├── 기획_업무일지_프로그램_요구사항_v2.md   ← 유일 기준
├── 인수인계.md                            ← 본 문서
├── AGENTS.md
├── 화면설계서_요약.md
├── 검수_배포_탭.md
├── server/                                ← 실제 앱
│   ├── cmd/server/main.go                 진입점. 기동 시 작업 디렉터리를 server 루트로 맞춤
│   ├── internal/
│   │   ├── handler/                       HTTP · HTML 렌더
│   │   ├── repository/                    SQLite
│   │   ├── model/                         도메인 · 역할 · 칸반 · 통계 산식
│   │   ├── config/
│   │   ├── backup/                        일일 백업
│   │   └── audit/ auditlog/               변경 이력 · 접속 기록
│   ├── web/templates/                     Go html/template
│   ├── web/static/vendor/                 Tailwind · Alpine · htmx (CDN 없음)
│   ├── migrations/                        001 ~ 041 (+ .down.sql)
│   ├── templates/                         보고서 HWPX 등
│   ├── data/                              로컬 SQLite (gitignore 대상일 수 있음)
│   ├── Dockerfile
│   ├── docker-compose.yml                 개발용, 포트 8080
│   ├── .env.example
│   └── scripts/pack_server_app_tar.py     Docker 데몬 없이 이미지 tar 만들기
├── deploy/                                운영 전달물
│   ├── server-app.tar                     docker load 용 이미지 (server-app:latest)
│   ├── server-app.tar.prev                직전 이미지
│   ├── docker-compose.yml                 호스트 8888, 볼륨 ./data
│   ├── start.sh / stop.sh
│   └── 서버배포매뉴얼.md
├── docs/
├── as_support/                            구 Python GUI. 운영 아님
└── job_planning/                          더 이전 코드. 참고만
```

역할(관리자·기술·영업·접수/행정·관찰자)은 `server/internal/model/role.go` 입니다.

---

## 5. 로컬에서 실행

```bash
cd server
cp .env.example .env          # Windows: copy .env.example .env
# JWT_SECRET, HOLIDAY_API_KEY, INTEGRATION_API_KEY 를 필요하면 채움
go run ./cmd/server
```

- 주소: `http://localhost:8080`
- DB가 없으면 `data/app.db`를 만들고 스키마·마이그레이션을 **기동 시 자동 적용**합니다.
- 관리자 계정이 없으면 `admin` 계정을 만듭니다. **최초 비밀번호는 운영 정책에 맞게 바꾸세요.** (개발 문서·배포 README에 `admin / admin` 안내가 남아 있을 수 있음)

테스트:

```bash
cd server
go test ./internal/handler ./internal/repository ./internal/model -count=1
```

전체 `go test ./...` 는 환경·기존 실패 건이 섞여 있을 수 있습니다. 고친 화면과 관련된 패키지부터 돌리는 것이 안전합니다.

Windows에서 `GOOS=linux`를 빌드에 썼다면, 테스트 전에 **`GOOS=windows`로 되돌리세요.** 안 그러면 로컬 `go.exe` 테스트가 깨집니다.

---

## 6. 운영 배포 (앱만 교체)

운영 DB가 이미 서버에 있으면 **`server-app.tar`만** 올리고 `data/`는 올리지 않습니다.

개발 PC에서 이미지 만들기 (Docker가 될 때):

```powershell
cd server
docker compose build
docker save server-app:latest -o ..\deploy\server-app.tar
```

서버:

```bash
cd /home/sys2/deploy          # 실제 경로는 인수 시 확인
docker compose down
docker load -i server-app.tar
docker compose up -d
docker compose ps
docker logs --tail 50 server-app-1
```

- 스키마 변경은 **앱 기동 시 마이그레이션으로 자동 반영**됩니다. 기동 전 백업을 권장합니다. (`관리 > 데이터 관리 > 지금 저장`)
- 롤백: `server-app.tar.prev`를 load 한 뒤 같은 방식으로 올림. (서버에 prev를 남겨 두었는지 확인)
- 상세: `deploy/서버배포매뉴얼.md`, `deploy/README.txt`

프론트는 이미지 안의 `web/`을 씁니다. 운영 compose에 소스 폴더를 볼륨으로 붙이지 마세요.

---

## 7. 데이터

- **엔진:** SQLite, WAL, `modernc.org/sqlite` (CGO 없음). PostgreSQL 전환은 기획에만 있고 코드 경로는 아직 SQLite 중심입니다.
- **스키마 출처:** `InitDB`의 기본 DDL + `server/migrations/` (001~041).
- **이관 데이터:** AS 등 `data_origin='import'` 는 과거 엑셀 적재분입니다. 지표는 **앱에서 생긴 건(`data_origin='app'`)** 과 구분해 셉니다. (기획서 §4)
- **정합성:** 기획서 부록 C (V-1~V-10). 화면 `관리 > 데이터 관리`의 정합성 점검.
- **백업:** 일일 자동 + 수동. 폴더와 `data_backups` 테이블.

운영 DB를 복사해 줄 경우, 받는 쪽은 개인정보 취급을 전제로 보관하세요.

---

## 8. 권한 요약 (기획 §37.3)

| 역할 | 오늘 내 업무 (`/work`) | 남의 업무 편집 |
|------|------------------------|----------------|
| 기술담당 · 영업담당 · 관찰자 | 본인만. 담당자 선택 없음 | 기술·영업은 본인만 편집. 관찰자는 읽기 전용 |
| 관리자 · 접수담당(office) | 담당자 드롭다운. 기본값 본인. **「전체」 없음** | 편집 가능 (`/work/all` 등) |

오늘 팀 전체는 **일일 업무 회의** (`/meeting`), 기간 전체는 **전체 업무** (`/work/all`)가 담당합니다. `/work`에 팀 전체 토글을 다시 넣지 마세요. (기획 §38.2 · §38.8)

---

## 9. 코드를 고칠 때 지킬 원칙 (기획 v2)

1. **계획 없는 업무는 만들지 않는다.** 예정일을 못 정하면 「미정 + 사유」.
2. **실행은 흔적을 남긴다.** 조치 저장 시 착수시각(자동)·소요시간·근무구분·원인분류.
3. **측정할 수 없으면 표시하지 않는다.** 분모 0이면 `0%`가 아니라 `—`.
4. **칸반은 상태 축이 있는 목록에만.** 열 판정은 공유 함수. (`model`의 칸반 매핑)

담당자 매칭은 **§38.4 공통 함수** (`repository`의 `assigneeMatch*` / `MergeAssigneeKeys`)를 씁니다. 화면마다 이름만 비교하는 필터를 새로 만들지 마세요.

통계 산식은 `internal/model` · `internal/repository`의 기존 함수를 재사용합니다.

---

## 10. 최근에 들어간 것 (참고)

받는 쪽이 “지금 소스가 어디까지인지” 볼 때 유용합니다. 배포 이미지 `deploy/server-app.tar` (2026-09-09)에 포함되어 있습니다.

- **§37** 전체 업무 (`/work/all`) · 정렬 공통 · 남의 업무 읽기 전용(역할별)
- **§38.3** 오늘 내 업무는 본인 기본. 범위 토글(팀 전체) 제거
- **§38.4** 담당자 매칭 통일 (이름·사용자 ID·다중 담당자)
- **§38.8** 관리자·접수담당만 `/work`에서 담당자 선택. 「전체」 없음
- **§38.9** 오늘 내 업무 조회 성능 (조회 경로에서 일괄 UPDATE 제거, 인덱스 041, 날짜 컬럼 `date()` 제거)

기획서 본문 상단 개정일에 **v2.34 (§38.8 · §38.9)** 가 적혀 있습니다.

---

## 11. 알려진 주의

- **JWT:** `.env`의 `JWT_SECRET`을 운영에서 반드시 바꿉니다. 기본값이면 다른 환경과 쿠키가 섞일 수 있습니다.
- **비밀번호:** 예전 SHA-256 해시와 bcrypt 전환이 코드에 있습니다. 운영 계정 정책은 인수 시 확인하세요.
- **공휴일 API · 연동 API:** 키가 없으면 해당 기능만 비활성/503입니다. 소스와 `docker-compose.yml`에 키를 적지 마세요.
- **검수_배포_탭.md §3** 의 일부 항목(통계 `project_id`, 엑셀 색상, 조치내용 필수 등)은 이후에 수정됐을 수 있습니다. 배포 전 차단 목록을 그대로 믿지 말고 **현재 코드·테스트를 보세요.**
- 루트에 요구사항 초안·엑셀 분석 문서가 더 있습니다. 현행이 아니면 참고만 하세요. (`기획_단품납품_영업관리_초안_v0.4.md` 등 — 반영분은 기획서 §32·§36)

---

## 12. 받은 뒤 권장 확인 (30분)

1. `기획_업무일지_프로그램_요구사항_v2.md` 표지(버전)와 `AGENTS.md`를 읽는다.
2. `server/.env.example` → `.env` 후 `go run ./cmd/server`, 로그인, `/work` · `/work/all` · `/meeting` · `/as` 가 열리는지.
3. 운영을 이어받을 경우: 서버에서 `docker compose ps`, `data/app.db` 백업 존재, 이미지 교체 시 **data 미업로드**.
4. 수정 시작 전: 새 기능이 **어느 지표·어느 화면 필드**를 채우는지 v2 §4.2에 적을지 정한다.

문의할 때 “v1 기획서 기준으로”라고 하면 잘못된 기능을 만들게 됩니다. **항상 v2**를 지정하세요.
