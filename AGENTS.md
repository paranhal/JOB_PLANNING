# JOB_PLANNING 에이전트 사용법

**현재 단계:** 베타 운영 중. **기획서 v2.0 기준으로 지표 복구(0단계) 진행.**
모든 설계·구현의 **유일 기준**은 `기획_업무일지_프로그램_요구사항_v2.md` 입니다.

---

## 기준 문서

| 문서 | 역할 | 상태 |
|------|------|------|
| **`기획_업무일지_프로그램_요구사항_v2.md`** | 기획·요구사항의 **유일 기준**. 구현/DB/테스트/패치 모두 이 문서를 따름 | **현행** |
| `기획_업무일지_프로그램_요구사항.md` (v1.0) | 이력 참조용. **작업 기준으로 삼지 말 것** | 폐기 |
| `화면설계서_요약.md` | 화면별 레이아웃·요소. v2.0 §6 메뉴 구조 기준 | 현행 |
| `검수_배포_탭.md` | 검수 절차·배포 방법 | 현행 |
| `JOB_PLANNING_기획검토의견.md` | 베타 데이터 실측 검토 결과 (v2.0 개정 근거) | 참고 |
| `JOB_PLANNING_E2E검증결과.md` | 실제 구동·클릭 검증 결과. 결함 목록 | 참고 |

> **v1.0을 참조하지 마세요.** v2.0에서 §번호가 전면 재편됐고, 정기점검 자동 수립(구 §17.7)은 범위가 축소됐습니다. v1.0 기준으로 작업하면 폐기된 요구사항을 구현하게 됩니다.

---

## v2.0에서 반드시 지켜야 할 3가지

에이전트에게 작업을 시킬 때 이 세 가지는 매번 확인하세요.

> **1. 계획 없는 업무는 만들지 않는다** — 모든 단위업무는 예정일을 가진다. 못 정하면 「미정 + 사유」.
>
> **2. 실행은 흔적을 남긴다** — 조치 저장 시 착수시각(자동)·소요시간·근무구분·원인분류가 함께 남는다.
>
> **3. 측정할 수 없으면 표시하지 않는다** — 분모 0이면 `0%`가 아니라 `—`.
>
> **4. 칸반은 상태 축이 있는 목록에만 붙인다 (§35.2·§35.5)** — 없으면 칸반 금지·그룹 보기. 있으면 열을 정하고 §35.3. 판정은 공유 함수. 열 7개 초과면 필터를 다시 본다.

새 기능을 만들 때 **"이 기능이 채우는 지표는 무엇이고, 어느 화면 어느 필드에서 채워지는가"** 를 v2.0 §4.2 표에 먼저 추가한 뒤 구현합니다.

---

## 에이전트 목록

| 역할 | 규칙 파일 | 담당 범위 |
|------|-----------|-----------|
| **기획** | `@.cursor/rules/agent-planning.mdc` | v2.0 유지·보완, 요구사항 정리, §4.2 지표 입력 지점 관리 |
| **구현** | `@.cursor/rules/agent-implementation.mdc` | v2.0 기준 코드·파일구조·화면 구현 |
| **DB 설계** | `@.cursor/rules/agent-db.mdc` | v2.0 §28 스키마, 부록 B 제약·DDL, 데이터 보정 |
| **테스트** | `@.cursor/rules/agent-test.mdc` | 단위/통합 테스트, 부록 C 검증 쿼리 |
| **패치** | `@.cursor/rules/agent-patch.mdc` | 버그수정·핫픽스·배포·검수 (`검수_배포_탭.md` 기준) |

### 호출 방법

1. Cursor 채팅(Cmd+L)을 연다
2. 입력창에 **@** 를 쳐서 **Rules**에서 에이전트 규칙을 선택
3. 지시를 입력한다

채팅마다 한 가지 에이전트만 붙이면 역할이 섞이지 않습니다.

규칙 파일 위치: `.cursor/rules/agent-*.mdc`

---

## 작업 지시 예시 (0단계 기준)

```
@agent-db
v2.0 부록 B.1~B.4를 적용해 줘. SQLite는 ALTER TABLE ADD CHECK을
지원하지 않으니 트리거로 구현하고, 부록 B.5 보정 SQL을
순서(B-3 → B-2 → B-1 → 트리거 → UNIQUE)대로 실행하는
마이그레이션 파일 003을 만들어 줘.
```

```
@agent-implementation
v2.0 §4.2에 따라, 첫 조치 등록 시 as_processes.process_datetime을
as_receipts.start_datetime에 자동 기록해 줘(비어 있을 때만).
사용자 입력란은 추가하지 말고 서버에서 채운다.
```

```
@agent-implementation
v2.0 부록 A.2에 따라 조치 화면(as/action.html)에 소요시간(분) 입력란을
추가하고, 저장 시 as_processes.time_spent에 넣어 줘. 기본값 30분.
현재 이 입력란이 아예 없어서 전 건 0으로 저장되고 있다.
```

```
@agent-test
v2.0 부록 C의 V-1~V-10 검증 쿼리를 관리 > 데이터 관리 화면에
「정합성 점검」 버튼으로 붙여 줘. 신규 건(data_origin='app') 기준으로 센다.
```

---

## 에이전트에게 넘기기 전 확인

- [ ] v2.0의 어느 §를 근거로 하는지 지시에 적었는가
- [ ] 새 필드를 만든다면 §4.2 「입력 지점」 표에 넣었는가
- [ ] 필수 입력을 늘린다면 **회피 경로**(미정+사유 등)를 함께 정의했는가
- [ ] 지표를 표시한다면 분모 0·표본 부족 시 표시를 정했는가 (§4.3)
- [ ] 새 지표 산식을 만들지 않고 `model/stats.go`의 기존 함수를 재사용했는가
- [ ] 새 목록에 상태 축이 없으면 칸반을 붙이지 않았는가 (§35.5)
