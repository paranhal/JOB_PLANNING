# 채팅 로그 — 2025-03-06 v1

업무일지/고객지원 프로그램 기획·구현 세션에서의 질의와 답변 요약입니다.

---

## 1. 고객정보에 상위기관 여부·상위기관 표시 없음

**질의:** 고객정보에 상위기관 여부와 상위기관 표시가 없어.

**답변·조치:**
- 고객(기관) 폼에 **상위기관 여부**(체크박스 "상위기관 있음")와 **상위기관** 선택(콤보박스) 필드 추가.
- 체크 시에만 상위기관 콤보 활성화, 값은 `parent_customer_id`로 저장.
- `as_support/ui/frames/customer.py`: `_refresh_parent_combo()`, `_get_form_data`/`_set_form`에 `parent_customer_id` 반영.
- `as_support/store/customer.py`: `update()` 시 `parent_customer_id` 등 허용 필드만 갱신하도록 `ALLOWED_UPDATE_KEYS` 적용.

---

## 2. 버튼 위치 및 스크롤

**질의:** 버튼들을 윗 줄로 비꿔저. 그리고, 하단 이나 우측에 내용 더 있으면 스크롤 바 생성해줘.

**답변·조치:**
- **버튼 상단 이동:** 기관(고객), 담당자, 설치자산, SW 상세 네 화면 모두에서 「신규」「저장」 버튼을 상세 영역 맨 위로 이동.
- **스크롤:** 위 네 화면의 상세 폼을 `QScrollArea`로 감싸서, 내용이 영역보다 클 때 가로·세로 스크롤바가 생기도록 설정 (`ScrollBarAsNeeded`).
- 수정 파일: `customer.py`, `contact.py`, `installation.py`, `installation_sw.py`.

---

## 3. Git 반영 및 채팅 로그 저장

**질의:** git에 변경사항 수정하고 저장해줘. 그리고, 채팅 내용(내가 질의하고 답변한 모든 내용)을 로그 파일 저장해줘. Chat_날짜_versionNo.md 또는 텍스트 파일로.

**답변·조치:**
- **Git:** `git add -A` 후 커밋 메시지  
  `feat: 고객 상위기관 UI, 버튼 상단 배치, 상세 영역 스크롤 적용 및 as_support 1단계 구현 반영`  
  로 커밋 후 `git push` 수행.
- **채팅 로그:** 이 파일 `Chat_2025-03-06_v1.md`에 질의·답변 요약 저장.

---

## 이전 세션 요약 (참고)

- 기획서 docx → md 변환, 에이전트 규칙·AGENTS.md·다음에_할_일.md 정리.
- 데이터/구현 설계 문서 작성, as_support 1단계 구현 (store, services, PySide6 UI).
- tkinter → PySide6 전환, 고객 폼 필드 너비·정렬, 사진 슬롯(링크 저장·미리보기) 추가.

---

*생성일: 2025-03-06*
