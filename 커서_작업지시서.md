# Cursor 작업지시서 — 기획서 v2.28 반영

**작성일:** 2026-08-14 (기획서 v2.5 기준)
**대상:** `C:\Users\테크센터\PROJECT\JOB_PLANNING`
**원칙:** 한 번에 한 단계. 각 단계는 **`go build ./...` 통과**로 끝낸다.

---

## 0단계 — 파일 배치 (Cursor 열기 전에 손으로)

에이전트가 옛 문서를 읽으면 폐기된 요구사항을 구현합니다. **v1.0을 먼저 치워야 합니다.**

```
JOB_PLANNING/
├ 기획_업무일지_프로그램_요구사항_v2.md      ← 새로 넣기 (유일 기준)
├ AGENTS.md                                ← 덮어쓰기
├ 화면설계서_요약.md                         ← 덮어쓰기
├ 검수_배포_탭.md                            ← 덮어쓰기
├ docs/
│  ├ JOB_PLANNING_기획검토의견.md            ← 넣기 (참고)
│  └ JOB_PLANNING_E2E검증결과.md             ← 넣기 (참고)
└ docs/archive/
   └ 기획_업무일지_프로그램_요구사항.md       ← v1.0을 여기로 이동
```

**추가 확인:** `.cursor/rules/agent-*.mdc` 5개를 열어 **v1.0 파일명을 참조하는 줄이 있으면 v2 파일명으로 바꿉니다.** 규칙 파일이 옛 기준을 가리키면 @로 붙일 때마다 잘못된 문서를 봅니다.

---

## 사용법 — 매 요청 공통

**1. 컨텍스트에 문서를 붙입니다.** Cursor 입력창에서 `@` → Files → `기획_업무일지_프로그램_요구사항_v2.md` 선택. 붙이지 않으면 에이전트가 기획서를 안 읽고 추측합니다.

**2. 역할 규칙도 붙입니다.** `@agent-implementation` 또는 `@agent-db`.

**3. 아래 프롬프트를 그대로 붙여넣습니다.** § 번호와 파일 경로가 들어 있어 에이전트가 헤매지 않습니다.

**4. 끝나면 검증 명령을 직접 돌립니다.** 에이전트 말을 믿지 말고 확인하세요.

```bash
cd server
go build ./... && go vet ./... && go test ./...
```

**5. 통과하면 커밋하고 다음 단계로.** 통과 못 하면 다음으로 넘어가지 마세요.

---

## 금지 사항 — 모든 프롬프트에 붙일 것

아래 4줄을 **매 요청 끝에** 복사해 붙이세요. 이 프로젝트에서 반복적으로 어긋난 지점들입니다.

```
제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

마지막 줄이 특히 중요합니다. 지금 소스가 깨진 이유가 정확히 그것입니다 — `main.go`에 라우트를 등록하고 `admin_work.go`에서 호출까지 해 놓고 타입·리포지토리·핸들러를 안 만든 상태로 멈춰 있습니다.

---

# 1단계 — 빌드 복구 [최우선]

> **지금 `go build ./...` 가 실패합니다.** 이걸 고치기 전에는 다른 어떤 작업도 검증할 수 없습니다.

### 프롬프트 1-1

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

현재 server 소스가 컴파일되지 않는다. 행정업무 다자 협업 기능이
DDL과 호출부만 있고 타입·리포지토리·핸들러가 없는 상태다.
기획서 §13(행정·지원업무 GTD)과 §13.13(구현 상태)을 기준으로 빌드를 복구해 줘.

이미 있는 것 (건드리지 말 것):
- internal/repository/db.go 의 work_actions / work_activities 테이블 DDL
- internal/repository/db.go 의 work_tasks 확장 컬럼
  (hold_reason, review_date, cancel_reason, wait_party_kind, wait_party,
   wait_request, reply_due_date, next_check_date, complete_note)
- cmd/server/main.go:268~271 의 라우트 등록
- internal/handler/admin_work.go:145,155 의 호출부

없어서 빌드가 깨지는 것 (만들어야 할 것):
1. internal/model/workboard.go
   - 타입 WorkAction, WorkActivity  (db.go의 컬럼과 1:1로 맞출 것)
   - 상수 WBTaskInbox="inbox", WBTaskWaitingFor="waiting_for",
          WBTaskCancelled="cancelled", WBActionWaiting, WBActionDone 외
          §13.7 조치 이력 유형(작성/수정/검토요청/회신/발송/독촉/완료/기타)
   - WorkTask 구조체에 누락 필드 추가
     (WaitPartyKind, WaitParty, WaitRequest, ReplyDueDate,
      NextCheckDate, CompleteNote, ReviewDate, CancelReason)
   - func IsAdminGTDTask(t WorkTask) bool
   - func AdminGTDErr(...) string   ← admin_work.go:135 의 호출 시그니처에 맞출 것
2. internal/handler/admin_work.go 또는 workboard.go
   - func applyAdminGTDForm(t *model.WorkTask, c echo.Context)
3. internal/repository/wb_repo.go
   - CreateAction, UpdateAction, ListActions
   - CreateActivity, ListActivities
4. internal/handler/workboard.go
   - CreateAction, UpdateAction, CreateActivity
     (main.go 가 등록한 시그니처에 맞출 것)

상태 전이는 기획서 §13.5 표(상태 9종)를 따른다.
waiting_for 진입 시 대기 대상·요청 내용·회신 예정일 또는 다음 확인일이 필수다.

주의: 「할 일」은 기존 코드 waiting 을 그대로 쓴다(화면 문구만 변경).
「회신 대기」가 신규 코드 waiting_for 다. 둘을 섞지 마라.

제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

### 확인

```bash
cd server && go build ./... && go test ./...
```

빌드가 통과하면 **여기서 한 번 커밋**하세요. 이후 작업의 기준점이 됩니다.

---

# 2단계 — 지표 복구 (기획서 §30 0단계)

> 관리 지표 3종이 계산되지 않는 원인을 제거합니다. **다섯 건 모두 작은 수정입니다.**

### 프롬프트 2-1 — 착수시각 자동 기록 (효과가 가장 큼)

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §4.2와 §12.5-4에 따라, 첫 조치 등록 시 as_receipts.start_datetime 을
서버가 자동으로 채워 줘.

현재 문제:
- internal/handler/as.go:859 가 c.FormValue("start_datetime") 로만 값을 받는다.
- 사용자가 안 채우면 영원히 빈다. 실측 채움률 3.2%.
- 그 결과 「접수→방문 3일」 KPI가 표본 부족으로 계산되지 않는다.

요구:
1. 조치 저장 시 as_processes 를 만들 때, as.StartDatetime 이 비어 있으면
   그 조치의 process_datetime 을 start_datetime 에 기록한다. 이미 값이 있으면 덮어쓰지 않는다.
2. 사용자 입력란은 늘리지 마라. 서버가 조용히 채운다.
3. as.go:1065 는 현재 start_datetime → process_datetime 방향으로 복사하는데,
   방향이 반대다. 두 로직이 충돌하지 않게 정리하라.
4. 기존 데이터 백필 SQL을 migrations 에 추가하라
   (기획서 부록 B.5 의 B-2. 대상 약 1,128건).

테스트: 착수시각 없이 조치를 저장하면 start_datetime 이 채워지는지 확인하는
단위 테스트를 internal/handler 에 추가하라.

제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

### 프롬프트 2-2 — 소요시간 입력란 추가

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 부록 A.2에 따라 AS 조치 화면에 소요시간(분) 입력란을 추가해 줘.

현재 문제:
- web/templates/as/action.html 의 입력 필드 35개 중 time_spent 가 없다.
- ASHandler.Update 도 time_spent 를 읽지 않는다.
- 유일하게 읽는 곳은 AddProcess(/as/:id/process) 인데 화면에서 쓰지 않는다.
- 그래서 as_processes 1,172건 전부 time_spent=0 이고,
  기획서 §15.5 「최장 소요」 통계가 항상 빈 값이다.

요구:
1. as/action.html 결과 입력부에 소요시간(분) 입력란 추가. 기본값 30.
2. ASHandler.Update 에서 time_spent 를 읽어 as_processes 에 저장.
3. 0 이하이면 저장을 거부하고 ?err=time_spent 로 되돌린다.
4. 화면 상단 오류 배너에 「소요시간을 입력하세요」 문구 매핑 추가.

제약: (위 4줄 동일)
```

### 프롬프트 2-3 — 예정일·확정 정합성 제약 + 데이터 보정

```
@agent-db
@기획_업무일지_프로그램_요구사항_v2.md

기획서 부록 B(전체)를 마이그레이션 파일 003 으로 만들어 줘.

포함할 것:
- B.1 컬럼 추가: as_receipts.data_origin / schedule_no_date_reason /
  asset_unlinked_reason, maintenance_visits.project_id / data_origin
- B.2 holidays 테이블 신설
- B.3 정기점검 방문 UNIQUE 인덱스
- B.4 트리거 4종 (SQLite는 ALTER TABLE ADD CHECK 미지원이므로 트리거로 구현)
- B.5 데이터 보정 SQL

보정 실행 순서는 반드시 B-3 → B-2 → B-1 → 트리거 생성 → UNIQUE 인덱스 로 한다.
부록에 적힌 예상 건수(B-1 1,124건 / B-2 1,128건 / B-3 1,124건)와
실제 실행 결과가 맞는지 로그로 남겨라.

주의: 부록 B의 DDL과 트리거는 운영 DB 사본에 실제로 실행해 검증된 것이다.
문법을 임의로 바꾸지 말고 그대로 쓰라.

제약: (위 4줄 동일)
```

### 프롬프트 2-4 — 이관 데이터 분리 + 지표 표시 규칙

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §4.3(데이터 공백 표시 규칙)과 §4.4(지표 신뢰도 등급)를 구현해 줘.

요구:
1. 지표 집계에서 data_origin='import' 건을 기본 제외한다.
   통계 화면 상단에 「과거 이관 데이터 포함」 토글을 두고, 켤 때만 합산한다.
   (현재 AS 1,174건 중 1,124건이 과거 엑셀 이관분이라 7일 지표가 왜곡되어 있다)
2. model/stats.go 의 ExecutionRatePct() 가 분모 0일 때 0을 반환하는데,
   화면에서 「0%」와 「측정 불가」가 구분되지 않는다.
   분모 0이면 값 대신 「—」와 사유를 표시하도록 반환 타입을 바꾸거나 플래그를 더하라.
3. 각 KPI 카드에 §4.4 신뢰도 등급(🟢 표본≥20 / 🟡 5~19 / ⚪ <5 또는 분모0)과
   표본 건수를 함께 표시한다.
4. 대시보드·통계·주간보고서 세 화면이 같은 산식을 쓰는지 확인하는 테스트를 추가하라.

제약: (위 4줄 동일)
```

### 프롬프트 2-5 — 조치내용 필수 검증

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

조치 결과가 「완료(done)」일 때 조치내용이 비어 있어도 저장되는 문제를 고쳐 줘.

재현: 결과=done, action_taken="" 로 저장하면 상태가 completed 가 되고
처리 이력에 자동 문구 「조치 저장」만 남는다.
재방문·이관·대기는 필수 검증이 있는데 완료만 빠져 있다.

요구: action_taken 이 공백이면 ?err=action_required 로 반려하고,
화면 상단 배너에 문구를 매핑한다.
근무구분(work_place)·원인분류(cause_type)·처리유형(process_type)도
기획서 부록 A.2 기준으로 필수 처리한다.

제약: (위 4줄 동일)
```

### 확인 — 2단계 전체

```bash
cd server && go build ./... && go test ./...
```

그리고 기획서 **부록 C의 V-1~V-10**을 DB에 직접 돌려 보세요. V-1(예정일 없이 확정)과 V-3(조치 있는데 착수시각 없음)이 **0**이 되어야 합니다.

---

# 3단계 — 배포 차단 결함

### 프롬프트 3-1

```
@agent-patch
@검수_배포_탭.md

검수_배포_탭.md §3 의 확인된 결함 3건을 고쳐 줘.

3.1 통계 사업 필터 500
  internal/repository/stats_dashboard.go:726 이 maintenance_visits.project_id 를
  참조하는데 컬럼이 없다. 지금 배포본은 이 코드가 없어 멀쩡하지만,
  현재 소스를 배포하면 /stats?project=... 가 죽는다.
  → 2단계에서 만든 마이그레이션 003 에 컬럼 추가가 포함됐는지 확인하고,
    포함됐다면 이 항목은 확인만 하고 넘어가라.

3.2 정기점검 엑셀 색상값 규격 위반
  internal/handler/maintenance_excel.go 가 excelize 에 8자리 ARGB("FF00B050")를
  넘기는데 excelize 는 6자리 RGB 를 받아 앞에 FF 를 붙인다.
  결과가 10자리(FFFF00B050)가 되어 OOXML 규격 위반이고 openpyxl 이 파일을 거부한다.
  → 폰트 색·채우기 색 전부에서 앞의 FF 를 제거하라.
    "FF00B050"→"00B050", "FF0070C0"→"0070C0", "FFE2EFDA"→"E2EFDA" 등.

3.4 조치내용 공란 완료 → 2단계 프롬프트 2-5 에서 처리했으면 확인만.

Dockerfile 도 함께 고쳐라:
  - COPY go.mod ./ → COPY go.mod go.sum ./
  - 빌드 중 `go mod tidy` 제거 (빌드마다 의존성이 달라진다)
  - 운영 docker-compose 에서 ./web:/app/web 볼륨 제거

제약: (위 4줄 동일)
```

### 확인

정기점검 엑셀을 내려받아 Python으로 열어 보세요. **열리면 규격 통과입니다.**

```python
import openpyxl
wb = openpyxl.load_workbook('유지보수계획.xlsx')
print(wb.sheetnames)   # ['1월', ... '12월'] 나오면 정상
```

---

# 4단계 — 계획 습관 정착 (기획서 §30 1단계)

### 프롬프트 4-1 — 미계획 업무함 신설

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §8(미계획 업무함)을 새로 만들어 줘. URL 은 /plan/unplanned 다.

배경: 예정일 없는 미완료 업무가 1,100건 넘게 쌓였는데 어느 화면에도
드러나지 않았다. 담당자는 잊었고 관리자는 몰랐다.

요구:
1. §8.1 의 5유형 판정과 뱃지
   (예정없음🔴 / 예정일경과🔴 / 다음일정미정🟡 / 담당자미배정🟠 / 미정사유만료🟡)
2. §8.2 동작: 목록에서 날짜 직접 입력 즉시 배정, 여러 건 일괄 배정,
   「미정+사유」 등록(14일 후 재검토 뱃지 자동), 원본 화면 이동
3. §8.3 대시보드에 「미계획 n건」 KPI 카드 추가 → 이 화면으로 링크
4. §8.4 계획 수립률 지표를 통계 화면과 주간보고서에 추가
5. §9 의 정기점검 「일정 배정안됨」 건수도 이 화면과 KPI 카드에 합산

기존 /work 의 판정 로직(예정일 경과 vs 다음 일정 미정, 기획서 §11.1)을
재사용하고 새로 짜지 마라.

제약: (위 4줄 동일)
```

### 프롬프트 4-2 — 메뉴 재편

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md
@화면설계서_요약.md

기획서 §6 에 따라 메뉴를 계획→실행→확인 축으로 재편해 줘.
web/templates/layout/base.html 만 고치면 된다.

중요: URL 은 하나도 바꾸지 마라(§6.3). 메뉴 라벨과 그룹만 재편한다.
베타가 돌고 있고 테스트 60여 개가 URL 에 결합되어 있다.

대메뉴 6개: 홈 / 계획 / 실행 / 확인 / 기준정보 / 관리
각 하위 화면 배치는 §6.2 와 §6.4 대응표를 그대로 따른다.
역할별 노출은 §6.5 표를 따른다.
일일 업무 회의(/meeting)는 계획 메뉴에 노출한다(종전 비노출).

제약: (위 4줄 동일)
```

---

### 프롬프트 4-3 — AS 접수 사진

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §12.9(접수 사진)를 구현해 줘.

현재: web/templates/as/form.html 에 파일 입력이 하나도 없다.
사진은 조치 화면(ref_type='as')에서만 올릴 수 있어서, 기술담당이
현장에 가 본 뒤에야 증상을 시각적으로 확인한다.

요구:
1. 저장 구분 (§12.9.1) — 조치 첨부와 섞지 마라
   - 접수 사진: ref_type='as_receipt', data/uploads/as/{접수ID}/receipt/
   - 조치 첨부: 기존 ref_type='as' 그대로
2. 등록 (§12.9.2)
   - /as/new, /as/{id}/edit, /as/{id} 에서 업로드·삭제
   - 최대 10장. 이미지(jpg/png/webp/heic) + 일반 파일. 원본 20MB 이하
   - 선택 입력. 완료·종료 건은 잠금(관리자 해제 시에만 가능)
   - 등록·삭제를 data_change_logs 에 남긴다
3. 표시 (§12.9.3)
   - 이미지는 썸네일, 클릭 시 라이트박스로 원본 확대(좌우 이동)
   - 이미지 아닌 파일은 기존대로 파일명만
   - AS 목록(/as)에는 썸네일을 깔지 말고 📷 n 뱃지만
4. 자동 축소 (§12.9.4)
   - 업로드 시 긴 변 1600px 로 축소해 저장, 원본 미보관
   - 별도로 320px 썸네일 생성
   - EXIF 회전 반영 (세로 사진이 눕지 않게)
   - HEIC 입력을 지원하는 라이브러리를 고를 것 (휴대폰 사진이 HEIC 로 온다)
5. 사진별 메모 (§12.9.5)
   - 기존 attachments.keywords 컬럼 재사용. 새 컬럼을 만들지 마라
6. 자산 사진 승격 (§12.9.6)
   - 접수에 asset_id 가 있을 때만 「자산 사진으로」 버튼 노출
   - 자산 사진 슬롯(최대 3장, §21.1)에 복사. 3장이 차 있으면 교체 슬롯 선택
   - 접수 사진은 그대로 남는다(이동 아님). 메모도 키워드로 함께 복사
7. 조치 화면 읽기전용 표시 (§12.9.7)
   - /as/{id}/action 상단에 접수 사진 썸네일. 추가·삭제·수정 불가
   - 「접수 시 증상 사진」과 「조치 첨부」를 시각적으로 구분
   - 사진이 없으면 영역 자체를 표시하지 않는다

기존 /attachments 핸들러와 attachments 테이블을 재사용하라.
ref_type 분기만 추가하면 된다. 새 테이블을 만들지 마라.

제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

### 확인

접수 등록 시 휴대폰 사진(HEIC, 5MB 이상)을 올려 보세요.

- [ ] 썸네일이 뜨고 클릭하면 확대되는가
- [ ] 세로로 찍은 사진이 눕지 않는가 (EXIF 회전)
- [ ] `data/uploads/as/{접수ID}/receipt/` 파일 크기가 원본보다 작은가
- [ ] 조치 화면 상단에 읽기전용으로 보이는가
- [ ] 자산이 연결된 접수에서 「자산 사진으로」가 동작하는가
- [ ] 완료 건에서 삭제 버튼이 막히는가

---

### 프롬프트 4-4 — 주간업무보고서 전면 개편

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §16.1(주간업무보고서)이 v2.4로 전면 개정됐다. 그대로 다시 만들어 줘.
현재 구현은 옛 기준(시트 4개 · 담당자 드롭다운)이라 통째로 바꿔야 한다.

핵심 변경:
- 시트 4개(통계·업무리스트·진행중건·미계획건) → 시트 2개(통계·전체 리스트)
- 담당자 드롭다운 제거. 담당자 전원을 한 표에 행으로 나열
- 신규 지표 3종: 일일 평균 · 일일 최대(+날짜) · 이월 건수
- 접수 건수는 접수자가 아니라 배정담당자 기준

시트 1 「통계」 — §16.1 표 그대로
  행: 첫 행 사업팀 전체, 아래 담당자별 (완료 건수 내림차순, 미배정은 맨 아래)
  열: 구분 · 계획대비 실행률 · 접수 · 완료 · 일일평균 · 일일최대(+날짜)
      · 이월 · 진행중 · 미계획
  추가 행: 접수→방문 평균일수(목표 3일) · 접수→완료 평균일수(목표 7일)
           · 계획 수립률 · 정기점검 월 진행(월목표/예약/완료/누적/남은)
  각 지표 옆에 신뢰도 등급(🟢🟡⚪)과 표본 건수 (§4.4)

  정의 주의:
  - 워킹데이 = 기간 내 평일 − 공휴일. holidays 테이블(§23.6) 사용.
    미등록 연도면 주말만 빼고 「공휴일 미등록」 주석을 시트에 남긴다
  - 일일 평균 = 완료 건수 ÷ 워킹데이 수 (소수 1자리)
  - 일일 최대 = 하루 최다 완료 건수. 동점이면 가장 이른 날짜
  - 이월 = 기간 종료일 24시 기준 미완료 건수 (이전 주에서 넘어온 건도 포함)
  - 팀 전체 행의 실행률·평균은 담당자 행의 합이 아니라 팀 기준 재계산

시트 2 「전체 리스트」 — §16.1 표 그대로
  그 주에 접수·조치·완료·방문 중 하나라도 발생한 건을 모두 싣는다
  한 건이 접수·조치·완료를 다 거쳤으면 각각 한 행
  열: 구분(접수/조치/완료/점검/행정) · 업무번호 · 발생일 · 유형 · 거래처
      · 제품·모델 · 담당자 · 내용 · 결과·상태 · 소요(분)
  헤더에 엑셀 필터. 발생일 오름차순, 같은 날은 업무번호 순

공통:
- 대상은 AS + 정기점검 + 행정/지원 합산
- data_origin='import' 건 제외. 시트 상단에 「과거 이관 데이터 제외」 명시
- 파일명 YYYYMMDD_주간업무보고.xlsx
- 실행률·소요일 산식은 model/stats.go 기존 함수를 재사용하고 새로 짜지 마라

제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

### 확인

`http://localhost:8080/stats/weekly-report.xlsx` 를 받아서 확인하세요.

- [ ] 시트가 **2개**인가 (통계 · 전체 리스트)
- [ ] 통계 시트에 담당자가 **전원 행으로** 나오는가 (드롭다운 없음)
- [ ] 일일 평균 · 일일 최대(날짜 포함) · 이월 열이 있는가
- [ ] 워킹데이 수가 시트에 표시되는가 (이번 주는 **5일**)
- [ ] 전체 리스트에 접수·조치·완료가 **각각 행으로** 나오는가
- [ ] 헤더에 엑셀 필터가 걸려 있는가

---

### 프롬프트 4-5 — 보고서 화면 신설

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §16(보고서)의 전용 화면을 만들어 줘. URL 은 /stats/reports 다.

현재 문제:
- 주간업무보고서 다운로드가 web/templates/stats/overview.html:207 의
  필터 줄 끝에 작은 버튼(text-xs)으로 숨어 있다. 사용자가 못 찾는다.
- 정기 산출물을 뽑으러 가는 경로가 메뉴에 없다.

요구:
1. /stats/reports 화면 신설. 산출물을 카드로 나열한다.
   각 카드: 제목 · 설명 · 기간 선택 · 큰 다운로드 버튼

   ┌ 주간업무보고서 ─────────────────────┐
   │ 통계 · 전체 리스트 2개 시트          │
   │ 주차 [2026-08-10 ~ 08-16 ▾]         │
   │            [ 엑셀 다운로드 ]         │
   └─────────────────────────────────────┘
   ┌ 월간업무보고 ───────────────────────┐
   │ 월 [2026-08 ▾]      [ 엑셀 다운로드 ]│
   └─────────────────────────────────────┘
   ┌ 정기점검 월별 캘린더 ───────────────┐
   │ 연도 [2026 ▾]       [ 엑셀 다운로드 ]│
   └─────────────────────────────────────┘
   ┌ 상세 통계 ──────────────────────────┐
   │ 종류·범위·기간 선택 → /stats/detail 로 이동 │
   └─────────────────────────────────────┘

2. 각 카드의 다운로드는 기존 엔드포인트를 그대로 쓴다. 새로 만들지 마라.
   - /stats/weekly-report.xlsx?date=
   - 월간은 §16.2 기준 (미구현이면 카드는 두되 「준비 중」 표시)
   - /maintenance/{계획}/export.xlsx
   - /stats/export.xlsx

3. 메뉴 「확인」 그룹에 **보고서** 항목을 추가한다 (§6.2).
   layout/base.html 만 고치면 된다.

4. stats/overview.html 의 기존 「주간업무보고서」 버튼은 그대로 둔다.
   통계 보다가 바로 받는 동선도 유용하다. 다만 크기를 Excel 버튼과
   같게 맞춰 눈에 띄게 한다.

5. 권한: 관리자·접수담당·기술담당 조회 (§24)

제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

### 확인

- [ ] 메뉴 「확인 > 보고서」가 보이는가
- [ ] `/stats/reports` 에서 주차를 골라 받으면 그 주 파일이 오는가
- [ ] 통계 화면의 기존 버튼도 그대로 동작하는가

---

# 5단계 — 행정·지원 GTD 완성 (기획서 §30 2.5단계)

1단계에서 빌드만 복구했다면, 여기서 기능을 완성합니다.
**기획서 §13은 v2.2에서 v1.0 §10.3.2의 GTD 설계와 병합됐습니다.** 용어가 바뀐 곳이 있으니 §13.14를 먼저 보세요.

### 프롬프트 5-1 — 접수일·완료일 컬럼

```
@agent-db
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §13.4 와 §28 S-11 에 따라 work_tasks 에 컬럼을 추가해 줘.
마이그레이션 004 로 만든다.

- receipt_date TEXT   (접수일, 기본 등록일)
- complete_date TEXT  (완료일, 완료 처리 시 서버가 기록)

이유: 현재 work_tasks 에 완료일 컬럼이 없어서 통계가 updated_at 을
완료일로 쓴다. 완료 후 담당자나 진행률을 한 번만 고쳐도 완료일이
그날로 밀려 소요일 지표가 조용히 틀어진다.
기획서 §13.1 사례(2일 걸린 일이 1일로 기록됨)의 직접 원인이다.

이어서 통계 산식에서 행정/지원의 완료 기준을
date(updated_at) → date(complete_date) 로 교체하라.
해당 위치: §10 전일 실적, §15.3 처리 기준, stats 관련 리포지토리.
기존 데이터는 complete_date 가 비면 updated_at 으로 대체 조회한다(COALESCE).

제약: (위 4줄 동일)
```

### 프롬프트 5-2 — 수집함·상태 세분화

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §13.5(상태 9종)를 구현해 줘.

상태: 수집함(inbox) / 할 일(waiting) / 진행중(in_progress) /
      회신 대기(waiting_for) / 보류(hold) / 이관(transfer) /
      검토중(review) / 완료(complete) / 취소(cancelled)

중요 — 하위호환:
- 「할 일」은 기존 코드 waiting 을 그대로 쓴다. 화면 문구만 「대기」→「할 일」로 바꾼다.
  코드를 바꾸면 기존 데이터와 테스트가 전부 흔들린다.
- 「회신 대기」는 신규 코드 waiting_for 다. 둘을 같은 말로 쓰지 마라.

요구:
1. /admin-work 에 수집함 등록(제목만으로 빠른 등록)과 분류 기능
2. §13.5 전환 시 필수값 검증
   - waiting_for → 대기 대상·요청 내용·회신 예정일 또는 다음 확인일
   - hold → 사유·재검토일
   - cancelled → 사유 (삭제하지 않고 보존)
3. /admin-work/stats 카드를 수집함·할 일·진행중·회신 대기·보류·이관·완료·오늘로 재구성
4. 칸반 검토 열 = 보류·이관·검토중·회신 대기 (§11). 수집함은 칸반에 올리지 않는다
5. 일일 업무 등록 팔레트에서 수집함은 분류 전까지 제외하거나 「수집함」으로 구분 (§7.3)

AS·정기점검에는 이 상태를 적용하지 마라 (§13.2 적용 범위).

제약: (위 4줄 동일)
```

### 프롬프트 5-3 — 다음 행동 · 조치 이력

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §13.6(다음 행동)·§13.7(조치 이력)·§13.9(완료 검증)·§13.11(화면)을 구현해 줘.

실제 사례가 §13.1 에 있다. 충남교육청통합도서관 IRM 평가지표 작성 건인데,
홈페이지·자료관리·전자도서관 3개 부문을 각각 다른 회사에 요청하고
1~2차례 회신·수정을 주고받아 2일 걸린 일이 업무일지에는 1건 1일로 남았다.
이 업무를 §13.7 의 트리 구조 그대로 기록할 수 있어야 한다.

요구:
1. 업무 상세(/workboard/tasks/{id})에 다음 행동 영역과 조치 이력 영역 추가.
   레이아웃은 §13.11 화면 스케치를 따른다.
2. 다음 행동(work_actions): 제목·상태·필수여부·예정일·기한·담당자
   + 대기 대상 구분(내부담당/부서/외부업체/고객)·대기 대상·요청 내용
   ·회신 예정일·다음 확인일
   → 상태가 waiting 이면 대기 항목 필수 (§13.6)
3. 조치 이력(work_activities): 유형(작성/수정/검토요청/회신/발송/독촉/완료/기타)
   ·내용·작성자·소요 시간(분)·첨부. 일반 사용자는 수정·삭제 불가, 추가만 가능
4. 완료 검증(§13.9): 미완료 필수 다음 행동 또는 미확인 회신 대기가 있으면
   일반 사용자 완료 불가. 관리자는 사유를 남기고 강제 완료 가능(이력에 기록)
5. 진행률 = 완료 다음 행동 수 ÷ 필수 다음 행동 수 (자동, 수동 입력도 허용)
6. 회신 예정일 경과 행동은 미계획 업무함(§8)과 대시보드 「지연」에 잡히게 한다
7. 다음 행동이 없는 진행 중 업무는 「다음 행동 미지정」 경고

주의 — 섞지 말 것 (§13.8):
- 일자 하위업무(parent_task_id)는 시간표 배치용이다. 다음 행동과 다르다.
- 기존 하위업무를 다음 행동으로 자동 변환하지 마라.
- 상태 waiting 인 다음 행동은 시간표에 올리지 않는다.
  다음 확인일이 오면 그날 할 일로 표시한다.

제약: (위 4줄 동일)
```

### 프롬프트 5-4 — 리드타임 분리 지표

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §13.10 의 리드타임 분리 지표를 구현해 줘.

  총 리드타임 = 실작업 시간 + 외부 대기 시간 + 내부 유휴

- 총 리드타임: complete_date − receipt_date
- 실작업 시간: work_activities.spent_minutes 합
  → spent_minutes 는 입력 필수로 한다. AS 의 time_spent 가 필드는 있는데
    입력란이 없어 1,172건 전부 0인 전례가 있다. 같은 실수를 반복하지 마라.
- 외부 대기 시간: 다음 행동의 요청 이력 → 회신 이력 구간 합.
  구간이 겹치면 한 번만 센다. 3개사에 동시에 요청한 하루를 3일로 세면 안 된다.

행정/지원의 착수 시각은 첫 조치 이력의 일시로 본다(AS 의 start_datetime 대응).

통계 화면 「업무 분석」(§15.5)에 평균 리드타임과 그중 외부 대기 비중을 표시하고,
주간업무보고서 통계 시트(§16.1)에도 행으로 추가하라.
「회신 대기」 건은 미계획건 시트에 대기 대상·회신 예정일과 함께 싣는다.

목적: 접수→완료 7일을 넘겼을 때 우리가 늦은 건지 업체 회신이 늦은 건지
나뉘어야 조치가 달라진다.

제약: (위 4줄 동일)
```

> **2단계(반복업무 템플릿·문서 버전·알림·주간 검토 화면·댓글/참조자)는 여기 넣지 마세요.** 기획서 §13.12에 2단계로 분리돼 있습니다.

# 6단계 — 운영 신뢰성 (기획서 §30 3단계)

### 프롬프트 6-1

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §25.2 에 따라 변경 이력 기록을 정비해 줘.

현재 문제: 접수 6건 생성 · 조치 2건 저장 · 정기점검 방문 추가/완료를
수행해도 data_change_logs 에 행이 하나도 늘지 않았다(운영 DB 전체 1건).
§25.2 의 롤백 기능은 전제가 성립하지 않는다.

요구: §25.2 의 「이력 기록 대상」 표대로 기록 지점을 붙여라.
● 기록 / ○ 미기록 구분을 그대로 따르고, 일상적인 시간표 배치는
기록하지 마라(노이즈로 가득 차면 롤백에 쓸 수 없다).

함께 고칠 것 — 백업 목록:
백업 폴더는 생성되는데 data_backups 테이블은 0건이라 목록 화면이 비어 보인다.
폴더 생성 시 테이블에도 행을 남기고, 서버 기동 시 폴더를 스캔해 누락분을 보정하라.

제약: (위 4줄 동일)
```

### 프롬프트 6-2 — 프런트엔드 CDN 로컬화

```
@agent-implementation
@화면설계서_요약.md

화면설계서_요약.md §4 에 따라 외부 CDN 의존을 제거해 줘.

현재 web/templates/layout/base.html 이 아래 3종을 실시간 로드한다.
  https://cdn.tailwindcss.com
  https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js
  https://unpkg.com/htmx.org@2.0.3/dist/htmx.min.js

실제로 외부 접속을 막고 구동해 보니 42개 화면 전부에서 로드가 실패했고
(총 132건), 스타일과 모달·드롭다운이 전부 죽었다.
web/static/ 은 비어 있다. 폐쇄망 고객 현장에서는 사용 불가다.

요구:
1. 3종을 web/static/vendor/ 에 로컬 배치하고 버전을 고정한다.
   alpinejs@3.x.x 는 버전 미고정이라 하위 릴리스 변경 시 예고 없이 깨진다.
2. Tailwind 는 Play CDN(cdn.tailwindcss.com)을 쓰지 않는다.
   공식 문서가 운영 비권장이라고 명시한 개발용이다. 빌드 시 CSS 를 생성해
   정적 파일로 서빙하라.
3. Dockerfile 이 web/static 을 이미지에 포함하는지 확인하라.
4. 인터넷을 끊은 상태에서 화면이 정상 동작하는지 확인하고 결과를 보고하라.

제약: (위 4줄 동일)
```

### 프롬프트 6-3 — 비밀번호 해시 (보안)

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

internal/handler/auth.go:24 의 HashPassword 가 무염 SHA-256 단독이다.
솔트도 키 스트레칭도 없어 레인보우 테이블로 즉시 역산된다.
실제로 현재 DB 의 해시 3종이 널리 알려진 값과 그대로 일치했다.

요구:
1. golang.org/x/crypto/bcrypt 로 교체한다.
2. 기존 SHA-256 해시는 첫 로그인 성공 시 bcrypt 로 자동 재해시한다
   (사용자가 비밀번호를 다시 칠 필요가 없도록).
3. 마이그레이션 완료 여부를 관리자 화면에서 확인할 수 있게 한다.
4. AS 수정 전용 비밀번호(app_settings.as_completed_edit_password_hash)도 동일하게 처리한다.

제약: (위 4줄 동일)
```

---

# 7단계 — 정기점검 안정화 · 접속기록 [v2.5 신설]

> **6-A·6-B 는 다른 모든 단계보다 먼저 하세요.** 운영 서버에서 정기점검 데이터가 두 번 사라졌습니다.

### 프롬프트 6-A — 자동배정 점검대상 누락 [1번 원인]

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

자동배정으로 일정을 만들었는데 그 사이트가 「일정 배정안됨」 목록에 그대로 남는다.
원인은 자동 생성이 점검대상(product_type)을 안 채우기 때문이다.

확인된 사실:
- service/maintenance_generate.go:70,94 가 InsertVisit(..., cat, "") 로 호출한다.
  InsertVisit 은 ProductType 을 설정하지 않으므로 항상 빈 값이 된다.
- model.VisitProductSlotKey("") 는 "generic" 을 반환한다.
- repository.ListUnassignedMonthSlots 는 사이트 설정의 도래 슬롯(klas/rfid)과
  대조하는데, have["klas"] 가 없으니 미배정으로 남는다.
- genericUsed 보정이 있지만 generic 1건이 슬롯 1개만 대신하므로,
  KLAS+RFID 둘 다인 사이트는 항상 1개가 미배정으로 남는다.

요구 — 기획서 §23.5 대로 고쳐라:
1. 자동 초안은 그 달에 점검주기가 도래한 사이트만 대상으로 한다
   (inspection_cycle 반영. 지금은 12개월 전부에 모든 사이트를 뿌린다)
2. 사이트마다 점검대상별로 방문을 나눈다.
   KLAS만=1건, RFID만=1건, 둘 다=2건. product_type 을 반드시 채운다
3. HasVisitInMonth 중복 검사도 product 를 넘겨 대상별로 판정한다
4. 고정규칙 사이트를 먼저 배치하고, 나머지를 지역별로 묶어 25일 이전 평일에 나열한다
5. 25일 이전에 못 넣으면 「일정 배정안됨」으로 보낸다. 26일 이후로 밀어 넣지 않는다
6. 하루 배치 상한은 설정값(기본 4건)으로 둔다

범위 밖(§23.5에서 제외됨): 이동시간 API · 지역 클러스터링 · 17시 도착 역산 · TSP

테스트: KLAS+RFID 둘 다인 사이트를 자동배정하면 방문 2건이 생기고
「일정 배정안됨」에서 사라지는지 확인하는 테스트를 추가하라.

제약:
- 새 테이블·엔터티를 만들지 마라. 기존 테이블의 컬럼·상태값으로 표현할 수 있는지 먼저 확인하라.
- 지표 산식을 새로 짜지 마라. model/stats.go의 기존 함수를 재사용하라.
- 작업을 끝내기 전에 반드시 `go build ./...` 와 `go test ./...` 가 통과하는지 확인하고, 결과를 보고하라.
- 라우트·호출부만 먼저 만들고 구현을 비워 두지 마라. 컴파일되지 않는 중간 상태로 끝내지 마라.
```

### 프롬프트 6-B — 데이터 소실 차단 [3·9번 · 최우선]

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

정기점검 데이터가 운영 서버에서 두 번 사라졌다. 지우는 경로를 전부 막아라.
기획서 §23.11(계획 삭제 보호)·§23.12(중복 정리)를 따른다.

(1) 조회가 데이터를 지우는 것부터 없애라 — §23.12
handler/maintenance.go:121 이 계획 화면 GET 요청에서
CollapseDuplicateMonthVisits 를 호출해 DELETE 를 실행한다.
- 자동 실행을 제거한다
- 대신 「중복 n건 발견 — 정리하기」 안내만 띄운다
- 정리하기를 누르면 지워질 목록을 먼저 보여주고, 확인해야 실행한다

(2) 기간 보호 — §23.11
- visit_date 가 당월 1일보다 이전인 방문은 어떤 경로로도 삭제 불가
- 당월 방문도 삭제 불가. 다음 달 이후만 가능
- 관리자·삭제 비밀번호로도 우회할 수 없다
- 계획 삭제 시 당월 이전 방문이 1건이라도 있으면 거부하고 건수를 알린다
- 일괄 삭제는 과거·당월 건을 제외하고 미래 건만 지운 뒤 제외 건수를 알린다

(3) 계획 삭제 3단계 — §23.11
- 영향 범위를 숫자로 표시 (방문 n건, 완료 n건 포함)
- 계획 제목을 손으로 입력해야 버튼 활성화
- 삭제 전용 비밀번호(app_settings.maintenance_delete_password_hash)
  AS 수정 비밀번호와 같은 방식, 다른 값
- 삭제 직전 방문을 JSON 으로 data/backups/계획삭제_{계획ID}_{일시}.json 에 백업
- 완료된 방문이 1건이라도 있으면 삭제 불가

(4) 자동배정도 확인을 받아라
service.AutoGenerateMaintenance 가 DeleteAutoVisits 로 기존 자동 생성분을
말없이 지운다. 실행 전 「기존 자동 생성 방문 n건이 삭제됩니다」를 보여주고
확인받아라.

제약: (위 4줄 동일)
```

### 프롬프트 6-C — 정기점검 관리 화면 [2번]

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §23.10(정기점검 관리)을 만들어 줘. /maintenance/{계획}/manage

연도 계획과 방문을 일괄로 손보는 화면이다.
지금은 방문을 한 건씩만 고칠 수 있어 정리에 수십 번 클릭이 필요하다.

기능 — §23.10 표 그대로:
- 연도 계획 목록 (연도·제목·상태·방문/완료 건수·최종수정일)
- 계획 생성 (연도당 1개. idx_maintenance_plans_year UNIQUE)
- 계획 복사 (지난해 사이트·주기·담당자를 새 연도로, 방문일은 비움)
- 계획 상태 변경 (초안↔승인↔보관)
- 계획 삭제 → §23.11 절차
- 방문 일괄 담당자 변경 (월·지역·점검대상 조건)
- 방문 일괄 삭제 → §23.11 기간 보호 적용
- 중복 정리 → §23.12
- 미배정 일괄 배정

권한: 생성·복사·상태변경·일괄변경·삭제는 관리자. 조회는 관리자·기술담당.

제약: (위 4줄 동일)
```

### 프롬프트 6-D — 휴무일 관리 [4번]

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §23.13(공휴일·휴무일 관리)을 만들어 줘. /admin/holidays
holidays 테이블은 마이그레이션 003 에서 이미 만들어져 있다.

요구:
- 연도 탭 → 그 해 목록. 추가·수정·삭제. 달력 미리보기
- 구분: 공휴일(public) · 대체공휴일(substitute) · 회사 휴무일(company)
- kr_holidays.go 의 2025~2027 하드코딩 값을 최초 1회 이 테이블로 시드
- 이후 IsKRHoliday 는 테이블을 우선 조회한다 (하드코딩은 폴백)
- 해당 연도 행이 없으면 정기점검 화면·보고서에
  「{연도} 휴무일 미등록 — 주말만 제외됩니다」 배너
- 「기본 공휴일 불러오기」로 그 해 법정공휴일 일괄 등록
- 권한: 조회 전 역할, 등록·수정·삭제는 관리자

적용 지점: 자동 초안 배치 제외일(§23.5) · 엑셀 캘린더 회색(§23.7)
· 주간/월간 보고서 워킹데이(§16.1) · 담당자별 일일업무 회색 열(§16.4)

제약: (위 4줄 동일)
```

### 프롬프트 6-E — 접속기록 시스템 [8번 · 별도 프로그램]

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §25.3(접속기록 시스템)을 만들어 줘.
개인정보보호법의 개인정보 처리시스템 접속기록 요건을 기준으로 하며,
업무 시스템과 테이블·DB 파일·실행 프로그램을 모두 분리한다.

구조 — §25.3.1:
  업무 서버 cmd/server (:8080)  →  data/app.db
        └ INSERT 만 ─────────→  data/audit/access.db  ← 조회 불가
  접속기록 뷰어 cmd/auditviewer (:8090) → data/audit/access.db (읽기 전용)

기록 항목 — §25.3.2 (법정 5개 항목)
  계정(user_id·username·full_name·role) / 접속일시 /
  접속지(client_ip·forwarded_for·user_agent·session_id) /
  처리한 정보주체(subject_type·subject_id·subject_name) /
  수행업무(action·target_table·target_id·detail)
  + before_json·after_json·reason·result·row_hash(연쇄 해시)

MAC 주소 컬럼은 만들지 마라. 웹에서 수집 불가능하며,
비어 있는 컬럼은 감사 시 오히려 문제가 된다. §25.3.2 주석 참조.

구현 순서 — §25.3.6 대로:
1. data/audit/access.db 스키마 + internal/auditlog (쓰기 전용)
2. 삭제 경로부터 연결 — 정기점검 계획·방문, AS 접수, 사업, 사용자
3. 로그인·로그아웃·로그인 실패
4. cmd/auditviewer 별도 바이너리 + 조회 화면
5. 월간 점검·무결성 검증
6. 고객·담당자 상세 조회 기록

이번 요청은 1·2·3 까지만 한다. 4 이후는 별도로 진행한다.

원칙:
- 업무 서버 코드에 접속기록 SELECT·UPDATE·DELETE 를 만들지 마라
- 두 DB 를 같은 트랜잭션으로 묶지 마라. 기록 실패가 업무를 막으면 안 된다
- append only. UPDATE·DELETE 구문을 코드에 두지 마라

제약: (위 4줄 동일)
```

### 프롬프트 6-F — 담당자별 일일업무 분석 + 보고서 정리 [5·6·7번]

```
@agent-implementation
@기획_업무일지_프로그램_요구사항_v2.md

기획서 §16.4(담당자별 일일업무 분석)와 §16.5(보고서 화면 출력물)를 구현해 줘.

(1) 새 산출물 — §16.4
파일명 YYYYMMDD_담당자별일일업무.xlsx, 시트 2개
  시트1 「일자별」: 행=담당자, 열=날짜 매트릭스. 합계·일평균·최대
    - 토·일·공휴일 열은 회색, 합계에서 제외 (holidays 테이블)
    - 건수 기준 표 아래에 소요시간(분) 기준 표를 하나 더
    - 0인 평일은 연한 빨강
  시트2 「담당자별 상세」: 담당자→날짜→시각 순. 그날 합계 행 포함
    열: 담당자·날짜·요일·구분·업무번호·거래처·내용·상태·소요(분)·누적(분)

(2) 보고서 화면 출력물 2종 — §16.5
  /stats/reports 에서 아래 2개를 기본 출력물로 둔다
  1. 주간업무보고서 (§16.1)
  2. 담당자별 일일업무 (§16.4)
  두 카드가 같은 기간 선택을 공유한다

(3) 주간보고서 버튼 이동 — §16.5
  web/templates/stats/overview.html:207 의 「주간업무보고서」 버튼을 제거하고,
  대신 「보고서로 이동」 링크 하나만 둔다.
  산출물을 받는 곳은 보고서 화면 하나로 통일한다.

제약: (위 4줄 동일)
```

---

# 8단계 — 전사 주간업무보고 시트 생성 [v2.6]

회사 공통 주간업무보고 파일(`(업무)` 시트를 매주 추가하는 그 파일)의 **도서관사업부 20~25행**을 자동으로 채우는 기능입니다. 기획서 §16.6 전체가 근거입니다.

**8-A → 8-B → 8-C 순서를 지키세요.** 8-A(데이터 정비)를 건너뛰면 8-B는 숫자를 사업별로 나눌 수 없어 전부 0이 나옵니다.

### 프롬프트 8-A — 사업 귀속 데이터 정비 [선행 · 필수]

```
@기획_업무일지_프로그램_요구사항_v2.md

§16.6.9 를 구현해줘. 이건 §15 사업별 통계에도 공통으로 필요한 기반이야.

현재 확인된 결함 (실제 DB 확인함):
  - maintenance_visits.project_id 가 76건 전부 NULL
  - 앤로보틱스 고객의 customers.parent_customer_id 가 비어 있음 (주간 방문 10건 영향)
  - 세종 고객의 상위기관이 C044-26-002 인데
    project_scope_rules 는 C000-26-005 를 기대함 (주간 방문 5건 영향)

(1) as_receipts 에 project_id 컬럼을 추가한다 (마이그레이션).

(2) 사업 귀속 해석 함수를 하나만 만든다.
    ResolveProjectID(customerID, productType, workKind) (string, error)
    - customers 의 상위기관을 조상 방향으로 끝까지 타고 올라가며
      project_scope_rules 와 매칭한다. 직속 부모만 보면 안 된다.
    - 이미 같은 규칙을 푸는 코드가 있으면 새로 만들지 말고 그 함수로 통일한다.

(3) 정기점검 방문 생성·수정, AS 접수 생성·수정 시점에
    이 함수로 project_id 를 채워 저장한다.
    조회할 때마다 다시 푸는 방식은 쓰지 않는다.
    규칙이 바뀌면 과거 보고서 숫자가 소급해 달라지기 때문이다.

(4) 기존 데이터 백필 마이그레이션을 만든다.
    maintenance_visits 76건, as_receipts 전건.
    해석 실패 건은 NULL 로 두고, 마이그레이션 로그에 건수를 남긴다.

(5) 해석 실패 건을 확인할 수 있는 목록을 관리 > 데이터 관리 에 추가한다.
    조용히 버리지 않는다.

검증:
  - 백필 후 SELECT COUNT(*) FROM maintenance_visits WHERE project_id IS NULL 을 출력
  - 상위기관 2단 구조(세종)에서 매칭되는지 테스트 케이스로 확인

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 8-B — 행 매핑 테이블 + 문장 생성

```
@기획_업무일지_프로그램_요구사항_v2.md

§16.6.5 · §16.6.6 · §16.6.7 을 구현해줘.

(1) weekly_report_rows 테이블을 만든다 (§16.6.5 컬럼표 그대로).
    초기 데이터도 §16.6.5 표대로 넣는다.
    사업은 매년 바뀌니 코드에 박지 말고 반드시 테이블로 둔다.

(2) 기간 계산 — §16.6.3
    기준일(월요일) 입력 → 전주 = 직전 월~일, 금주 = 기준일~일요일
    시트 이름 기본값 = YY.M월N주(업무),
    N = 전주 월요일이 그 달의 몇 번째 월요일인가
    ※ 이름은 화면에서 사용자가 고칠 수 있어야 한다. 강제하지 않는다.

(3) F열 문장 생성 — §16.6.6, 완료 기준
    정기점검: completed=1 AND completed_date ∈ 전주
    AS: complete_datetime 날짜 ∈ 전주
    행정·지원: status=완료 AND 완료일 ∈ 전주
    조립: 정기점검과 AS 는 한 줄로 합침
          → "·정기점검 8사이트, AS 10건 처리완료"
          행정·지원은 건별 한 줄 "·{제목}({m}/{d})"
          줄 구분은 \n, 각 줄은 · 로 시작
    실적이 없으면 셀을 비운다. "없음" 이라고 쓰지 않는다.

(4) G열 문장 생성 — §16.6.7
    정기점검: visit_date ∈ 금주 (완료 여부 무관)
    행정·지원: due_date ∈ 금주 AND 상태 ≠ 완료
    이월분: 전주 예정이었으나 미완료 → "·{제목}(이월)"
    이월분은 반드시 표시한다.

(5) 어느 행에도 귀속되지 않은 활동은 "미배정"으로 모아 반환한다.

검증 (§16.6.10 기준값):
  2026-08-17 기준으로 생성했을 때 완료 기준 숫자가
  충남 KLAS 정기점검 5, 충남 AS 5, 세종 KLAS 정기점검 2,
  앤로보틱스 정기점검 6, 앤로보틱스 AS 4 로 나오는지 테스트로 고정한다.

제약: (위 4줄 동일)
```

### 프롬프트 8-C — 엑셀 시트 추가 + 화면

```
@기획_업무일지_프로그램_요구사항_v2.md

§16.6.2 · §16.6.4 · §16.6.11 · §16.6.12 를 구현해줘.

(1) 화면 — 확인 > 보고서 에 "전사 주간업무보고 시트 생성" 카드 추가
    회사 xlsx 업로드 + 기준 월요일 선택 + 시트 이름(수정 가능)
    미리보기에서 F·G열 문장을 직접 고칠 수 있어야 한다. 자동 생성은 초안이다.
    H(지원 필요사항)·I(주요 의사결정) 은 화면에서 직접 입력받는다.

(2) 엑셀 생성 — excelize 사용. §16.1 과 같은 라이브러리.
    업로드 파일의 사본에 (업무) 시트를 1장 추가한다.
    삽입 위치: 가장 최근 (업무) 시트 바로 앞
    시트 탭 색: FF0000FF
    열 너비·글꼴·틀고정 F20 등은 §16.4 규격표 그대로
    20~25행만 채운다. 다른 팀 행은 골격도 만들지 않는다.

(3) [가장 중요] 원본 보존 — §16.6.11
    시트 추가 외의 어떤 변경도 하면 안 된다.
    특히 26.8.7(사업) 시트의 VLOOKUP·NETWORKDAYS·CHOOSE 수식이
    값으로 굳으면 안 된다. 조건부 서식·데이터 유효성·숨김 열도 유지돼야 한다.

    저장 후 파일을 다시 열어 아래를 실제로 확인하는 테스트를 반드시 만든다:
      - 시트 수가 1 증가했는가 (7 → 8)
      - 사업 시트의 N3·W3·Y3 이 여전히 수식인가 (= 로 시작하는가)
      - AB 열이 여전히 숨김인가

    보존되지 않으면 이 기능을 "시트 추가" 대신
    "한 장짜리 파일 생성" 으로 후퇴시킨다.
    회사 공통 파일을 망가뜨리는 것보다 낫다. 임의로 강행하지 말고 보고해줘.

(4) 업로드 원본은 서버에 보관하지 않는다. 처리 후 즉시 삭제한다.
    타 부문 계약금액이 들어 있는 파일이다.
    처리 사실만 접속기록(§25.3)에 남긴다.

(5) 결과 화면에 "원본 시트 n개 유지 · 수식 m개 유지" 를 표시한다.
    첫 사용 시 "회사 공통 파일입니다. 생성 전 원본을 따로 보관하세요." 배너를 띄운다.

제약: (위 4줄 동일)
```

---

# 9단계 — 조치완료보고서 (시스템 장애 처리 보고서) [v2.7]

AS 조치를 완료하면 고객 제출용 「시스템 장애 처리 보고서」가 나오게 하는 기능입니다. 기획서 §12.10 이 근거입니다.

**9-A 를 먼저 하세요.** 보고서의 핵심 두 칸(장애원인·결론)이 지금 DB에 없어서, 9-B 만 하면 빈 문서가 나옵니다.

### 프롬프트 9-A — 장애원인·결론 필드 추가

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.10.4 를 구현해줘.

(1) as_receipts 에 컬럼 2개를 추가한다 (마이그레이션 + 롤백).
    cause_detail TEXT   -- 장애원인 서술형
    conclusion  TEXT   -- 결론 서술형

    기존 cause_type(HW고장/SW오류/네트워크/환경문제/사용자오류 코드)과 다른 것이다.
    cause_type 은 통계용 코드, cause_detail 은 고객이 읽는 문장이다.
    절대 하나로 합치지 마라. 기존 cause_type 은 그대로 둔다.

(2) 조치 화면 /as/{id}/action 에 입력란 2개를 추가한다.
    결과가 "완료" 로 선택됐을 때만 열린다.
    다른 결과(추가조치/재방문/이관/대기)에서는 표시하지 않는다.

(3) conclusion 에 자동 초안을 채워준다.
    "{cause_detail}(으)로 {symptom} 문제가 발생했고,
     {최종 as_processes.work_content}하여 정상작동하는 것으로 확인됨."
    초안이므로 사용자가 반드시 수정할 수 있어야 한다.
    문장을 강제하지 마라.

(4) 완료 저장 시 두 칸이 비어 있으면 경고만 하고 저장은 막지 않는다.
    보고서 발급 시점에 다시 요구한다. 조치 저장을 방해하면 안 된다.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 9-B — HWPX 생성

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.10.2 · §12.10.3 · §12.10.5 · §12.10.6 · §12.10.7 을 구현해줘.

전제: server/templates/report/장애처리보고서.hwpx 템플릿이 준비돼 있다.
      HWPX 는 ZIP + XML 이다. Go 표준 archive/zip 으로 다룬다.
      .hwp 바이너리를 직접 만들려고 시도하지 마라. 불가능하다.

(1) 템플릿의 자리표시자를 치환해 HWPX 를 생성한다.
    {{고객명}} {{부서}} {{담당자}} {{연락처}} {{서비스}}
    {{장애사항}} {{장애원인}} {{결론}}
    {{보고일자}} {{점검자}} {{확인자}}
    {{작업일자}} {{조치내용}}

    필드 매핑은 §12.10.3 표 그대로.

(2) 조치사항 표는 1단계 방식으로 간다.
    조치가 여러 건이면 행을 늘리지 말고
    한 셀 안에 날짜별 여러 줄로 넣는다.
    XML 행 복제는 하지 마라. 나중 단계다.

(3) 치환 시 반드시 지킬 것 — §12.10.6
    - XML 특수문자 이스케이프: & < > (실제 접수 내용에 들어 있다)
    - 줄바꿈 \n 은 HWPX 문단 태그로 변환 (그대로 넣으면 한 줄로 붙는다)
    - 템플릿 원본은 절대 덮어쓰지 않는다. 메모리에서 사본을 만든다
    - 결과에 {{ 가 남아 있으면 에러로 처리한다
      자리표시자가 그대로 고객에게 나가면 안 된다

(4) AS 상세 /as/{id} 에 "조치완료보고서" 버튼 + 미리보기 화면 (§12.10.5)
    - 상태가 완료가 아니면 비활성 + 사유 표시
    - cause_detail 이나 conclusion 이 비었으면
      무엇이 없는지 명시하고 입력으로 유도한다
    - 미리보기의 모든 칸은 수정 가능하다
    - 고친 내용을 as_receipts 에 되쓰지 마라.
      문구를 다듬었다고 조치 이력이 바뀌면 안 된다

(5) 파일명 (§12.10.7)
    {고객명}_{증상요약 20자}_조치완료보고서_{YYYYMMDD}.hwpx
    \ / : * ? " < > | 는 _ 로 치환

(6) 발급 이력 — attachments 에 ref_type='as_report', ref_id={as_id}
    재발급은 덮어쓰지 말고 새 이력으로 쌓는다.
    AS 상세에 발급 이력(발급일시·발급자·내려받기)을 보여준다.

검증 (§12.10.8):
  - 증상에 < & " 가 들어간 AS 로 생성해도 파일이 열린다
  - 조치 3건인 AS 로 생성하면 3건이 모두 보인다
  - 결과 파일에 {{ 가 남아 있지 않다
  - 2회 발급 후 attachments 에 2건이 남는다
  생성한 hwpx 를 unzip 해서 XML 이 유효한지 확인하는 테스트를 만든다.

제약: (위 4줄 동일)
```

### 프롬프트 9-C — PDF 변환 [선택 · 마지막]

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.10.6 의 PDF 변환을 구현해줘. 9-B 가 안정된 뒤에만 한다.

(1) Dockerfile 에 HWPX → PDF 변환기(LibreOffice 계열)와 한글 글꼴을 넣는다.
    한글 글꼴이 없으면 글자가 깨진다.

(2) 미리보기 화면에 "PDF 내려받기" 버튼을 추가한다.

(3) 변환 실패해도 HWPX 는 정상 제공한다.
    PDF 가 안 나온다고 전체를 실패시키지 마라.

먼저 보고해줘:
  변환기를 넣었을 때 도커 이미지 크기가 얼마나 늘어나는지 측정해서 알려줘.
  현재 server-app.tar 는 12MB 다. 수백 MB 로 늘면 배포 시간과
  서버 디스크에 영향이 크다.
  증가폭이 부담되면 PDF 는 포기하고 PDF 버튼을 숨긴다.
  한글에서 열어 PDF 로 저장하면 되므로 필수가 아니다.
  임의로 강행하지 말고 크기부터 보고해줘.

제약: (위 4줄 동일)
```

---

# 10단계 — 공휴일 API 연동 · 연차 [v2.8]

「휴일이 제대로 표시되지 않는다」의 원인을 코드에서 확인했습니다. 기획서 §23.13 이 근거입니다.

**10-A 를 오늘 바로 하세요.** 지금 2026년 추석(9/24~25)이 근무일로 잡혀 있어서, 그대로 두면 **추석에 정기점검 방문 일정이 배정됩니다.**

### 프롬프트 10-A — [긴급] 공휴일 데이터 정정

```
@기획_업무일지_프로그램_요구사항_v2.md

§23.13.1 · §23.13.9 를 반영해줘. 데이터가 틀려서 급한 건이다.

internal/service/kr_holidays.go 의 2026년 목록이 잘못돼 있다.
2025년 추석(10/6~10/8) 블록이 2026년 자리에 복사돼 있다.
2026년 추석은 9월 25일이다.

(1) 2026년 블록을 §23.13.9 표 20건으로 교체한다.
    반드시 추가: 2026-05-24, 2026-09-24, 2026-09-25, 2026-09-26
    반드시 삭제: 2026-10-06, 2026-10-07, 2026-10-08

(2) 2027년 블록도 추석이 빠져 있다(9/14~9/16).
    이건 10-B 에서 API 로 받아올 거니까 지금은 손대지 말고,
    2027년 데이터는 신뢰할 수 없다는 주석만 달아둬.

(3) 정기점검 자동 초안을 2026년 9월로 돌려서
    9/24, 9/25 에 방문이 잡히지 않는지 확인해줘.
    이미 잡혀 있는 일정이 있으면 몇 건인지 알려줘. 지우지는 마라.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 10-B — 공공데이터포털 연동 + 함수 정리

```
@기획_업무일지_프로그램_요구사항_v2.md

§23.13.2 · §23.13.3 · §23.13.7 을 구현해줘.

(1) holidays 테이블에 컬럼 2개 추가 (마이그레이션 + 롤백)
    source    TEXT  -- 'api' | 'manual'
    synced_at DATETIME

(2) 한국천문연구원 특일 정보 API 연동
    base: https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService
    오퍼레이션: getRestDeInfo   ← 이것만 쓴다
      getHoliDeInfo(국경일)는 제헌절처럼 쉬지 않는 날도 포함하므로 쓰지 마라
    파라미터: serviceKey, solYear, solMonth, numOfRows=100, _type=json
    응답: locdate(20260925), dateName(추석), isHoliday(Y)

    인증키:
      - server/.env 의 HOLIDAY_API_KEY 에서 읽는다 (.gitignore 에 이미 있음)
      - 소스 코드에 직접 넣지 마라
      - deploy/docker-compose.yml 에 넣지 마라.
        이 파일은 gitignore 대상이 아니라서 커밋되면 키가 저장소에 남는다
        docker-compose.yml 에는 env_file: ./.env 만 쓴다

    [필수] 이중 인코딩 함정 — 여기서 대부분 막힌다
      공공데이터포털 키는 Encoding(%2B...%3D%3D) 과 Decoding(+...==) 두 종류다.
      Go 의 url.Values.Encode() 를 쓰면 이미 인코딩된 키를 또 인코딩해서
      %2B 가 %252B 가 되고 인증이 실패한다.
      오류는 SERVICE_KEY_IS_NOT_REGISTERED_ERROR 로 나와서
      키가 잘못된 것처럼 보이지만 원인은 인코딩이다.

      둘 중 하나로 통일해라:
        A(권장) Decoding 키를 .env 에 넣고 url.Values.Encode() 사용
        B       Encoding 키를 쓰면 쿼리 문자열을 직접 조립. Encode() 금지
      어느 쪽 키를 넣었는지 .env 와 코드에 주석으로 남겨라.

(3) 동기화는 관리자가 버튼을 누를 때만 한다.
    화면을 그릴 때 API 를 부르지 마라.
    인터넷이 끊기거나 포털이 점검 중이면 화면이 통째로 멈춘다.
    저장된 holidays 테이블만 보고 그린다.

    실패 시: 저장된 값을 그대로 쓰고
    "{연도} 공휴일 동기화 실패 - 마지막 동기화 {날짜}" 배너만 띄운다.
    절대 테이블을 비우지 마라.

(4) [중요] 재동기화 시 source='manual' 행은 건드리지 않는다.
    창립기념일 같은 회사 휴무일은 API 에 없다. 지우면 안 된다.

(5) IsKRHoliday() 를 버리고 3개로 나눈다 (§23.13.7)
    IsHoliday(date) (bool, name string)
    IsWorkingDay(date) bool
    WorkingDaysBetween(from, to) int

    현재 IsKRHoliday 는 토·일에도 true 를 반환한다.
    그래서 공휴일과 주말을 구분할 수 없고, 이걸로 색을 칠하면
    토요일까지 빨갛게 된다.

    - 셋 다 holidays 테이블만 본다. kr_holidays.go 하드코딩 참조를 전부 없앤다
    - 연도를 별도 인자로 받지 마라. 날짜에서 뽑는다
    - 연도 단위 메모리 캐시를 둔다. 달력 한 장에 30번씩 DB 치지 마라
    - 테이블이 바뀌면 캐시를 비운다

    기존 호출부를 전부 찾아서 의미에 맞는 함수로 바꿔줘.
    "쉬는 날인가" 를 묻던 곳은 IsWorkingDay 의 반대로 바꾼다.

(6) 하드코딩 데이터를 최초 1회 holidays 테이블로 옮기는 마이그레이션.
    이후 kr_holidays.go 는 삭제한다.

제약: (위 4줄 동일)
```

### 프롬프트 10-C — 날짜 색 · 연차

```
@기획_업무일지_프로그램_요구사항_v2.md

§23.13.3 · §23.13.4 · §23.13.5 · §23.13.6 · §23.13.8 을 구현해줘.

(1) staff_leaves 테이블 신설 (§23.13.3)
    leave_id, user_id, leave_date, leave_kind, note, created_by, created_at
    leave_kind: annual | half_am | half_pm
    UNIQUE(user_id, leave_date)

(2) 관리 > 휴무일 관리 /admin/holidays — 탭 2개 (§23.13.8)
    공휴일 탭: 연도 선택, 불러오기 버튼, 목록, 회사 휴무일 직접 추가
    연차 탭:   직원·기간·종류 선택 후 등록
    기간 등록 시 주말·공휴일은 자동으로 건너뛴다
    공휴일과 겹치는 연차는 등록을 막는다
    권한: 조회 전체, 등록·수정·삭제는 관리자만

(3) 날짜 색 (§23.13.4) — 한국 달력 관행
    일요일        빨강 #DC2626
    공휴일·대체    빨강 #DC2626 + 배경 #FEF2F2   ← 평일이어도 빨강
    회사 휴무일    빨강 #DC2626 + 배경 #F3F4F6
    토요일        파랑 #2563EB                  ← 빨강 아니다
    오늘 강조는 휴일 색보다 우선

    공휴일 칸에는 이름도 함께 적는다. 색만으로는 무슨 날인지 모른다.
    title 속성에도 공휴일명을 넣는다.

(4) 연차 표시 (§23.13.5)
    연차는 그 사람만 쉬는 날이다. 날짜를 빨갛게 칠하지 마라.
    날짜 칸 아래에 이름 뱃지를 붙인다.
      "{이름} 연차"      주황 #F97316
      "{이름} 오전반차"  연한 주황 #FDBA74
      "{이름} 오후반차"  연한 주황 #FDBA74
    3명 넘으면 "외 n명" 으로 접고 클릭 시 펼친다

(5) 적용 지점 (§23.13.6)
    /workboard/register, /maintenance/…, /work-status, /meeting
    정기점검 자동 초안: 공휴일·회사 휴무일에 배정하지 않는다
    정기점검 자동배정: 연차인 사람에게 배정하지 않는다
    정기점검 엑셀 캘린더: 휴일 회색 배경 + 공휴일명

(6) [중요] §16.4 담당자별 일일업무 분석
    연차인 사람의 그 날 칸에 "연차" 를 표기하고,
    그 사람의 일일 평균 분모에서 그 날을 뺀다.
    이걸 빼먹으면 휴가 쓴 사람이 성과가 낮아 보인다.

검증 (§23.13.10):
  - 2026년 불러오면 20건이고 2026-09-25 있고 2026-10-07 없다
  - 토요일 파랑, 일요일·공휴일 빨강
  - 회사 휴무일 넣고 재동기화해도 안 지워진다
  - 연차 등록하면 뱃지 뜨고 그 사람에게 자동배정 안 된다
  - API 키 없이 불러오기 눌러도 화면이 안 죽는다

제약: (위 4줄 동일)
```

### 연동 전 손으로 확인할 것

커서에 10-B 를 넣기 **전에** 개발 PC 명령창에서 키가 살아 있는지 먼저 보세요. 키가 죽어 있으면 커서가 코드를 다 짜고 나서야 알게 됩니다.

```
curl "https://apis.data.go.kr/B090041/openapi/service/SpcdeInfoService/getRestDeInfo?serviceKey=<인코딩키>&solYear=2026&solMonth=09&numOfRows=100&_type=json"
```

`locdate` 에 `20260924` `20260925` `20260926`, `dateName` 에 「추석」 이 나오면 정상입니다. 이게 §23.13.9 정정 시드와 맞는지 확인하는 근거가 됩니다.

| 응답 | 뜻 |
|------|-----|
| `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` | 이중 인코딩이거나 활용신청 승인 전 |
| `LIMITED_NUMBER_OF_SERVICE_REQUESTS_EXCEEDS_ERROR` | 일일 호출 한도 초과 |
| `resultCode 00` + 빈 `items` | 그 달에 공휴일 없음. 정상 |

---

# 11단계 — 일일 업무 등록 담당자별 열 [v2.9]

일일 보기에서 같은 사람의 카드가 좌우로 흩어지는 문제를 고칩니다. 기획서 §7.6 이 근거입니다.

**11-A 와 11-B 를 나눈 이유** — 11-A(레이아웃)는 되돌리기 쉽고, 11-B(색 정리)는 화면 여러 곳에 영향이 갑니다. 한 번에 하면 뭐가 깨졌는지 못 찾습니다.

### 프롬프트 11-A — 담당자별 열 레이아웃

```
@기획_업무일지_프로그램_요구사항_v2.md

§7.6.1 ~ §7.6.4, §7.6.6, §7.6.7 을 구현해줘.

현재 문제 (코드 확인함):
  internal/handler/register_layout.go 의 buildRegisterDayColumns() 에서
  assignAssigneeLanes(evs) 가 "겹치는 묶음 안에서만" 레인을 매긴다.
  그래서 최혜영이 07:30 에는 lane=0(왼쪽 끝),
  09:00 에는 태자운과 겹쳐 lane=1(가운데) 이 된다.
  레인 번호가 사람에 고정돼 있지 않다.
  거기에 registerCardMaxWidthPct 가 폭을 열의 1/5 로 묶어서
  혼자인 시간대에도 오른쪽 4/5 가 빈다.

(1) [중요] buildRegisterDayColumns() 를 고치지 마라.
    이 함수는 work_status.go:136 (업무처리현황) 에서도 쓴다.
    고치면 그 화면이 같이 바뀐다.

    일일 보기 전용으로 buildRegisterAssigneeColumns() 를 새로 만들고
    workboard.go 의 일일(view=day) 분기에서만 부른다.
    주간·월간·업무처리현황은 기존 함수를 그대로 쓴다.

    기존 테스트 2개는 손대지 마라:
      TestBuildRegisterDayColumnsSameAssigneeAllVisible
      TestBuildRegisterDayColumnsPosition
    새 함수용 테스트를 따로 추가한다.

(2) 열 구성 (§7.6.3)
    - 그날 배치된 업무가 있는 담당자만 열로 만든다
      (실제로 하루 2~3명이다. 9명 전원을 열로 두면 빈 열이 화면을 다 먹는다)
    - 열 순서는 고정. 상단 담당자 칩과 같은 순서
    - 담당자가 빈 배치 건이 있으면 맨 오른쪽에 "미배정" 회색 열. 0건이면 열 없음
    - 머리글 오른쪽에 "＋담당자" 버튼 → 빈 열을 임시로 연다
      (새로고침하면 사라진다)
    - 상단 담당자 필터로 한 명 고르면 그 사람 1열만
    - 배치 건이 0이면 "배치된 업무가 없습니다" + ＋담당자 버튼만

(3) 폭 (§7.6.3 · §7.6.4)
    - 열 최소 폭 160px. 더 좁아지면 가로 스크롤
    - 시간 열은 sticky left-0 으로 고정 (스크롤해도 시각이 보이게)
    - 카드는 열 폭을 가득 채운다.
      일일 보기에서는 registerCardMaxWidthPct 를 적용하지 않는다

(4) 같은 담당자 겹침 (§7.6.4)
    저장 시 이미 거부하지만 과거·반입 데이터에 있을 수 있다.
    그 열 안에서만 좌우로 쪼개고,
    카드 좌상단에 ⚠ 를 붙이고 title 에 "같은 담당자 시간 겹침" 을 넣는다.
    조용히 나란히 그리지 마라. 데이터 오류가 묻힌다.

(5) 드래그 (§7.6.6)
    - 드롭 칸에 data-assignee 를 추가한다 (지금은 data-date, data-time 뿐)
    - 어느 열에 놓느냐로 담당자가 정해진다. 모달에 그 값이 채워진 채 열린다
    - 배치된 카드를 다른 열로 끌면 담당자가 바뀐다. 시각은 그대로
    - 그 사람 그 시간대가 차 있으면 거부하되
      "{이름}의 09:00~09:30 에 다른 업무가 있습니다" 로 알린다.
      지금 overlap 문구는 누구 몇 시인지 안 알려준다

(6) 카드에서 담당자 이름을 뺀다 (§7.6.5)
    열 머리글이 알려주므로 중복이다.
    확보된 폭으로 제목을 더 보여준다.
    머리글에 "최혜영 · 6건 · 3시간" 처럼 건수와 총 소요를 넣는다.

검증 (§7.6.8):
  - 8/18 로 열면 최혜영·양기헌 2개 열이 뜨고
    최혜영 카드가 모두 같은 열 같은 가로 위치에 있다
  - 주간·월간 보기와 업무처리현황 화면이 이전과 똑같다
  - 담당자 6명 이상인 날은 가로 스크롤되고 시간 열이 고정된다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 11-B — 색 체계 정리

```
@기획_업무일지_프로그램_요구사항_v2.md

§7.6.5 의 색 정리를 해줘. 11-A 가 끝난 뒤에 한다.

현재 문제:
  화면 상단에 점검대상 범례(앤로보틱스/K-LAS/세종 K-LAS/행정·기타)와
  담당자 칩 9명이 각기 다른 색으로 동시에 있고,
  카드는 AssigneeColor(담당자 색)로 칠해진다.
  그래서 파란 카드가 "최혜영" 인지 "K-LAS" 인지 알 수 없다.

(1) 일일 보기에서 담당자는 열 위치가 표현하므로 담당자 색이 필요 없다.
    카드 색을 점검대상·업무유형 색으로 바꾼다.
      초록 = 앤로보틱스
      파랑 = K-LAS
      보라 = 세종 K-LAS
      주황 = 행정·기타

(2) 상단 담당자 칩에서 색을 빼고 필터 버튼으로만 남긴다.

(3) 주간·월간 보기는 담당자를 위치로 표현하지 못하므로
    카드 왼쪽 색마를 담당자 색으로 유지한다.
    일일과 주간의 색 의미가 다르다는 점을 범례에 명시한다.

먼저 보고해줘:
  AssigneeColor() 를 쓰는 곳이 어디어디인지 전부 찾아서 알려줘.
  일일 시간표 말고 다른 화면에서도 쓰고 있으면
  그 화면 색이 같이 바뀌므로, 바꾸기 전에 목록을 보고 판단하겠다.

제약: (위 4줄 동일)
```

---

# 12단계 — 다중 담당자 (공동 작업) [v2.10]

한 업무에 여러 명이 참여할 수 있게 합니다. 기획서 §7.7 이 근거입니다.

**세 단계로 나눴습니다.** `assignee` 를 참조하는 코드가 **30개 파일 296곳**입니다. 한 번에 바꾸면 통계·보고서·시간표·동기화가 동시에 흔들려서 무엇이 깨졌는지 찾을 수 없습니다.

### 프롬프트 12-A — 테이블 신설 + 이관 (통계는 건드리지 않음)

```
@기획_업무일지_프로그램_요구사항_v2.md

§7.7.1 · §7.7.2 를 구현해줘.

[가장 중요] work_tasks.assignee 컬럼을 없애지 마라.
  "주담당" 의 의미로 그대로 둔다.
  이 컬럼을 참조하는 코드가 30개 파일 296곳이다.
  참여자는 별도 테이블로 "더한다". 기존 296곳은 손대지 않는다.

(1) work_task_members 테이블 신설 (마이그레이션 + 롤백)
    task_id, assignee, member_role, duration_min, sort_order, created_at
    member_role: 'owner'(주담당) | 'support'(지원)
    PK (task_id, assignee)

    제약:
    - owner 는 정확히 1명. 0명이거나 2명 이상이면 저장 거부
      (SQLite 는 ALTER TABLE ADD CONSTRAINT CHECK 를 지원하지 않으니
       트리거 + RAISE(ABORT) 또는 저장 계층 검증으로 처리한다)
    - work_tasks.assignee 와 owner 의 assignee 는 항상 같은 값
      한쪽만 바뀌지 않도록 저장 시 함께 쓴다
    - 참여자가 1명뿐인 업무도 행 1건을 만든다.
      "1명이면 없고 2명이면 있다" 로 하면 조회 코드가 두 갈래가 된다

(2) 기존 데이터 이관
    work_tasks 135건 전부에 대해 assignee 를 owner 1건으로 넣는다.
    빈 값('') 1건은 "미배정" 으로 둔다.

(3) [필수] 이 단계에서 통계·보고서 코드는 절대 건드리지 마라.
    집계는 계속 work_tasks.assignee(주담당) 기준으로 둔다.
    이관 후 통계 숫자가 이관 전과 똑같아야 한다.
    이게 이 단계의 합격 기준이다.

검증:
  - 이관 후 SELECT COUNT(*) FROM work_task_members 가 135
  - owner 를 0명/2명으로 만들려 하면 거부된다
  - 통계 화면 숫자가 이관 전과 동일하다 (스크린샷으로 비교해줘)

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 12-B — 입력 화면 + 담당자별 열 표시

```
@기획_업무일지_프로그램_요구사항_v2.md

§7.7.4 · §7.7.5 를 구현해줘. 11-A(담당자별 열)가 끝난 뒤에 한다.

(1) 업무 등록·수정 모달에 "참여자" 칸 추가 (§7.7.5)
    담당자(주담당) [최혜영 ▼]     ← 기존 칸. 라벨만 "주담당" 으로
    참여자 [＋추가]
           양기헌  지원  [120]분  [×]
    - 주담당은 비울 수 없다
    - 참여자에 주담당을 다시 넣을 수 없다
    - 소요분을 비우면 업무 전체 소요시간을 쓴다
    - 연차인 사람(§23.13)을 넣으면 확인만 받고 막지는 않는다

(2) 담당자별 열에 참여자 전원 표시 (§7.7.4)
    참여한 사람 열에 모두 카드를 띄운다.
    지원자가 그 시간에 비어 있는 것처럼 보이면 배정 판단을 그르친다.

    주담당 카드: 지금과 동일. 조치·수정·× 버튼 있음
    지원 카드:   불투명도 60% + 점선 테두리 + 좌상단 "지원" 뱃지
                 버튼 없음. 드래그 불가
    카드 하단:   "09:00~11:00 · 외 2명"
    클릭:        지원 카드를 눌러도 같은 업무 상세로 간다

    지원 카드를 못 옮기게 하는 이유:
    지원자 열에서 끌면 "내 시간만 옮기는 것" 인지
    "업무 전체를 옮기는 것" 인지 알 수 없다.
    시간 변경은 주담당 카드에서만 한다.

(3) 겹침 검사를 참여자 전원으로 확대 (§7.7.4)
    지금은 주담당만 본다.
    지원으로 넣으려는 사람이 그 시간에 다른 일이 있으면 거부하고
    "{이름}의 09:00~11:00 에 다른 업무가 있습니다" 로 알린다.

(4) 이 단계에서도 통계는 건드리지 마라.

검증 (§7.7.7):
  - 참여자 2명 넣으면 work_task_members 에 3행
  - 일일 보기에서 3개 열에 같은 업무가 뜨고 지원 카드에 버튼이 없다
  - 지원자가 이미 찬 시간이면 이름·시각이 들어간 오류가 뜬다

제약: (위 4줄 동일)
```

### 프롬프트 12-C — 통계 전환 [데이터가 쌓인 뒤]

```
@기획_업무일지_프로그램_요구사항_v2.md

§7.7.3 을 구현해줘.
12-B 로 공동 작업 데이터가 몇 주 쌓인 뒤에 한다.
기록과 집계를 동시에 바꾸면, 숫자가 변한 게
새 데이터 때문인지 새 산식 때문인지 구분할 수 없다.

집계를 두 갈래로 나눈다:

(1) 담당자별 건수 → work_task_members 기준. 참여자 각자 1건
    쓰는 곳: 주간보고서 담당자별 표(§16.1),
             담당자별 일일업무(§16.4), 개인 대시보드

(2) 팀 건수 → COUNT(DISTINCT task_id)
    쓰는 곳: 계획 대비 실행률(§4), 팀 전체 KPI, 월간 보고

    [필수] 팀 행을 담당자 행의 합으로 만들지 마라.
    3명이 한 건을 하면 담당자 합은 3, 팀은 1이다.
    합으로 만들면 실행률이 실제보다 부풀려진다.

(3) [필수] 차이를 화면에 명시한다.
    주간보고서 통계 시트 표 아래에 한 줄:
    "공동 작업 n건이 포함되어 담당자별 합계(23)가 팀 건수(19)보다 큽니다"

    이 문장이 없으면 보고 자리에서 "숫자가 안 맞는다" 는 말이 먼저 나오고
    그때부터 이 시스템의 모든 숫자를 의심하게 된다.
    차이를 숨기지 말고 설명한다.

(4) 리드타임 지표(접수→방문 3일, 접수→완료 7일)는 업무 단위다.
    참여자 수와 무관하게 1건으로 센다. 사람 수로 나누거나 곱하지 마라.

(5) 투입 시간 집계를 추가한다.
    참여자별 duration_min 합. 공수 분석·사업별 투입량에 쓴다.

먼저 보고해줘:
  전환 전후로 8월 각 주의 팀 건수와 담당자별 합계를 표로 뽑아서 보여줘.
  숫자가 얼마나 달라지는지 보고 판단하겠다. 임의로 반영하지 마라.

제약: (위 4줄 동일)
```

---

# 13단계 — 운영 중 발견 4건 [v2.11]

베타 사용 중 나온 4건입니다. **서로 독립적이라 순서 없이 하나씩** 넣으시면 됩니다.

### 프롬프트 13-A — 일일 업무 회의 담당자 선택·정렬

```
@기획_업무일지_프로그램_요구사항_v2.md

§10.1 을 구현해줘.

현재 문제 (코드 확인함):
  internal/handler/meeting.go 는 "내 것(mine=1) / 전체(mine=0)" 토글만 넘긴다.
  담당자 목록(Users)을 템플릿에 전달하지 않아서
  회의 중에 "양기헌 건만 보자" 가 안 된다.

(1) 제목 옆에 담당자 드롭다운. 기본값 팀전체.
    userRepo.ListAssignable() 결과를 채운다.
    ?assignee= 로 선택을 유지한다. 날짜를 옮겨도 안 풀린다.

(2) [중요] 전일 실적·오늘 예정·상단 요약 3곳에 모두 건다.
    목록만 걸리고 상단 요약이 팀 전체로 남으면 숫자가 안 맞는다.
    지금 코드의 ParseMeetingFilter(model.StatsScopeAssignee, name, "")
    를 그대로 쓰면 된다. 새 산식을 만들지 마라.

(3) 기존 "내 것/전체" 토글은 드롭다운에 흡수한다.
    두 개를 같이 두면 조합이 4가지가 되어 무엇이 보이는지 알 수 없다.
    기술담당은 자기 이름이 기본 선택된 상태로 연다.

(4) 정렬 — 두 목록 모두 담당자 → 시각 → 유형 순
    담당자별 소제목 줄을 넣는다: "최혜영 · 6건"
    담당자 순서는 §7.6.3 열 순서와 같은 기준을 쓴다
    담당자 미배정 건은 맨 아래 "미배정" 묶음. 접지 말고 펼친 상태로

(5) 기술담당도 드롭다운으로 다른 사람을 볼 수 있게 한다.
    회의는 팀 전체가 함께 보는 자리다. 쓰기 권한은 그대로.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 13-B — 미계획 분류 정리 + 날짜 검증

```
@기획_업무일지_프로그램_요구사항_v2.md

§8.2.1 · §8.2.2 · §8.2.3 을 구현해줘.

배경:
  R2608-059 는 예정업무일 2026-08-18(확정), 담당자 최혜영 이 다 있는데
  미계획 업무함에 D+1 로 올라와 있다.
  분류 오류가 아니라 이름 문제다.
  work_board_unplanned.go 의 ListUnplanned() 는 5가지를 한 함에 담는데
  그중 예정일경과·다음일정미정·재검토는 "계획이 있는데 밀린 것" 이다.

(1) 화면 안에서 두 묶음으로 나눈다 (§8.2.1)
    "계획을 세워야 할 건"  = 예정없음 · 담당자미배정
    "일정이 지난 건"       = 예정일경과 · 다음일정미정 · 재검토
    탭 줄을 두 줄로 나누고 사이에 구분선.
    부제를 "계획이 없거나, 일정이 지났거나, 다음 일정이 정해지지 않은 업무" 로.

    [중요] 메뉴 이름(미계획 업무함)과 URL(/plan/unplanned)은 바꾸지 마라.
    대시보드·회의 화면·문서가 이미 이 이름을 쓴다. 화면 안만 고친다.

(2) §8.4 계획 수립률의 분자에 "계획이 없는 건" 만 쓴다.
    밀린 건을 섞으면 계획을 세워도 지표가 떨어져서
    담당자가 계획을 세울 이유를 잃는다.

(3) 착수한 건을 구분한다 (§8.2.2)
    예정일 지남 + 착수 안 함(start_datetime 없음)        → D+n      빨강
    예정일 지남 + 착수함(start_datetime 또는 as_processes 있음)
                                                          → 진행중 D+n 노랑
    정렬은 빨강 먼저.
    손댄 일과 손도 안 댄 일이 같은 빨강이면 정말 방치된 건이 묻힌다.

(4) [버그] 날짜 범위 검증 (§8.2.3)
    목록에 예정일이 0206-08-12 인 건(R2608-039)이 있다.
    2026 을 0206 으로 잘못 친 값이 그대로 저장됐다.
    연도가 0206 이면 영원히 "예정일 경과" 로 남고
    워킹데이·리드타임 계산도 오염시킨다.

    - 서버에서 연도 2000~2100 범위를 검증한다.
      화면 검증만으로는 반입·API 를 못 막는다
    - <input type="date"> 에 min="2000-01-01" max="2100-12-31"
    - 대상: visit_scheduled_date, visit_date, work_date, due_date,
            receipt_datetime, complete_datetime
    - 정합성 점검(부록 C)에 "연도가 2000~2100 밖인 날짜" 항목을 추가하고
      기존 데이터에 몇 건 있는지 알려줘. 자동으로 고치지는 마라.

제약: (위 4줄 동일)
```

### 프롬프트 13-C — 조치 사진 3장 원복

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.9.8 을 구현해줘. 없어진 기능을 되살리는 거다.

현재 상태 (web/templates/as/action.html 확인함):
  440: <h3>조치 첨부</h3>
  441: "조치완료보고서·관련 파일. 서버에 저장하고 화면에는 파일명만 표시합니다."
  471: <button>파일 업로드</button>

  사진 전용 영역이 범용 파일 업로드로 대체되면서
  조치 사진이 "파일명만 보이는 문서" 취급을 받게 됐다.
  152행의 as_receipt_photos 는 접수 사진 읽기전용(§12.9.7)이라 무관하다.

(1) 조치 화면에 "조치 사진" 영역을 되살린다. 문서 첨부와 별개 영역이다.
      최대 3장 / accept="image/*"
      썸네일 + 클릭 시 원본 확대(라이트박스)
      사진별 메모
      긴 변 1600px 축소 + 320px 썸네일 + EXIF 회전 반영
        → §12.9 접수 사진과 같은 규칙이다.
          그 코드를 재사용해라. 새로 짜지 마라
      attachments.ref_type = 'as_action_photo'

(2) 기존 "조치 첨부" 는 그대로 둔다. 문서용이다.
    두 영역 제목을 "조치 사진" / "조치 첨부(문서)" 로 다르게 적고
    사이에 구분선을 둔다.
    한 덩어리면 담당자가 사진을 문서 쪽에 올리고 썸네일이 안 나온다.

(3) "조치 첨부" 설명 문구에서 "조치완료보고서" 를 뺀다.
    보고서는 이제 프로그램이 생성하므로 사람이 올릴 이유가 없다.
    (사진 영역이 사라진 것도 보고서 올릴 자리를 만들면서
     덮어쓴 것으로 보인다)

(4) 조치완료보고서(§12.10)에 이 사진을 넣을 수 있게 한다.
    넣을지는 발급 화면에서 고른다.

제약: (위 4줄 동일)
```

### 프롬프트 13-D — 보고서 발급 위치·부분완료·DOCX

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.10.5 · §12.10.6 개정분을 반영해줘.

현재 상태 (internal/handler/as_report.go 확인함):
  - 발급 버튼이 AS 상세에 있고
  - 30, 61, 75 행에서 "완료된 건만 발급할 수 있습니다" 로 막고 있다
  - hwpx.Replace() 로 HWPX 만 낸다

(1) 발급 버튼을 조치 화면(/as/{id}/action)으로 옮긴다.
    AS 상세의 버튼은 재발급용으로 남긴다.
    조치를 마친 그 자리에서 뽑을 수 있어야 한다.
    다른 화면으로 옮겨가야 나오면 그 단계에서 그냥 안 만들게 된다.

(2) 부분완료(partial_complete)도 발급 허용한다.
    제목 아래에 "부분 조치 상태입니다" 한 줄을 넣는다.
    30/61/75 행의 차단 조건을 완료 + 부분완료로 바꾼다.

(3) 조치를 완료·부분완료로 저장하면
    저장 결과 화면에 "조치완료보고서 만들기" 버튼을 띄운다.
    이 순간이 내용이 가장 정확한 때다.

(4) cause_detail·conclusion 이 비어도 버튼은 그대로 두고,
    누르면 무엇이 없는지 명시해서 입력으로 유도한다. 막지 마라.

(5) DOCX 생성을 추가한다. 기본 형식으로 둔다.
    DOCX 도 실체가 ZIP + XML 이라 치환 방식이 HWPX 와 완전히 같다.
    이미 만든 internal/hwpx 의 구조를 그대로 복제해서
    internal/docx 를 만든다. 새로 배울 게 없다.

    템플릿: server/templates/report/장애처리보고서.docx
    미리보기 화면에 [DOCX 내려받기] [HWPX 내려받기] 버튼 2개.

    [중요] DOCX 템플릿이 없으면 DOCX 버튼만 숨기고
    HWPX 는 그대로 동작하게 한다. 한쪽이 없다고 전체가 실패하면 안 된다.

    [필수] Word 는 한 문단 텍스트를 여러 <w:t> 런으로 쪼개 저장한다.
    {{고객명}} 이 {{고객 / 명}} 으로 갈라져 있으면
    단순 문자열 치환이 안 먹는다.
    문단 단위로 런을 합친 뒤 치환해라.
    남은 {{ 가 있으면 오류로 처리한다.

(6) 파일명
    {고객명}_{증상요약 20자}_조치완료보고서_{YYYYMMDD}.{docx|hwpx}

검증:
  - 부분완료 건에서 발급되고 "부분 조치 상태입니다" 가 나온다
  - 증상에 < & " 가 든 건으로 생성해도 Word 에서 열린다
  - 결과 파일에 {{ 가 남아 있지 않다
  - DOCX 템플릿 파일을 지워도 HWPX 발급이 정상 동작한다

제약: (위 4줄 동일)
```

---

# 14단계 — 조치 화면 정리 [v2.12]

### 먼저 — 13-C(사진/파일 분리)는 이미 반영돼 있습니다

소스를 확인했습니다. 오늘 13:12~13:29에 다음이 들어갔습니다.

- `web/templates/as/_photo_gallery.html` 신설
- `action.html:456` — `{{if .ActionGallery}}{{template "as_photo_gallery" .ActionGallery}}{{end}}`
- `action.html:464` — 제목 「조치 첨부(문서)」, 문구에서 「조치완료보고서」 제거
- `as.go:880` — `h.mergeActionPhotoData(...)`
- `as.go:895` — `PhotoSectionTitle: "조치 사진"` · `PhotoMax: MaxActionPhotos` · `HideIfEmpty: false`

**화면에 「조치 첨부」 하나만 보이고 옛 문구가 그대로면 실행 중인 컨테이너가 옛 빌드입니다.** 커서에 다시 시키기 전에 먼저 빌드하세요.

```
cd server
docker compose down
docker compose up -d --build
docker compose logs -f app
```

**이미 반영된 것을 다시 고치라고 지시하면 멀쩡한 코드를 바꿔 놓습니다.** 화면이 안 바뀌면 코드보다 빌드를 먼저 의심하세요.

### 프롬프트 14-A — 「조치 정보」 섹션 분리

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.10.4 의 "입력 위치 — 독립 섹션 조치 정보" 와
"[모순] 추가조치 필요를 고르면 두 칸이 사라진다" 를 반영해줘.

현재 상태 (web/templates/as/action.html 확인함):
  262행  <section> 조치 결과 시작
  288행  <div id="as-cause-report-wrap" ... 초록 상자 "완료 보고 문장"
  293행  장애원인 (cause_detail)
  298행  결론 (conclusion)
  401행  <!-- 확인 · 후속 -->
  402행  <section> 확인 · 후속

  즉 장애원인·결론이 "조치 결과" 섹션 안쪽 맨 아래에 끼어 있다.
  소요시간·결과·원인분류·처리유형을 지나 한참 스크롤해야 나오고,
  같은 상자 안이라 "조치 결과의 부속 항목" 처럼 보인다.
  실제로는 고객에게 나가는 문서의 본문이다.

(1) as-cause-report-wrap 을 "조치 결과" 섹션 밖으로 꺼내
    독립 <section> "조치 정보" 로 만든다.
    위치는 "조치 결과" 섹션과 "확인 · 후속" 섹션 사이.

    지켜야 할 것:
    - [필수] <form> 안에 그대로 둔다.
      폼 밖으로 나가면 장애원인·결론이 저장되지 않는다
    - id="as-cause-report-wrap" 을 유지한다.
      721~755행 스크립트가 이 id 로 표시/숨김과 결론 자동 초안을 다룬다
    - 다른 섹션과 같은 서식으로 맞춘다
      <section class="bg-white rounded-xl border border-slate-200 p-4 sm:p-5 space-y-4">
      + <h4 class="text-sm font-semibold text-slate-800">조치 정보</h4>
    - "원인분류(코드)와 다른 칸입니다" 안내는 섹션 안 작은 글씨로 남긴다

(2) [모순 수정] 표시 조건을 넓힌다.
    지금은 {{if ne .AS.ResultCode "done"}}hidden{{end}} 라서
    "추가조치 필요"(ResultPartial = "partial") 를 고르면
    장애원인·결론 칸이 숨겨진다.

    그런데 §12.10.5 는 부분완료(partial_complete)도
    조치완료보고서를 발급하도록 했다.
    지금 상태면 두 칸이 빈 채로 문서가 나간다.

    done 또는 partial 일 때 열리게 바꾼다.

(3) 자동 초안 스크립트도 같이 넓힌다.
    action.html:748 의 selectedResult() === 'done' 조건.
    한쪽만 고치면 칸은 열리는데 초안이 안 채워진다.
    partial 일 때는 "(부분 조치)" 로 시작하는 초안을 쓴다.

검증:
  - 결과를 "완료" 로 고르면 조치 정보 섹션이 조치 결과와 확인·후속 사이에 뜬다
  - 결과를 "추가조치 필요" 로 골라도 두 칸이 열리고 초안이 채워진다
  - 두 칸에 값을 넣고 저장하면 DB(as_receipts.cause_detail, conclusion)에 남는다
    ← 폼 밖으로 나갔는지 확인하는 가장 확실한 방법이다
  - 재방문·이관·대기를 고르면 섹션이 숨는다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

---

# 15단계 — [긴급] 사이드바 메뉴 사라짐 [v2.13]

**권한 판정이 간헐적으로 무너지는 버그입니다.** 메뉴만의 문제가 아니라 인증 미들웨어 문제라서, 같은 순간 권한 검사를 하는 다른 기능도 함께 어긋납니다.

### 프롬프트 15-A

```
@기획_업무일지_프로그램_요구사항_v2.md

§24.1 을 구현해줘. 원인까지 코드에서 확인했다.

증상: 왼쪽 메뉴에서 대시보드 다음에 "기준정보" 이하만 보인다.
      계획·실행·확인 3개 그룹이 통째로 사라진다. 가끔 그렇다.

원인 (internal/handler/auth.go 인증 미들웨어):
  327행  c.Set("role", claimString(claims, "role"))       ① JWT 원문
  332행  if u, err := h.userRepo.GetByID(uid); err == nil && u != nil {
  338행      c.Set("role", model.NormalizeRole(u.Role))   ② DB 값으로 덮어씀
  341행      c.Set("permissions", u.PermList())           ③ 권한은 여기서만
         }

  GetByID 가 실패하면 ②③ 이 통째로 건너뛰어지고 err 는 버려진다.
  permissions 가 nil 이 되어 hasUserPerm 이 전부 false 가 된다.

  SQLite 는 database is locked 등으로 순간 실패할 수 있다.
  이 프로젝트는 백업·마이그레이션·엑셀 일괄 생성이 같은 DB 를 쓴다.
  한 번의 요청에서만 실패해도 그 페이지 사이드바가 축소된다.
  새로고침하면 멀쩡해져서 원인을 잡기 어렵다.

  그리고 base.html 은 계획·실행·확인에만 가드가 있고
  기준정보·관리 그룹 헤더에는 가드가 없다.
  그래서 $canPlanExec 하나가 false 가 되면
  정확히 "기준정보 이하만" 남는다.

(1) [핵심] DB 조회 실패를 삼키지 마라.
    u, err := h.userRepo.GetByID(uid)
    if err != nil { log.Printf("[auth] 사용자 조회 실패 uid=%s: %v", uid, err) }

    실패해도 permissions 와 role 을 반드시 채운다.
    JWT 클레임 값으로라도 채우고 role 은 항상 NormalizeRole 을 거친다.

(2) JWT 에 permissions 클레임을 추가한다 (auth.go:47, 153 두 곳).
    DB 가 잠깐 막혀도 권한이 비지 않게.

(3) 매 요청 DB 조회를 주기 재검증으로 바꾼다 (§24.1.4).
    JWT 를 기본으로 쓰고 마지막 검증 후 5분이 지났을 때만 DB 를 본다.
    그 조회가 실패하면 JWT 값을 계속 쓰고 로그만 남긴다.
    is_active=0 차단은 다음 재검증(최대 5분)에 반영된다.

(4) 역할이 확정되지 않은 채 렌더되면 상단에 배너를 띄운다.
    "권한 정보를 불러오지 못했습니다. 새로고침하거나 다시 로그인해 주세요."
    조용히 축소된 메뉴를 보여주는 게 가장 나쁘다.
    사용자는 기능이 없어진 줄 알고 관리자는 아무것도 모른다.

(5) base.html 가드를 대칭으로 만든다.
    "기준정보"·"관리" 그룹 헤더와 그 안의 무가드 항목
    (고객현황·설치자산·휴무일 관리)에도 역할 가드를 넣는다.
    역할을 모르면 메뉴 전체가 안 나오게 한다.
    절반만 나오면 로그인 문제인 줄 모른다.

(6) 역할 비교를 한 곳으로 모은다.
    auth.go:175 가 정규화 없이 원문을 비교한다:
      return ctxString(c, "role") == "admin"
    currentRole(c) 는 NormalizeRole 을 거친다.
    같은 사용자에 두 함수가 다른 답을 낼 수 있다.
    모든 역할 비교를 currentRole(c) + model.RoleAdmin 상수로 통일한다.
    문자열 리터럴 "admin" 비교를 전부 찾아서 바꿔줘.

(7) model.NormalizeRole (internal/model/role.go:44)
    지금은 빈 값이 RoleObserver 를 돌려준다.
    인증이 깨진 상태가 조용히 열람 권한이 된다.
    빈 값과 모르는 값을 "미인증" 으로 구분해 로그인으로 보낸다.
    모르는 값은 로그를 남긴다.

검증 (§24.1.5):
  - userRepo.GetByID 가 항상 오류를 내게 임시로 만든 뒤 화면을 연다
    → 메뉴가 축소되지 않고 배너가 뜬다
  - 같은 조건에서 로그에 [auth] 사용자 조회 실패 가 남는다
  - role 이 빈 토큰으로 접근하면 로그인으로 보내진다
  - 관리자로 로그인해 5개 그룹이 모두 보인다
  - 역할을 바꾸면 5분 안에 메뉴가 바뀐다
  - 백업·엑셀 일괄 생성을 돌리면서 화면을 반복 새로고침해도
    메뉴가 흔들리지 않는다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

---

# 16단계 — 영업관리 (수주 전) [v2.15]

**별도 영업관리 시스템이 구축되기 전까지** 이 시스템이 영업관리 일부를 대신합니다. 기획서 **§32** 전체가 근거입니다.

**중요 — v2.14 지시(16-A~16-C)는 폐기합니다.** `work_projects`에 유형을 더하는 방식이었는데, **별도 표로 분리**하는 것으로 바뀌었습니다. 아직 안 돌리셨다면 그냥 넘어가시고, 16-A를 이미 돌리셨다면 프롬프트 16-0을 먼저 넣으세요.

### 프롬프트 16-0 — v2.14 작업 되돌리기 [이미 16-A를 돌린 경우만]

```
@기획_업무일지_프로그램_요구사항_v2.md

§22.1.1 이 v2.15 에서 바뀌었다.
work_projects 에 project_kind 를 추가하는 방식은 폐기됐다.
영업 사업은 별도 표(§32)로 간다.

16-A 로 추가한 project_kind 컬럼과 관련 변경이 있으면 롤백해줘.
work_projects 와 그 화면·집계를 v2.14 이전 상태로 되돌린다.
되돌린 뒤 통계 숫자가 이전과 같은지 확인해줘.

없으면 없다고만 알려주고 아무것도 하지 마라.
```

### 프롬프트 16-A — 영업 사업 본체 + 「영업」 메뉴 [1단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.1 ~ §32.5, §32.13 의 1단계를 구현해줘.

[설계 전제 — 반드시 지켜라]
  work_projects 를 건드리지 마라. 새 표로 간다.
  기존 사업관리(§22.1) 화면·데이터·집계는 하나도 바꾸지 않는다.
  이유는 §32.1.3 에 있다. 나중에 별도 시스템으로 떼어낼 계획이라
  섞어 두면 분리할 때 모든 집계 쿼리를 다시 손봐야 한다.

(1) sales_projects 테이블 신설 (마이그레이션 + 롤백)
    sales_id, name, is_tentative_name,
    stage, probability, probability_override,
    customer_id, prospect_name, prospect_region, customer_confirmed,
    expected_ym, expected_precision, expected_ym_confirmed,
    expected_amount, expected_amount_confirmed,
    sales_owner, sales_owner_id,
    competitor, lead_source, lost_reason,
    status, notes, created_at, updated_at

(2) [v2.27 변경] 6단계 + 확도 (§32.3.1) — codes 테이블에 둔다. 코드에 박지 마라
    순서 그대로:
      contact     담당자 접촉   10
      lead        정보 입수     25
      proposal    제안 진행     40
      negotiation 협상          70
      won         수주         100
      lost        실주           0

    ※ 8단계(quote 견적요청 / rfp RFP제안 / submit 제안서제출 /
      contracted 계약완료) 는 폐기됐다. 만들지 마라.
    ※ "담당자 접촉" 이 "정보 입수" 보다 앞이다. 뒤집지 마라.
      확도가 10 → 25 로 오르는 것과 방향이 맞다.
    ※ proposal 40% 만 기획 단계에서 정한 값이다. codes 로 두어
      사용자가 바꿀 수 있게 한다.

(2-1) 없어진 3단계는 활동으로 살린다 (§32.3.2)
    견적 요청·RFP 제안·제안서 제출은 실제로 일어나는 사건이다.
    단계에서 뺐다고 없던 일이 되면 안 된다.
    activity_type 의 견적제출·제안서제출·입찰 이 그 자리를 대신한다.
    "제안 진행" 단계인 사업 상세 상단에 진척 체크를 보여준다:
      견적 ✔ · RFP ✔ · 제안서 ○
    판정은 그 사업에 해당 유형 활동이 있는지로 자동으로 낸다.
    따로 입력받지 마라. (활동 로그는 2단계에서 만든다.
    1단계에서는 이 줄을 자리만 잡아두고 비워둬도 된다)

(3) 확도 표시 규칙 (§32.3.3)
    1~4단계(수주 전): 확도 % 를 앞세운다   "제안 진행 · 40%"
    5단계 수주: 단계명을 상태로 앞세운다. 확도는 표시하지 않는다
    실주: "실주" + 실패 사유
    확도를 손으로 고치면 probability_override 에 저장하고
    화면에 "수동 조정" 표시를 붙인다

(3-1) 컬럼 3개를 더 만든다
    won_at         stage 가 won 이 된 날 (자동)
    contracted_at  계약서 날인일 (손으로, 선택)
    legacy_stage   옛 8단계 코드 보존용 (§32.3.5)

(4) 단계는 되돌릴 수 있다 (§32.3.3)
    어느 단계로든 이동 가능. 뒤로 갈 때는 사유를 받는다.
    영업은 후퇴한다. 앞으로만 가는 구조로 만들면
    실제와 안 맞아 담당자가 단계를 안 고치게 된다.
    sales_stage_history 표에 언제·누가·어디서 어디로·사유를 남긴다.

(5) [핵심] 등록은 사업명 하나만 필수 (§32.4)
    나머지 칸은 전부 비워도 저장된다.
    §5 의 필수 입력 원칙을 여기에 적용하지 마라.
    영업은 정보가 없는 상태에서 시작하는 것이 정상이다.
    is_tentative_name 체크로 임시 사업명임을 표시한다.

(6) 정보 확정도 (§32.5)
    시기·금액·고객·사업명 4개에 확정 플래그를 둔다.
    미확정이면 값이 있어도 회색 + "미확정" 뱃지.
    상세 상단에 "정보 확정도 2/4" 표시.
    수주(won)로 넘어갈 때 4개를 모두 확정할 것을 요구한다.

    사업 시기는 월 단위다. 날짜가 아니다.
    expected_ym='2027-03' 로 저장하고 정밀도에 따라 표시만 넓힌다:
      2027-03 / 2027년 1분기 / 2027년 상반기 / 2027년
    정렬·집계는 월 하나의 축으로 처리한다.

(7) "영업" 대메뉴 신설 (§32.2)
    계획·실행·확인과 나란히. base.html 에 추가한다.
      영업 사업        /sales
      영업 활동        /sales/activities   (2단계에서 채운다)
      영업 파이프라인  /sales/pipeline     (4단계에서 채운다)
    권한: 관리자·영업담당(sales)·접수담당 = 등록·수정
          기술담당 = 조회만 / 삭제 = 관리자

    §24 의 sales 역할이 지금 "일시 중단" 인데 되살린다.
    이 메뉴가 그 역할의 첫 실사용처다.

    [주의] base.html 가드는 §24.1 의 대칭 규칙을 따른다.
    영업 그룹에도 역할 가드를 반드시 넣어라.

검증 (§32.14 의 1·2·3·9·10번):
  - 사업명만 넣고 저장된다. 다른 칸을 다 비워도 오류가 없다
  - 단계를 바꾸면 확도가 자동으로 바뀐다
  - 확도를 손으로 고치면 "수동 조정" 이 붙는다
  - 단계를 뒤로 옮길 수 있고 사유를 받아 이력에 남는다
  - 수주(won)로 넘길 때 미확정 항목 4개를 요구한다
  - 단계 pill 이 6개다 (담당자 접촉·정보 입수·제안 진행·협상·수주·실주)
  - 유지보수 사업 등록·수정 화면(/projects)이 이전과 완전히 같다
  - 계획 대비 실행률·3일·7일 숫자가 변하지 않는다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 16-B — 영업 활동 로그 + 일일 업무 연동 [2단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.8 · §32.9 를 구현해줘. 16-A 가 끝난 뒤에.

(1) sales_activities 테이블 신설
    activity_id, sales_id, activity_date, start_time, duration_min,
    activity_type, title, content, place,
    our_members, counterparts,        ← 여러 명
    next_action, next_action_date,
    stage_at_time,                    ← 그 시점 단계 스냅샷
    created_by, created_at

    활동 유형: 정보수집·전화·방문미팅·온라인미팅·메일·자료송부·
              견적제출·제안서제출·입찰·기타  (codes 에 둔다)

(2) 사업 상세에 활동 로그 탭 — 리스트(시간 역순 타임라인)
    단계 변경·항목 변경도 같은 타임라인에 섞어 보여준다.
    "8/11 방문미팅 → 8/14 견적 제출 → 8/14 단계: 견적요청→RFP제안"
    이 한 줄기로 읽혀야 한다.

(3) [핵심] 활동을 등록하면 work_tasks 에 자동 생성한다
    source_type='sales_activity', source_id=activity_id

    그러면 일일 업무 등록(§7) 시간표, 담당자별 열(§7.6),
    일일 업무 회의(§10), 주간보고(§16)에 자연히 들어간다.
    반대로 일일 업무에서 영업 활동으로 만들 수도 있게 한다.

    별도 화면을 하나 더 만들지 않는 이유:
    영업 담당자가 "일일 업무" 와 "영업 활동" 을 따로 적어야 하면
    둘 다 부실해진다. 한 번 적으면 양쪽에 보이게 한다.

(4) /sales/activities — 일일 단위 · 담당자별 (§32.9)
    §7.6 의 담당자별 열 구조를 그대로 재사용해라.
    화면마다 구조가 다르면 눈이 헤맨다.

    [차이점 하나] 그날 활동이 없는 영업 담당자도 열을 만든다.
    §7.6 은 일정 있는 사람만 열로 두지만,
    영업은 아무 활동이 없는 것이 문제다. 빈 열이 그 사실을 보여준다.

(5) 다음 행동이 없는 영업 사업을 §8 미계획 업무함의
    "계획을 세워야 할 건" 묶음에 새 유형 "영업 후속없음"(주황)으로 올린다.
    예정월이 지났는데 단계가 그대로면 "재검토" 로 올린다.

(6) §32.11 — 실행률 집계에 source_type='sales_activity' 를 포함한다(기본).
    통계 화면에 "영업 활동 포함" 토글을 둬서 빼고 볼 수 있게 한다.

검증 (§32.14 의 6·7·8번):
  - 활동을 등록하면 일일 업무 등록 시간표에 나타난다
  - /sales/activities 에서 활동이 없는 영업 담당자도 열로 보인다
  - 영업 사업을 만들어도 계획 대비 실행률이 변하지 않는다
    (활동을 만들면 변할 수 있다 — 토글로 확인)

제약: (위 4줄 동일)
```

### 프롬프트 16-C — 관계자 + 변경 이력 [3단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.6 · §32.7 을 구현해줘.

(1) sales_parties 테이블 (§32.7)
    party_id, sales_id, party_type, org_name,
    person_name, title, phone, email,
    party_role, is_primary, user_id, contact_id,
    is_active, replaced_by, replaced_reason, note

    party_type: own(당사) | partner(협력사) | customer(고객)
    party_role: 결정권자 | 실무 | 구매 | 기술검토 | 영업 | 기타

    - 당사 담당자도 여러 명이다. 영업·기술·관리가 함께 붙는다
    - 미등록 협력사·고객은 org_name 자유입력
    - [중요] 미팅 당사자가 바뀌면 옛 사람을 지우지 마라.
      is_active=0 으로 내리고 replaced_by 로 잇는다.
      누구와 이야기해 왔는지가 영업 자산이다

(2) sales_changes 테이블 (§32.6)
    change_id, sales_id, field_key, old_value, new_value,
    changed_at, changed_by, note

    시기·금액·고객·단계·관계자 변경을 전부 남긴다.
    "금액이 3천만 → 1억 8천으로 올랐다" 는 그 자체가 보고 재료다.

(3) 상세에 "변경 이력" 탭. 활동 로그와 한 타임라인에 섞어 볼 수 있게.

제약: (위 4줄 동일)
```

### 프롬프트 16-D — 칸반 · 타임라인 · 파이프라인 [4단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.8 의 칸반 보기와 §32.11 의 영업 지표를 구현해줘.

[v2.27] §32.11.1 이 이 프롬프트의 기준이다. 26-C 를 대신 써도 된다.

(1) /sales 목록에 보기 3종
    - 리스트
    - 단계 칸반: 열 = 6단계 (§32.3.1). 카드를 끌어 단계 이동(사유 입력 포함)
    - 예정월 타임라인: 가로축 = 월, 세로 = 사업

(2) 활동 로그도 칸반 보기를 준다 (열 = 활동 유형)

(3) /sales/pipeline — §32.11.1
    단계별 건수·금액 · 가중 파이프라인(금액 × 확도) ·
    예정월별 분포 · 수주율(won/(won+lost)) ·
    단계별 평균 체류일(sales_stage_history 로 계산) ·
    담당자별 활동 건수
    /sales 의 단계 칸반과 같은 컴포넌트를 쓴다. 두 벌로 만들지 마라.

제약: (위 4줄 동일)
```

### 프롬프트 16-E — 계약완료 → 사업관리 승격 [5단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.10 을 구현해줘. 실제 계약이 날 때까지는 급하지 않다.

[v2.27] 단계가 won(수주) 이 되면 "사업관리로 등록" 버튼이 열린다.
(계약완료 단계가 없어졌다. §32.10 참고)

1. 고객이 미등록이면 먼저 고객 마스터에 등록한다
   (자산·AS 가 붙으려면 customer_id 가 필요하다)
2. work_projects 에 행을 만들고 사업명·고객·발주처·금액을 옮긴다
3. work_projects.sales_project_id 로 원본을 잇는다 (컬럼 추가)
4. 계약기간·계약방식·청구방식·유상무상·범위 규칙은
   여기서 처음 입력한다 (§22.1 화면 그대로 쓴다)
5. 영업 사업은 지우지 마라. status=승격완료 로 두고
   활동 로그를 보존한다

재계약도 이 흐름을 탄다.
"2027년 충남교육청" 처럼 기존 고객의 차년도 계약도
영업 사업으로 시작해서 승격된다.

제약: (위 4줄 동일)
```

---

# 17단계 — 기간 내 반복 실행 [v2.16]

행정·지원업무에서 「9월 한 달 동안 3일마다 확인」 같은 업무를 다룹니다. 기획서 **§13.15** 가 근거입니다.

**정기점검(`maintenance_plans` → `maintenance_visits`)과 같은 구조를 두 번째로 만드는 것입니다.** 그때 겪은 사고 5건이 §13.15.3 에 정리돼 있으니 반드시 함께 읽히세요.

### 프롬프트 17-A — 반복 설정 + 실행 예정일 생성 [1단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§13.15.1 ~ §13.15.5 를 구현해줘.

[설계 전제]
  실행 작업은 새 표로 만들지 않는다.
  work_tasks 의 일자 하위업무(parent_task_id)로 만든다.
  §13.8 이 이미 "일자 하위업무는 시간표 배치용" 으로 정의해 뒀고
  이번 기능은 그것을 자동 생성하는 것이다.

  별도 표로 만들면 일일 업무 등록 시간표(§7)에 안 올라온다.
  시간표에 안 보이는 일은 결국 아무도 안 한다.
  담당자별 열·일일 업무 회의·주간보고도 전부 work_tasks 를 읽는다.

(1) work_tasks 에 컬럼 추가 (마이그레이션 + 롤백)
    recurrence_role    parent | occurrence | NULL
    occurrence_seq
    occurrence_status
    not_done_reason

(2) work_recurrence 테이블 신설 (상위 업무 1건당 1행)
    task_id, start_date, end_date,
    rule_type    daily|weekly_dow|every_n_days|every_n_weeks|manual|none
    interval_n, weekdays, holiday_policy, complete_policy,
    progress_include_future, final_result

(3) 세 날짜를 구분한다 (§13.15.1)
    최종 마감일 = 상위 due_date        전체가 끝나야 하는 날
    실행 예정일 = 실행 작업 work_date   실제로 작업할 개별 날짜
    다음 조치일 = 실행 작업 next_check_date  (§13.5 기존 컬럼 재사용)

    [원칙] 실행일에 못 끝냈다고 최종 마감일을 옮기지 마라.
    그 실행 작업에 다음 조치일을 붙일 뿐이다.

(4) 주말·공휴일 처리는 §23.13 의 IsWorkingDay() 를 그대로 쓴다.
    새 판정 로직을 만들지 마라.
      as_is        주말·공휴일에도 실행
      next_workday 다음 근무일로 이동
      prev_workday 이전 근무일로 이동
      skip         해당 실행 일정 제외

    [경고] 그 연도 휴무일이 미등록이면 주말만 제외된다.
    생성 화면에 "{연도} 휴무일 미등록 - 주말만 제외됩니다" 배너를 띄운다.

(5) [필수] 생성 전에 미리보기를 보여준다.
    "실행 예정일 12건이 생성됩니다. 9/1 9/4 9/7 ...
     ※ 9/24는 추석이라 9/25로 옮겨집니다"

    생성 건수 상한: 200건 초과 경고, 500건 초과 거부.
    매일×1년이면 365건이다. 대부분 기간이나 주기 입력 실수다.

(6) [정기점검에서 겪은 일을 반복하지 마라 — §13.15.3]
    - 실행 작업 생성 시 상위의 담당자·사업·업무구분을 반드시 복사한다.
      정기점검 자동생성이 product_type 을 안 채워서
      일정이 등록됐는데도 미배정 목록에 남는 사고가 있었다
    - 생성·재생성·정리는 명시적 버튼으로만 한다.
      정기점검은 화면을 여는 것(GET)만으로 중복정리 DELETE 가 돌았다
    - 재생성 시 parent_task_id 연결을 검증하고 건수를 보고한다.
      상위 ID 불일치로 하위가 고아가 되어 화면이 빈 적이 있다

(7) [이 단계에서는 지표를 건드리지 마라]
    실행 작업을 기존 하위업무와 똑같이 취급해서
    지금 통계 숫자를 그대로 유지한다.
    지표 반영은 17-D 에서 한다.

검증 (§13.15.14 의 1·2·3·9번):
  - 9/1~9/30 · 3일마다 → 미리보기 10건, 생성 후 하위 10건
  - holiday_policy=next_workday 로 9월을 만들면
    추석(9/24·9/25)이 다음 근무일로 밀린다
  - 실행 작업이 일일 업무 등록 시간표 그날 칸에 올라온다
  - 매일×1년으로 만들려 하면 거부된다
  - 통계 숫자가 작업 전과 같다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 17-B — 상태·완료 처리·진행률 [2단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§13.15.6 · §13.15.7 · §13.15.11 을 구현해줘.

(1) 실행 작업 상태 6종 (§13.15.6)
    예정 · 진행중 · 완료 · 미완료 · 연기 · 제외
    상위 업무 상태는 §13.5 의 기존 9종을 그대로 쓴다. 새로 만들지 마라.

(2) 예정일이 지나면 자동으로 미완료가 된다.
    [중요] 이전 회차가 미완료여도 다음 실행 일정은 그대로 유지한다.
    하나 밀렸다고 나머지가 무너지면 안 된다.

(3) 미완료 실행 작업을 §8 미계획 업무함의
    "일정이 지난 건" 묶음에 올린다. 예정일 경과와 같은 취급.

(4) 완료 정책 3종 (§13.15.7)
    auto            모든 실행 작업 완료·제외 시 상위 자동 완료
    manual          담당자가 최종 완료
    require_result  최종 결과 입력해야 완료

    수동 완료 시 미완료가 남아 있으면:
    "완료되지 않은 실행 작업이 {n}건 있습니다.
     전체 업무를 완료 처리하시겠습니까?"

    상위를 완료해도 미완료 실행 작업 기록은 남긴다.
    지우면 "몇 번 중 몇 번 했는가" 를 다시 볼 수 없다.

(5) 진행률 (§13.15.11)
    완료 수 ÷ 대상 수 × 100
    progress_include_future 설정으로 대상 범위를 고른다
      미래 제외(기본): 오늘까지 도래한 실행 작업
      미래 포함:       전체 실행 작업
    "제외" 상태는 분모에서 뺀다.
    하지 않기로 한 회차를 미이행으로 세면 안 된다.

검증 (§13.15.14 의 4·5번)

제약: (위 4줄 동일)
```

### 프롬프트 17-C — 일정 변경·삭제 보호 [3단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§13.15.8 을 구현해줘.

시작일·마감일·주기가 바뀌면 셋 중 하나를 고르게 한다.
  1. 미완료 미래 일정만 다시 생성 (기본)
  2. 기존 유지 + 새 일정만 추가 (중복을 미리보기에 표시)
  3. 모든 미완료 일정 삭제 후 재생성 (확인 한 번 더)

[절대 규칙]
  이미 완료된 실행 작업과 그 처리 기록은
  어떤 선택에서도 변경·삭제되지 않는다.

  상위 업무를 지울 때도 마찬가지다.
  완료된 실행 작업이 있으면 상위 삭제를 거부하고 "보관" 을 안내한다.

  정기점검에서 계획 삭제가 하위 방문을 전량 지워
  데이터가 두 번 사라진 일이 있다(§23.11). 같은 일을 반복하지 마라.

검증 (§13.15.14 의 6·7번):
  - 완료된 실행 작업이 있는 상위 업무는 삭제가 거부된다
  - 주기를 바꿔 재생성해도 완료된 회차와 처리 기록이 그대로다

제약: (위 4줄 동일)
```

### 프롬프트 17-D — 지표 반영 + 화면 [4단계 · 마지막]

```
@기획_업무일지_프로그램_요구사항_v2.md

§13.15.9 · §13.15.10 · §13.15.12 를 구현해줘.
데이터가 쌓인 뒤에 한다. 앞 단계와 동시에 하지 마라.
그래야 숫자가 변한 원인이 새 데이터인지 새 산식인지 구분된다.

(1) [핵심] 집계 단위를 지표별로 다르게 한다 (§13.15.9)
    실행 작업을 그대로 1건씩 세면 숫자가 부풀려진다.
    9월 한 달 매일이면 한 업무가 30건이 된다.

    계획 대비 실행률(§4)      → 실행 작업 단위
      이 지표의 정의가 "그날 계획한 것을 그날 했는가" 다
    주간보고 완료 건수(§16.1) → 상위 업무 1건 + 횟수 부기
      "정기 데이터 확인 · 5회 중 4회". 30건으로 세면 다른 업무가 묻힌다
    담당자별 일일업무(§16.4)  → 실행 작업
    일일 업무 등록 시간표(§7) → 실행 작업
    미계획 업무함(§8)         → 실행 작업
    전사 주간보고 F열(§16.6)  → 상위 1건 + 횟수 "·정기 데이터 확인 5회"

    [필수] 모든 집계 쿼리에서 recurrence_role 을 명시적으로 처리해라.
    조건을 빠뜨리면 상위와 실행 작업이 둘 다 세어져 이중 계상된다.

    먼저 보고해줘: recurrence_role 조건을 넣어야 할 쿼리 목록과
    반영 전후 숫자 비교표. 임의로 반영하지 마라.

(2) 업무 상세 화면 (§13.15.10)
    상단 요약: 전체 기간 · 마감까지 D-n · 진행률 막대 ·
              실행 주기 · 다음 실행 예정일 · 완료/미완료/예정 건수
    날짜별 실행 이력: 체크리스트 / 타임라인 전환
    실행 작업 상세: 처리 내용 · 실제 처리일 · 처리 담당자 ·
                   첨부 · 미완료 사유 · 다음 조치일

(3) 알림 (§13.15.12) — 화면 안에서만 한다
    [현실 확인] 이 시스템에는 메일·푸시 발송 수단이 없다.
    SMTP 설정도 없다. 메일 발송을 만들지 마라.
    동작하지 않는 기능이 하나 늘어날 뿐이다.

      실행 예정일 당일   → 시간표·오늘 내 업무 (기존 흐름)
      N일 전            → 대시보드 "다가오는 실행 {n}건" 카드
      실행 미완료        → 미계획 업무함
      마감 임박          → 상세 상단 D-n, 3일 이내 주황
      마감 경과          → 대시보드 "마감 경과 {n}건" 빨강

    개별 실행 알림과 전체 마감 알림을 구분해 표시한다.

검증 (§13.15.14 의 8·10번):
  - 주간보고에서 이 업무가 30건이 아니라 1건 + 횟수로 집계된다
  - 상위 업무와 실행 작업이 이중으로 세어지지 않는다

제약: (위 4줄 동일)
```

---

# 18단계 — 지표 기준일 · AS 검색 [v2.17]

두 갈래입니다. **18-A(지표 기준일)를 먼저** 하세요. 지금 지표가 이관 데이터 1,100건 위에서 계산되고 있습니다.

### 프롬프트 18-A — 지표 기준일 + 실행률 대상 한정

```
@기획_업무일지_프로그램_요구사항_v2.md

§4.5 를 구현해줘.

(1) app_settings 에 두 값을 추가한다. 코드에 박지 마라.
    metrics_base_date = '2026-08-03'
    progress_scope    = 'as,maintenance'

(2) 모든 지표 집계에 기준일 하한을 건다.
    대상: 대시보드 KPI, 통계(§15), 주간/월간 보고서(§16),
          담당자별 분석(§16.4), 전사 주간보고(§16.6),
          부록 C 검증 쿼리

    [중요] 목록·상세·검색에는 걸지 마라.
    AS 이력 조회·재발 판정·장애빈도 분석은 옛 데이터가 있어야 한다.
    보이는 것과 세는 것을 분리한다.

(3) [중요] 기준일과 이관 데이터는 다른 축이다.
    실측 교차표:
                    기준일 이전   기준일 이후
      app(앱 입력)      24건         26건
      import(이관)    1,108건        16건

    기준일 이후에도 이관 데이터가 16건 섞여 있다.
    "기준일 이후 = 앱에서 제대로 입력한 데이터" 가 아니다.

    지표 대상 = receipt_datetime >= 기준일 AND data_origin <> 'import'

    "이관 포함" 토글을 켜면 data_origin 조건만 풀린다.
    기준일은 토글로 풀리지 않는다.

(4) 계획 대비 실행률 대상을 AS·정기점검으로 한정한다 (임시).
    work_type IN ('admin','support') 를 실행률 분모·분자에서 뺀다.
    다른 지표(건수·소요시간)에서는 빼지 마라. 실행률만이다.

    이유: work_actions 0건, work_activities 1건이다.
    행정·지원업무의 GTD 구조가 아직 자리 잡기 전이라
    미입력이 AS·정기점검 실적까지 끌어내린다.

    되돌릴 조건: 행정·지원의 예정일 입력률이 4주 연속 90% 이상.
    이 조건을 코드 주석과 화면 안내에 남겨줘.
    조건 없이 "임시" 로 두면 영구히 빠진 채로 남는다.

(5) 화면 표시
    통계·보고서 상단에 "2026-08-03 이후 데이터 기준"
    실행률 카드에 "AS · 정기점검 기준"
    무엇이 빠졌는지 안 보이면 나중에 아무도 기억 못 한다.

(6) 표본이 줄어드는 것을 숨기지 마라.
    기준일 적용 후 AS 예정일 보유 건이 17건이다(정기점검 76건 별도).
    주 단위로 쪼개면 대부분 §4.4 의 🟡 참고 등급이 된다.
    등급 기준을 낮추거나 등급 표시를 빼지 마라.
    사실을 정확히 표시하는 것이 맞다.

검증 (§4.5.5):
  - AS 목록·상세·검색에서 기준일 이전 건이 그대로 보인다
  - 실행률 분모에 행정·지원 33건이 안 들어간다
  - "이관 포함" 토글을 켜면 AS 표본이 26 → 42 로 늘고 기준일은 그대로
  - 기준일을 설정에서 바꾸면 모든 지표가 함께 바뀐다
    (한 군데만 바뀌면 산식이 흩어진 것이다)

  적용 전후 KPI 숫자 비교표를 먼저 보여줘.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 18-B — AS 전문 검색 [1단계 · 효과 대비 가장 쌈]

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.11.1 ~ §12.11.5 를 구현해줘.

(1) [먼저 확인] modernc.org/sqlite 가 FTS5 를 포함하는지 검증해줘.
    이 프로젝트는 CGO_ENABLED=0 으로 빌드한다.
    FTS5 가 없으면 빌드는 되고 테이블 생성에서 런타임 오류가 난다.

    작은 테스트로 CREATE VIRTUAL TABLE ... USING fts5 가
    실제로 되는지 확인하고 결과를 먼저 알려줘.

    안 되면 LIKE '%검색어%' 로 간다. 1,174건 규모면 체감 차이가 거의 없다.
    FTS5 가 안 된다고 이 기능을 포기하지 마라.

(2) [필수] FTS5 를 쓴다면 tokenize='trigram' 을 써라.
    기본 unicode61 토크나이저는 한국어에 안 맞는다. 조사 때문이다.
      본문 "무인예약이 안 됩니다" → 무인예약이 로 색인
      검색어 "무인예약"          → 안 걸린다

    CREATE VIRTUAL TABLE as_search USING fts5(
      as_id UNINDEXED, symptom, action, cause_detail, conclusion,
      tokenize='trigram'
    );

(3) /as/search 화면 + AS 목록 상단 검색창
    별도 메뉴만 만들면 안 쓴다. 목록에도 넣어라.

    검색 대상: 증상 · 조치내용 · 장애원인 · 결론 · 처리 이력
    필터: 기간 · 기관 · 제품 · 원인분류 · 처리유형 · 담당자
    결과: 접수번호 · 기관 · 날짜 · 증상 발췌(강조) · 조치 발췌 · 원인분류

    [중요] 결과에서 증상과 조치를 나란히 보여줘라.
    접수번호만 나오고 눌러야 내용이 보이면
    열 건 비교하려고 열 번 들어갔다 나와야 한다.

    [중요] 검색에는 §4.5 의 기준일을 적용하지 마라.
    검색은 옛 자료를 찾는 것이 목적이다.

(4) 접수·조치 화면에 "비슷한 사례" (§12.11.5)
    검색하러 가는 기능이 아니라 접수하는 순간 옆에 뜨는 기능이어야 한다.

    /as/new     증상 입력 시 오른쪽에 비슷한 과거 건 3~5개
    /as/{id}/action  상단에 같은 목록

    가중치: 같은 자산 > 같은 기관 > 같은 제품 > 전체
    각 카드에 증상 요약 + 조치 요약 + 처리일
    조치 기록이 없으면 "조치 기록 없음" 으로 표시
      (1,174건 중 조치가 있는 건은 508건뿐이다)

    비슷한 사례가 완료 상태이고 같은 자산이면
    그 자리에서 §12.7 "동일 증상 재접수로 만들기" 를 제안한다.

    결과가 없으면 영역을 표시하지 마라. "없음" 은 화면만 차지한다.

검증 (§12.11.9 의 1~5번)

제약: (위 4줄 동일)
```

### 프롬프트 18-C — 키워드 사전 [3단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.11.6 을 구현해줘. 18-B 가 자리 잡은 뒤에.

(1) as_keywords 사전 테이블
    keyword_id, keyword, kw_group(증상|원인|조치|대상),
    synonyms, is_active, sort_order, note

    초기 데이터는 §12.11.6 의 표를 넣는다.
    코드가 아니라 표에 둔다. 운영하면서 담당자가 늘린다.

(2) as_keyword_links 연결 테이블
    as_id, keyword_id, source(auto|manual), field(symptom|action)

(3) [핵심] 자동 확정하지 마라.
    실제 본문으로 단어 빈도를 뽑아보니
    고유 단어가 증상 5,403개 · 조치 3,490개이고
    상위권에 "안녕하세요(220)" "부탁드립니다(195)" "감사합니다(193)"
    같은 인사말이 그대로 올라온다. 041 · kr · go 같은 조각도 섞인다.

    빈도순 자동 추출을 그대로 키워드로 쓰면 인사말이 키워드가 된다.

    - 불용어 사전(인사말·조사·숫자·URL 조각)을 만들어 후보에서 제외
    - 후보를 제시하고 사람이 고른다
    - 새 접수·조치 저장 시 사전과 대조해 후보를 제안하고 담당자가 체크

(4) 키워드로 사례 모아보기 — "팝업 오류" 같은 묶음 조회

검증:
  - 키워드 후보에 "안녕하세요" "감사합니다" 가 나오지 않는다

제약: (위 4줄 동일)
```

### 프롬프트 18-D — 과거 데이터 정제 반입 [4단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.11.7 을 구현해줘.

[경고] cmd/asimport 가 과거에 예정일 없이 확정된 1,141건을 만들었다.
E2E 검증에서 확인된 사실이다.
웹 화면은 그 조합을 막고 있었지만 반입 스크립트가 검증을 건너뛰었다.
이번에 같은 일을 반복하면 안 된다.

반입 절차를 4단계로 고정한다.
  1. 업로드 — 엑셀·CSV 를 올린다. 바로 반영하지 않는다
  2. 검증 리포트 — 필수값 누락 · 날짜 범위(연도 2000~2100, §8.2.3) ·
     고객/자산 매칭 실패 · 중복 의심을 건수와 목록으로 보여준다
  3. 사용자 확인 — 오류 건을 고치거나 제외를 선택
  4. 반영 — data_origin='import' 로 저장하고 반입 배치 ID 를 남긴다

[필수] 배치 ID 로 일괄 취소가 가능해야 한다.
잘못 반입한 1,000건을 손으로 지울 수는 없다.

반입 데이터는 §4.5.3 에 따라 지표에서 제외된다.
반입 건에도 키워드 자동 후보를 붙이되 확정하지 마라.
한 번에 1,000건을 확정하면 사전이 오염된다.

검증 (§12.11.9 의 7·8번)

제약: (위 4줄 동일)
```

---

# 19단계 — 화면 개편 [v2.18]

기획서 **§33** 전체가 근거입니다. **19-A(버그 1줄)를 먼저** 하시고, 나머지는 순서대로 가세요.

**⚠ 시작 전에 §33.1을 꼭 읽으세요.** 「예정일 → 업무 등록일」은 라벨 변경이 아니라 **지표 변경**입니다.

### 프롬프트 19-A — [1줄] 오늘 내 업무 메뉴 강조 버그

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.8 을 고쳐줘. 한 줄이다.

internal/handler/work.go:52
  "Active": "dashboard",   →   "Active": "work",

base.html 사이드바는 {{if or (eq $a "work_plan") (eq $a "work")}} 로
"오늘 내 업무" 를 강조하는데 핸들러가 dashboard 를 넘겨서
대시보드가 강조되고 있다.

그리고 같은 실수가 다른 화면에도 있는지 전부 확인해줘.
각 핸들러가 Active 를 문자열로 직접 넘기는 구조라 오타가 조용히 통과한다.
Active 값을 상수로 정의하고 핸들러가 그 상수를 쓰게 바꾸면 재발하지 않는다.
목록을 먼저 보고해줘.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 19-B — 등록 모달 항목 정비

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.1 · §33.2 를 구현해줘.

[가장 중요 — §33.1]
  "예정일 → 업무 등록일" 은 라벨 변경이 아니라 지표 변경이다.
  §4.2 에서 예정일은 계획 대비 실행률의 분모다.
  "등록일" 은 created_at 이라 분모로 쓸 수 없다.

  세 날짜를 이렇게 매핑한다:
    업무 등록일 = created_at    자동·읽기 전용 (신규 표시 항목)
    업무 시작일 = work_date     라벨만 변경
    업무 종료일 = due_date      ← 기존 "예정일" 이 여기로 온다. 실행률 분모

  즉 예정일이 사라지는 게 아니라 "업무 종료일" 로 이름이 바뀐다.
  컬럼(due_date)은 그대로 두고 라벨만 바꾼다.
  §5 의 예정일 필수 규칙도 그대로 승계한다(미정+사유 포함).

(1) 라벨 변경
    "접수·처리내용" → "업무 내용"
    "업무 배정일"   → "업무 시작일"
    "예정일"        → "업무 종료일"
    + "업무 등록일" 자동 표시 항목 신규

(2) 폼에서 "상태" 와 "우선순위" 입력란을 뺀다.
    [중요] 컬럼은 지우지 마라.
    status 는 칸반·통계·미계획 업무함이 전부 쓰고
    priority 는 목록 정렬에 쓴다.
    폼에서 숨기고 기본값으로 저장한다: status='waiting', priority='normal'

    실측: priority 는 normal 132 / high 2 / low 1 로 사실상 안 쓰인다.
    입력란을 없애는 건 맞지만 컬럼을 지우면 정렬·이관이 깨진다.

(3) "＋하위 업무 등록" 버튼과 "기간 내 반복 실행" 체크박스를 넣는다.
    (동작은 19-C, 19-D 에서)

검증:
  - 업무 종료일이 실행률 분모로 그대로 동작한다 (숫자가 변하지 않는다)
  - 상태·우선순위 없이 저장해도 기본값이 들어간다
  - 부록 A 의 필수 입력 일람도 새 라벨로 갱신한다

제약: (위 4줄 동일)
```

### 프롬프트 19-C — 하위 업무 다단계

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.3 을 구현해줘.

(1) work_tasks.parent_task_id 로 다단계 하위 업무를 만든다.
    번호: 상위번호 뒤에 단계마다 -순번
      WT-113 → WT-113-1 → WT-113-1-1
    순번은 같은 부모 안에서 1부터.
    형제가 삭제돼도 번호를 다시 매기지 마라. 번호는 식별자다.

(2) "＋하위 업무 등록" → "행정·지원업무 등록" 팝업
    상위의 담당자·거래처·사업을 기본값으로 채우고 고칠 수 있게 한다.

(3) [필수] 깊이를 3단계로 제한한다.
    무제한 중첩은 번호가 길어지고 조회가 재귀가 되며
    순환 참조(A가 B의 하위이고 B가 A의 하위)를 막기 어렵다.
    3단계를 넘기면 거부하고 사유를 표시한다.
    부모 지정 시 자기 자신과 자기 자손을 후보에서 제외한다.

(4) [필수 — 충돌 주의] parent_task_id 를 §13.15 의 실행 작업도 쓴다.
    구분하지 않으면 9월 매일 반복 30건이
    하위 업무 30개처럼 트리에 나타난다.

    recurrence_role 로 갈라라:
      NULL         → 하위 업무. 번호 -1, 들여쓰기
      occurrence   → 실행 작업. 트리에 넣지 말고
                     "실행 이력" 영역에 따로 묶는다 (§13.15.10)

    하위 업무 조회 쿼리에 recurrence_role IS NULL 조건을 반드시 넣어라.

(5) 하위가 있는 상위는 삭제를 거부하고
    "하위 업무 n건을 먼저 처리하세요" 를 표시한다.

제약: (위 4줄 동일)
```

### 프롬프트 19-D — 반복 주기 단위 보강

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.4 를 구현해줘. §13.15 의 17-A 가 끝난 뒤에 한다.

rule_type 에 다음을 추가한다:
  monthly    매월 N일 · 매월 마지막 영업일
  quarterly  분기 시작월 지정
  yearly     월·일 지정
  manual     지정일자 — 여러 날 직접 선택

[지정일자] 달력에서 다중 선택한다. 여러 달에 걸쳐 고를 수 있다.

  [중요] 주말·공휴일 정책(§13.15.5)을 지정일자에는 적용하지 마라.
  사람이 직접 고른 날짜를 프로그램이 옮기면 안 된다.
  고른 날이 휴일이면 경고 표시만 한다.

나머지(생성 미리보기·건수 상한 200/500·완료 정책·
일정 변경·삭제 보호)는 §13.15 를 그대로 따른다.

제약: (위 4줄 동일)
```

### 프롬프트 19-E — 행정·지원업무 메뉴 (수집함 삭제 · 칸반 · 정렬)

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.5 를 구현해줘.

(1) 수집함(inbox) 상태를 뺀다.
    실측: work_tasks 상태는 complete 61 / waiting 55 / in_progress 19
    세 가지뿐이다. inbox·waiting_for·hold·transfer·review·cancelled 는 0건.
    §13.5 가 정의한 9종 중 6종이 한 번도 안 쓰였다.

    마이그레이션에 inbox → waiting 변환을 넣어라.
    개발 DB에는 0건이지만 운영 DB에 있을 수 있다.

    §13.1 · §13.2 · §13.5 · §13.11 의 수집함 서술도 정리한다.
    "분류 전 업무" 는 할 일(waiting)로 들어가 담당자가 정리한다.

(2) 칸반 3열: 할 일 / 진행중 / 완료
      할 일   waiting
      진행중  in_progress + hold + transfer + waiting_for + review
      완료    complete
      cancelled 는 어느 열에도 넣지 마라. 필터로만 본다

    보류·이관·회신대기는 진행중에 넣되 카드에 뱃지로 구분한다.
    별도 열을 만들면 3열 요구와 어긋나고
    할 일에 넣으면 지금 할 수 있는 일과 섞인다.

    드래그로 상태 변경. "완료" 로 끌면 §13.9 완료 검증을 요구한다.

(3) 리스트 정렬 — 컬럼 머리글 클릭, 오름/내림 토글
    대상: 업무번호·업무명·거래처·담당자·시작일·종료일·상태·소요시간·등록일
    기본 = 업무 종료일 오름차순 (마감 가까운 것이 위로)
    선택은 ?sort= 로 유지

제약: (위 4줄 동일)
```

### 프롬프트 19-F — 조치 화면 정리 · 다음 행동 삭제

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.6 · §33.7 을 구현해줘.

(1) "영업 활동으로 만들기" 를 별도 버튼으로 독립시킨다.
    누르면 §32 의 영업 활동 등록으로 넘어가고
    이 업무를 sales_activities 에 연결한다. (§32 16-B 이후)

(2) "시간표" → "일정표" 로 명칭 변경.
    화면·문서 전체에서 통일해라. 한 군데만 바꾸면 더 헷갈린다.

(3) 업무 구분을 읽기 전용으로 고정하고
    등록 정보를 리스트 형태로 표시만 한다.
    조치 화면에서 업무 성격을 바꾸면 지표 귀속이 흔들린다.

(4) [중요] 조치 화면에서 "기간 내 반복 실행" 을 삭제한다.
    반복 설정은 등록·수정 화면에서만 한다.
    조치 중에 주기를 바꾸면 §13.15.8 의 재생성이 돌아서
    지금 처리 중인 실행 작업이 지워질 수 있다.

(5) "다음 행동" 을 화면에서 뺀다 (§33.7)
    실측: work_actions 0건, work_activities 1건.
    §13.6·§13.7 에서 설계했으나 한 번도 안 쓰였다.

    [중요] work_actions 테이블은 지우지 마라. 0건이라 지울 이유가 없고
    나중에 되살릴 수 있다. 화면에서만 뺀다.
    대체는 하위 업무(§33.3)로 한다.

    §13.5 의 회신 대기(waiting_for) 상태는 남긴다.
    다음 행동과 별개 개념이고 다자 협업의 핵심이다.

(6) 조치 이력은 등록 폼을 없애고 목록만 남긴다.
    이력은 조치 저장 시 자동으로 쌓인다.

(7) "하위업무 추가" 는 버튼만 남기고 인라인 폼을 없앤다.
    §33.3 의 팝업을 연다.

(8) [별건] 일일 업무 등록의 [수정] 버튼이
    등록 메뉴로 연결되도록 고친다.

제약: (위 4줄 동일)
```

### 프롬프트 19-G — 오늘 내 업무 전면 개편

```
@기획_업무일지_프로그램_요구사항_v2.md

§33.9 를 구현해줘. 19-A 가 끝난 뒤에.

(1) 3분류: 진행 예정 / 진행중 / 완료
      진행 예정  waiting
      진행중     in_progress + 보류·이관·회신대기(뱃지)
      완료       complete

    [중요] §33.5.2 의 행정업무 칸반과 같은 매핑을 쓴다.
    화면마다 분류가 다르면 같은 업무가 다른 열에 보인다.

    이 화면은 AS·정기점검·행정·지원을 모두 모은 통합 목록이므로(§11)
    각 원본 상태를 3분류로 바꾸는 매핑 함수를 하나만 만들어라.
    model/workboard.go:249 에 이미 partial_complete·in_progress·assigned
    매핑이 있으니 그 함수를 확장해 재사용해라. 새로 만들지 마라.

(2) 보기 2종: 칸반 / 리스트
    칸반 카드: 접두어([AS]·[정기점검]·[일반업무]) · 기관 · 종료일 · 담당자
    리스트: §33.5.3 과 같은 정렬 규칙. 기본 = 종료일 오름차순
    선택은 ?view=kanban|list 로 유지

(3) 역할별 범위
    관리자·접수담당  팀 전체
    기술담당         본인 배정분 (토글로 팀 전체 조회 가능 — 막지 마라)
    영업담당         본인 + 영업 활동
    열람사용자       팀 전체 조회
    관리자는 담당자 드롭다운으로 한 사람만 볼 수 있다 (§10.1 방식)
    선택은 ?assignee= · ?mine= 으로 유지

(4) [중요] 범위 판정은 §24.1 의 역할 확정 절차를 따른다.
    역할을 못 읽으면 빈 화면 대신 배너를 띄워라.
    사이드바가 조용히 축소된 것과 같은 사고를 여기서 반복하지 마라.

검증 (§33.9.4)

제약: (위 4줄 동일)
```

---

# 20단계 — AS · 정기점검 개편 [v2.19]

기획서 **§34** 전체가 근거입니다. 요청 26건을 6개 프롬프트로 나눴습니다.

**⚠ §34.1을 먼저 읽으세요.** 삭제 요청 3건이 관리 지표의 근거 데이터입니다. 그대로 지우면 지표가 죽습니다.

### 프롬프트 20-A — [먼저] 지표 근거 3건 처리

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.1 을 구현해줘. 나머지 삭제 작업보다 먼저 한다.

삭제 요청 3건이 관리 지표의 근거다. 실사용을 확인했다:
  start_datetime          1,165 / 1,174건 채워짐
  as_processes.time_spent 0 / 1,172건
  conclusion              v2.7 신설

(1) 착수 시각 — 화면에서만 없애고 자동으로 채운다
    start_datetime 은 §4.1 "접수 → 방문 3일" 지표의 근거다.
    E2E 검증에서 이 값을 채워 표본을 6 → 69건으로 늘렸던 필드다.

    - 조치 화면의 입력란을 제거한다
    - 조치를 처음 저장하는 순간 서버가 자동으로 넣는다
    - 상세에는 읽기 전용으로 표시한다 ("착수 2026-08-19 10:14")

    손으로 넣는 것보다 자동 기록이 정확하다.
    이 요청은 오히려 데이터 품질을 올린다.

(2) 소요시간 — A안으로 간다
    조치 화면에서만 없애고
    일일 업무 등록의 work_tasks.duration_min 으로 대신한다.
    시간표에 배치할 때 이미 넣는 값이다.

    §16.1 주간보고 일일 처리 평균, §16.4 담당자별 소요(분),
    §7.7 투입시간, 시간표 카드 높이가 이 값을 쓴다.
    조치 화면에서 지운다고 이 4곳을 걷어내지 마라.

(3) 결론 — 보고서 발급 화면으로 옮긴다
    conclusion 은 §12.10 조치완료보고서의 "5. 결론" 칸이다.
    조치 화면에서 입력란을 없애고
    §12.10.5 보고서 미리보기 화면에서 입력하게 한다.
    자동 초안(§12.10.4)은 그대로 제시한다.

    보고서를 안 내는 건이 대부분이라 이 편이 덜 번거롭다.

검증:
  - 착수 시각이 화면에 없지만 DB에는 계속 쌓인다
  - "접수 → 방문 3일" 지표 숫자가 작업 전과 같다
  - 조치완료보고서 5. 결론이 비지 않는다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 20-B — AS 접수: 장비별 건 분리

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.2.1 을 구현해줘.

as_receipts.asset_id 는 이미 1:1 이다. 바꿀 것은 접수 화면이다.

(1) 접수 화면에서 장비를 여러 대 고를 수 있게 한다.
    공통: 기관 · 요청자 · 연락처 · 접수채널 · 접수일시
    장비별: 자산 · 증상 · 긴급 사유 · 예정업무일 · 담당자

    저장하면 자산 수만큼 각각 접수번호를 받는다.

(2) receipt_group_id 컬럼을 추가하고 같은 값을 넣는다.
    "그날 그 기관에서 함께 접수한 건" 을 나중에 볼 수 있어야 한다.
    묶음이 없으면 2건이 서로 남남이 되어
    방문 한 번에 둘 다 처리한 사실이 기록에서 사라진다.

    - 목록에서 같은 묶음 건에 🔗 뱃지
    - 상세에 "함께 접수된 건 n건"

(3) 저장 전에 "접수 2건이 생성됩니다" 를 명시한다.
    한 건인 줄 알고 저장했다가 두 건이 생기면 놀란다.

(4) 지표에는 2건으로 센다. 장비별 조치·소요·원인이 다르므로 맞다.
    별도 처리를 넣지 마라.

제약: (위 4줄 동일)
```

### 프롬프트 20-C — AS 접수: 나머지 6건

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.2.2 ~ §34.2.6 을 구현해줘.

(1) 긴급도·중요도 입력란 삭제 → 긴급 사유 드롭다운 (§34.2.2)
    실사용: urgency 는 normal 1,171 / low 2 / high 1. 사실상 안 쓰인다.

    제품에 따라 항목이 바뀐다:
      RFID    장비 운영 불가 / 대출·반납 전면 중단 / 안전 위험 / 해당 없음
      KLAS    서버 다운 / 자료관리 접속 불가 / 대출·반납 처리 불가 /
              DB 오류 / 해당 없음
      홈페이지 사이트 접속 불가 / 로그인 전면 불가 / 해당 없음
      공통    기타(직접 입력)

    "해당 없음" 이 기본값. 그 외면 긴급 건이다.
    urgency='high' 로 저장해 기존 컬럼을 그대로 쓴다.
    통계·정렬이 계속 동작해야 한다.
    항목은 codes 에 둔다.

(2) 일정 확정 체크박스 삭제 (§34.2.3)
    [주의] schedule_confirmed 컬럼을 지우지 마라.
    §10 일일 업무 회의의 "오늘 예정" 조건이 이 값이고
    §4.2 실행률 분모 판정에도 관여한다.

    새 규칙: 저장 시 서버가 자동으로 채운다
      schedule_confirmed = (visit_scheduled_date 가 비어 있지 않은가)
    "미정 + 사유" 로 저장하면 0.

(3) 기관 선택 검색 + 초성 자동완성 (§34.2.4)
    고객 125건 전부를 화면에 내려보내고 브라우저에서 거른다.
    서버 왕복이 필요 없다.

    초성: 한글 음절 코드에서 (code-0xAC00)/588 로 초성 인덱스를 얻는다.
    라이브러리를 추가하지 마라.
    ㅈㅊㅇ → 조치원도서관

    [중요] 초성 검색은 입력이 전부 초성일 때만 적용한다.
    "조치" 를 초성으로 해석하면 안 된다.
    키보드 ↑↓·Enter 로 고를 수 있게 한다.

(4) 협력사·고객 구분 (§34.2.5)
    customers 에 party_kind 추가: customer(기본) | partner | own | vendor
    AS 접수·정기점검은 customer 만 본다. 다른 화면은 전부 그대로 둔다.

    지금 비젼아이티(C000-26-008)·채움씨앤아이(C000-26-009) 같은
    협력사·자사가 고객 목록에 섞여 있다.
    이 둘은 마이그레이션에서 own/partner 로 지정하고
    나머지는 기본값 customer 로 둔다.
    고객현황 화면에 구분 필터와 수정 기능을 넣는다.

(5) 요청주체명·요청주체유형 삭제 → 접수 채널로 흡수 (§34.2.6)
    실사용: requester_type 은 빈값 1,124 / customer 44 / internal 3 /
    partner 1 / onecall 1. 거의 안 쓰인다.

    접수 채널 항목을 넓힌다:
      전화·이메일·방문·원콜·제조사·협력사·원청·내부

(6) 요청자 선택 시 연락처 자동 표시 (§34.2.6)
    - 기관을 고르면 주담당자(contacts.is_primary=1)가 기본 선택
    - 요청자를 바꾸면 그 사람 연락처로 바뀐다
    - 연락처는 읽기 전용. 여기서 고치면 기준정보와 어긋난다
    - 그날만 다른 번호인 경우를 위해 "이번 건 연락처" 별도 입력란
      이 값은 as_receipts 에만 저장하고 contacts 는 건드리지 않는다

제약: (위 4줄 동일)
```

### 프롬프트 20-D — AS 조치: 항목 정리 + 연동

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.3.1 ~ §34.3.4 를 구현해줘. 20-A 가 끝난 뒤에.

(1) 폼에서 제거 (컬럼은 §34.3.1 표를 따른다)
    조치내역 "상태"        폼만. 컬럼 유지 (칸반·통계가 쓴다)
    "사용부품/교체품"      실사용 2건. 폼·컬럼 모두 제거 가능
    "후속 조치"            실사용 4건. 제거
    "하부 업무"            폼에서 제거
    하부 업무 조치의 "상태" 폼에서 제거

(2) 근무구분 ↔ 처리유형 연동 (§34.3.2)
    처리유형을 3종으로 줄인다.
    실사용: visit 17 / inquiry 14 / remote 2.
    replace(부품교체)·config(설정변경)는 0건이라 지워도 잃을 게 없다.

      내근(office) → 원격지원 · 문의응대
      외근(field)  → 현장방문 · 문의응대

    근무구분을 바꾸면 처리유형이 첫 항목으로 초기화된다.
    근무구분을 안 고르면 처리유형은 비활성.
    순서를 강제해 잘못된 조합을 원천 차단한다.

    운영 DB에 replace·config 가 있으면 visit 으로 이관하는
    마이그레이션을 넣어라.

(3) 원인분류 3단계 (§34.3.3) — 엑셀 파일을 받았다. 전체 트리가 확정됐다.
    시드 SQL 을 만들어 뒀다: AS_원인분류_시드.sql (프로젝트 루트)
    1차 4 · 2차 23 · 3차 27 = 총 54행. 그대로 실행해라. 손으로 옮겨 적지 마라.

    테이블: as_cause_categories
      code, level(1/2/3), parent_code, label, sort_order,
      cause_type_map, is_active, is_fault

    코드 규칙:
      1차  rfid · klas · web · server
      2차  {1차}.{슬러그}   rfid.hw · klas.ill · web.popup
      3차  {2차}.{슬러그}   rfid.hw.part_check

    [중요] "기타" 가 여러 층에 반복된다.
    rfid.hw.etc 와 rfid.sw.etc 는 라벨이 같지만 다른 항목이다.
    부모를 포함한 코드로 저장해라.
    라벨만으로 저장하면 통계에서 "기타" 가 한 덩어리로 뭉개진다.

    KLAS 2차 "기타" 의 3차에 "RFID" 가 있다.
    1차 rfid 와 이름이 같지만 다른 코드(klas.etc.rfid)다.
    이름으로 판단하는 코드를 쓰지 마라.

    [중요] 깊이가 고르지 않다.
      RFID     3단계 (전부)
      KLAS     2단계, 단 "기타" 만 3단계
      홈페이지 2단계
      서버     2단계
    화면은 "있는 단계까지만" 보여준다.
    자식이 없으면 그 드롭다운을 숨겨라. 빈 드롭다운이 뜨면 안 된다.
    필수: 2차까지 필수, 3차는 있으면 필수.

(4) [판단 수정] 기존 cause_type 을 함께 묻지 마라.
    v2.19 에서 "축이 다르니 둘 다 남긴다" 고 했는데 뒤집는다.

    RFID 2차(HW·SW·서버/네트워크·환경문제·사용자/도서)와
    서버 2차(HW고장·SW오류·네트워크)가
    기존 cause_type 5종과 사실상 같은 것이다.
    둘 다 물으면 담당자가 같은 것을 두 번 고른다.

    담당자는 새 3단계만 고르고, cause_type 은 서버가 2차 값에서 자동으로 채운다.
    매핑은 시드 SQL 의 cause_type_map 컬럼에 들어 있다:
      rfid.hw, server.hw                    → hw
      rfid.sw, server.sw, web.service_error → sw
      rfid.net, server.network              → network
      rfid.env                              → env
      rfid.user                             → user
      그 외 14종                             → 빈값

    - 기존 통계 쿼리가 그대로 동작해야 한다. cause_type 은 계속 채워진다
    - 기존 1,174건은 건드리지 마라. 옛 데이터는 cause_type 만 있다
    - 조치 화면에서 cause_type 입력란은 없앤다

(4-1) is_fault 플래그
    이 표는 "원인" 만 담고 있지 않다.
    홈페이지의 게시물수정·메뉴수정·팝업요청·단순문의,
    RFID 의 설치(신규 납품·이동·폐기)와 기타(견적서 요청·계약 관련)는
    고장의 원인이 아니라 요청 유형이다.

    is_fault=0 으로 두고, §17 장애 빈도 분석에서 뺀다.
    팝업 요청을 "장애" 로 세면 교체 대상 판정이 왜곡된다.

(4-2) 1차는 자산 제품에서 자동 선택
      앤로보틱스(RFID)        → rfid
      KLAS · 세종 KLAS        → klas
      홈페이지 · 전자도서관    → web
      서버 · WAS · DB         → server
    바꿀 수 있게 둬라. KLAS 자산인데 원인이 서버일 수 있다.

(4-3) "장애원인"(cause_detail)을 원인분류 드롭다운 옆으로 옮긴다.
    코드와 문장이 나란히 있어야 무엇을 적는 칸인지 안 헷갈린다.

(5) 결과에서 "재방문 필요"·"대기" 제거 (§34.3.4)
    실사용: done 1,157 / transfer 2 / partial 1. 재방문·대기는 0건.
    남는 것: 완료 · 추가조치 필요 · 이관

    §12.5 결과→상태 전이표에서 revisit·hold 행을 뺀다.
    hold 상태 자체는 남긴다(이관 대기 등).
    조치 결과로만 고를 수 없게 한다.

제약: (위 4줄 동일)
```

### 프롬프트 20-E — 이관 + 추가 조치 · 첨부 통합

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.3.5 · §34.3.6 을 구현해줘.

(1) [핵심] 이관 완료 처리가 안 되는 문제를 고친다 (§34.3.5)

    결과: ( ) 완료  ( ) 추가조치 필요  (•) 이관
                                       └ ☑ 추가 조치 필요

    체크 해제 → 이관 처리 후 이 건은 완료된다
    체크 선택 → 이관 처리 후 새 접수 건을 자동 생성한다

    자동 생성 규칙:
      1. 원 접수의 기관·자산·요청자·접수채널·증상을 복제
      2. 복제된 내용은 수정할 수 없다 (읽기 전용 표시)
      3. 그 아래 "추가 접수 내용" 입력란이 열린다
      4. 새 건은 새 접수번호를 받고 parent_as_id 로 원 건과 이어진다
      5. 원 건의 조치는 전부 완료 처리된다
         (이미 신규 접수로 넘어갔기 때문이다)

    [중요] §12.7 동일 증상 재접수와 다른 흐름이다. 섞지 마라.
      재접수  = 완료 후 같은 증상이 다시 남
      이관후속 = 이관하면서 우리 몫이 남음
    목록 뱃지도 "재접수" / "이관후속" 으로 다르게 붙인다.

    원 건 상세에 "이관 후속 건 — {새 번호}",
    새 건 상세에 "이관 원 건 — {원 번호}" 를 서로 표시한다.

(2) 첨부 영역 통합 (§34.3.6)
    조치 사진을 조치 사항 박스 안으로 올리고
    사진 영역과 문서 영역을 하나로 합친다.

    종류 드롭다운으로 사진/문서를 고른다.
      사진 → ref_type='as_action_photo'
             1600px 축소 + 320px 썸네일 + EXIF 회전
             썸네일로 표시, 클릭 시 확대
      문서 → ref_type='as_action'
             원본 그대로, 파일명만 표시

    [주의] v2.11(§12.9.8)에서 나눈 것을 다시 합치는 것이다.
    그때 문제는 사진이 문서와 섞여 썸네일이 안 나오는 것이었다.
    이번에는 종류를 사람이 고르므로 그 문제가 안 생긴다.
    저장 규칙은 그대로 두고 화면만 합쳐라.

    이미지 파일을 골랐는데 종류가 "문서" 이면 확인을 받는다.

제약: (위 4줄 동일)
```

### 프롬프트 20-F — 정기점검 자동배정 + 일일업무 범위

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.4 · §34.5 를 구현해줘.

(1) [먼저 조사] 주기 규칙이 적용 안 되는 원인 (§34.4.1)

    가장 중요한 단서:
    maintenance_site_config.fixed_rule 이 67건 전부 비어 있다.
    화면에서 월별 고정일을 체크해도 저장이 안 되고 있을 가능성이 크다.

    생성 로직을 고치기 전에 저장 경로부터 확인해줘.
    저장이 안 되고 있다면 생성 쪽은 멀쩡할 수 있다.
    운영 DB에서도 확인하고 결과를 먼저 알려줘.

    그 다음 생성기가 주기별로 무엇을 하는지 확인한다:
      monthly         매월 1회               (63건)
      quarterly       1·4·7·10월             (2건)
      odd_bimonthly   홀수 달                 (1건)
      even_bimonthly  짝수 달                 (1건)
    monthly 만 처리하고 나머지를 건너뛰는지 봐라.

    fixed_rule 이 있으면 그 날짜를 우선하고
    휴일이면 §23.13 정책에 따라 옮긴다.

(2) 자동 배정 버튼을 드롭다운으로 (§34.4.2)
    기본값은 "전월"
      전월   전월 방문의 날짜·순번을 그대로 옮긴다
      근거리 당사 기준 가까운 곳부터
      원거리 근거리의 역순

    "전월" 예외 처리 — 전월에 다른 지역이 섞인 경우:
      그 날짜에 가장 많은 지역을 기준으로 삼는다
      동수면 사이트 수가 많은 지역, 그래도 같으면 지역명 가나다순
      판단 기준을 고정해야 매달 결과가 안 흔들린다

(3) 거리 순서 (§34.4.3)
    [제약] customers 에 위경도가 없다. addr_sido·addr_sigungu 만 있다.
    실제 거리 계산은 불가능하다. 외부 API 를 붙이지 마라.

    region_distance_order 표로 대신한다:
      sido, sigungu, sort_order (당사 기준 가까운 순)
    관리자가 화면에서 순서를 정한다. 한 번 정하면 계속 쓴다.
    같은 지역 안에서는 기존 사이트 순번을 따른다.
    순서 미등록 지역은 맨 뒤로 보내고 "순서 미등록 n개 지역" 을 표시한다.

(4) 버튼 정리 (§34.4.4)
    제거: 지난 방문 일괄 완료 · 승인 · 보관 · 계획삭제
    화면에서만 제거(기능은 다른 곳에 있음):
      엑셀받기     → §16.5 보고서 메뉴에 남긴다
      사이트 설정  → 기준정보 > 점검 사이트 설정 메뉴는 그대로

    [중요] 기능을 지우는 것과 버튼을 치우는 것을 구분해라.
    계획삭제만 진짜로 없앤다. §23.11 삭제 보호와 같은 방향이다.

(5) 일일 업무 등록 — 관리자가 아니면 본인 업무만 (§34.5)
    관리자·접수담당  팀 전체
    기술담당         본인만 (상단 토글로 팀 전체 조회 가능 — 막지 마라)

    §33.9.3 · §10.1 과 같은 규칙을 쓴다. 화면마다 다르면 혼란스럽다.
    역할 판정은 §24.1 절차를 따른다. 못 읽으면 빈 화면 대신 배너.

검증 (§34.6 의 11·12·13번)

제약: (위 4줄 동일)
```

---

# 21단계 — 일일 업무 등록 칸반 [v2.20]

기획서 **§7.8** 이 근거입니다. 프롬프트 하나입니다.

### 프롬프트 21-A

```
@기획_업무일지_프로그램_요구사항_v2.md

§7.8 을 구현해줘.

계획 > 일일 업무 등록에 칸반 보기를 추가한다.
기존 일정표(시간표) 보기는 그대로 두고 상단에 [일정표 | 칸반] 전환 버튼.
선택은 ?display=timetable|kanban 으로 유지.
날짜·담당자 필터는 두 보기가 공유한다.

(1) [핵심] 열 정의를 새로 만들지 마라.
    요청한 5개 열 중 4개는 이미 서버에 있다.
    internal/model/work_board.go:88 의 버킷이고
    repository 의 ListBucket() 이 이미 조회한다.
    대시보드 KPI 카드가 쓰는 바로 그 산식이다.

      오늘 예정   WorkBucketToday           있음
      지연        WorkBucketDelayed         있음
      오늘 완료   WorkBucketCompletedToday  있음
      미계획업무  §8 ListUnplanned          있음 (아래 단서 참고)
      진행중      —                          신설 WorkBucketInProgress

    ListBucket() 을 그대로 재사용해라.
    산식을 새로 짜면 대시보드 KPI 숫자와 칸반 열 건수가 어긋난다.
    같은 사람이 "대시보드는 12건인데 칸반은 9건" 을 발견하면
    두 숫자 다 못 믿게 된다.

    열 순서(일이 흘러가는 방향):
      미계획업무 │ 오늘 예정 │ 진행중 │ 지연 │ 오늘 완료

(2) [필수] 한 카드는 한 열에만 넣는다.
    5개 열은 서로 겹친다.
    예정일이 지났는데 착수한 건은 "지연" 이자 "진행중" 이다.
    그대로 두면 같은 카드가 두 열에 뜨고 합계가 실제보다 커진다.

    위에서부터 먼저 맞는 곳 하나에만 넣어라:
      1 오늘 완료   완료 + 완료일 = 선택일
      2 미계획업무  예정일 없음 또는 담당자 미배정
      3 진행중      status='in_progress'
      4 지연        예정일 < 선택일 · 미완료
      5 오늘 예정   예정일 = 선택일 · 미완료
      - 예정일 > 선택일 → 표시하지 않음

    지연이면서 진행중인 건은 "진행중" 에 두고 D+n 뱃지를 붙인다.
    §8.2.2 의 "진행중 D+n(노랑)" 과 같은 규칙이다.
    그래야 "지연" 열에 진짜 방치된 건만 남아서
    아침에 그 열만 봐도 먼저 손댈 것이 나온다.

    미계획업무 열에는 §8.2.1 의 "계획을 세워야 할 건" 만 넣어라.
    "일정이 지난 건" 까지 넣으면 지연 열과 통째로 겹친다.

    각 열 머리글에 건수를 적는다.
    다섯 열 합계가 그 날 전체 업무 수와 같아야 한다.
    다르면 배타 배정이 깨진 것이다.

(3) "오늘" 은 화면에서 고른 날짜다 (§7.8.3)
    선택일이 오늘이 아니면 열 이름을 "8/20 예정" · "8/20 완료" 로 바꿔라.
    "오늘" 이라고 써 놓고 다른 날을 보여주면 안 된다.
    "지연" 도 선택일 기준으로 판정한다.

    칸반은 일일 보기에서만 제공한다.
    주간·월간에는 칸반 버튼을 만들지 마라.
    7일치를 한 칸반에 담으면 열마다 수십 건이 되어 못 쓴다.

(4) 카드 (§7.8.4)
    칸반에는 담당자 축이 없다(§7.6 은 일정표 전용).
    담당자를 카드에서 드러낸다.
      제목   [AS]·[정기점검]·[일반업무] + 업무명
      부제   기관 · 예정일
      담당자 카드 하단 이름 + 좌측 색 마감(점검대상 색)
      뱃지   D+n(지연) · 🔴 긴급 · 🔗 묶음 접수
      버튼   조치 · 수정 (일정표와 동일)
    담당자 필터는 그대로 동작해야 한다.

(5) 드래그 동작 (§7.8.5) — 무엇이 바뀌는지 명확히
      미계획 → 오늘 예정   예정일을 선택일로 배정
      미계획 → 진행중      예정일 배정 + 상태 in_progress
      지연 → 오늘 예정     예정일을 선택일로 옮김(원래 예정일은 이력에)
      오늘 예정 → 진행중   상태만 변경
      진행중 → 오늘 완료   완료 처리 + §13.9 완료 검증 요구
      오늘 완료 → 다른 열  확인 받기 "완료를 취소하고 되돌릴까요?"
      어디서든 → 미계획    막는다. 예정일을 지우는 동작이라
                          실수로 일어나면 안 된다

    담당자 미배정 건을 "오늘 예정" 으로 옮기면 담당자를 묻는다.
    담당자 없이 예정일만 잡으면 §8 미계획으로 다시 돌아온다.
    모든 이동은 data_change_logs 에 남긴다.

(6) [중요] §33.9 "오늘 내 업무" 칸반과 열 구성이 다르다 (§7.8.6)
      일일 업무 등록 = 계획·배치 → 5열 (미계획·지연 포함)
      오늘 내 업무   = 실행      → 3열
    일부러 다르게 둔다.

    단 판정 함수는 하나를 공유해라.
    "진행중" 이 두 화면에서 다르게 판정되면 안 된다.
    §33.9.1 의 매핑 함수를 확장해서
    5열용·3열용 두 묶음을 같은 함수에서 내라.
    열 구성만 다르고 판정 기준은 같다.

검증 (§7.8.7):
  - 다섯 열 건수 합계 = 그 날 전체 업무 수 (한 카드가 두 열에 없다)
  - 각 열 건수가 대시보드 KPI 카드 숫자와 일치한다
  - 예정일 지났고 착수한 건은 "진행중" 에 D+n 뱃지로 있다 (지연 열에 없다)
  - 날짜를 어제로 옮기면 열 이름이 "8/18 예정" 으로 바뀐다
  - 주간·월간 보기에 칸반 버튼이 없다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

---

# 22단계 — [긴급] 정기점검 자동배정 월 단위 [v2.22]

**고른 달이 아니라 다른 달이 재배정되는 버그입니다.** 기획서 §34.4.5 가 근거이고, 원인까지 코드에서 확인했습니다.

### 프롬프트 22-A

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.4.5 를 고쳐줘. 원인을 코드에서 확인했다.

증상: 9월 탭에서 "자동 배정" 을 누르면 확인 화면이 이렇게 나온다.
  "2026년 계획 · 2026년
   기존 자동 생성 방문 200건이 삭제됩니다.
   당월·이전(또는 완료) 489건은 기간 보호로 그대로 둡니다.
   다음 달 이후 자동 생성분만 지운 뒤 …"
  고른 9월은 손도 안 대고 10~12월이 지워지고 재생성된다. 정반대다.

원인 (internal/service/maintenance_generate.go:52):
  func AutoGenerateMaintenanceWithOrder(repo, planID, order string) error {
      ...
      if _, _, err := repo.DeleteAutoVisits(planID); err != nil {  // 계획 전체를 지운다
      ...
      for m := 1; m <= 12; m++ {                                   // 12개월 전부 재생성

  인자가 planID 와 order 뿐이다. 화면에서 고른 월이 전달될 자리가 없다.
  확인 화면(plan_generate.html)에도 월이 없고
  폼이 보내는 값은 confirm 과 assign_order 둘뿐이다.

(1) 월 인자를 받게 한다
    AutoGenerateMaintenanceWithOrder(repo, planID, order, year, month int)
    DeleteAutoVisits(planID) → DeleteAutoVisitsInMonth(planID, year, month)
    for m := 1; m <= 12 → 고른 달 한 번만

    [중요] 기존 함수를 지우지 마라.
    month=0 이면 12개월을 도는 지금 동작을 유지한다.
    연 단위 일괄 생성이 필요할 수 있다.
    다만 화면에서는 항상 월을 넘긴다.

(2) 확인 화면에 월을 넣는다
    plan_generate.html 에 <input type="hidden" name="month"> 추가
    제목을 "2026년 9월 자동 배정" 으로 (지금은 "2026년 계획 · 2026년")
    계획 화면의 "자동 배정" 링크에 현재 월(?month=)을 붙인다

    달을 제목에 반드시 적어라.
    "2026년 계획" 만 보고는 무엇이 바뀌는지 알 수 없다.

(3) [핵심] 보호 기준을 "당월 1일" 에서 "오늘" 로 바꾼다
    지금은 §23.11 기간 보호가 당월 전체를 잠근다.
    그래서 9월을 골라도 9월은 아무것도 안 바뀐다.
    9월 자동 배정 자체가 불가능하다.

      지난 달                            보호 유지 (이미 지났다)
      당월 · 오늘 이전                    보호 유지
      당월 · 오늘 이후 · 미완료 · 자동생성  재배정 대상  ← 여기만 바뀐다
      완료된 방문 completed=1             보호 유지 (어떤 경우에도)
      수동 방문 auto_generated=0          보호 유지

    지난 달을 고르면 실행을 거부한다:
      "지난 달은 배정할 수 없습니다. 이미 지난 일정입니다."

(4) 이미 방문한 곳은 배정에서 뺀다
    HasVisitInMonth() 가 이미 "그 달에 그 고객·제품 방문이 있으면 건너뛴다"
    를 하고 있다. 문제는 그 앞에서 DeleteAutoVisits 가 지워 버리는 것이다.
    삭제를 월 단위 · 오늘 이후로 좁히면 이 검사가 제대로 동작한다.

    완료된 방문이 있는 슬롯은 어떤 순서 옵션에서도 다시 만들지 마라.

(5) 확인 화면 문구를 건수별로 나눠 적는다
      "2026년 9월 자동 배정
       9월 자동 생성 방문 12건이 삭제되고 다시 채워집니다.
       방문 완료 8건 · 오늘(9/1) 이전 3건은 그대로 둡니다.
       수동 방문은 건드리지 않습니다."

(6) 방문 완료 뱃지
    완료된 방문(completed=1)에 뱃지를 붙인다. 캘린더·칸반·목록 전부.
      완료           "방문완료" 초록 + 완료일(completed_date)
      예정           없음
      예정일 경과·미완료  D+n 빨강

    자동 배정 확인 화면의 미리보기에도 표시해라.
    "이건 이미 다녀왔구나" 가 눌러 보기 전에 보여야 한다.
    완료 뱃지가 붙은 방문은 드래그·삭제를 막는다.

검증 (§34.4.5):
  - 9월 탭에서 누르면 확인 화면 제목이 "2026년 9월" 이다
  - 실행 후 10~12월 일정이 변하지 않는다
  - 8월(지난 달)을 고르면 거부되고 사유가 뜬다
  - 9월의 완료된 방문·오늘 이전 방문·수동 방문이 그대로 남는다
  - 완료된 방문에 "방문완료" 뱃지 + 완료일이 보인다

  실행 전후로 월별 방문 건수를 표로 뽑아서 비교해줘.
  고른 달 외에 숫자가 바뀐 달이 있으면 (1)이 덜 된 것이다.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

---

# 23단계 — 업무처리현황 캘린더 [v2.23]

**「한 일이 안 보인다」의 원인을 코드에서 찾았습니다.** 기획서 §14 전체가 근거입니다.

### 프롬프트 23-A — [먼저] 날짜 기준 바로잡기

```
@기획_업무일지_프로그램_요구사항_v2.md

§14.1 을 고쳐줘. 화면을 바꾸기 전에 데이터부터 맞춘다.

"한 일이 있는데 안 보인다" 의 원인이 셋이다. 코드에서 확인했다.

(1) 행정·지원업무가 완료일이 아니라 배정일 기준이다
    internal/repository/wb_completed.go:13 주석에 그대로 적혀 있다:
      // 행정/지원: status=complete + 배정일(없으면 예정일)

    8월에 배정해서 9월에 끝낸 업무가 9월 화면에 안 나온다. 8월에 있다.

    [중요] §10 일일 업무 회의는 같은 건을 date(complete_date) 기준으로 센다.
    v2.1 에서 updated_at 을 대체하며 고친 것이다.
    두 화면이 같은 완료 건에 서로 다른 날짜를 쓰고 있다.

    adminTaskCompleteDateSQL 을 complete_date 기준으로 바꾼다.
    업무처리현황은 "처리한 날" 을 보는 화면이다.

    바꾸기 전에 먼저 보고해줘:
    adminTaskCompleteDateSQL 이 지금 무슨 식이고 어디어디서 쓰는지,
    바꾸면 어느 화면 숫자가 달라지는지.

(2) AS 가 updated_at 으로 흘러내린다
      date(COALESCE(ar.complete_datetime, ar.updated_at))

    complete_datetime 이 비면 마지막 수정일로 대신한다.
    나중에 그 건의 아무 필드나 고치면 완료 실적이 그 날짜로 옮겨간다.
    어제 본 숫자가 오늘 달라진다.

    폴백을 없애라.
    complete_datetime 이 없는 완료 건은 집계에서 빼고
    "완료일 미기록 n건" 으로 따로 세어 화면에 표시한다 (§14.4).
    조용히 다른 날에 끼워 넣지 마라.

    정기점검도 같은 폴백이 있는지 확인해줘 (completed_date → visit_date).

(3) 먼저 실측을 보여줘
    - complete_datetime 이 비어 있는 완료 AS 건수
    - complete_date 가 비어 있는 완료 행정·지원 건수
    - (1)을 적용하기 전후로 최근 3개월 월별 조치 건수 비교표

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 23-B — 예정업무 탭 제거 + 캘린더 전환

```
@기획_업무일지_프로그램_요구사항_v2.md

§14.2 · §14.3 · §14.4 · §14.5 를 구현해줘. 23-A 가 끝난 뒤에.

(1) "예정업무"(kind=planned) 탭을 없앤다 (§14.2)
    work_status.go:85 의 ListPlannedSourcesBetween() 이
    "아직 일일업무로 만들지 않은 예정 건" 을 합치는데
    여기에 예정일이 확정되지 않은 건이 섞여 들어온다.
    "예정에 예정업무가 없는 항목이 보인다" 가 이것이다.

    예정 관리는 §8 미계획 업무함과 §7 일일 업무 등록이 이미 한다.
    세 화면이 같은 일을 하면 셋 다 조금씩 다르게 틀린다.

    kind 는 action(기본) · receipt 둘만 남긴다.
    kind=planned 로 들어오면 action 으로 되돌린다.
    즐겨찾기·링크가 깨지지 않게.

(2) 시간표를 버리고 월 캘린더로 바꾼다 (§14.3)
    [이유] 업무처리현황은 지난 일을 훑는 화면인데
    시간표는 하루를 배치하는 도구다.
    실제로 완료 건 대부분은 시각이 없어서
    ensureTimelineDisplayTimes() 로 표시용 시각을 지어내 채우고 있다.
    있지도 않은 시각을 만들어 그리는 중이다.

    월간(기본) 일~토 캘린더 · 주간 7열 · 일간 목록
    날짜 칸: "조치 5 · 접수 2" + 카드 3건 + "외 n건"
    카드 색은 점검대상 색(초록 앤로보틱스 / 파랑 K-LAS /
                          보라 세종 K-LAS / 주황 행정·기타)
    담당자는 카드에 이름으로 적는다. 캘린더에는 담당자 축이 없다
    날짜 클릭 → 그 날 일간 보기
    휴일 색은 §23.13 을 따른다

    [중요] 카드에 조치·수정 버튼을 두지 마라. 이 화면은 보는 곳이다.
    클릭하면 원본 상세로 간다.

    [재사용] 정기점검의 buildVisitCalendar() 와 RegisterMonthDay 가
    이미 월 캘린더를 그린다. 새로 짜지 마라.
    buildRegisterDayColumns() 와 ensureTimelineDisplayTimes() 는
    이 화면에서 더 이상 쓰지 않는다.
    다만 일일 업무 등록(§7)이 계속 쓰므로 함수를 지우지는 마라.

(3) 안 보이는 건을 드러낸다 (§14.4)
    상단 배너:
      "완료일 미기록 n건"   완료 상태인데 완료일이 없는 건. 클릭 시 목록
      "담당자 미배정 n건"   완료됐는데 담당자가 없는 건
      "{연도} 휴무일 미등록"

    "완료일 미기록" 이 이 화면의 신뢰도를 좌우한다.
    지금 그 건들이 updated_at 으로 아무 날짜에나 흩어져 있다.
    드러내면 고칠 수 있다.

(4) 범위는 §33.9.3 과 같은 규칙 (§14.5)
    관리자·접수담당 팀 전체 / 기술담당 본인 (토글로 전체)

검증 (§14.6):
  - 8월 배정 · 9월 완료한 행정업무가 9월 캘린더에 보인다
  - 같은 건이 §10 일일 업무 회의 전일 실적과 같은 날짜에 잡힌다
  - 완료 건 필드를 고쳐도 캘린더 위치가 안 바뀐다
  - kind=planned 로 들어오면 조치로 되돌아간다
  - 조치 건수 합계가 §15 통계의 "처리" 숫자와 같다

제약: (위 4줄 동일)
```

---

# 24단계 — 사진 저장 · 리드타임 · 기간 선택 [v2.24]

세 건입니다. **24-A(사진)를 먼저** — 지금 사용자가 일을 못 하고 있습니다.

### 프롬프트 24-A — [긴급] 조치 사진이 저장되지 않는다

```
@기획_업무일지_프로그램_요구사항_v2.md

§12.9.8 의 "[2026-09-01] 사진 저장이 안 되는 건 — 확인 순서" 를 따라 고쳐줘.

첨부 영역을 사진·문서 하나로 합친 뒤(§34.3.6) 사진 저장이 안 된다.

코드에서 확인한 것: attachment.go:29 Upload() 흐름과
_photo_gallery.html:82 의 폼은 원래 맞게 되어 있었다.
  <form method="POST" action="/attachments" enctype="multipart/form-data">
    <input type="hidden" name="ref_type" value="{{.PhotoRefType}}">
    <input type="file" name="file" multiple accept="image/*,…">

위에서부터 확인해줘. 짐작으로 고치지 말고 하나씩 확인한 결과를 알려줘.

  1. "종류" 드롭다운이 보내는 값이 정확히 as_action_photo 인가
     photo·doc 같은 값으로 바꿨다면
     canUploadAttachment()(attachment.go:354)가 default 가지로 떨어져
     기술담당이 403 을 받는다

  2. <form> 에 enctype="multipart/form-data" 가 남아 있는가
     통합하며 폼을 다시 짜면 가장 흔히 빠진다. 없으면 파일이 안 실린다

  3. 파일 input 의 name="file" 이 유지됐는가
     uploadFileHeaders(c) 가 이 이름을 찾는다

  4. [가장 유력] 폼이 중첩되지 않았는가
     "조치 사진을 조치 사항 박스 안으로 올린다"(§34.3.6 13번)를
     그대로 구현하면 조치 저장 폼 안에 업로드 폼이 들어간다.
     HTML 은 <form> 중첩을 허용하지 않아 브라우저가 안쪽 폼을 버린다.
     버튼을 눌러도 아무 일이 안 일어난다.

     해결: 업로드 폼을 조치 저장 폼 바깥에 두고 위치만 시각적으로 붙이거나
           <button form="photo-form"> 으로 폼을 명시적으로 연결한다

  5. guardActionPhotoWrite() 에 막히는가 (완료 잠금 §12.6)
  6. MaxActionPhotos 초과인가

[함께 고칠 것 — 실패가 조용하다]
  redirectAttachErr() 는 오류를 ?err= 로 붙여 되돌려 보내는데
  갤러리 템플릿이 그 값을 표시하지 않는다.
  그래서 실패해도 화면에 아무 말이 없다.

  첨부 영역 상단에 오류 배너를 넣어라.
  "사진은 최대 3장입니다" · "권한이 없습니다" · "파일이 필요합니다" 가
  그 자리에서 보여야 한다.
  원인을 모른 채 "저장이 안 된다" 고 말하게 두지 마라.

검증:
  - 기술담당 계정으로 사진 3장을 올리면 저장되고 썸네일이 보인다
  - 4장째에 "최대 3장입니다" 배너가 뜬다
  - 네트워크 탭에서 /attachments 요청의 ref_type 이 as_action_photo 다
  - 조치 저장 폼과 업로드 폼이 중첩되지 않는다 (HTML 검사)

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 24-B — 리드타임 두 지표가 모순된다

```
@기획_업무일지_프로그램_요구사항_v2.md

§4.6 을 고쳐줘.

증상: "접수 → 조치 완료 1.2일" 인데 "접수 → 방문 3일" 이다.
완료는 방문 뒤에 오므로 이럴 수 없다.

원인 (internal/repository/stats_dashboard.go:421 avgASDays):

  방문:  visitExpr = COALESCE(date(start_datetime), visit_scheduled_date)
         WHERE visitExpr 있음
           AND visitExpr BETWEEN from..to      ← 방문일 기준 기간
           AND status NOT IN ('cancelled')      ← 미완료 건도 포함

  완료:  WHERE status IN (완료 계열)             ← 완료 건만
           AND date(COALESCE(complete_datetime, updated_at)) BETWEEN from..to
                                                ← 완료일 기준 기간

  두 평균이 서로 다른 건들을, 다른 기간축으로 재고 있다.
  같은 건이 한쪽에만 들어갈 수 있어서 대소가 뒤집힌다.
  비교 자체가 성립하지 않는 상태다.

  게다가 방문 쪽은 start_datetime 이 없으면 visit_scheduled_date(예정일)를 쓴다.
  예정일은 "가기로 한 날" 이지 "간 날" 이 아니다. 미래일 수도 있다.
  실측이 아니라 계획을 재고 있다.

(1) 두 지표를 하나의 모집단·하나의 기간축으로 통일한다.
    대상   status IN (완료 계열)
           AND complete_datetime 있음
           AND start_datetime 있음
    기간축 complete_datetime  ← 둘 다 같은 축

    접수→방문  AVG(date(start_datetime)    - date(receipt_datetime))
    접수→완료  AVG(date(complete_datetime) - date(receipt_datetime))

(2) 폴백을 전부 없앤다.
    visit_scheduled_date 도, updated_at 도 쓰지 마라.
    표본이 줄어든다. 그게 맞다.
    지어낸 값으로 채운 평균보다 작아도 참인 평균이 낫다.
    §4.4 신뢰도 등급을 그대로 적용한다. 5건 미만이면 ⚪ 측정불가.

(3) [필수] 불변식을 검사한다
    접수→방문 ≤ 접수→완료 는 항상 참이어야 한다.
    어기면 화면에 경고를 띄운다:
      "지표 계산 오류: 방문(3.0일)이 완료(1.2일)보다 큽니다"

    건별로도 검사한다. start_datetime > complete_datetime 인 건은
    데이터 오류이므로 부록 C 정합성 점검에 항목을 추가한다.

    두 지표의 표본 수(n)를 나란히 표시한다.
    n 이 다르면 비교할 수 없다는 뜻이므로 같아야 정상이다.

먼저 보고해줘:
  수정 전후로 두 지표의 값과 표본 수를 표로.
  그리고 start_datetime > complete_datetime 인 건이 몇 건인지.

제약: (위 4줄 동일)
```

### 프롬프트 24-C — 통계 기간 선택

```
@기획_업무일지_프로그램_요구사항_v2.md

§15.7 을 구현해줘.

(1) 기본은 "오늘 기준 1개월"
    기간 [숫자] [단위 ▼]
      일  오늘 기준 N일 전 ~ 오늘     ?range=2d
      주  오늘 기준 N주 전 ~ 오늘     ?range=2w
      월  오늘 기준 N개월 전 ~ 오늘   ?range=1m  ← 기본
      사용자 지정 → ?from=&to=

    끝은 항상 오늘이다(사용자 지정 제외).
    숫자는 1 이상 정수. 상한 일 365 · 주 104 · 월 60.

    [중요] 상단에 환산된 실제 날짜를 적어라.
    "최근 1개월 (2026-08-02 ~ 2026-09-01)"
    숫자만 보고는 어디부터인지 모른다.

(2) 기존 view 와는 다른 축이다. 함께 쓴다.
      range / from·to  얼마나 긴 기간을 볼 것인가
      view=day|week|month  그 기간을 어떤 단위로 쪼개 그릴 것인가
    "최근 3개월을 주 단위로" 가 가능해야 한다.
    range 가 짧은데 view 가 크면 view 를 자동으로 좁히고 그 사실을 표시한다.

(3) [필수] §4.5 기준일(2026-08-03)보다 이른 기간을 고르면 기준일에서 자른다.
    "최근 6개월" 을 골라도 실제 집계는 2026-08-03 부터다.
    화면에 "기준일(2026-08-03) 이후만 집계됩니다" 를 함께 적어라.
    자른 사실을 숨기면 "6개월인데 왜 이것밖에 없나" 가 된다.

(4) 통계 · 대시보드 KPI · §16 보고서 기간 선택에 같은 구성 요소를 쓴다.
    화면마다 기간 고르는 법이 다르면 안 된다.
    단 주간업무보고서(§16.1)는 주 단위 고정이라 예외다.

검증 (§15.7.5)

제약: (위 4줄 동일)
```


### 프롬프트 24-D — 원인분류 시드 갱신 + `cause_type` 중복 제거

```
@기획_업무일지_프로그램_요구사항_v2.md

§34.3.3 이 두 가지 이유로 다시 손봐야 한다.

[1] 엑셀이 개정됐다 (2026-09-02 판)
    AS_원인분류_시드.sql 을 새로 만들어 뒀다. 프로젝트 루트에 있다.
    v1(54행) → v2(55행). 그대로 실행해라.

    바뀐 것:
      서버 2차가 4종 → 5종. "패치" 가 추가됐다
      라벨 정정: HW고장→HW 장애 · SW오류→SW 장애 · 네트워크→네트워크 장애
      홈페이지 띄어쓰기: 게시물수정→게시물 수정 등

    코드는 그대로 두고 라벨만 바뀐다.
    server.hw · web.post_edit 등 기존 코드로 저장된 데이터가 안 깨진다.
    신규는 server.patch 하나뿐이다.

    시드 끝에 "사라진 코드 비활성화" UPDATE 가 들어 있다.
    데이터는 지우지 않고 is_active=0 으로만 내린다.

    "패치" 는 is_fault=0 이다. 계획된 작업이지 고장이 아니다.
    §17 장애 빈도에서 뺀다. cause_type_map 은 sw 로 뒀다.

[2] cause_type 중복 — §34.3.3 의 "판단 수정" 이 아직 반영 안 됐다
    지금 화면(action.html):
      244행  <select name="cause_type" required>  ← 기존 5종 그대로
      251행  시스템 분류 (cause_cat1)
      267행  cause_cat2
      274행  cause_cat3

    담당자가 네 개를 골라야 한다.
    원인분류(5종) + 시스템 분류 + 2차 + 3차.

    RFID 2차(HW·SW·서버/네트워크·환경문제·사용자/도서)와
    서버 2차(HW 장애·SW 장애·네트워크 장애)가
    기존 cause_type 5종과 사실상 같은 것이다.
    같은 것을 두 번 고르게 하지 마라.

    (1) cause_type select 를 화면에서 지운다
    (2) 서버가 2차 코드의 cause_type_map 값으로 cause_type 을 자동으로 채운다
        매핑이 빈 항목이면 cause_type 도 빈값으로 둔다
    (3) cause_type 컬럼은 그대로 둔다. 기존 통계 쿼리가 계속 동작해야 한다
    (4) 화면 순서를 1차 → 2차 → 3차 가 먼저 오게 바꾼다
        지금은 "원인분류" 가 위, "시스템 분류" 가 아래라
        무엇이 주된 칸인지 알 수 없다
    (5) 필수: 2차까지 필수, 3차는 있으면 필수
        (홈페이지·서버는 3차가 없으므로 2차에서 끝난다)

검증:
  - 시드 실행 후 as_cause_categories 가 55행이고
    parent_code='server' 인 2차가 5건이다
  - 조치 화면에 드롭다운이 3개다 (원인분류 5종 select 가 없다)
  - 1차에서 "서버" 를 고르면 2차에 "패치" 가 보이고
    3차 드롭다운은 숨겨진다
  - 저장 후 as_receipts.cause_type 이 매핑값으로 자동으로 채워진다
  - 기존 통계의 원인분류별 집계 숫자가 그대로다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

---

# 25단계 — 목록·칸반 공통 규칙 [v2.26]

**「지금부터 모든 리스트에 칸반」** 요구를 화면마다 따로 만들지 않고 **공통 규칙 + 공통 컴포넌트**로 처리합니다. 기획서 §35 가 근거입니다.

### 프롬프트 25-A — 공통 칸반 컴포넌트

```
@기획_업무일지_프로그램_요구사항_v2.md

§35.1 · §35.3 을 구현해줘.

[배경] 칸반을 요청받을 때마다 그 화면에서 따로 만들다 보니
열 구성·판정 기준·드래그 동작이 제각각이 됐다:
  일일 업무 등록  5열 (§7.8)
  오늘 내 업무    3열 (§33.9)
  행정·지원업무   3열 (§33.5.2)
  AS 목록         4열 (§12.8)
  영업 사업       8열 (§32)

열이 다른 건 괜찮다. 화면마다 목적이 다르다.
판정이 다른 건 안 된다.
같은 업무가 이 화면에선 "진행중", 저 화면에선 "완료" 로 보이면
아무 숫자도 못 믿는다.

(1) 공통 칸반 컴포넌트를 하나 만든다.
    화면은 열 정의만 넘긴다.
      KanbanView(columns []KanbanColumn, items []Card, opts)
    상태 판정 함수는 공유한다 — §33.9.1 의 매핑 함수를 확장해서 쓴다.
    카드 서식·드래그·건수 표시·빈 열 처리는 컴포넌트가 한다.

(2) 공통 동작 (§35.3)
    전환    상단 [리스트 | 칸반] 버튼. ?display=list|kanban 으로 유지
    기본값  리스트 (일일 업무 등록만 일정표가 기본)
    필터    날짜·담당자·사업·기간을 두 보기가 같이 쓴다. 전환해도 안 풀린다

    [필수] 배타 배정 — 한 카드는 한 열에만.
    겹치는 열은 §7.8.2 처럼 우선순위를 정해
    위에서 먼저 맞는 곳 하나에만 넣는다.

    [필수] 열 머리글에 건수. 모든 열 합이 리스트 건수와 같아야 한다.
    다르면 배타 배정이 깨진 것이다.

    빈 열   없애지 마라. "0건" 으로 남긴다. 열이 사라지면 위치를 못 외운다
    카드 색 점검대상 색 (초록 앤로보틱스 / 파랑 K-LAS /
                        보라 세종 K-LAS / 주황 행정·기타)
    드래그  데이터가 바뀌는 이동만 허용하고 무엇이 바뀌는지 화면별로 명시.
            되돌리기 어려운 이동(완료 취소 등)은 확인을 받는다
    읽기전용 화면은 드래그를 막는다 (업무처리현황 §14 등)
    정렬    열 안에서 날짜 오름차순, 같으면 업무번호
    모바일  열 가로 스크롤, 최소 폭 260px

(3) 이미 만든 칸반들을 이 컴포넌트로 옮긴다.
    한 번에 다 하지 말고 하나씩, 옮길 때마다 건수가
    옮기기 전과 같은지 확인해줘.

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 25-B — 일일 업무 회의 칸반

```
@기획_업무일지_프로그램_요구사항_v2.md

§35.4 를 구현해줘. 25-A 의 공통 컴포넌트를 쓴다.

/meeting?display=kanban

회의 화면의 축은 "어제 무엇을 했고 오늘 무엇을 하나" 다. 그대로 열로 만든다.

  전일 완료 │ 오늘 예정 │ 진행중 │ 미계획

    전일 완료  §10 의 "전일 실적" 산식 그대로
    오늘 예정  §10 의 "오늘 예정" 산식 그대로
    진행중     착수했고 아직 안 끝난 것 (어제부터 이어지는 일)
    미계획     §8.2.1 의 "계획을 세워야 할 건"

[중요] 산식을 새로 만들지 마라.
§10 이 이미 전일 실적·오늘 예정을 정의했다.
그 결과를 열에 나눠 담을 뿐이다.

배타 배정 순서: 전일 완료 → 진행중 → 오늘 예정 → 미계획

[중요] 드래그를 막는다.
회의는 보고 이야기하는 자리다.
고칠 일이 있으면 카드를 눌러 원본으로 간다.

§10.1 의 담당자 드롭다운·정렬이 칸반에도 걸린다.
한 명을 고르면 네 열이 그 사람 것만 보여준다.
담당자별 소제목은 칸반에서 카드의 담당자 이름으로 대신한다.

검증:
  - 네 열 합계가 리스트 건수와 같다
  - 리스트 ↔ 칸반 전환해도 날짜·담당자 필터가 유지된다
  - 같은 업무가 두 열에 안 나타난다
  - 드래그가 안 된다

제약: (위 4줄 동일)
```

### 프롬프트 25-C — 나머지 목록에 칸반 추가

```
@기획_업무일지_프로그램_요구사항_v2.md

§35.2 를 구현해줘. 25-A 이후.

[핵심 판단] 칸반은 "상태가 있는 목록" 에만 의미가 있다.
상태 축이 없는 목록에 칸반을 붙이면
열이 하나뿐이거나 없는 분류를 지어내게 된다.

칸반을 새로 붙일 곳:
  /plan/unplanned  미계획 업무함 — 유형 5종 (§8.1)
  /assets          설치자산 — 운영상태 5종
                   (운영중·점검중·장애·철수·폐기)

칸반을 붙이지 않을 곳 (상태 축이 없다):
  /customers /spaces /contacts /contact-history /relations
  /codes /users /maintenance/sites /admin/holidays

  대신 그룹 보기를 준다.
  고객현황은 상위기관별, 담당자관리는 소속별로 접고 펼 수 있게.
  없는 상태를 지어내 칸반을 만드는 것보다 낫다.

[앞으로의 기준 — §35.5]
새 목록 화면을 만들 때:
  1. 상태 축이 있는가? 없으면 칸반을 만들지 않는다
  2. 있으면 열을 정의하고 §35.3 공통 동작을 따른다
  3. 판정은 공유 함수를 쓴다. 그 화면에서 새로 판정하지 않는다
  4. 열이 7개를 넘으면 칸반이 아니라 필터가 맞는지 다시 본다

이 기준을 AGENTS.md 에도 한 줄로 적어줘.
앞으로 목록을 만들 때마다 이 판단을 반복하게 된다.

검증:
  - /customers 에 칸반 버튼이 없다
  - /plan/unplanned 칸반의 다섯 열 합계가 리스트 건수와 같다
  - 같은 업무의 상태가 여러 칸반에서 서로 모순되지 않는다

제약: (위 4줄 동일)
```

---

# 진행 관리

---

# 26단계 — 영업 화면 확정 (피그마 반영) [v2.27]

피그마로 그린 영업 화면 5장이 기준이 됐습니다. 기획서 **§32.3 · §32.4 · §32.7.1 · §32.8 · §32.11.1** 이 근거입니다.

**16단계를 아직 안 돌렸다면 26단계는 필요 없습니다.** 16-A·16-B·16-D 를 v2.27 기준으로 이미 고쳐 두었으니 그대로 돌리면 됩니다.
**16단계를 이미 돌렸다면** 아래 26-A 부터가 「고치는 작업」입니다.

가장 큰 변화는 **단계가 8개에서 6개로 줄어든 것**입니다. `sales_projects.stage` 코드값이 바뀌므로 **26-A 를 반드시 먼저** 돌리세요.

### 프롬프트 26-A — [먼저] 영업 단계 8 → 6

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.3 을 구현해줘. 영업 단계를 8개에서 6개로 줄인다.

[배경] v2.15 는 8단계였다. 화면 설계가 6단계로 확정됐다.
기획서 §32.3.1 표가 유일한 기준이다. 옛 8단계 표는 폐기다.

(1) 단계·확도를 6개로 바꾼다 (codes 테이블)
      contact     담당자 접촉   10
      lead        정보 입수     25
      proposal    제안 진행     40
      negotiation 협상          70
      won         수주         100
      lost        실주           0

    순서를 이대로 둬라. "담당자 접촉" 이 "정보 입수" 앞이다.
    상식과 어긋나 보여도 확도가 10 → 25 로 오르는 것과 방향이 맞다.
    피그마 화면 그대로다.

(2) 컬럼 3개 추가 + 마이그레이션 (§32.3.5)
      legacy_stage    옛 코드 보존 (필수)
      won_at          stage 가 won 이 된 날
      contracted_at   계약서 날인일 (손으로, 선택)

    옮기기:
      quote / rfp / submit  → proposal
      contracted            → won   (contracted_at 에 그때 날짜를 넣는다)
      won                   → won
      lead / contact / proposal / lost → 그대로 (lost 라벨만 "실주")
      lead 확도 10 → 25, proposal 확도 15 → 40 으로 갱신

    [필수] 옛 값을 지우지 마라. legacy_stage 에 원래 코드를 남긴다.
    되돌릴 수 있어야 한다. 롤백 스크립트도 같이 만들어라.

    [필수] sales_stage_history 의 옛 코드는 그대로 둔다.
    이력은 그때 그 이름이 맞다. 화면에 보일 때만 옛 이름으로 보여준다.

(3) quote/rfp/submit 에서 옮겨온 건은 활동 로그를 자동 생성한다
    각각 견적제출 / 제안서제출(RFP) / 제안서제출 유형으로,
    activity_date = 그 단계로 넘어간 날 (sales_stage_history 에서 찾는다),
    title = "[이관] 견적 요청" 처럼 이관임을 밝힌다.

    이걸 안 하면 "제안서까지 냈던 건" 과
    "이제 막 제안 시작한 건" 이 구분되지 않는다.
    (sales_activities 가 아직 없으면 이 항목은 16-B 뒤로 미룬다)

(4) 진척 체크 (§32.3.2)
    stage='proposal' 인 사업 상세 상단에만 표시한다:
      견적 ✔ · RFP ✔ · 제안서 ○
    판정은 그 사업에 해당 유형 활동이 있는지로 자동으로 낸다.
    입력받지 마라. 다른 단계에서는 이 줄을 숨긴다.

(5) 확도 표시 (§32.3.3)
    1~4단계: "제안 진행 · 40%" 처럼 확도를 앞세운다
    수주:     단계명만. 확도 표시 안 함
    실주:     "실주" + 실패 사유

(6) 승격 방아쇠를 옮긴다 (§32.10)
    contracted → won 이 되면 "사업관리로 등록" 버튼이 열린다.
    contracted_at 이 비어 있어도 승격할 수 있다.
    수주 후 contracted_at 이 30일 넘게 비면
    미계획 업무함에 "계약 미체결"(🟠) 로 올린다.

검증:
  - 단계 pill 이 6개고 순서가 담당자 접촉·정보 입수·제안 진행·협상·수주·실주
  - 단계를 바꾸면 확도가 10·25·40·70·100·0 으로 자동으로 바뀐다
  - 옛 quote/rfp/submit 건이 proposal 에 모이고 legacy_stage 에 원래 값이 남는다
  - 옛 contracted 건이 won 이 되고 contracted_at 이 채워졌다
  - stage='proposal' 사업에만 진척 체크가 보인다
  - 롤백 스크립트로 8단계로 되돌릴 수 있다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 26-B — 영업 활동 화면 (목록 + 슬라이드오버)

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.8.2 · §32.8.3 · §32.8.4 · §32.8.5 를 구현해줘. 26-A 와 16-B 뒤에.

(1) 활동 유형을 정리한다 (§32.8.2, codes)
    칩으로 보이는 6개:
      방문 · 전화 · 이메일 · 온라인미팅 · 내부회의 · 정보수집
    드롭다운에만 있는 5개:
      자료송부 · 견적제출 · 제안서제출 · 입찰 · 기타

    "내부회의" 는 신규다. 고객을 안 만나도 그 사업에 시간을 썼으면 활동이다.
    옛 라벨 "방문미팅" → "방문", "메일" → "이메일" (코드값은 유지).
    견적제출·제안서제출·입찰은 §32.3.2 진척 체크의 열쇠다. 없애지 마라.

(2) /sales/activities 목록 — §32.8.3
    상단 요약 3칸: 이번 달 활동 N건 · 총 소요 시간("2시간 30분") ·
                   유형별 최다 1종
                   → 기간·유형 필터를 바꾸면 같이 바뀐다
    검색 한 칸: 활동명 · 사업명 · 고객명 세 곳을 한 번에.
               §12.11 의 trigram 검색 규칙을 그대로 쓴다
    유형 칩: 전체 + 위 6개. 다중 선택, "전체" 는 배타.
            ?type= 로 유지하고 칩에 건수를 붙인다

    본문은 날짜별 그룹 타임라인이다. 표가 아니다.
      그룹 머리글  2026-09-01 (화) · 1건    ← 최신 날짜가 위
                   §23.13 휴일 규칙 적용 — 휴일은 빨간색
      카드         [유형 뱃지]  사업명(상세로 가는 링크)      우측: 90분
                   📍 장소   👤 상대   담당 이름
                   본문 2줄까지
                   🟠 다음: 제안서 작성 및 제출 · 2026-09-15

    다음 행동 칩은 주황색이다.
      날짜가 오늘 이전이면 빨강, 오늘이면 굵게.
      없으면 회색 "다음 행동 없음" 을 일부러 보여준다. 숨기지 마라.

    빈 상태: "아직 등록된 활동이 없습니다" + [첫 활동 등록하기] 버튼

(3) 등록은 오른쪽 슬라이드오버 — §32.8.4
    모달이 아니다. 뒤의 목록이 보인 채로 적는다.

    기본 정보   활동일*  유형*  시작 시간  소요 시간(분, 기본 30)
    활동 내용   제목*  장소  상대(실명)  내용
    다음 행동   다음에 할 일  다음 행동일
                                  [취소] [활동 저장]

    필수는 활동일·유형·제목 셋뿐이다.
    소요 시간 옆에 30·60·90·120 버튼을 둔다.
    사업 상세에서 열면 사업 고정, /sales/activities 에서 열면
    사업 선택 칸이 맨 위에 하나 더 생긴다.
    저장하면 패널만 닫히고 목록이 제자리에서 갱신된다.
    "다음에 할 일" 이 비면 저장 시 한 번 되묻는다. 막지는 마라.

(4) 사업 상세의 활동 로그 — §32.8.5
    같은 카드 서식을 쓴다. 다른 점은 두 가지뿐이다:
      - 사업명 줄이 없다 (이미 그 사업 안이다)
      - sales_stage_history · sales_changes 가 같은 타임라인에 섞인다
    활동과 변경을 다른 탭으로 나누지 마라. 흐름이 끊긴다.

(5) 일일 업무 연동에서 duration_min 을 work_tasks 소요 시간으로 넘긴다
    두 번 적게 하지 마라.

(6) 칸반 보기 — §35.3 공통 컴포넌트(25-A)를 쓴다. 열 = 활동 유형

검증 (§32.14 의 11~16, 19번):
  - 날짜별로 묶이고 최신 날짜가 위. 휴일이 빨간색
  - 요약의 총 소요 시간이 카드의 분 합과 같다. 칩을 누르면 같이 바뀐다
  - 다음 행동 칩이 주황색이고 지난 날짜는 빨강. 없으면 회색 안내가 보인다
  - 활동 0건이면 "첫 활동 등록하기" 가 나온다
  - 패널이 오른쪽에서 나오고, 저장하면 패널만 닫히며 목록이 갱신된다
  - 활동을 등록하면 일일 업무 시간표에 뜨고 소요 시간이 같다

제약: (위 4줄 동일)
```

### 프롬프트 26-C — 영업 파이프라인 칸반

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.11.1 을 구현해줘. 26-A 뒤에. 25-A 의 공통 칸반 컴포넌트를 쓴다.

[중요] /sales 의 "단계 칸반" 과 /sales/pipeline 은 같은 컴포넌트다.
다른 점은 헤더의 3지표와 금액 합계 줄뿐이다. 두 벌로 만들지 마라.

(1) 열 = §32.3.1 의 6개. 순서 그대로.
    열 머리글에 단계명 · 건수 · 총 예상 금액(억/천만으로 줄여 쓴다)

(2) 헤더 3지표
    파이프라인 총액   수주 전 단계 금액 합
    가중 금액         Σ (예상 금액 × 확도)
    수주 확정 금액    stage='won' 금액 합

(3) 카드
    사업명(임시명이면 "(가칭)") · 고객 또는 prospect_name ·
    예상 금액(미확정이면 회색 + "미확정") · 사업 시기 ·
    영업담당 · 마지막 활동일
    클릭하면 사업 상세로 간다

(4) 드래그로 단계를 옮긴다
    뒤로 옮기면 사유 입력창 (§32.3.4)
    won 으로 옮기면 §32.5 의 미확정 4항목 검사가 걸린다.
      미확정이면 옮기지 말고 무엇이 비었는지 알려준다
    lost 로 옮기면 실패 사유 필수. 사유 없이는 옮기지 않는다

(5) 실주 열은 기본으로 접어둔다. 건수만 보이고 펼치면 카드가 나온다.
    늘 펼쳐두면 화면이 실패로 채워진다.

(6) §35.3 공통 규칙을 지킨다
    [필수] 열 머리글 건수의 합 = /sales 목록 건수.
           다르면 어딘가 빠진 것이다
    빈 열을 없애지 마라. "0건" 으로 남긴다
    모바일 열 가로 스크롤, 최소 폭 260px
    열 안 정렬: 사업 시기 오름차순, 같으면 금액 내림차순
    필터(영업담당·시기·고객·금액확정)를 리스트 보기와 공유한다

검증 (§32.14 의 21~25번)

제약: (위 4줄 동일)
```

### 프롬프트 26-D — 활동의 「상대」를 관계자로 자동 등록

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.7.1 을 구현해줘. 16-C(sales_parties) 와 26-B 뒤에.

[배경] 화면에서는 상대를 "이름 한 줄" 로만 받기로 했다.
하지만 글자로만 두면 "이 사업에서 지금까지 누구를 만나 왔는가" 를
사업 단위로 모아 볼 수 없고, 담당자 교체 이력도 사라진다.
그래서 표는 sales_parties 그대로 두고 입력만 한 칸으로 한다.

입력은 한 칸, 저장은 표. 이게 핵심이다.

(1) 활동 폼의 "상대" 는 한 줄 자유입력이다.
    관계자를 미리 등록하지 않아도 활동을 적을 수 있어야 한다.

(2) 저장할 때 그 이름을 sales_parties 에 자동 생성한다
      party_type='customer'
      org_name  = 그 사업의 고객명(없으면 prospect_name)
      is_auto=1                      ← 컬럼 추가
    같은 사업에 같은 이름이 이미 있으면 새로 만들지 말고 잇는다.
    두 번째부터는 자동완성으로 뜬다.

(3) 여러 명이면 쉼표로 나눈다.  "김철수, 이영희" → 두 행.
    입력칸은 칩(chip) 형태로 보여준다.

(4) 당사 참석자(our_members)는 로그인 사용자가 기본으로 들어가고
    드롭다운으로 더한다. 이름을 치게 하지 마라.

(5) is_auto=1 인 행에는 관계자 탭에서 "보완 필요" 표시를 붙인다.
    직책·연락처·역할을 나중에 채운다.

(6) 자동 생성이라도 교체 이력은 똑같이 적용된다.
    is_active / replaced_by / replaced_reason.
    사람이 바뀌어도 옛 사람을 지우지 마라.

(7) 16-B 를 이미 돌려서 counterparts 가 글자로만 저장돼 있다면
    마이그레이션으로 sales_parties 에 옮긴다.
    쉼표로 나누고, 사업별로 같은 이름은 하나로 묶는다.

검증 (§32.14 의 17·18번):
  - 이름을 치고 저장하면 관계자 탭에 그 사람이 생기고 "보완 필요" 가 붙는다
  - 같은 이름을 또 쳐도 관계자 행이 늘지 않는다
  - "김철수, 이영희" 를 치면 두 명이 생긴다
  - 관계자를 교체하면 옛 사람이 지워지지 않고 비활성으로 남는다

제약: (위 4줄 동일)
```

### 프롬프트 26-E — [긴급] 사업 상세에서 화면이 잠긴다

```
@기획_업무일지_프로그램_요구사항_v2.md

/sales/SP-001 에서 모든 버튼이 안 먹고 화면을 벗어날 수 없다. 고쳐줘.

[원인 — 이미 찾았다. 추측하지 말고 이대로 확인해라]

web/templates/sales/show.html

  4번째 줄:   <div class="space-y-4" x-data="salesActPanel(...)">
  391번째 줄: </div>                       ← 이 x-data 스코프가 여기서 끝난다
  392번째 줄: {{if .CanWrite}}{{template "sales_activity_panel" .}}{{end}}
                                            ← 스코프 밖이다

show.html 은 391줄까지 <div 가 57개, </div> 가 57개다. 이미 균형이 맞다.
즉 392줄의 패널은 x-data 밖에 있다.

그래서 _activity_panel.html 안의 open / dur / counterChips /
counterDraft / memberChips / nextAction / nextDate 가 전부 스코프를 잃는다.
콘솔 오류가 정확히 그 목록이다.

화면이 잠기는 건 이것 때문이다:
  _activity_panel.html 2번째 줄
    <div x-show="open" x-cloak class="fixed inset-0 z-40">
  open 이 스코프에 없으니 Alpine 이 전역으로 올라가 window.open 을 집는다.
  내장 함수라 truthy → 전체 화면 오버레이가 영원히 켜진다.
  그 밑의 <div class="absolute inset-0 bg-slate-900/20"> 가 클릭을 다 삼킨다.
  "Alpine Expression Error: Illegal invocation / Expression: open" 이 그 증거다.

activities.html 은 같은 패널을 쓰는데 119줄까지 <div 17 / </div> 16 이라
패널이 스코프 안에 있다. 그래서 /sales/activities 는 멀쩡하다.
상세 화면만 잠긴다.

[고칠 것]

(1) show.html 의 392줄을 391줄 안쪽으로 옮긴다.
    {{template "sales_activity_panel" .}} 이
    x-data="salesActPanel(...)" div 의 마지막 자식이 되게 한다.
    스코프 밖으로 나가면 안 된다.

(2) [재발 방지] 같은 사고가 다시 안 나게 패널이 스스로 방어하게 한다.
    _activity_panel.html 최상단 div 를 이렇게 바꾼다:

      <div x-show="typeof open !== 'undefined' && open" ...>

    이건 임시방편이 아니라 안전장치다. x-show 가 window.open 을 집어
    화면을 통째로 잠그는 일은 두 번 다시 나오면 안 된다.
    화면 전체를 덮는 fixed inset-0 요소는 스코프가 깨졌을 때
    반드시 "안 보이는 쪽" 으로 넘어져야 한다.

(3) 오버레이를 눌러도 닫히게 한다.
    지금 배경 div 의 @click="open=false" 도 스코프가 없어 안 먹었다.
    (1)을 고치면 같이 풀린다. 그래도 Esc 키로 닫는 걸 추가해라:
      @keydown.escape.window="open=false"

(4) 같은 실수가 다른 데도 있는지 찾아라.
    web/templates 전체에서 fixed inset-0 을 쓰는 요소를 모두 찾아
    그 x-show / x-if 변수가 조상 x-data 안에 실제로 있는지 확인해라.
    없으면 (2)와 같은 방어를 넣어라.
    특히 htmx 로 뒤늦게 끼워 넣는 조각들을 봐라.

검증:
  - /sales/SP-001 에서 왼쪽 사이드바 메뉴가 눌린다
  - [활동 추가] 를 누르면 패널이 열리고, 닫기·취소·배경 클릭·Esc 로 닫힌다
  - DevTools 콘솔에 Alpine Expression Error 가 0건이다
    (open / dur / counterChips / counterDraft 넷 다 사라져야 한다)
  - 소요 시간 30·60·90·120 버튼이 입력칸 값을 바꾼다
  - 상대 칸에 "김철수, 이영희" 를 치면 칩 두 개가 생긴다
  - /sales/activities 가 이전과 똑같이 동작한다 (건드리지 마라)

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 26-F — 상세 화면 · 등록 버튼 중복 · 칸반 열 폭

```
@기획_업무일지_프로그램_요구사항_v2.md

§32.4.1 · §32.8.3 · §32.11.1 을 구현해줘. 세 건이다.

────────────────────────────────────────────────
(1) 영업 사업 상세가 피그마 이전 화면으로 남아 있다  — §32.4.1
────────────────────────────────────────────────
web/templates/sales/show.html

지금 이렇게 돼 있다:
  - 오른쪽 사이드에 "단계 변경" 카드
      <select name="stage"> + 사유 입력 + [단계 저장] 버튼
  - 그 아래 "단계 이력" 카드 — 제목과 설명만 있고 내용이 없다
  - "활동 등록" 버튼이 활동 로그 카드 안에 있다
  - 정보/사이드가 lg:grid-cols-3 로 2:1 좌우 분할

피그마 기준으로 바꾼다:

  a. "영업 단계" 카드를 만들고 §32.3.1 의 6단계를 pill 가로줄로 놓는다.
       (담당자 접촉●)(정보 입수)(제안 진행)(협상)(수주)(실주)
     현재 단계를 활성 표시하고, 누르면 그 단계로 이동한다.
     <select> + [단계 저장] 방식은 지운다.

  b. 사유는 뒤로 가거나 lost 를 누를 때만 창으로 받는다.
     앞으로 갈 때는 바로 이동한다.
     [필수] 이 판정은 파이프라인 칸반 드래그(_dnd.html 의 dropProject)와
     완전히 같은 규칙이어야 한다. 같은 함수를 쓴다.
     won 으로 갈 때 미확정 4항목 검사도 여기서 똑같이 걸린다.

  c. "+ 활동 추가" 를 페이지 헤더로 올린다. [수정] 왼쪽, 주황색.
     활동 로그 카드 안의 버튼과 "한 번 적으면 일일 업무 일정표에도
     보입니다" 안내 줄은 지운다.

  d. "단계 이력" 카드를 지운다.
     내용이 없고, 단계 변경은 이미 활동 로그 타임라인에 섞여 나온다
     (§32.8.5). 같은 것을 두 군데 두지 않는다.

  e. 카드 배치를 세로로 바꾼다:
       영업 단계 → 사업 정보 → 탭(활동 로그·관계자)
     2:1 좌우 분할이 아니다.

  f. 사업 정보 카드에 "확률" 행을 넣는다. 피그마에 있다.

────────────────────────────────────────────────
(2) 영업 활동 화면에 주황 등록 버튼이 2개다  — §32.8.3
────────────────────────────────────────────────
web/templates/sales/activities.html 헤더의 [활동 등록] 과
web/templates/sales/_activity_log.html 빈 상태의 [첫 활동 등록하기] 가
동시에 보인다. 둘 다 @click="open=true" 로 같은 일을 한다.

  a. 목록이 비면 헤더 버튼을 숨긴다. 빈 상태 CTA 만 남긴다.
     목록이 있으면 반대로 헤더 것만 남는다.
     화면에 주황 등록 버튼은 항상 하나다.

  b. [중요] 빈 상태 문구를 두 가지로 나눈다.
     지금은 활동이 있어도 그 달에 없으면
     "아직 등록된 활동이 없습니다" 가 나온다. 고장 난 줄 안다.

       그 사업/전체에 활동이 0건  → "아직 등록된 활동이 없습니다"
                                     + [첫 활동 등록하기]
       이 달만 0건               → "2026년 9월에 등록된 활동이 없습니다"
                                     + [전체 기간 보기] 링크

     판정은 월 필터를 뺀 건수로 한다.
     renderActivitiesLogData 에서 이미 chipActs 를 월 필터로 뽑고 있으니,
     월 필터 없는 전체 건수를 하나 더 세어 템플릿에 넘긴다.

  c. 머리 도구줄을 줄인다. 피그마 헤더에는 "+ 활동 추가" 하나뿐이다.
     로그/일일/칸반·유형/칸반·단계 4개 버튼은 §35.3 대로
     [리스트 | 칸반] 으로 묶는다. (일일 보기는 리스트 안의 하위 전환)
     "사업 목록" 버튼은 지운다. 사이드바에 이미 있다.

────────────────────────────────────────────────
(3) 칸반에서 빈 열이 좁아진다  — §32.11.1 · §35.3
────────────────────────────────────────────────
web/templates/kanban/_board.html

  5번째 줄:  <div class="flex gap-3 overflow-x-auto pb-2 items-start">
  7번째 줄:  <div class="shrink-0 min-w-[260px] w-[260px] ...">

  w-[260px] + shrink-0 이라 화면이 넓어도 열이 260px 에 고정된다.
  6열이면 1600px 만 쓰고 오른쪽이 비어, 카드 없는 열이 유독 좁아 보인다.

  고칠 것:
    7줄:  shrink-0 min-w-[260px] w-[260px]
       →  flex-1 basis-0 min-w-[260px]
    5줄:  items-start  →  items-stretch

  이러면 넓은 화면에서는 열이 폭을 균등하게 나눠 갖고,
  좁아지면 260px 에서 멈추고 가로 스크롤이 생긴다.
  높이도 서로 맞는다.

  [주의] 이 파일은 공통 컴포넌트라 일일 업무 등록·오늘 내 업무·
  행정지원업무·AS 목록·영업 사업 칸반이 전부 같이 바뀐다.
  그게 §35 의 의도다. 다만 바꾼 뒤 그 화면들을 하나씩 열어
  열이 깨지지 않았는지 눈으로 확인해라.
  특히 열이 3개인 화면(오늘 내 업무·행정지원업무)에서
  열이 너무 넓어지지 않는지 본다. 필요하면 max-w-[420px] 를 더한다.

검증:
  - /sales/SP-001 에 단계 pill 6개가 가로로 있고, 눌러서 단계가 바뀐다
  - 뒤로 가거나 실주를 누를 때만 사유 창이 뜬다
  - "+ 활동 추가" 가 페이지 헤더에 있고, 활동 로그 카드 안에는 없다
  - 빈 "단계 이력" 카드가 없다
  - /sales/activities 에서 주황 등록 버튼이 화면에 하나만 보인다
  - 활동이 있는 달이 아닌 달을 보면 "이 달에 등록된 활동이 없습니다
    + 전체 기간 보기" 가 나온다
  - /sales/pipeline 에서 0건 열도 다른 열과 폭이 같고,
    6열이 화면 폭을 균등하게 채운다
  - 창을 좁히면 260px 에서 멈추고 가로 스크롤이 생긴다
  - 일일 업무 등록·오늘 내 업무·행정지원업무·AS 목록 칸반이 안 깨졌다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

# 27단계 — 단품·납품 영업관리 [v2.28]

기획서 **§36** 이 근거입니다. **§32(구축·입찰)와 표를 같이 쓰고 `deal_type` 으로만 가릅니다.**

**이 단계를 만드는 이유가 §36.1.7 에 있습니다.** 실제로 나간 견적서에서 소계 산식이 첫 품목을 빼먹어 **32,010,000원이 틀린 채로 두 번 발행**됐습니다. 엑셀을 복사해 쓰는 한 이 사고는 반복됩니다. **27-C 가 그것을 막는 작업입니다.**

**순서를 지키세요.** 27-A → 27-B → 27-C 까지가 값의 대부분입니다.

### 프롬프트 27-A — 사업 유형 분기 + 단품 5단계 [1단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.4 · §36.5 를 구현해줘. 표를 새로 만들지 마라.

(1) sales_projects 에 deal_type 컬럼을 더한다
      build   구축·입찰  (기본값 — 기존 데이터는 전부 이 값)
      supply  단품·납품
    [필수] 기존 행은 전부 build 로 채운다. §32 화면이 하나도 달라지면 안 된다.

(2) 단품 단계 5개 (§36.5) — codes 에 둔다
      inquiry   문의 접수
      quoted    견적 제출
      ordered   수주
      delivered 납품 완료
      dropped   취소·실주        (사유 필수)

    [중요] 단품은 확도(%)를 쓰지 않는다.
    1~3주에 끝나는 건에 "40%" 는 의미가 없다.
    화면에서 확도 자리에 견적 금액을 보여준다.

    stage 컬럼을 같이 쓰되, deal_type 에 따라 고르는 목록이 다르다.
    build 면 §32.3.1 의 6개, supply 면 위의 5개.
    섞이지 않게 막아라.

(3) 단계는 자동으로 올라간다
      견적 저장     → quoted
      발주서 입력   → ordered
      납품일 입력   → delivered
    손으로 고를 일이 거의 없어야 한다.
    뒤로 가는 것은 허용하되 사유를 받는다 (§32.3.4 와 같은 함수를 쓴다).

(4) 목록·상세 화면
    /sales 에 유형 탭 [구축·입찰 | 단품·납품] 을 둔다. ?deal= 로 유지.
    칸반 열은 유형에 따라 6개 또는 5개.
    §35.3 공통 칸반 컴포넌트를 쓴다. 열 폭은 균등(§32.11.1).

    단품 상세는 §32.4.1 의 pill 줄을 그대로 쓰되 5개다.
    머리글은 "단계명 · 견적 금액 · 정보 확정도 n/4" 로 한다.

(5) 공유하는 것을 건드리지 마라
    고객 · sales_parties · sales_activities · 지역 · 영업담당 ·
    변경 이력은 두 유형이 같이 쓴다.
    같은 고객에게 구축도 하고 소모품도 판다.
    이걸 나누면 "이 고객과 무슨 거래를 해 왔나" 를 못 본다.

검증:
  - 기존 영업 사업이 전부 build 이고 §32 화면이 이전과 완전히 같다
  - 단품으로 만들면 확도가 안 보이고 그 자리에 견적 금액이 뜬다
  - 단품 단계 목록에 "제안 진행" 같은 구축 단계가 안 나온다
  - 계획 대비 실행률·3일·7일 숫자가 변하지 않는다

제약:
- 기존 파일·함수를 먼저 찾아 재사용한다. 새로 만들기 전에 검색부터 한다.
- 기존 동작을 바꾸지 않는다. 되돌릴 수 있게 작게 나눠 커밋한다.
- 스키마를 바꾸면 마이그레이션과 롤백을 함께 만든다.
- 작업 후 cd server && go build ./... && go test ./... 를 통과시킨다.
```

### 프롬프트 27-B — 품목 마스터 [2단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.6 을 구현해줘. 27-A 뒤에.

[배경] 지금 DB 에 판매 품목 카탈로그가 없다.
assets 는 고객에게 설치된 개체(시리얼 단위)라 카탈로그가 아니다.
그래서 같은 감열지를 견적할 때마다 이름과 단가가 달라졌다:
  260330  "나이콤 감열지"          1 Box(50Roll)  식   220,000
  260816  "자동대출반납기용 감열지" 59x80mm 50롤   EA   150,000

(1) sales_items 테이블 신설
    item_id
    item_kind      goods 물품 / service AS·작업 / labor 개발용역 / etc
    category       RFID장비 · 소모품 · SW · 공사
    name           품명
    spec           규격 (여러 줄. 견적서의 "내용"/"규격" 칸)
    model, manufacturer
    unit           EA · 식 · 대 · Box · 인 · M/M
    gov_item_no    조달 물품식별번호   ← 양식 B 에 필요
    list_price     소비자가·조달단가
    last_price, last_quoted_at, last_customer   ← 마지막으로 견적한 단가
    default_supplier
    is_active, notes

(2) 초기 데이터를 assets 에서 뽑는다
    product_name / model_name / manufacturer / product_category 를
    distinct 로 모아 sales_items 초안을 만든다.
    우리가 실제로 납품해 온 물건이라 가장 정확한 출발점이다.
    is_active=0 으로 넣고 사람이 확인하며 켜게 해라.
    자동으로 만든 행에 "확인 필요" 표시를 붙인다.

(3) /items 화면 — 목록 + 등록·수정
    §35 대로 칸반도 붙인다 (열 = item_kind).

(4) [중요] 자유 입력을 막지 마라
    견적 라인에서 품목을 고르면 규격·단위·최근 단가가 자동으로 채워지되,
    목록에 없는 것도 그냥 칠 수 있어야 한다.
    처음 파는 물건까지 막으면 아무도 이 기능을 안 쓴다.
    자유 입력한 품목은 견적 저장 후
    "품목으로 등록할까요?" 를 한 번 묻는다.

(5) 견적을 저장하면 last_price / last_quoted_at / last_customer 를 갱신한다.

검증:
  - assets 에서 뽑은 품목이 "확인 필요" 표시와 함께 들어와 있다
  - 품목을 고르면 규격·단위·최근 단가가 자동으로 채워진다
  - 목록에 없는 품목도 칠 수 있고, 저장 후 등록을 묻는다

제약: (위 4줄 동일)
```

### 프롬프트 27-C — [가장 중요] 견적 작성 · 채번 · 합계 검산 [3단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.7.1 ~ §36.7.5 · §36.7.7 을 구현해줘. 27-B 뒤에.

[이 작업의 이유 — §36.1.7]
실제로 나간 견적서에서 소계 산식이 =SUM(I17:I17) 이라
첫 품목(29,100,000)이 통째로 합계에서 빠졌다.
  나간 금액 37,950,000 / 맞는 금액 69,960,000 / 차이 32,010,000원
7월 건의 산식이 그대로 복사돼 8월 재발행 때도 똑같이 나갔다.
엑셀을 복사해 쓰는 한 SUM 범위는 반드시 어긋난다.
이 프롬프트는 그 사고를 구조적으로 불가능하게 만드는 작업이다.

(1) sales_quotes / sales_quote_lines 신설 — §36.7.1 · §36.7.2 의 컬럼 그대로

(2) [필수] 합계는 계산값이다. 사람이 못 고친다 — §36.7.5
    라인 금액 = 수량 × 단가
              (용역이면 × 투입율 × (1-할인율))
    소계      = 그 견적의 모든 라인 금액의 합
                범위를 지정하는 칸을 만들지 마라. 화면에도 읽기 전용이다.
    부가세    = vat_mode 에 따라
    최종      = 소계 + 부가세 → 절사 적용

    [필수] 저장할 때 Σ라인 ≠ 소계 이면 저장을 막아라.
    있을 수 없는 일이지만 이 한 줄이 재발을 막는다.

    [필수] 내려받는 xlsx 의 합계 셀에는 산식이 아니라 "값" 을 넣어라.
    산식을 넣으면 받는 쪽에서 행을 건드릴 때 또 깨진다.
    화면 숫자와 파일 숫자가 반드시 같아야 한다.

(3) 채번 — §36.7.3
    VI-견적-YYYYMMDD-NNN  하나로 통일한다. 양식 A·B 공통.
    옛 VIT-YYMMDD-NN 체계는 더 쓰지 않는다.

    [필수] 번호의 날짜 = quote_date. 둘이 어긋날 수 없게 묶어라.
    실제 견적서 10건 중 5건이 번호와 견적일이 달랐다.
      VI-견적-20250629-001 인데 견적일 2026-06-29
      VIT-250731-02       인데 견적일 2026-01-13
    [필수] 사람이 번호를 못 고치게 한다. 입력칸을 두지 마라.
    [필수] 중복이면 저장을 막는다. VI-견적-20260409-002 가
           두 건에 붙어 있었다.
    [필수] 견적일도 프로그램이 넣는다.
           실제 파일에 "2026년 07월 74일" 이 있었다.
    "이 견적으로 새로 만들기" 는 번호·견적일을 오늘로 다시 매긴다.
    옛 견적 반입은 있는 번호를 그대로 두고 is_legacy=1 로 표시한다.

(4) VAT — §36.7.4.1
    양식 A 기본 excluded (친 단가 = 공급가, 10% 를 더한다)
    양식 B 기본 included  (친 단가 = VAT 포함, ÷1.1 로 공급가 역산)
    견적마다 바꿀 수 있다.
    화면에 공급가·부가세·합계 세 숫자를 다 보여준다.
    [주의] 역산하면 원 단위 끝수가 생긴다 (5,700,000/1.1 = 5,181,818.18…).
    버림으로 통일하고, 합계가 넣은 목표액과 다르면
    마지막 라인에서 1원을 맞춰라.
    "5,700,000 을 넣었는데 합계가 5,699,999" 가 나오면 안 된다.

(5) 절사 — 양식별로 고정하지 마라
    none / hundred(100원) / thousand(1,000원) 를 견적마다 고른다.
    [필수] 라벨과 실제 자릿수가 반드시 같다.
    지금 엑셀은 "100원 단위 절사" 라 써 놓고 ROUNDDOWN(x,-3) 으로
    천원을 자르고 있다. 그런 일이 불가능해야 한다.
    직전에 쓴 값을 기본으로 제안한다.
    절사 전 금액을 같이 보여준다 — "25,220,000 (절사 전 25,222,587)".

(6) 필수 입력
    담당자·연락처가 비면 저장을 막아라.
    실제 견적서 중 담당자 칸이 빈 채로 나간 것이 있었다.

(7) 출력 — §36.7.7. 양식을 새로 그리지 마라.
    받은 원본 xlsx 를 템플릿으로 저장소에 넣고 값만 채운다.
      server/web/templates/quote/form_a1.xlsx   물품·작업 (A1:L33)
    셀 지도는 §36.7.7 표에 있다. 그대로 따라라.
    A31 의 QEP-710-01(Rev.0) 은 사내 품질문서 번호다. 고정이다.
    회사 정보(사업자등록번호·상호·대표이사·업태·종목·주소·전화)와
    직인 이미지는 설정에 한 번 넣고 모든 견적서가 가져다 쓴다.
    라인 수에 따라 행을 늘리고 아래 블록·병합·인쇄영역이 따라가게 해라.
    excelize 를 쓴다 (§16.6 주간보고서에서 이미 쓰고 있다).

(8) /quotes 목록 + /quotes/new 작성 화면
    작성은 라인 표가 중심이다. 품목 자동완성 → 수량·단가 → 합계 자동.
    미리보기 후 xlsx 내려받기.

검증 (§36.17 의 1~9번):
  - 라인을 11줄 넣어도 한 줄도 합계에서 빠지지 않는다
  - 라인을 지우고 순서를 바꿔도 소계가 즉시 다시 난다
  - 소계를 손으로 고칠 방법이 없다
  - 번호의 날짜와 견적일이 항상 같고, 중복이면 저장이 막힌다
  - "이 견적으로 새로 만들기" 가 번호·견적일을 오늘로 새로 매긴다
  - VAT 포함으로 5,700,000 을 넣으면
    공급가 5,181,818 · 부가세 518,182 · 합계 5,700,000 이 되고
    합계가 넣은 값과 정확히 같다
  - "100원 절사" 를 고르면 100원 단위로 잘리고 문서 라벨도 그렇다
  - 내려받은 xlsx 의 합계 셀에 값이 들어 있고 화면 숫자와 같다
  - 담당자·연락처가 비면 저장이 막힌다

제약: (위 4줄 동일)
```

### 프롬프트 27-D — 양식 3종 추가 · 개정 · 노임단가 · 견적 용도 [4단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.7.4 · §36.7.6 · §36.7.6.1 · §36.7.8 · §36.1.8 을 구현해줘. 27-C 뒤에.

(1) 양식 3종을 더한다 (§36.1.8) — 원본 xlsx 를 템플릿으로 쓴다
      form_a2.xlsx  개발 용역   (A1:I42, "유지보수부분" 블록 있음)
      form_b1.xlsx  조달 단순   (A1:K28, 공급가|부가세|공급금액 열)
      form_b2.xlsx  조달 그룹   (A1:L32, 묶음별 소계)

    [신규 기능] B-2 는 품목을 묶음으로 나누고 묶음마다 소계를 낸다.
    sales_quote_lines.group_label 로 묶는다.
    [필수] 그룹 소계의 합 = 전체 소계 임을 저장할 때 확인해라.

(2) 개정 (rev) — §36.7.4
    원본을 고치지 마라. "개정" 을 누르면 rev+1 로 새 행을 만들고
    라인을 복사한다. 번호는 그대로, 화면에 (r2) 로 붙인다.
    이전 개정은 읽기 전용으로 남는다.
    개정 사유를 한 줄 받는다.
    고객에게 어떤 금액을 언제 줬는지가 영업 자산이다.

(3) labor_rates 신설 — §36.7.6
    받은 견적서 파일 안에 "2026노임단가" 시트가 들어 있었다.
    한국소프트웨어산업협회가 해마다 공표하는 SW기술자 평균임금이다.
    17개 직군 × 월(M/M)·일(M/D)·시(M/H) 평균임금.
    year, job_code, job_name, monthly, daily, hourly,
    effective_from, effective_to, source_note

    2026년 값을 시드로 넣어라 (§36.7.6 표):
      ⑦ UI/UX기획/개발자 6,901,660
      ⑧ UI/UX디자이너   5,159,246
      ⑨ 응용SW개발자    7,754,124
      … 나머지는 기획서 표를 그대로

    용역 견적 산식을 프로그램이 계산한다:
      직접인건비 = Σ(노임단가 × M/M × 투입율)
      제경비    = 직접인건비 × 110%  → 할인율 적용, 백원 단위 절사
      기술료    = 제경비 × 20%       → 할인율 적용

    [주의] 실제 견적서에 2025년 단가와 2026년 단가가 섞여 있었다.
    견적일 연도의 표를 기본으로 잡되, 다른 연도를 쓰면
    라인에 "2025년 단가" 표시를 붙여 눈에 보이게 해라.

(4) 요율은 표준값이되 견적마다 바꿀 수 있다 — §36.7.6.1
    제경비율 110% / 기술료율 20% 를 app_settings 에 둔다.
    견적 화면은 표준값으로 채워놓고 시작한다.
    고치면 overhead_rate / tech_fee_rate 에 그 견적의 값으로 저장한다.
    [필수] 나중에 표준이 바뀌어도 옛 견적의 금액은 안 변한다. 소급 금지.
    표준과 다르면 "표준 110% → 100%" 로 무엇을 바꿨는지 보여줘라.
    노임단가를 손으로 고치면 "단가 수정" 표시를 붙인다.

(5) 목표 금액 역산 — §36.7.5
    실제 견적서에 =19990000/1.1, =25220000/1.1 처럼
    VAT 포함 목표액에서 단가를 거꾸로 구한 흔적이 있다.
    "1,999만원에 맞춰 주세요" 가 실무다.
    목표 총액을 넣으면 단가를 역산한다.
    라인이 여럿이면 비율대로 나눈다.
    is_reverse_calc 로 역산했다는 사실을 남긴다.

(6) 견적 용도 — §36.7.8
    purpose: deal 실거래 / budget 예산용 / reference 참고
    budget 이면 budget_year 를 받는다.
    [중요] budget · reference 는 파이프라인과 전환율에서 뺀다.
    예산용은 지금 수주될 건이 아닌데 섞이면 지표가 어긋난다.
    대신 "예산 반영 예정" 목록으로 따로 본다.
    대상 연도 1월에 §8 미계획 업무함으로 "예산 견적 후속" 을 올린다.
    목록·상세에 용도 뱃지. 예산용은 회색.

검증 (§36.17 의 9~11 · 10-1~10-6):
  - 양식 B-2 에서 그룹 소계의 합이 전체 소계와 같다
  - 개정하면 이전 견적이 지워지지 않고 읽기 전용으로 남는다
  - 용역 견적에서 M/M × 노임단가 × 투입율 → 제경비 → 기술료가 자동 계산된다
  - 다른 연도 단가를 쓰면 "2025년 단가" 표시가 붙는다
  - 제경비율을 110% → 100% 로 고치면 "표준 110% → 100%" 가 보이고
    설정의 표준값은 안 바뀐다
  - 설정에서 표준 요율을 바꿔도 이미 만든 견적의 금액이 변하지 않는다
  - 목표 총액을 넣으면 단가가 역산되고 "역산" 표시가 남는다
  - 용도를 "예산용" 으로 하면 파이프라인·전환율에서 빠진다

제약: (위 4줄 동일)
```

### 프롬프트 27-E — 수주 · 발주 · 마진 [5단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.8 · §36.9 를 구현해줘. 27-D 뒤에.

(1) sales_orders 신설 — §36.8 컬럼 그대로
    견적에서 수주로 넘길 때 라인을 그대로 복사한다.
    복사한 뒤 수량·단가를 조정할 수 있다.
    다르면 화면에 "견적과 다름" 을 표시한다.
    고객 발주서(po_no, po_date)와 PDF 첨부를 받는다.
      attachments ref_type='sales_order'
    billing_status / invoice_no / invoiced_at / paid_at / paid_amount
    컬럼을 미리 만들되 화면에는 내지 마라 (§36.11).
    나중에 청구를 붙일 때 마이그레이션이 작아진다.

(2) sales_purchases 신설 — §36.9
    재고는 다루지 않는다. 수량 잔고를 계산하지 마라.
    수주 라인별로 공급사에 발주한 기록만 남긴다.
    supplier_name, purchase_date, qty, cost_price,
    eta_date, arrived_date, status

(3) [핵심] 마진
    마진 = 판매가 - 매입가(cost_price)
    단품 영업은 마진으로 판단한다. 견적 금액만 봐서는
    남는 장사인지 알 수 없다.

    [필수] 권한을 나눈다 (§36.16-9).
    매입가·마진은 관리자와 영업담당(sales)에게만 보인다.
    기술담당에게는 숨긴다. 화면에서 숨기는 것으로 끝내지 말고
    핸들러에서 값 자체를 안 내려보내라.

(4) /orders 목록 — 칸반 열 = 수주 → 발주 → 입고 → 납품 완료
    §35.3 공통 컴포넌트를 쓴다.

검증:
  - 견적에서 수주로 넘기면 라인이 그대로 복사된다
  - 수량을 고치면 "견적과 다름" 이 뜬다
  - 기술담당으로 로그인하면 매입가·마진이 응답에 아예 없다

제약: (위 4줄 동일)
```

### 프롬프트 27-F — 납품 → 설치자산 자동 등록 [6단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.10 · §36.10.1 을 구현해줘. 27-E 뒤에.

[이 단계가 이 시스템을 우리 회사 것으로 만든다]
납품한 장비는 곧 AS 대상이고 정기점검 대상이다.
지금은 납품 후 누가 손으로 assets 에 넣어야 하고,
안 넣으면 그 장비는 AS 를 받을 때까지 시스템에 존재하지 않는다.

(1) sales_deliveries 신설 — §36.10
    delivery_date, qty, place, receiver_name, installed_by
    부분 납품을 허용한다.
    인수증·납품 사진 첨부 — ref_type='sales_delivery'

(2) [핵심] item_kind='goods' 인 라인을 납품하면 assets 행을 만든다
    채우는 값:
      customer_id, product_name, model_name, manufacturer,
      install_date = 납품일,
      installer_type = '자사',
      install_location
    assets 에 sales_order_id 컬럼을 더해 원본을 잇는다.

    [필수] 시리얼은 납품 시점에 받는다.
    수량이 3이면 시리얼 3개를 입력받아 3행을 만든다.

    [필수] 유지보수 계약 정보(maint_*)는 비워 둬라.
    계약은 따로 맺는 것이다. 여기서 추측해 채우면
    정기점검 대상이 잘못 잡힌다.

    소모품·용역·AS 작업은 자산을 만들지 마라.

(3) 시리얼 미입력 처리 (§36.16-12)
    "나중에 입력" 을 허용하되, 시리얼이 빈 자산을
    §8 미계획 업무함에 "자산 시리얼 미입력" 으로 올린다.

검증 (§36.17 의 13~14번):
  - 물품을 납품하면 assets 에 행이 생기고 시리얼 수만큼 나뉜다
  - 소모품·용역·AS 를 납품해도 자산이 생기지 않는다
  - 생긴 자산의 maint_* 가 비어 있고 정기점검 대상에 안 들어간다
  - 시리얼을 비워두면 미계획 업무함에 올라온다

제약: (위 4줄 동일)
```

### 프롬프트 27-G — 지표·대시보드 [7단계]

```
@기획_업무일지_프로그램_요구사항_v2.md

§36.13 을 구현해줘.

(1) 단품 전용 지표
    문의 → 견적 소요일
    견적 → 수주 전환율   (purpose='deal' 만. budget·reference 제외)
    수주 → 납품 소요일
    마진율  (판매가-매입가)/판매가   ← 권한 있는 사람만
    품목별 판매 수량·금액
    만료된 견적 (유효기간 지났는데 status='sent')

(2) [필수] §4 의 계획 대비 실행률 · 접수→방문 3일 · 접수→완료 7일
    어디에도 단품 거래가 들어가면 안 된다.
    §36.13 이 근거다. 테스트로 고정해라.

(3) 단품 대시보드 4칸
    이번 달 견적 건수·금액 / 수주 금액 / 납품 대기 / 마진율

검증 (§36.17 의 16번):
  - 단품 거래를 만들어도 실행률·3일·7일이 변하지 않는다
  - 예산용 견적이 전환율 분모에 안 들어간다

제약: (위 4줄 동일)
```


## 순서와 의존 관계

```
1단계 빌드 복구        ← 반드시 먼저. 나머지 검증이 불가능
   ↓
2단계 지표 복구 (5건)  ← 효과 대비 비용이 가장 좋음
   ↓
3단계 배포 차단 결함   ← 여기까지 하고 한 번 배포하면
   ↓                     미배포 4건(주간보고서·근무구분·행정업무·데이터관리)도 함께 해소
4단계 계획 습관 정착
   ↓
5단계 행정·지원 GTD
   ↓
6단계 운영 신뢰성
   ↓
7단계 정기점검 안정화·접속기록  ← v2.5 신설. 단 6-A·6-B 는 최우선
   ↓
8단계 전사 주간업무보고 시트    ← v2.6 신설. 8-A(데이터 정비)를 건너뛰면 숫자가 전부 0
   ↓
9단계 조치완료보고서            ← v2.7 신설. 9-A(필드 2개) 없이는 빈 문서가 나옴
                                  9-C(PDF)는 도커 이미지 크기 확인 후 판단
   ↓
10단계 공휴일 API·연차          ← v2.8 신설. 단 10-A 는 오늘 바로.
                                  2026 추석이 근무일로 잡혀 있음
   ↓
11단계 담당자별 열 배치         ← v2.9 신설. 화면 개선이므로 순서는 자유.
                                  11-A 만 해도 효과가 난다
   ↓
12단계 다중 담당자              ← v2.10 신설. 12-B 는 11-A 뒤에.
                                  12-C(통계 전환)는 데이터가 쌓인 뒤
   ↓
13단계 운영 중 발견 4건         ← v2.11. 서로 독립적이라 순서 없음.
                                  13-B 의 날짜 검증은 지표 오염 건이라 먼저
   ↓
14단계 조치 정보 섹션 분리      ← v2.12. 14-A 하나뿐
   ↓
15단계 사이드바 권한 판정       ← v2.13. 인증 미들웨어 결함이라 우선
   ↓
16단계 영업관리 (수주 전)       ← v2.15 신설. 별도 표라 기존 기능과 독립.
                                  16-A 만 해도 영업 건이 기록되기 시작한다
   ↓
17단계 기간 내 반복 실행        ← v2.16 신설. 정기점검과 같은 구조의 2회차.
                                  §13.15.3 의 사고 5건을 먼저 읽을 것
   ↓
18단계 지표 기준일 · AS 검색    ← v2.17. 18-A 를 먼저.
                                  지금 지표가 이관 1,100건 위에서 계산된다
   ↓
19단계 화면 개편                ← v2.18. 19-A 는 1줄이니 지금 바로.
                                  §33.1(예정일→종료일)을 먼저 읽을 것
   ↓
20단계 AS·정기점검 개편         ← v2.19. 20-A 를 반드시 먼저.
                                  삭제 요청 3건이 지표 근거다
   ↓
21단계 일일 업무 등록 칸반      ← v2.20. 독립. 기존 버킷 재사용이 핵심
   ↓
22단계 자동배정 월 단위         ← v2.22. [긴급] 고른 달이 아닌 달이 재배정된다
   ↓
23단계 업무처리현황 캘린더      ← v2.23. 23-A(날짜 기준)를 먼저.
                                  화면보다 데이터가 먼저 틀렸다
   ↓
24단계 사진·리드타임·기간선택   ← v2.24. 24-A(사진)가 긴급
   ↓
25단계 목록·칸반 공통 규칙      ← v2.26. 25-A(공통 컴포넌트)를 먼저.
                                  화면마다 따로 만들면 판정이 갈린다
   ↓
26단계 영업 화면 확정           ← v2.27. 16단계를 아직 안 돌렸으면 불필요
                                  (16-A/B/D 를 v2.27 로 이미 고쳐 뒀음).
                                  이미 돌렸으면 26-A(단계 8→6)를 반드시 먼저.
                                  stage 코드값이 바뀌므로 뒤 작업이 다 걸린다
                                  ※ 26-E 는 [긴급] · 순서 무시하고 지금 바로.
                                    /sales/SP-001 이 통째로 잠겨 있다
```

**3단계까지 끝나면 한 번 배포하세요.** 베타 사용자가 주간업무보고서를 못 쓰고 있는 상태가 그때 풀립니다.

## 단계별 완료 판정

| 단계 | 판정 기준 |
|------|-----------|
| 1 | `go build ./...` · `go test ./...` 통과 |
| 2 | 부록 C의 V-1·V-3이 **0**. 조치 저장 시 `start_datetime`·`time_spent`가 DB에 남음 |
| 3 | 정기점검 엑셀이 openpyxl로 열림. 사업 필터 정상. `go mod tidy` 제거됨 |
| 4 | 계획 수립률 **95% 이상** 4주 연속 |
| 5 | IRM 사례 업무를 §13.7 트리 구조대로 기록 가능. `complete_date` 기준 소요일 산출. 기존 `task_id` 보존 |
| 6 | 롤백 시연 성공. 인터넷 끊고 화면 정상 |

## 에이전트가 자주 놓치는 것

작업 후 아래를 직접 확인하세요.

- [ ] **컴파일되는가** — 라우트만 등록하고 구현을 빼놓지 않았는지
- [ ] **기존 함수를 재사용했는가** — 지표 산식을 새로 짜면 화면마다 숫자가 달라짐
- [ ] **회피 경로를 만들었는가** — 필수 입력을 늘렸다면 「미정+사유」가 있는지
- [ ] **테스트를 추가했는가** — 새 규칙에 대응하는 테스트가 없으면 다음에 조용히 깨짐
- [ ] **§4.2 표를 갱신했는가** — 새 필드가 지표를 채운다면 기획서에도 반영

## 한 번에 하나씩

여러 프롬프트를 묶어 던지지 마세요. 지금 소스가 깨진 것도 여러 갈래를 동시에 손대다 중간에서 멈춘 결과로 보입니다. **한 프롬프트 → 빌드·테스트 확인 → 커밋** 을 지키면 어디서 깨졌는지 항상 알 수 있습니다.
