# 검수 · 배포

**기준 문서:** `기획_업무일지_프로그램_요구사항_v2.md`
**담당 에이전트:** `@agent-patch`
**최종 갱신:** 2026-08-14

---

## 0. 지금 배포 상태 (2026-08-14 기준)

베타 서버에 올라간 이미지(`deploy/server-app.tar`, 빌드 **08-14 00:16**)와 현재 소스가 어긋나 있습니다. 그날 오전에 들어간 변경이 배포되지 않았습니다.

| 기능 | 소스 | 배포본 | 영향 |
|------|:---:|:---:|------|
| **주간업무보고서** (`/stats/weekly-report.xlsx`) | ○ | **✗ 404** | 베타 사용자가 주간 보고서를 못 받습니다 |
| **근무구분 내근/외근** (`work_place`) | ○ | **✗** | 조치 화면에 입력란 자체가 없음 → 전건 미수집 |
| **행정관련업무** (`/admin-work`) | ○ | **✗ 404** | 메뉴 진입 불가 |
| **데이터 관리** (`/admin/data`) | ○ | **✗ 404** | 백업·이력·롤백 화면 진입 불가 |

> **먼저 할 일:** 현재 소스를 다시 빌드해 배포하면 위 4건이 한 번에 해소됩니다.
> **단, 그 전에** `v.project_id` 결함(아래 3.1)을 고쳐야 합니다. 지금 그대로 배포하면 통계 사업 필터가 500으로 죽습니다.

---

## 1. 검수 절차

### 1.1 단계

```
① 로컬 검증  →  ② 스테이징 구동 검증  →  ③ 데이터 정합성 점검  →  ④ 배포  →  ⑤ 배포 후 확인
```

### 1.2 ① 로컬 검증 (개발 PC)

```bash
cd server
go build ./...          # 컴파일
go vet ./...            # 정적 분석
go test ./...           # 단위·통합 테스트 (60여 개)
```

**통과 기준:** 셋 다 실패 0건. 특히 `tmpl_parse_check_test.go`(템플릿 파싱)와 `stats_*_test.go`(지표 산식)는 반드시 통과해야 합니다.

### 1.3 ② 스테이징 구동 검증

Docker로 띄운 뒤 아래를 **순서대로** 확인합니다.

**A. 전 화면 200 확인** — 로그인 후 아래 URL을 순회해 500·404가 없는지 봅니다.

```
/  /work  /work-status  /work-status?view=week  /work-status?view=month
/workboard/register  /workboard/register?view=week  /workboard/register?view=month
/workboard/kanban  /workboard/tasks  /meeting  /plan/unplanned
/as  /as/kanban  /as/new  /as?status=hold  /as?status=transfer  /as?status=cancelled
/stats  /stats?view=week  /stats?view=month  /stats/detail
/stats?view=week&project={사업ID}          ← 사업 필터 (3.1 결함 확인용)
/stats/weekly-report.xlsx                   ← xlsx 다운로드
/maintenance  /maintenance/sites  /maintenance/{계획}/export.xlsx
/projects  /customers  /spaces  /contacts  /contact-history  /assets
/relations  /analysis  /codes  /users  /admin/data  /admin-work  /account
```

**B. 핵심 시나리오 4종**

| # | 시나리오 | 통과 기준 |
|---|----------|-----------|
| 1 | AS 접수 → 조치 → 완료 | 예정일 없이 「일정확정」 체크 시 **저장 거부**. 조치 저장 시 **착수시각·소요시간이 DB에 기록**됨 |
| 2 | 조치 결과별 필수값 | `재방문필요`에 방문예정일 없으면 거부 · `이관+우리팀 추가작업`에 확인예정일/대상자/연락처 없으면 거부 · **조치내용 공란이면 거부** |
| 3 | 일일 업무 배치 | 같은 담당자·같은 시간 겹침 배치 **거부**. `×` 로 내리면 대기열 복귀 |
| 4 | 정기점검 | 같은 `날짜+기관+점검대상` 중복 추가 **거부**. 방문 완료 시 연결 일일업무 동기화 |

**C. 프런트엔드 오프라인 확인** — 인터넷을 끊고 화면을 엽니다. 스타일과 모달이 정상 동작해야 합니다. 깨지면 CDN 의존이 남아 있는 것입니다(`화면설계서_요약.md` §4).

**D. 엑셀 산출물 확인**

| 파일 | 확인 항목 |
|------|-----------|
| 주간업무보고서 | 시트 4개(통계·업무리스트·진행중건·미계획건). 담당자 드롭다운 동작 |
| 정기점검 월별 캘린더 | 시트 12개(`1월`~`12월`). 일~토 헤더. **색상이 6자리 RGB인지**(3.2 결함) |

> 엑셀 검증 팁: Python `openpyxl`로 파일을 열어 보세요. Excel은 관대해서 잘못된 색상값도 열리지만, openpyxl은 거부합니다. **openpyxl로 안 열리면 OOXML 규격 위반입니다.**

### 1.4 ③ 데이터 정합성 점검

배포 전후로 v2.0 **부록 C**의 V-1~V-10을 실행합니다. 신규 건(`data_origin='app'`) 기준으로 셉니다.

| 지표 | 기대값 |
|------|--------|
| V-1 예정일 없이 확정 | **0** |
| V-2 미계획 AS(예정일·사유 모두 없음) | **0** |
| V-3 조치 있는데 착수시각 없음 | **0** |
| V-4~V-7 소요시간·근무구분·원인분류·자산연결 누락 | 신규분 **0** |
| V-9 정기점검 방문 중복 | **0** |

`관리 > 데이터 관리`의 **「정합성 점검」** 버튼으로 실행합니다.

### 1.5 ⑤ 배포 후 확인 (30분 내)

- [ ] 로그인 → 대시보드 KPI 3종이 값과 **신뢰도 등급**을 함께 표시
- [ ] 주간업무보고서 다운로드 성공
- [ ] 서버 로그에 `no such column` / `panic` / `template:` 없음
- [ ] 백업 폴더 생성 + `data_backups` 테이블에 행 추가 (둘 다여야 함)
- [ ] 실제 사용자 1명이 접수→조치→완료 1건 수행

---

## 2. 배포 방법

### 2.1 현행 절차

```bash
# 1) 이미지 빌드 (VERSION·커밋·시각 주입)
./deploy/build.sh
# Windows PowerShell: .\deploy\build.ps1

# 2) 서버로 전송 후
docker load -i deploy/server-app.tar
cd deploy && ./stop.sh && ./start.sh

# 3) 배포 확인 — built 가 방금, started 가 방금이어야 끝
curl -s http://lib.visionit.co.kr/version
```

상세는 `deploy/서버배포매뉴얼.md`, `server/docs/맥미니_Docker_설치_및_서버_실행.md` 참조.

### 2.2 배포 전 반드시 고칠 것 (v2.0 §26)

| # | 항목 | 현재 | 조치 |
|---|------|------|------|
| 1 | **빌드 재현성** | `COPY go.mod ./` 후 빌드 중 `go mod tidy`, `go.sum` 미복사 | `COPY go.mod go.sum ./` → `go mod download`. **`go mod tidy` 제거** |
| 2 | **개발용 볼륨** | `docker-compose.yml`에 `./web:/app/web` | 운영 compose에서 **제거** (이미지 내 템플릿 사용) |
| 3 | **JWT 시크릿** | 기본값 `change-this-secret-in-production` | 서버 `.env`에서 실제 값으로 교체. 기본값이면 기동 시 경고 |
| 4 | **비밀번호 해시** | 무염 SHA-256 (3.3 참조) | bcrypt/argon2로 교체 + 전원 비밀번호 재설정 |

> **1번이 중요한 이유:** 빌드할 때마다 의존성 버전이 달라질 수 있습니다. 같은 소스인데 어제 빌드와 오늘 빌드가 다르게 동작하면 원인 추적이 불가능해집니다.

### 2.3 롤백

```bash
cd deploy && ./stop.sh
docker load -i server-app.tar.prev      # 직전 이미지
cp -r data/backups/자동백업_YYYY-MM-DD/* data/   # 필요 시 데이터 복구
./start.sh
```

**데이터 보정(v2.0 부록 B.5)을 포함한 배포는 반드시 수동 백업 후 진행합니다.**
`관리 > 데이터 관리 > 지금 저장` → `수동저장_YYYY-MM-DD_HH시MM분SS초` 폴더 생성 확인.

---

## 3. 배포 전 해결해야 할 확인된 결함

### 3.1 통계 사업 필터 500 [배포 차단]

`internal/repository/stats_dashboard.go:726` 이 `maintenance_visits.project_id` 를 참조하는데 **컬럼이 없습니다.**

```
sqlite3.OperationalError: no such column: v.project_id
```

- 이 코드는 08-14 06:54에 소스에 들어갔고, 배포본(00:16)에는 없어서 **아직 사고가 안 났을 뿐**입니다.
- 지금 상태로 배포하면 `/stats?project=...` 가 죽습니다.
- **조치:** v2.0 부록 B.1의 `ALTER TABLE maintenance_visits ADD COLUMN project_id TEXT` 를 마이그레이션에 포함한 뒤 배포.

### 3.2 정기점검 엑셀 색상값 규격 위반

`internal/handler/maintenance_excel.go` 가 excelize에 **8자리 ARGB**(`"FF00B050"`)를 넘기는데, excelize는 6자리 RGB를 받아 `FF`를 앞에 붙입니다. 결과가 **10자리**(`FFFF00B050`)가 되어 OOXML 규격 위반입니다.

- 폰트 색·채우기 색 **전부** 해당됩니다.
- Excel은 열리지만 색이 의도대로 안 나올 수 있고, openpyxl 등 엄격한 도구는 파일을 **거부**합니다.
- **조치:** `"FF00B050"` → `"00B050"`, `"FF0070C0"` → `"0070C0"`, `"FFE2EFDA"` → `"E2EFDA"` 처럼 **앞 `FF` 제거**.

### 3.3 비밀번호 무염 SHA-256 [보안]

`internal/handler/auth.go:24` — `sha256.Sum256([]byte(pw))` 단독. 솔트도 키 스트레칭도 없습니다.

- 레인보우 테이블로 즉시 역산됩니다. 실제로 현재 DB의 해시 3종이 공개된 값과 일치했습니다.
- **조치:** `golang.org/x/crypto/bcrypt` 로 교체 → 기존 해시는 첫 로그인 시 자동 재해시 → 전원 비밀번호 변경 안내.

### 3.4 조치내용 공란으로 완료 처리 가능

결과 `완료(done)` 선택 시 **조치내용이 비어 있어도 저장**되어 상태가 `completed`가 됩니다. 처리 이력에는 자동 문구 `조치 저장`만 남습니다.

- 재방문·이관·대기는 필수 검증이 제대로 걸려 있는데 **완료만 빠져 있습니다.**
- **조치:** `action_taken` 공백이면 `?err=action_required` 로 반려.

### 3.5 변경 이력이 기록되지 않음

접수 생성·조치 저장·정기점검 방문 추가/완료를 수행해도 `data_change_logs` 에 행이 추가되지 않았습니다(운영 DB 전체에 **1건**).

- v2.0 §25.2의 롤백 기능이 성립하지 않습니다.
- **조치:** v2.0 §25.2의 「이력 기록 대상」 표에 따라 기록 지점을 붙입니다. 일상적인 일정표 배치는 제외해 노이즈를 막습니다.

### 3.6 백업 목록 테이블 미반영

백업 **폴더는 생성**되는데 `data_backups` 테이블은 **0건**입니다. 백업 목록 화면이 비어 보입니다.

- **조치:** 폴더 생성 시 테이블에도 행을 남기고, 기동 시 폴더를 스캔해 누락분을 보정.

---

## 4. 정상 확인된 항목 (회귀 감시 대상)

아래는 실제 구동 검증에서 **기획서대로 동작**함을 확인했습니다. 앞으로 고칠 때 깨지지 않는지 보세요.

| 항목 | 확인 내용 |
|------|-----------|
| 예정일 없이 일정확정 | 웹 접수 폼에서 **저장 거부** (확정 플래그 자동 해제) |
| 조치 결과 필수값 | 재방문필요→방문예정일 / 이관·대기→확인예정일·대상자·연락처 **모두 반려** |
| 완료 건 잠금 | 잠금 해제 없이 수정 시도 → **반영 안 됨** |
| 시간 겹침 배치 | 같은 담당자·겹치는 시간 → **두 번째 건 저장 안 됨** |
| 정기점검 중복 방문 | 같은 `날짜+기관+점검대상` → **HTTP 400 반려** |
| 권한 분리 | 접수담당(office) 계정으로 `/users`·`/codes`·`/spaces`·`/relations` → **전부 403** |
| 정기점검 엑셀 구조 | 시트 12개(`1월`~`12월`), `2026년 1월` 제목, 일~토 헤더 **정상** |

> **DB의 위반 데이터 1,124건은 웹 폼이 아니라 엑셀 임포터(`cmd/asimport`)에서 생겼습니다.** 임포터에도 같은 검증을 붙여야 재발하지 않습니다.

---

## 5. 체크리스트 (배포 1회분)

```
[ ] go build / go vet / go test 통과
[ ] 3.1 project_id 마이그레이션 포함
[ ] 3.2 엑셀 색상 6자리로 수정
[ ] 3.4 조치내용 필수 검증 추가
[ ] Dockerfile go.sum 복사 · go mod tidy 제거
[ ] 운영 compose에서 ./web 볼륨 제거
[ ] JWT_SECRET 실제 값 확인
[ ] 수동 백업 실행 및 폴더 확인
[ ] 스테이징 전 화면 200 순회
[ ] 핵심 시나리오 4종 통과
[ ] 오프라인 상태에서 화면 정상
[ ] 엑셀 2종 openpyxl로 열림
[ ] 부록 C 정합성 점검 통과
[ ] 배포 후 30분 확인 항목 완료
```
